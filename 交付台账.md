# 交付台账(单一真相源)

> **状态:✅ 已启用(2026-08-15,随公约生效)。项目状态/进度的唯一权威;CLAUDE.md 进度节已冻结为历史快照。**
> **并发纪律**:①②按组分行/分块,**各组只准覆盖式更新自己的行,不碰他组的行**;写前先 `git pull` 拿最新版;主R 维护合并态并裁决冲突。用法见 [`docs/交付公约.md`](交付公约.md)「★ 一本台账」。

---

## ① 断点表(每组一行,覆盖式更新自己那行)

| 组 | 当前动作 + 下一步 + 悬空项 | 更新日期 |
|----|---------------------------|----------|
| 主R | **PR#84 C4 导入护栏合入**(数据组下架护栏件闭环,Opus 对抗审过;F1-4/F5 转 M7);**PR#80 终审合入+两道真库发布门亲验通过**(dev库126条 80 OPEN/46 CLOSED;对外口径"下架后~93条有效、校招42");部署安全组包处理中(两终审进行中+三裁决已出+两P0已派);悬空:简历组M1~M4统一终审、录音红线 | 2026-08-26 |
| 数据组 | **实现期第四次送审中**。①**PR #99 已合入 main**(`42e4ff3`)= 迁移批 1a(`Job.category` 可空列)+ **存量 77 条回填**(回填归属主R 2026-09-07 拍板归数据组、在 #99 内追加;拆 1a/1b 后 spec 原写的「后端组一并做」失去落点)。主R 五关 PASS-WITH-FIXES,唯一必修=迁移目录时间戳重编(分支视野停在 0830、main 已到 0902130000,**分支 CI 绿是基线旧造成的假绿**),已用 `date +%Y%m%d%H%M%S` 真实取号重编并 merge 后实测:卫生闸绿 / fresh PG 全量重放 / 回填 1-1-1、NULL 清零、已有值不覆盖、重放 0-0-0 幂等 / 索引 12-83。②**PR #114 已合** = spec 勘误订正件(:977 示意代码 fail-open —— 实测 `new URL("mailto:…")` 不抛错、hostname 为空,`catch` 兜不住;:997/:1000「四条」与 :934 裁定冲突)+ 台账⑤ 自报。③**实现批 `feat/jobdata-impl-01-r2` 第四次送审**(锚点 `7e3159b`):M2 §4.2 host 白名单+红档 SoT、M3 §4.1 保鲜闸复活(硬编码 `2026-06-01` 让它永久失效)、§4.3 JD 三级判定、§4.4 红档硬闸+白名单消费改造、§4.5 source 三分支+删批级闸、§4.6 shortJdFlags+中文误填纳入 category(19 个中文名 **18 个**含正则特殊字符,必须 Set 判等)、M4 §4.5 导入器写 category。**codex 连打三轮 REJECT,均成立**:轮1 导入器可绕 category 必填 create 出 NULL + 校验器/导入器分裂与双轴错配 + 两套活跃时间线;轮2 只分别校验两字段、从不校验**组合关系**(`tech_dev+PRODUCT` 双入口放行)+ `assertEnum` 用 `in` 走原型链(实测写库值是**函数对象** `"function toString() { [native code] }"`);轮3 legacy 回放绕过配对门(**校验解析值而写重组值**,实测写出 `{operations, PRODUCT}`)。逐条已修,新增 `scripts/lib/job-category-rules.mjs` 配对规则单一实现点 + `assertFinalWritePairs()` 最终写库值闸。全量 1206 绿。④**r2 连带风险**(分支带旧戳迁移目录副本,merge main 会成两份):主R 2026-09-07 裁 A —— 不动复审中的 r2,**codex 结论后**再 merge main+手删双目录+复跑卫生闸;主R 已核双保险(旧戳相对 base 属新增且小于现最大戳,闸③B 必拦,无静默上 main 路径)。**待**:codex 第四轮 → 主R 五关 → 合并 → **批次B 66 条重核实**(主R 裁 (a):真核实、逐条确认在招+applyUrl 可达,**不许只 touch 时间戳**;7 天保鲜从核实日起算)→ 过修好的校验器导入。**未开工**:迁移批 1b(direction 可空;简历组适配已合 `9365f61`,但三件联动要改 r2 的文件,须等 r2 合)、迁移批 2(F1-4 根治,前置=`source` 存量 NULL 治理**需生产库审计**)、M7 生产执行。⑤**spec 变更件已合**(PR #134,`04b50d0`)= M4 §4.7 公开岗位库「可发布开关」,codex 六轮终审第六轮 PASS-WITH-FIXES,zzh 2026-09-08 收口「方案终审通过,可进入实现派工,无需再送审」。定性取 B(关闭时派生消费面对「来自平台岗位」的内容一并不可用,用户自贴不受影响),含四层作用面+第⑤层入口面、来源标记单调锁定生命周期、三类例外、存量回填三态、13 条验收。⚠️ codex 保留条款:仅文档与既有代码对照,**不代表实现验收/生产回填/OFF→ON 发布闸通过**。🔨 **主R 2026-09-08 已裁:实现由数据组承接**(后端组在途队列已满,移交=纯上下文损耗),四条件已逐条执行。⑥**当前卡点全部是「同文件在途」,不是授权**:B1 等 #136;B2/B4/D1 等 #142;B3 等 #142;B5 等 #136+#142+#131(`interview*` 两文件三方同);S1/S2 等 r2。派工单 8 张卡见 `docs/plans/2026-09-08-数据组-岗位库可发布开关-实现派工单.md`。⑦**卡 D1 已交付但需返工**(PR #139 CI 绿,已请暂缓合并):主R 裁决把发布闸第 3 项统计面放宽为「与 C 端可见性同谓词」且须共享谓词定义,现脚本是手写的第二份谓词;返工前置 = #142 的 `PUBLIC_JOB_SCOPE(now)`。 | 2026-09-08 |
**本轮审查账**:定点对抗审 REJECT 7🔴+3🟡 → codex **五轮**窄复核,前四轮每轮各抓一处**同源未同步**(closeReason 语义只改 §4.10.1 / 交接件把已证伪风险当实施依据 / 交接件查询层仍黑名单 / reviveJob 判断条件改了白名单但 if 体内错误码与日志仍黑名单致标准 11 验收不可能通过)。第三、四轮组长自查各连带揪出 codex 未点名的同源项(第二道闸+reviveJob 伪代码同为黑名单式=两闸被同类脏数据一起穿透;§4.10.4 引言黑名单残留)。
**组长自评**:同一个「只改一处」的毛病本轮犯 6 次,已改工作法为「按语义全文扫→一次性改→反扫残留」。
**下一步**:①**待领 oc**(方案已定稿,按广播找 zzh 领)②实现批拆卡与派工,送审**攒批**不碎片 ③生产 50 条下架+21 条刷新(F5 前置已由 PR #90 解除,可排期)④批次B 66 条入库。
**组长不外包的三处**(质量责任在组长一人):所有 Prisma 迁移(#32 静默 DROP 前科,CI 抓不到)/ diff 失效判据鉴别力(首轮判据曾致 41 条全绿、实漏 33 条死岗位)/ 合规闸(误放=合规事故)。
**✅ M1–M4 已合 main**(PR #77/#80,过 codex 六轮终审)。**M5–M7 spec = PR #87,codex 窄复核第三轮已修完待窄确认**(`1d6a375`)——对抗审 REJECT 7🔴+3🟡 全回填后,codex 三轮各抓一处**同源未同步**:①`closeReason` 语义只改了 §4.10.1、权威表未同步 ②交接件仍把已证伪的「反连接重复行误伤」当实施依据 ③交接件查询层仍是黑名单「排除 MANUAL_COMPLIANCE」。第③轮组长自查连带发现 codex 未点名的三处:**§4.5 第二道闸与 §4.10.4 `reviveJob` 伪代码同为黑名单式**(第一道闸被破坏时拦不住 NULL/未知值,两闸被同类脏数据一起穿透)+ 标准 11 变异测试 fixture 只有 `MANUAL_COMPLIANCE`(对「第二道闸是否退化成黑名单」无鉴别力)。现四道判断点共用 `REVIVABLE_CLOSE_REASONS` 常量。
**组长自评**:同一个「只改一处」的毛病本轮犯了 5 次(M4 可见性/C4 写路径/F5 两处/spec-台账不同步/closeReason 三轮),已改为「先按语义全文扫→一次性改→反扫残留」。
**待办**:①codex 窄确认 PR #87 → G4 解禁 ②生产 50 条下架 + 21 条刷新(**F5 前置已由 PR #90 解除,可排期**)③批次B 66 条入库(dry-run 已验 66/66 解析正确)④实现批派工待领 oc,**送审按新规攒批**(一个实现批为单位,不碎片送) | 2026-08-27 |
| 后端组 | 🎉 **六个 PR 全部开出待主R合并**(#82/#83/#85 复盘轮三件 + #86/#89/#88 主R 派工三件)。六节 spec 全过终审;需求清单 **v3 = 28 PR / 40.2 人日**。**🔴 本轮最重要的事:独立审查(无上下文 Opus,不喂结论)在我签过「组内审核过关」的三个 PR 里各找到东西,其中一条是安全属性零保护。**① **#83 的 F2 归属校验测试完全没牙** —— 组长亲手复现:把 `assertResumeVersionOwned` 的 `where` 里 `resume:{userId}` **整个删掉,794/794 依旧全绿**,三处谓词删任一处都测不出。根因=测试桩完全无视 `where`(默认桩按 id 无条件返回、负例桩无条件返回 null),那 6 条用例测的是「查不到就抛 400」,**从没测过「查询本身按 owner 过滤」**——而后者是这个 PR 的全部价值。组长初版变异的是 throw 分支(最弱处),恰好绕开空洞。已重写桩为「小数据集+真求值」,同样三个变异现在 **2/2/1 条红**。② **#85 的 fail-loud 只装在算子上**:`include.orderEntitlementGrants` 被整个忽略→调用方 `?? []` 一兜即「这单没有 grant」→静默拒退;`cursor`/`skip` 被忽略→游标分页**永不终止**;会话级 advisory lock 返回 `[]`→sweep **静默跳过整轮**(而 §1.6#31/#32 正是要测抢不到锁)。全部改 fail-loud + 补齐 8 个缺失方法(order.findFirst/count 等,缺了 PR-C 一开工就撞墙,而 §1.6#43 是本 PR 自己的验收条)。829 绿,四维度各变异一次全红。③ **#82 我把一条假事实写进了代码注释**:「staging 每次重启自动重跑 seed」——该服务带 `profiles:["ops"]`(`docker-compose.staging.yml:52`),`docker compose up` 根本不启动它。注释已订正,**源头在 spec §2,5 处逐一订正**,残留检查零命中。④ **台账映射错**:#83 **不是** `BE-M4-01`(它只做 F2 那一半,60~70 端点复扫一行没写;CI 守护脚本其实属 `BE-M4-05`)。按 01 勾掉会把 **P1 的复扫静默吞掉** ⇒ 拆 `BE-M4-01a`(F2,已完成)/`BE-M4-01b`(复扫,**未开工**),各 0.9,总人日不变。⑤ 守护脚本补两处 spec 声称覆盖却漏掉的绕过:**展开不在首位**(`data:{userId,...body}` 这种更自然的写法整个逃过)、`upsert`/`createMany`;并把四种确实抓不到的写法(中间变量/Object.assign/rest解构/as any)**如实写进脚本头**,定位改为「挡最坏情况」而非「挡所有 mass-assignment」。**🔴 主R 派工三件 P0 全部完成**:**#86 JWT** 生产缺失/空串/纯空白/等于 `change-me` 即启动失败;真起三态亲验(合法起、漏配崩、默认值崩),变异 5/2/1 红。PR 里写明**为何此处 fail-closed 而 M5 方案(b) fail-open**——认证根凭证 vs 可选定时任务,极性按威胁模型分类。**#89 OSS** `OSS_SIGN_ENDPOINT` 双配置 + 启动闸;**两处签名点都换**(`getUrl` 与 `signUrlForUpload`,只改前者不够);逃生开关命名 `OSS_SIGN_WITH_ENDPOINT`(承认的是「签名与传输同 endpoint」,不是「允许内网」);真起四态亲验,变异 1/2/2/3 红;组内审核另加一处——**校验调到建客户端之前**(否则先撞 ali-oss 内部报错,我们那条写明后果的说明看不到)。**#88 shared 真实构建** 三条只有实测才暴露的坑:🔴 **`exports` 才是运行时起作用的字段**(只改 `main` 毫无效果,变异实测);🔴 **不能只靠 `prepare`**——`pnpm install` 在「Already up to date」时**跳过它**,dist 又是 gitignore 的 ⇒ **别的组一 pull 就 16 条红**,改成显式前置钩子(删 dist 后跑 test/lint/build 均自愈);🔴 **Dockerfile COPY 顺序原本就会让镜像构建失败**(install 跑在 `COPY . .` 前,那时 shared/src 不存在)。验收按主R 两条做:**镜像内实测** 23 模块装配、SyntaxError 计数 0、解析到 `dist/index.js` 而非 `.ts`;并把 `--no-experimental-strip-types` 加进 CI 冒烟做成**常驻断言**。SoT 内联口径不受影响(`src/index.ts` 零改动)。**冲突面复查**(main 今晚一直在动):六个分支 `merge-tree` 全部无冲突;main 自各 merge-base 起只多**一个**非 docs 文件(`scripts/import-jobs.ts`,数据组),六个 PR 一个都不碰 ⇒ 基线仍成立。⚠️ 组长自陈:**今晚共 5 次纠错,零次自查发现**。**待主R**:六个 PR 的合并与顺序(建议 #88 shared 先合、#85 testbed 次之)、`@nestjs/schedule` 归属、跨后端组两件、`createOrder()` 五节插入顺序裁定、BI role 派工。**未开工**:`BE-M4-01b` 端点复扫(P1)、`BE-M2-01b` 商品五字段(前端月/季切换卡它)。**卡 zzh**:隐私政策 owner(W-1~W-4,发布前硬时限)、`badge` 取值(N-4)。  **🆕 终审第五轮(2026-08-30)REJECT 3🔴 已全数闭合**,窄复核包 = `docs/review/2026-08-30-后端组-终审第五轮窄复核包.md`。①②**BI 守护器又被四种合法 PG 语法绕过**(codex 点名 2 条:`WITH GRANT OPTION`+`GRANTED BY` 同现、`GRANTED BY "带空格标识符"`;组长按同一病因**自查再出 2 条**:非首位 `GROUP` 前缀、`ALTER DEFAULT PRIVILEGES`),外加动态 SQL 一族(`DO $$ EXECUTE 'GRANT…'`/plpgsql 函数体两种)。**这个闸连续三轮被打回,前两轮我都在补语法** —— v4 改失败方向本身:(a)**校验解析产物不只是解析**(受让方必须是合法角色名,三条漏网全是解析器吐出「bangtoudi_bi_readonly WITH GRANT OPTION」这类残渣、比对不上就放行);(b)fail-closed 真正覆盖整条语句(三分类 grant/inert/blocked,dollar-quote 体按**代码**而非惰性字符串处理)。blocked 给**语句内容摘要**窄豁免(改一字即失效,与 table 互斥,强制 reason)。测试 24→40。🔴 **变异检验里「撤掉受让方产物校验」最初 39/39 全绿存活** —— 那是 v4 的核心防线却没有一条测试踩到,已补用例单独钉住,现五处变异全红。③**#86 分支污染成立且危害更硬**:merge-base 停 8/26、PR diff **63 files/+17358/−3848**,且分支上 `.github/workflows/ci.yml` 是旧版 —— **合入会静默回退主R #96 CI 瘦身与 AITask 全局门那一步**。已从当前 main 重建为 **#97**(**4 files/+313/−2**,ci.yml 只改 JWT_SECRET 一行),#86 已关闭注明取代关系、分支保留、未 force-push。留痕:805/805 + 真起 dist/main.js 验 DI + 反向验证旧值 `ci-test` 在 auth.module 加载处即崩(闸是活的)+ CI 新值过**真闸函数**(33 字节)而非数字符。非阻断:第三四轮包 §三 已加状态横幅同步现状。**CI 仍停摆**(zzh 裁 B,9/1 额度重置),交付状态如实标「代码与本地验证已闭合,CI 待补」。  **🆕 BE-M4-01b 端点越权复扫交付(PR #98,2026-08-30)** —— 主R 8/26 点名「不得随 #83 勾销」的那一半。AST 枚举器(43 条自测)+ 64 端点逐条复核工单(每条给 `文件:行号` 实证),**零 FAIL**。口径:脚本 IN_SCOPE 63/无 Guard 13/ADMIN 20 = 96;工单人工判定后 64/12/20 = 96,差的一个是 `GET /api/jd-analysis/:id`(无 Guard 但可选鉴权返私有数据,人工上移,PASS)。🔴 **组内审在枚举器自己身上抓到 fail-open**:删掉 application.controller 的类级 Guard 后,`PATCH /api/applications/:id`(全仓最典型 IDOR 面)掉出复核范围并被自动盖「不涉他人私有数据」,IN_SCOPE 静默 63→60 而总数仍 96 —— **这工具的意义就是抓归属校验缺失,它却把 Guard 缺失当成了安全的证明**,与 #93 前三版同一个跟头。修法不是补信号(最裸的 IDOR 零信号),而是**无 Guard 一律不给安全结论**;变异固化进自测 39→43。**未改动任何端点复核结论**。另登记两条不在本 PR 改码:①**F2 不在 main 上**(#83 仍是开着的 PR,任务书说法与事实不符,已更正)——属数据完整性面非越权读取面,是否另立项待主R裁;②🔴 **发布门**`POST /api/interview/runs/:id/answer` 音频路径缺 audioRef 归属闸(只验 run 属本人、未验 audioRef 属本人;当前 StubAsr 无数据面,真实 ASR 接入前必须补),owner 待主R裁。 **致主R 汇报件** = `docs/review/2026-08-30-后端组-致主R-合并汇报与三个裁决点.md`(经 zzh 转,含裁决类不走直连)。**🔴 合并顺序须改一处:主R 8/26 定的 `#88→#85→#86/#89→#82/#83` 里 #86 已作废换 #97**;建议 #94(全局门本体)插最前,#93/#98 不碰业务代码可并行。**8 个 PR 待合**:#94/#88/#85/#97/#89/#82/#83/#93/#98。  **🆕 主R 批次回执后的三件(2026-08-30)**:①**#83 回填**——主R 判据「interviewId 归属校验改永远拒绝须转红」实测确认 10/10 全绿。根因=只有负例没有正例。🔴 这是同一 PR 同一文件同类问题的**第二次**:上轮独立审抓的是「删掉谓词仍全绿」(负向),本轮主R 抓的是反方向。补三条 owned 正例,断言写两条(不抛 **且** 值真写进 data——只断不抛是半个正例)。主R 只点 interviewId,复核发现 resumeVersion/matchReport 同样存活,三条一起补。变异双向各三处**六条全红**;10→13,全量 908/908。⚠️ ci.yml 加法冲突按主R 解法**两条守护步都保留**(AITask 门 + request-body guard),YAML 解析+step 名双重核实没丢 main 侧那步。②**#89 回填**——(a)`.env.production.example` 补 `OSS_SIGN_ENDPOINT` 与三态说明块(原模板照抄上线即抛 REQUIRED);(b)逃生开关 `OSS_SIGN_WITH_ENDPOINT` 生产下不再放行内网地址,判据用 `-internal.aliyuncs.com` 全后缀不裸匹配。🔴 值得记:原注释写着「不是允许内网」而代码没兑现——注释断言了代码不保证的事,与本轮 #98 清掉的同类。验证用真解析函数喂模板值(不数字符)五态全过;并把「模板照抄即可用」做成机械断言。31→37,变异三处全红,920/920。lint 抓到一处 tsx 容忍而 tsc 不容忍的 `import.meta`。③**NP0 已开工交付 = PR #100**(详见本表 118 行)。 **合并顺序现状**:6 已合(#94/#88/#85/#97/#98/#82,main=faae0f4 起);待合 **#83 / #89**(回填已推,待主R 复验)、**#93**(等 codex 第五轮复核)、**#100**(NP0 新开)。  **🆕 CoverageRegistry 真身 = PR #101(2026-08-31)** —— 当时唯一卡在后端组手上的跨组阻塞项(模面组 PR-G4 已按冻结契约 `2bee2b7` 开工、合入等真身;我 8/29 冻结契约后压了两天)。六个导出全部内联 `packages/shared/src/index.ts`,**签名语义逐字照冻结件未改一处**;初始内容行=主R 裁的最小集(产品/运营/AI × MATCH/OPTIMIZE)属草案,数据组会签只改数据行。🔴 **契约守护测试抓到一个真 fail-open**:`?? "none"` 写法在传入 `__proto__` 时查到 `Object.prototype`(真值、`??` 不触发)⇒ 返回对象 ⇒ `isCovered` 返 true ⇒ **未覆盖职能族被当已覆盖放行**;改为校验产物(与 #93 v4 同一条教训)。🔴 **变异又抓到测试盲区**:`isSpecialized` 混同 `generic` 仍 18/18 全绿(因表里当前无 generic 行,两种写法在 76 格上重合),已补用例钉住,五处变异全拦。🔴 **干净复验抓到会炸所有人的一条**:测试文件被 `prepare` 钩子的 tsc 收进构建 ⇒ **整个 `pnpm install` 失败**,已加 `exclude`——#88 那条教训换形式重演。留痕:`pnpm -r test` 三处全绿(shared 19/web 84/api 899)+ lint 0 +删 dist 后完整 install 复验、dist 内无测试文件。⚠️ 动共享缝命中 **A1 需独立对抗审**,合并权主R 直管,对抗审材料已在 PR 说明备齐。 **🆕 9/1 CI 债补跑清单已出** = `docs/review/2026-09-01-后端组-CI债补跑清单.md`:6 个已合 PR(#94/#88/#85/#97/#98/#82)只凭本地留痕进的 main,明日额度恢复后**先跑 main 基线再逐个 PR**;5 个在审(#83 含 ci.yml 加法 / #89 / #93 / #100 / **#101 使 shared 测试首次真正进 CI**)。写清单的理由写进文件了:这个日期我在三个复核包里签过字,**不能变成第二个 NP0**。 **🆕 9/1 CI 债补跑执行完毕 + 存量开发批(2026-09-02)**:①**六个在审 PR 全部跑通真实 CI**(#83/#89/#93/#100/#101/#103)。8 月配额期那批 `checks=FAILURE` 全是 3 秒的配额死亡,**一次真实 CI 都没跑过**;本次用 merge 而非 rebase 并入 main,主R 已看过的 commit 未改写。②🔴 **真实 CI 抓出两个本地三关全绿却真坏的洞**:**#103** 编译产物冒烟 `MODULE_NOT_FOUND` —— `apps/api/tsconfig.json` 没显式写 `rootDir`,tsc 按「全部输入文件的公共父目录」推断,新增守护里一行跨包静态相对 import 把公共父目录抬到仓库根,`nest build` 产物整体下移(`dist/main.js`→`dist/apps/api/src/main.js`),**lint 绿、单测绿、变异也红得对,唯独 `node dist/main.js` 全挂**,同时命中 `start:prod` 与 Dockerfile;已钉死 `"rootDir":"./src"`,并以「先只加 rootDir、不改测试 → lint 立刻 TS6059」验证这道防线被测试踩到。**#100** 冒烟报 `PRODUCTION_ENV_MISSING`(闸生效的证据),补齐冒烟 env 后顺手加了一步**负向冒烟**(空配置必须启动失败),该步当场抓出真洞:`assertProductionEnv()` 虽在 `bootstrap()` 首行,但 `main.ts` 顶部静态 `import { AppModule }` 在 CJS 下先于函数体求值,而 `auth.module` 在模块顶层调了 `resolveJwtSecret()` —— **空配置下实际只报 JWT 一条,「一次报全 10 个」的设计目标在真实启动路径上失效**,而单测永远看不见(直接调函数,不经过 import 求值顺序);已改为断言后动态 import。③**#93 的 DIRTY 已解**,ci.yml 加法冲突两侧守护步全留、main 侧在前(主R 点过的坑),YAML 18 步可解析、三个守护脚本 + 40/40 自测实跑。④**存量开发批六件全部交付**(zzh 特批当日额度充裕,派 sub 实现、组长机械复核,不做组内审、直送终审):**#105** BE-M4-03 登录 IP 限流(955)/ **#106** BE-M2-01b 商品五字段(953,**解前端月付季付阻塞**)/ **#107** BE-M5-03 告警去重(946)/ **#108** BE-P0-02 M3 测试台(939)/ **#109** BE-M5-02 health 支付三态(957)/ **#110** BE-M4-02 AI 两档限流+熔断(967)。测试数均为组长本地独立重跑,非引用实现方报告;各分支基线均 931,不可横向相加。⑤🔴 **三处跨件的共通薄弱面已横向登记**(见材料包 §三):(a) **Redis Lua 在单测里是 JS 镜像不是真执行** —— 改坏 Lua 文本行为用例全绿,#105 与 #110 各自补了文本级结构闸,**但既有的 `sms-rate-limit`/`resume-agent-rate-limit` 两处仍是纯镜像、未补强**;(b) **tsx 不产出 decorator metadata**,`Test.createTestingModule` 在构造参数全 undefined 时照样 compile 通过 ⇒ **本仓所有「DI 装配」类单测的证明力有限**,真正的 DI 验证只有 CI 的编译产物冒烟;(c) **细粒度假桩普遍存在** —— #108 用「按测试标题声称的行为做小粒度变异」在两个文件里命中 5 处,其中一处成因是**测试数据恰好让错误实现与正确实现返回同一个值**。⑥⚠️ **#110 带出一条需产品拍板的阈值问题**:spec 算 worst-case 时默认「一次用户请求=一次 `aiGateway.run()`」,**实测不成立** —— `resume-agent.service.ts` 的 `advanceRun` 是状态机循环,一次绑 JD ≈ 3~4 次 run(`:1271`/`:1590`/`:1988`,上传另有 `:467`),分钟档 5 会打断简历 Agent 主链路。**阈值属产品口径,后端组未自行决定。** ⑦**#106 组长追加一个 commit**:把 `tier`/`billingPeriod`/`badge` 收进合规改值通道 —— 需求清单承诺「拍板后改数据即可、不需要二次发版」,而原通道不认这三列,唯一兑现路径是手写 SQL 改生产库、绕开 CAS 与审计;`equivalentMonthlyPriceCents`/`savingPercent` 刻意排除(现算值非存量,开放改写=同一价格两个来源)。⑧**错误码契约 spec = PR #104**(纯方案零代码,前端组等了 4 天的那件),自列四条风险两处待裁。⑨**送终审材料包** = `docs/review/2026-09-02-后端组-送终审材料包.md`(13 件,自包含、中立不给倾向);**致主R 合并批次提示词**已出:本批只 4 件(#103→#83→#100→#89),#93 等 codex、#101 等 A1 对抗审、#99 属数据组,**不把等外部裁决的件塞进合并单**。⑩**决策 0007 定案**(`.mjs` 如何消费 shared:按包名 import,副本数为零)—— **数据组的过渡方案(词表J 派生)不必落地**,该知会尚未发出:🔴 `ListAgents` 的会话名是临时的(实测:同一会话上次叫 `zhuzhaohui-5f`,现列表里已无此名),**认不出哪两个是数据组/简历组**,已请 zzh 给名字或代转。 | 2026-09-02 |
| 前端组 | ✅ spec 终审通过 + **批 0 已合(PR #79 → `4f305a1`,主R 四绿亲验)** + **PR #76 设计稿已合(`7721288`,23 稿进 main)**。两个现网必 400 入口已修(二态:合入 ≠ 已发布,真账号回归待彩排)。**当前:批 1 前置产出已完成**——37 token 迁移映射表 `docs/design/2026-08-26-前端组-tokens迁移映射表.md`,37 个全部有处置零待定;三悬案定案,其中 `--line/--line-strong` 映射按用法审计**与色值配对相反**(证明当初挂起要求做用法审计是必要的)。**批 1 前置闸已解除,可开工**(绝缘批次,不吃他组方案)。待转(主R 已登台账③,请 zzh 转):①模面组=debrief 真实现被禁用桩遮蔽,法务放行前修掉;②部署安全组=彩排 checklist 追加「批0 链路真账号端到端回归 + 野值断言必跑」。悬空:①四件产品点按 zzh 裁决挂起等各组方案齐;②压后批次(批2–5)只做不吃外部口径部分;③移动 390 实测待补 | 2026-08-26 |
| 模面组 | **v2.0 生效后已推完两件无前置活**:①**模块④⑤ spec 出稿**(`2cb56fe`,+505 行;语音 ASR 接线/台风第5轴/法务三件挂标记/难度模型阶段一+三阶段预留/评测三层/E3)②**变更承接 v2 回执**(`763df45`,已并入无冲突,受影响6条不受影响8项)——**回执可转主R**。**组长裁决**:难度分与 D-5 双尺撞 `defaultTargetBar` ⇒ 语义正交(双尺答『用哪把尺』/难度分答『尺定多高』)⇒ 难度分走外层 `applyDifficultyAdjust` 加法式加成、不侵入本体;阶段一不必碰。**新增承接(v2.0 §6)**:🆕 **模面组=合库 spec owner**(简历组消费方会签,检索契约节先冻结)+ `Interview` 读路径 null-safe/category 切换(通用档开放硬前置)+ §1.7 整节重做为档位制 + 模块三照 F3 回填时对齐。**卡点(全线第一块骨牌)**:v2.0 §6 第0步(a)**数据组全职类值表提案未冻结** ⇒ 后端 SoT 未落 ⇒ 我在第2步(与第1步并行)。**现场实证**:CoverageRegistry 与 category 白名单 SoT **全仓零命中**,④⑤ spec 已用占位并如实标注「职能族 day-1 无 profile 可跑」。**待办**:①④⑤ spec 未过对抗审(需含安全一路)②合库 spec 可先写不依赖值域的结构/契约/迁移编排 ③KB 风格库 spec ④两个自查缺口留 §1.7 重做稿(私有粘贴JD无 category 查不了档位 / careerChange 比较轴须切 category)。G4 零实现代码 | 2026-08-26 |
| 简历Agent组 | ✅ **G3 第1步已闭环**(PR #81 合入,`784ead3`;口径:路由修复已上线、内容供给待交付)。🎯 **复盘轮 Step0–3 全部完成**:八节 spec 落稿 + **5 轮全局审 + §10 模块级对抗审全部跑完**,**六路 findings 已全部回填收口**。**全组总账 = 48.25 人日**(2026-08-26 按主R 裁定四-4 自改分节值:§4 6.85→6.60/§5 2.90→2.80/§6 1.40→1.25/§7 7.30→7.80;**总数不变纯属两处 0.5 抵消的巧合,已按主R 点名订正**)(§1 15.5/§2 10.5/§3 1.45/§4 6.60/§5 2.80/§6 1.25/§7 7.80/§10 2.35,逐步累计已验算;如实标注仍含约 0.5 已完成写稿工时待剔除)。**审查产出**:§10对抗审 6🔴+8🟡(判方案级返工,**已执行**)/审1 跨节自洽 7条无🔴(44.30 独立复算确认)/审2 因果与时序 3🔴+7🟡+4🟢+回滚R1–R5/审3 红线全覆盖 13条含2🔴(**无一条红线被突破**)/审4 范围与遗漏 13条含3🔴/审5 可执行性 5🔴+6🟡(**全是前四路漏掉的**)。**最重的几条**:①§2.4.4 代码块是 zzh **已否掉**的方案,照抄即实现出会回锁的版本(已改回候选A);②闸门① `value` 放行被 §4.4.0 corpus(b) **升格为「简历原文语料」**⇒ 闸门②数字+权属双豁免(新增 §1.4.19);③corrections 通道无上限,绕过 §1.4.7 直通 patch prompt token 成本(新增 §1.4.18);④§2.4.1 第③行让**重跑诊断直接坏掉**——「🔴-1 修完换个出口复发」(已拆两支);⑤§1 契约 v1.3.1 **零实现落点零人日**(新增 §1.4.20);⑥§10 实体门**粒度错**:把召回优先的检测器接到抛错即全废的付费路径(已改逐条丢弃)。**立三条全文级待办**:T-A 行号统一重刷(§6/§10 已抽验结清,§1 收窄到一个文件)/T-B 跨节依赖双向登记/**T-C 对外义务清单**(审4 点出「八节视角始终朝内,把『要交给谁看』整层漏了」)。**过程留痕**:本轮共 5 次「说了没做」(含 F2 自己只落一半——而 F2 正是为修这毛病而设),已升级为交付前 checklist 硬项;另 2 次宽匹配插错位置,均自查发现并撤销重插。🎉 **G4 已解禁(2026-08-29,主R 确认)**:三条件齐 —— 映射表 **29/29 逐条定点核验全部落地**(含负向检查:四类旧表述残留均为订正留痕、单值 verifyFlag 恰 3 处与组长自述逐一吻合)+ codex 窄复核 PASS + 申请件 v5 在案。**即日起可写实现代码**;两个已会签 PR(shared 三处加法、anti-fabrication 搬迁)随之开工,按会签条件走(后端 custodian review / 搬迁单独纯机械 + 对抗审)。**主R 判语**:「从『回填不实 11 条』到 29/29 全落 + 根因(共用主树错锚)挖到并物理根治——这个闭环质量是对的,照这个标准进实现期」。🔴 **三件不豁免**:①FE 接 `verifyFlags` + 逐项渲染验收 = **B 案发布门**;②**T-A 行号重刷在交付实现前完成**(主R 指出的 §7.6#20 行号笔误 :2092→:3842/:4648 已就地修,重刷时复验);③`hint` 文案等 zzh。**📌 终审进展(2026-08-27)**:主R 预审 = §1–§7 PASS-WITH-FIXES / §10 退回;修订工单四块全落。**codex 独立终审两轮 REJECT 已闭环**:第一轮三项(基线错锚/裁定二未进送审包/状态卫生)——根因是主树共用导致送审包建在错误基点;第二轮 §10 对抗审四阻断点(单值 verifyFlag 装不下多风险 / NUMBER_UNVERIFIED 冒用于权属时间跨度 / 丙案语义仍在活跃正文 / **免责声明能替模型自解风险**)全数回填,**并按 codex 建议加「全文守护」清单,终检 §10 活跃残留 0 处**。其中第四项 codex 说服组长:问题不在「谁写 note」而在「免责声明不能当事实核验」⇒ 冻结原则 + A2b 待裁项随之消解。**已收口**(主件 `e5bc13d`→`59d10b0`,窄复核 PASS)。总账 **48.35 人日**。🔴 **剩余阻断不在 spec 本身,而是外部未决项**:**B1 卡 zzh** = `dsv4pro`/`v4flash` 确切型号参数(卡 §7 的3.90人日,并经F10传导卡死 §4.4.2 交付gate);**B2 卡 zzh** = 红线集模型额度;**B3 卡主R** = `anti-fabrication.ts` 上提 `common/` 会签 + **`packages/shared` 排他窗口是否已解除**(未解除则两个 PR 提都提不了);**B4 卡主R** = 终审(G4);**B5 待 zzh 确认** = 「不能调用失败」的收窄翻译。**悬空**:M3/M2 依赖部分等模面组合库 spec 检索契约节冻结;欠模面组 §3 回执(背景方向能否规则派生,已验到关键处、不阻塞对方)。**G4 零实现代码** **🆕 新配置知悉**(2026-08-27):①oc 定位=只执行不决策不审核,派工时把边界原文写进提示词;②质量责任只在组长一人;③codex 全组共用、经 zzh 转、**送审须攒批禁碎片**;④在途任务不改道;⑤**git 写操作已迁 `~/bangtoudi-resume`,主树只读** —— 本组今日 5 起共用 checkout 事故中占 2 起(卷走他组文件、送审包错锚),迁 worktree 直接消灭该类根因。 🔴 **§1.4.20 状态钉死(2026-09-02 组长自纠,防误读)**:确认屏取数端点 `GET /runs/:id/canonical` = **契约 v1.3.2 已生效 + spec 已定稿 + 实现零行**。送审材料 `docs/review/2026-08-26-简历agent组-终审申请-v3.md:54` 写的「裁定二 §1.4.20 六项 ✅ 全部落地」**是 spec 文本的 grep 计数,不是实现**,孤立看会被读成端点已做——**正是这句让本项在组长视野里消失了两个月,期间前端组确认屏整屏被硬阻断**。亲验:`apps/api/src` 内 `resumeGroupId`/`resumeLabel` **零命中**,`RESUME_GROUP_ID_SECRET` 仅存在于 `docs/`(未进 env 模板、未进生产 env 断言)。⇒ 已排**批 3**(a+c,~1.25 人日,与反编造链正交不排队)+**批 4**(b + `input-quality` 三个判定纯函数接主流程)。**同类风险**:`resume-agent-input-quality.ts` 三个纯函数**已写、有测试、生产调用点为零**——测试全绿但功能在生产不存在,与本组假绿清单同类,已登记⑤。 🆕 **2026-09-07 事后审回执落地(主R 回件当日)**:批1 **PASS**、批3 **PASS-WITH-FIXES**。两个 PR 已按新流程开出,**待主R 终审**:**#118**(🔴 界外预存原型污染,`a46b5a9`/PR#45 引入)、**#119**(批3 三条 🟡 回填 + 主R 点名三顺手项)。🔴 **#118 的实质**:`parseFieldPath` 正则放行 `__proto__`,`setValueByFieldPath` 又用 `node[token] !== undefined` 判存在(命中原型链),`isPlainObject` 对 `Object.prototype` 返回 true ⇒ 任意登录用户对**自己的** run 发 `corrections:[{path:"__proto__.toString"}]` 即可污染 `Object.prototype`,此后任意 `${obj}` 抛错 —— **进程级 DoS,不是单请求失败**。可达性已实证:`readCorrections`(service:2761)只校验形状、**无键白名单**(§1.4.16 未实现)。修在 parser 一处封住三个消费者,`Object.hasOwn` 三个遍历现场**逐个**改(只改 `setValueByFieldPath` 一处即假绿)。主R 提的「能上白名单更好」**本次未做**并在 PR 写明理由(完整白名单属 §1.4.16,被 codex 卡在 §1.4.2/§1.4.4 之后,与在制的批4 撞车),请主R 终审时裁。 **组长自评**:引入不是本组的账,但本组两批改的正是这个文件、这段路径语法,`parseFieldPath` 就在本组所改 `hasUnsupportedNumericToken` 下方三十行,**两次经过未发现**。 📌 **⑤ 假绿两条新登记**:㈠ `anti-fabrication.ts` **含真实 NUL 字节**(`:401` 的 `"\x00".repeat()` 写成裸 0x00),`file` 判为 `data`、**裸 `grep` 静默返回「无匹配」退出码 1**(`grep -a` 才有 3 处命中)⇒ **任何基于 grep 的检查(含 CI 漂移闸)都会静默跳过反编造引擎本体**;运行时无害,但对审查完整性是真隐患——本组组长本次自己就被坑过一次(误判为 glob 报错)。预存问题、非本批引入,**未改**,已在 #119 描述里请主R 定处置。㈡ `resume-group.test.ts:185`「只读 resumeJson」原为**安慰剂用例**(函数签名令替代路径不可达、**永远不会红**),已改为把抽取中间产物真塞进入参 + 函数体源码断言,变异实测可红;该项属主R 🟢 备注(不要求返工),**超出点名范围、已在 #119 显式声明请主R 裁是否保留**。 ⚠️ **#119 含一处行为变更(非纯放宽)**:千分位折叠后原文 `12,345` 只剩 token `12345`,value 里的 `345` 由过闸变为降级(`100,200,300` 类逗号列表同理)。方向是过杀→低置信清单等用户确认(安全侧),并堵住「拿原文逗号切出的碎片凑数」这条假豁免,但**不得表述为「原来能过的现在都能过」**。 📋 **跟进项(建议单开,#119 未做)**:`anti-fabrication.ts` **同样有千分位过杀**(实测 `before="营收 3000万"`/`after="营收 3,000万"` → HIGH「新增绝对数字『000万』」),但修它 = **放松 patch 侧 HIGH 风险闸**,方向是放宽而非收紧,属批3 界外需单独评审。 🔴 **§1.4.19 组长裁决 A′(2026-09-07,OC 请裁「联系方式数字校验与日期归一化判据冲突」)**:放宽面**只开在 `basics.phone` 一个字段**——该字段定义上就是单个号码,run 边界对它无意义;`contacts[*]` 与 `basics.email` **照常走 §1.4.19 不放宽**。**否掉了 OC 原提的 A**(把 `contacts[*]` 一并放宽):`contacts` 是自由 `string[]`,数字串子串匹配**丢掉边界保护** ⇒ `value="200万"`/`source="1200万"` 会过闸,而这正是 extraction.ts `:275` 注释**点名要拦**的 case。另纠正 OC 一处论断:该校验**不是「更严」**——截断(`13800` ⊆ `13800138000`)在子串匹配下会放行、§1.4.19 反而拦得住,已要求补「命中区间须起于 run 起点、止于 run 终点」堵掉(手机号截断 = 用户收不到面试通知)。配套要求:分支在**调用点**不在 `gate()` 内、归一化单点定义、抽取 prompt 须把主号路由到 `basics.phone`(否则 A′ 空转)、**spec `:583` 同 commit 改**(`gate()` 对「任一字段」加除外)否则按未交付算。**随批4 PR 走,不进回填 PR。** ⚠️ **请主R 顺带订正自己两行的一处数字**:④ 2026-09-07 行(:261-262)与 ⑤ 2026-09-07 行(:287)仍写「批1 约九个单亲提交」「九个单亲提交」,而主R 回执已钉正为**7 个**(另 3 个经 worktree 分支随批3 merge 进入、已在批3 侧全审)。本组不改他组行,仅提请。 📌 **纪律登记**:裁决类请示 **48h 无回执主动催办**(主R 2026-09-07 定),不默认默许也不默认禁止。 🆕 **2026-09-08 夜:组长派 3 个 sub 并行 + 逐条复核,开出三个 PR(均待主R 终审;#126/#127 建议连 codex)**。基线 `6627181`(#123/#124/#125 + codex 阻断订正 `613a79d` 均已在 main),干净基数 **1298**(DATABASE_URL 未设,真库 suite 自跳过,全程无真库覆盖)。**#126**(`14684d2`,`fix/resume-ratelimit-groupid`)per-resume 限流键 `contentSha256` → `resumeGroupId`(契约 §3.D.1);派生**放在 try 之外**,否则密钥缺失会被 catch 伪装成 503「Redis 挂了」;三处后果口径补上「paste/upload 起 run 第一步也 500」(`production-env.ts:66` / `.env.production.example` / `.env.example`)+ 主R #125 回执回填① `RESUME_AGENT_DOC_CLASSIFY_ENABLED` 进生产模板。1307 绿。**#127**(`c577f85`,`fix/resume-phone-candidate-sep`)主R #125 回执**候补**:`PHONE_CANDIDATE_RE` 的 `\s` 含换行 ⇒ PDF/DOCX 抽出的**纯数字表格列**被拼成伪造手机号放行(实测 `138`+换行+`0013`+换行+`8000` → accepted=true),与 codex 阻断的那条同类。**本组比主R 建议的 `[ \t]` 再严一档、收到仅行内空格** —— 实测 `[ \t]` 仍放行制表符列(变异 P2 恰好证伪);**此为对主R 建议的加严偏离,请主R 复核是否照收**。1302 绿。**#128**(`26d3e9a`,`test/resume-false-green-audit`)零实现改动:假绿变异普查(95 次主变异)补测 15 条 + 未接线导出普查报告 + 两处 TODO。1313 绿。 📌 **⑤ 假绿新登记(本轮变异实测,均已补测转红)**:`p3-logic` 的 `?? ""` 若改成 `?? leaf.value`,canonical 里不存在的**新增字段**整条编造溜过闸门④(旧用例 14 条全绿 —— 组长已独立复现「旧测试 + 变异 = 全绿」的完整假绿证明);`patch-logic` 删 evidenceRefs 越界清洗;`notify` 的 `findUnique` 桩无视入参 ⇒「通知发给了谁」从未被测过;`extraction.test.ts` 闸门①只钉左边界,去掉 `containsNumericToken` 的 `(?!\d)` 后 49 条全绿(已在 #127 补右边界)。🔴 **未解决的覆盖缺口**:`RUN_RATE_LIMIT_LUA` 全程被 stub,Lua 一行未执行 —— 整段挖空 / `>=`→`>` / 先增后查三种致命变异全绿,需 `REDIS_URL` 门控 integration 用例,另案。 📌 **组长自认一笔**:#125 我判 PASS,codex 随后 REJECT 两条阻断,其中 🔴1「`extractDigitRuns` 跨语义拼接」正在我审过的那段。我当时只跑了设计预期内的 9 条 A′ 用例,还把 `indexOf` 只看首次命中标成「降级=安全侧」,**没造对抗输入**,而 codex 抓的正是反方向的放行。教训已落:验闸门必须造对抗输入,不能只跑设计预期。本轮据此对当前 main 跑了 13 条对抗探针(MISMATCH=0),#127 那两条洞就是这么找出来的。 🆕 **报告订正纪律实例**:sub 的未接线普查落笔于 `6baaf47`,写完前 main 已跳;组长按「做不到/没人做类结论必须回码核一遍再收」重核,**10 条 🔴 里 3 条已被 #125 接线证伪**(`classifyDocumentIsResume`/`detectPasteInputQuirks`/`collectSectionGaps`),修正为 ✅86/🟡0/🔴7,订正块写在报告开头。 ⏳ **仍待 zzh 转发/处置**:①前端组解封通知(`GET /runs/:id/canonical` 已在 main,`resume-agent.controller.ts:82`;引用回复文档只能用 `4b118a1` 不是 `6c60913`;**#126 合入后,本地 `.env` 缺 `RESUME_GROUP_ID_SECRET` 会连起 run 都 500**,需同步告知)②`RESUME_GROUP_ID_SECRET` 生产落位(zzh 本人,VNC/SSH 直接进 ECS,时点=批3 合并后首次生产发布之前)③§7 的 3.9 人日仍卡 B1(dsv4pro/v4flash 型号参数)。 🔴 **2026-09-08 codex 终审回件**:**#126 PASS**(可独立合入;非阻断提醒二条:换键窗口内单份简历可能多得一次额度、总日限额仍在风险有界;**部署时必须确认生产 `RESUME_GROUP_ID_SECRET` 已真实落位,不能只确认模板里有字段**)。**#128 PASS**(可独立合入;边界保留:该 PR 未真实执行 `RUN_RATE_LIMIT_LUA`,**不得据此宣称 Redis Lua 原子行为已被集成验证**)。**#127 REJECT**。 🔴 **#127 驳回理由成立,组长已复现并修(第二版 `12b3357`)**:第一版只收紧了 sourceText 候选段字符集,**没动 value 侧格式约束** —— `isPhoneValueSupported` 只抽 value 里的数字、其余字符全忽略,于是 `sourceText="138-0013-8000"` + `value="13800138000 任意伪造文本"` 数字部分能回推 ⇒ 放行,**伪造文本逐字进 canonical 的 `basics.phone`**。组长在第一版分支尖 `c577f85` 上端到端复现,输出与 codex 给的一致。**这是我今晚第二次栽在同一个形态上**:我造了「非数字值」的对抗用例(`wxid_abcdef`,因不含数字被拒),却没造「合法数字 + 伪造文本」;上午认的 #125 那笔也是「只跑设计预期内的输入」。教训升级为:**对抗用例必须按「攻击者能控制哪几个自由度」逐个穷举**,不是想一两个反例。 🔴 **第二版按 codex 裁法合并两边正确部分**:取 `9ef09ada`(OC,在 `origin/feat/resume-batch4`,**不在 main**,已用 `git merge-base --is-ancestor` 核)的 value 整体白名单 `PHONE_VALUE_RE`,但**不取它重新纳入的 `\t`** —— 变异 Q2 量化:退回 `[ \t]` ⇒ 53 中 2 红(制表符列仍放行)。value 与候选段共用同一份 `PHONE_CHAR_CLASS = [\d+\-()（） ]`。四类反例齐(伪造文本夹带 / value 自身含换行制表符 / sourceText 跨行 / sourceText 制表符),正例四条(空格、横线、半角括号区号、`+86`)。1305 绿、tsc 0;变异 Q1–Q4 全红、还原全绿;20 条端到端对抗探针 MISMATCH=0。 ⚠️ **请主R 裁一处合并冲突**:`9ef09ada` 与 #127 改同一个函数,**若 9ef09ada 后落,`\t` 会被重新放进字符集、制表符列的洞复现**。**组长逐字比对后收敛为单一裁法(2026-09-08,已核 GitHub 现状)**:#129(`feat/resume-batch4`,尖 `f881c33`)与 #127(尖 `79feb29`)**相对 main 只动同样 2 个文件**;两边 phone 实现 `PHONE_CHAR_CLASS`/`PHONE_VALUE_RE`/`PHONE_CANDIDATE_RE`/白名单调用**逐字相同**(OC 已自行去掉 `\t`,codex「剩一条制表符拼接」是对更早的尖说的);#129 宣称独有的 documentType 五值白名单与 FSM 双源**早在 `613a79d` 就已在 main**,与 main 逐字一致、非 #129 独有。测试上 **#127 是 #129 的严格超集**:#129 的 ⑪⑫⑬⑭ 分别对应 #127 的 ⑧a/⑧b/⑧c/⑧d,#127 另有 ⑧a2(value 自身含换行/制表符)、⑧e(空格/横线/半角括号/`+86` 四类正例)、闸门①右边界(200 不得被 2001 豁免)。⇒ **#129 已无独有内容,不存在「合并两边」的问题**;裁法=合 #127、关 #129,不做 rebase。 另:#127 的 `PHONE_VALUE_RE` **只约束字符集不约束结构** —— 实测 `(13800138000)`/`-13800138000-`/`1 3 8 0 0 1 3 8 0 0 0` 这类「格式怪但数字逐位来自原文」的 value 会放行(方向=脏数据非伪造);不加结构校验是刻意的(`(010) 1234-5678` 本身以 `(` 开头,每加一条结构规则多一个误伤面),已在 PR 正文主动交代请裁。 另修一处自己留下的过期注释:`isPhoneValueSupported` 的「分机『转 8』天然放行」在加了 value 白名单后已不成立(sourceText 带「转 8」不影响主号,value 逐字带则降级),已就地改正。 ✅ **夜批闭环(2026-09-08)**:**#126(`1b36840`)/ #127(`532f468`)/ #128(`aba4be6`)三个 PR 全部合入 main**;OC 的 #129 经其**独立复核后自行关闭**(剥注释后 diff 为空、确认 #127 为严格超集、确认 documentType/FSM 早在 `613a79d`),批4 codex 终审链闭环。组长合并树亲验(`origin/main` detached):**1329 绿 / 0 红**,恰等于 1298 基线 + 9(#126)+ 7(#127)+ 15(#128),逐项对得上;tsc 0;18 条对抗探针在合并树上 MISMATCH=0(含伪造文本夹带、value 自身含换行/制表符、跨行与制表符数字列、分机两个方向)。另核:主R 在 `review/pr126-fix`、`review/pr128-fix` 上只做了「把 main 并进来测合并树」,**本组交付内容逐字落地、零改动**(逐文件 diff 为空)。 📌 **本夜三笔教训归档**:①#125 判 PASS 后被 codex 抓「跨语义拼接」、②#127 第一版被 codex 抓「只核数字不核 value 整体」——同一形态两次,根因都是**只跑设计预期内的输入**;③自己写的锚点断言 M3 首版全绿(干净 `userId` 下 `normalizeKey` 恒等,证伪不了)。⇒ 立纪律:**对抗用例按「攻击者能控制哪几个自由度」逐个穷举**(此处=value 侧字符集 × sourceText 侧分隔符 × 命中位置),且**每条新断言都要先用变异证明它会红**,否则不算交付。 ⏳ **夜批之后仍未了**:①`RUN_RATE_LIMIT_LUA` 零执行(需 `REDIS_URL` 门控 integration 用例,另案);②`RESUME_GROUP_ID_SECRET` 生产落位(zzh 本人;#126 已合 ⇒ **漏配现在会让 paste/upload 起 run 就 500**,不再只是已有 run 的操作);③前端组解封通知待转(须同步告知②这条本地 `.env` 陷阱);④千分位过杀提案 `docs/review/2026-09-08-…-反编造千分位过杀-提案.md` 待评审;⑤未接线普查剩 7 条 🔴 待接线;⑥§7 的 3.9 人日仍卡 B1。 📩 **主R #126/#127/#128 合入 + #129 关闭回执(2026-09-08)六条逐条落**:**①加严偏离照收**——主R 亲测复现本组 Q2(字符集退回 `[ \t]` → 53 中 2 红,制表符列实锤)与「去 value 白名单 → 2 红」,判「比主R 建议严一档,你们是对的」,记正账;②**`PHONE_VALUE_RE` 不加结构校验,裁不收、维持现状**(「每条结构规则多一个误伤面」论证成立,脏格式方向由确认屏兜);③#129 裁合 #127 关 #129(已闭环),**OC 修法方向与 codex 一致记正账,病根=开工前没 `gh pr list` 查同题在途**——撞车批教训第二例;④报数口径(skip 的真库 suite 不进计数器,自报须带此句)采纳,本组 mock/真库数字从此对得上;⑤`RUN_RATE_LIMIT_LUA` 全 stub 缺口与后端 PR-甲同病,**列本组候补,等 PR-甲真 Redis 手法落地后照抄**;⑥「按攻击者自由度逐个穷举」的自纠升级获认可、入本组纪律;另主R 点名 #126 的「try 外派生=一个错误码只表达一件事」与「调用点断言钉在 `resolveResumeGroupId` 上」两处形态好。主R 逐级合并树全量 **1846 绿**(本地含真库,宽于本组 mock 层 1329);`docs/INDEX.md` 三方同位行冲突由主R 代解(纯索引两行并存),不算本组的账 —— 组长已复核本组三行索引全在。 ✅ **裁③已执行**:新建 `docs/plans/简历agent组-OC派工模板-2026-09-08.md`(已进 INDEX),§1 即「开工前必做:`git fetch` + `gh pr list` 查同题在途,目标文件与任一 OPEN PR 有交集就先停下来问」,并写明撞车的真实代价不是重写而是**静默覆盖**(若 #129 后落,已堵掉的伪造手机号洞会原样复现);另收纳角色边界、10 条硬红线(含 `git cat-file -t` 对悬空提交照样返回 commit、验证命令不得接管道、禁裸 stash)、变异验证与调用点断言的交付判据、`DATABASE_URL` 报数口径、行号与 commit 引用纪律。今后派 OC 一律原文贴 §1–§5。 🔴 **2026-09-08 晚 · 三条外部输入落账**:**①zzh 裁千分位取候选 A**(入口整体折叠,含全角 `，`;代价=`matchedText` 折叠后形态,已实测风险指纹不落库、刷页自愈)。**配套硬约束**:`resume-agent-extraction.ts` 的同名 `THOUSANDS_SEPARATED_NUMBER` 必须**同批收全角**,否则一条链认全角一条不认 = 给编造留明路。**仍走 codex**(放松 HIGH 风险闸属红线级)。**②模面组上报的跨岗位 MatchReport 引用,组长裁「非法」**:`match-report.service.ts:330` `getOwnedMatchReport` 只校验 `userId`;定性依据不是「少一道校验」而是**同函数自相矛盾** —— 兜底分支 `:227-233` 的 where 带 `jdAnalysisId`,显式传 id 那条路不带,同一份代码两套标准。归属不分叉(`MatchReport.jdAnalysisId` 是 schema `:605-606` 直接非空外键,不需进 `jd-analysis/`;§7.9#6 亦写明 match-report 归本组)。本组接下**修 + 存量盘点(只统计不清洗)**。模面组消费端已 fail-closed(`interview-run.service.ts:788` 无 jobId 不带 match),零改码。**③前端组已全量接完 resume-agent 域**(12 个域函数真调、mock 引擎删净),并对本组两条警告**加断言上锁**(写一处非空断言即红 / `resumeLabel` 与 URL·埋点·日志 sink 同行即红)——「『我们现在没这么写』是个会过期的回答」这句已收进本组纪律。其自述**最大空白=整个深改域从未在真环境跑过**,已建议其列为独立验收项。 🔴 **主R v4-flash 实测回报(应本组请测)+ 组长后续核算**:`max_tokens=4000` **全灭**(content 空/`finish=length`/`reasoning_tokens=4000` 吃满且未想完);`16000` 全通(completion 5577 = reasoning 3425 + 正文 2152,过真 `parseExtractionOutput`)。**第二颗雷**:`LLM_REQUEST_TIMEOUT_MS` 默认 30s,实测 32.9s/34.6s ⇒ **只改预算照样全灭**。⚠️ **组长核算后认为现有数据不足以定值,已请主R 代跑测试矩阵**:主R 那发 prompt 仅 **1117 tokens**,而 `RESUME_AGENT_MAX_RAWTEXT_CHARS` 上限 **10000 字**(≈6–9 倍)、`RESUME_AGENT_MAX_RESUME_JSON_CHARS` 上限 **50000 字**(进 patch prompt,再大一个量级)⇒ **16000 只是「小输入下测通」,不是「够用」**。且同一输入两跑 reasoning 为 3425 与 ≥4000(后者被 cap 截断=右删失数据),**波动上界未知**。按台账⑤「禁无实测断言」,五值+超时**待矩阵结果再定**。推论(有据,非猜):另四处预算(诊断 2000/patch 3000/explainability 2000/追问 1200)**全部低于 4000**,而 4000 已被证明光思考就吃满 ⇒ **五个值现无一可用**。 📌 **纪律新增(采纳模面组自留教训)**:引台账一律用**内容锚点(行首日期 + 组别)**,不用行号 —— 模面组引「175 行」数小时即漂到 177。本组「符号锚点 > 行号锚点」原只用于代码,即日起**推广到台账/文档**(台账比代码漂得更快)。 📌 **组长自认(本周第三次同形态)**:派 §7 阶段零任务 2(`finish_reason`)前**未查同题在途**,与后端组在做的撞车,已整条撤回该任务并向后端组认账。本组本周刚立的派工模板 §1 正是治这个,组长自己没执行。 📋 **派工队列(zzh 2026-09-08 定:等 OC 当前两批做完再派,不并行加载)**——按可开工性排,**队列本身写进台账不靠组长记忆**:**〔在途〕**①批 5 = §1.4.20(b)(c) run 列表按简历筛选 + §1.4.4 第二层最小单元接线(分支 `feat/resume-batch5`,未推);②§7 阶段零 = 单价漏配 fail-loud(分支 `feat/resume-s7-stage0`,未推)。⚠️ **§7 阶段零原含任务 2(`finish_reason` 透出)已整条撤回**——后端组在做同一件事,若该派工已发出,须补发撤回补丁。**〔排队·无撞车,可随时派〕**③千分位候选 A 落地(`anti-fabrication.ts` + `resume-agent-extraction.ts` 同名正则同批收全角;**走 codex**,属放松 HIGH 风险闸);④match-report 跨岗位一致性校验 + 存量盘点(`match-report/`,只统计不清洗)。③④与批 5 的改动文件零交集,已核。**〔阻塞〕**⑤五个 `max_tokens` + `LLM_REQUEST_TIMEOUT_MS` 定值 —— 卡主R 代跑测试矩阵;⑥§7.4.1 三级链 —— 卡 zzh 裁链序(spec 的 `pro→flash→qwen` 三级自动链 vs zzh 最新「便宜档 flash/强档 pro + 手动候补」是两套架构);⑦`RESUME_GROUP_ID_SECRET` 生产落位 —— zzh 本人,等彩排。 📌 **组长自认(派工负载)**:B1 解锁当天,在批 5 仍在途时又追派 §7 阶段零,**未先核 OC 手上负载**,由 zzh 点出。两批文件虽零交集(已核)不构成撞车,但单执行方并行两个 PR 本身抬高出错概率。**纪律补进派工模板**:派新批前除查「同题在途」外,**还须查「同一执行方是否有未合 PR」**,有则默认排队、不默认并行。 🆕 **2026-09-08 深夜 · OC 两批交付,组长逐条复核完毕(两个 PR 均 codex PASS + CI build pass + 相对 main MERGEABLE/CLEAN,尖已现取)**:**#132**(`feat/resume-batch5`,尖 `6ebdaef`;`ca03309` 任务A run 列表筛选+每项四字段 / `c05512a` 任务B §1.4.4 最小单元接线+V4 max_tokens 登记 / `6ebdaef` codex 退回修复:链断时 `resumeLabel`+`resumeGroupFirstCreatedAt` 同步置空)。**#133**(`feat/resume-s7-stage0`,尖 `cdb5836`;`b575274` 单价漏配模块加载期 fail-loud / `cdb5836` codex 补强:单侧漏检用例+非生产显式 development)。OC 自报 1351 / 1348 全绿 —— **该数为 OC 本地带 `DATABASE_URL` 的真库跑**,与本组 mock 层历史数字不同基,组长未重跑。 🔴 **合并顺序:实测结论,非推测**。用 `git merge-tree --write-tree` 对现存 OPEN PR 两两试合:**#133 × #138(后端组·强档 provider 可配)在 `.env.production.example` 必冲突** —— 两边都插在 `LLM_STRONG_MODEL=""` 之后同一锚点(`@@ -84,6`);同 PR 的 `domestic-llm.provider.ts` 反而能自动合(#133 追加在文件尾)。**其余全部无冲突**:#133×#136、#133×#139、#132×{#131,#136,#138,#139}。⇒ **#132 与任何在途 PR 零交集,可立即合、无需排队**;#133 与 #138 谁后落谁解那一处冲突(冲突面=模板文件相邻两块注释,机械可解)。**OC 原报的冲突对象(`fix/be-provider-error-truncation`)在 GitHub 已无对应 OPEN PR**,真正的对手是 #138,组长已改正。 ✅ **组长追认 #133 的 ci.yml 占位(OC 判其超工单范围,判断谨慎但结论不必要)**:亲验 `.github/workflows/ci.yml` 的 `Smoke test compiled API bootstrap` 步是**全仓唯一一处 `NODE_ENV=production` 的真启动**(该步自带注释即如此声明),而 #133 的自检恰以 `NODE_ENV === "production"` 为条件 ⇒ 不补 `LLM_*_PRICE_PER_1K_TOKENS` 非零占位,该步必崩。**占位不是超范围,是同一处改动的另一半**;两个 `0.001` 均带注释写明「非真实价格、真值由生产机 .env.production 落位」。CI build 已 pass,反证占位生效。 🔴 **OC「遗留提示」两条经复核均不成立,但各自指向一个真问题(账记组长,不记 OC)**:**㈠「派工模板仓库内不存在」= 假否定**。文件在 `docs/plans/简历agent组-OC派工模板-2026-09-08.md`,由 `fe67b74` 加入,`git merge-base --is-ancestor fe67b74 origin/main` 通过。**成因**:`git ls-tree` 默认开 `core.quotepath`,把非 ASCII 路径转义成 `\346\211\223...`,于是拿中文串裸 grep **必然空手而归**。**组长自己第一条复核命令原样复现了同一假否定**,加 `-c core.quotepath=false` 后立刻命中 ⇒ 这正是台账已立的「否定结论需正控」。**纪律补进派工模板**:①凡查中文路径一律 `git -c core.quotepath=false`;②「不存在 / 零命中 / 没人做」类结论**必须同跑一条已知会命中的正控**,正控不出数则结论不成立。**㈡「分机『转 8』降级口径 spec 未写」= 措辞不成立,但底下的漂移比 OC 说的更重**。spec 四处写了分机(:294 / :301 / :304 / :668,已带正控 `grep -c phone`=34 证明检索有效)。问题在**写反了**:spec :668 的 F8 口径是「`+86` 前缀、分机『转 8』/『ext.8』…**命中(能回推 sourceText)入库**」,而 #127 v2 加上 `PHONE_VALUE_RE` 白名单后,**只有 sourceText 带「转 8」才不受影响;value 逐字带「转 8」一律降级**。spec 通篇**没有区分 value 侧与 sourceText 侧**,照 spec 实现会得到与现码相反的行为。**这是 HIGH 风险闸(闸门①)上的 spec-码漂移,归组长回填,不归 OC**;#127 当时只就地改了代码注释,没回头改 spec —— 与本组自己立的「实现改口径必须同 commit 改 spec」是同一条,组长没执行。 📌 **契约层缺口(组长自查所得,#132 实现零偏离)**:#132 的 `take: readResumeGroupScanLimitEnv()`(默认 1000)与「未命中回空页不报错」**逐字照 契约 v1.3 §3.E.3(a)**(该条原文即写 `take: RESUME_GROUP_SCAN_LIMIT, // 上限,建议 1000`,且明写空页是为不泄露 id 存不存在)⇒ **实现无偏离,不阻断合并**。但**契约本身漏了截断的假否定**:当该用户 distinct `contentSha256` 超过 scan 上限时,落在截断外的**合法** `resumeGroupId` 同样回空页,与「猜错的 id」在响应上不可分辨,且**服务端不留任何信号**。`shaGroups.length === take` 即可判定「本次发生过截断」,成本为零,可据此打点/告警而不改响应形状。⇒ 列**契约级候补**(改契约 §3.E.3(a) + 实现补信号),**按台账⑤不对「1000 够不够」做任何无实测断言**。 🔴 **#138 已把 `finish_reason` 两支全做完 ⇒ OC 问的「§7 任务2 将来谁接」答案 = 无人需要接**:`length` → `LLM_PROVIDER_BUDGET_EXHAUSTED`(不重试),`stop` / `content_filter` / 缺失 → `LLM_PROVIDER_EMPTY_OUTPUT`(沿用不重试),五条用例齐(含「`length` 但 content 非空 ⇒ 正常返回」这条反向边界)。⚠️ **一处与组长上一封给主R 的意见相左,组长服裁**:组长据 preflight「同输入 reasoning 飘 7019/7247/13010」主张 `stop`+空输出是**非确定性**的、重试有意义;#138 裁两支均不重试(主R 已裁,PR 标题即「裁定:不重试」),**本组不再议**。但**用户文案两支必须仍分开**:`length` 支的「请稍后重试」是骗用户(同输入同预算必然同结果),只有 `stop` 支说得通。 🔴 **新收到的跨组硬约束,直接压住本组定值权(来自 #138 正文,本组尚未独立核实,须核)**:#138 引入 `LLM_STRONG_REASONING_HEADROOM_TOKENS`(默认 4096),实发 `max_tokens = 调用点 maxTokens + 该余量`,并写明约束「**调用点 maxTokens + 余量 必须小于型号输出上限(deepseek-v4-pro 为 8192)**」。⇒ 若 8192 属实,**本组 patch 档(强档,`complexity: "STRONG"`)的可用预算被压到 < 4096**;而主R preflight 实测 **reasoning 单项已到 13010**。两者**不可能同时成立**:要么 8192 不是该型号的真实上限,要么那次 preflight 跑的不是 v4-pro。**若 8192 属实且 reasoning 真能到 13010,则 patch 档不存在任何可行的 `max_tokens` 取值 —— 这不是调参问题,是型号/链路选择问题。****列为矩阵结论落定前的头号待澄清项**,已向主R 提问,澄清前本组不出任何定值。 📋 **派工队列变动**:〔在途〕①②已交付待合(#132/#133)⇒ 按 zzh「上一轮派工全做完再派」,③千分位候选 A、④match-report 跨岗位校验 **在 #132/#133 合入后即可派**;新增〔排队〕⑧ spec F8 分机口径回填(组长自己写,不派 OC)、⑨ §3.E.3(a) 截断信号(契约+实现,须先改契约)。〔阻塞〕⑤⑥⑦ 不变,⑤ 另叠上述 8192 待澄清项。 🆕 **2026-09-08 深夜二 · #132(`b2d1573`)/#133(`289a3e4`)已由主R 合入 main(均已 `git merge-base --is-ancestor` 亲验),本组 merge-tree 实测的合并顺序被采纳(#132 先合、#133 排 #138 前),ci.yml 占位追认成立**。 🔴 **推理预算定值已出件**(`docs/review/2026-09-08-简历agent组-推理预算定值.md`,`6c8b6eb`;依据主R 代跑 27 发矩阵 `docs/research/推理预算矩阵实测-2026-09-08.md`)。**口径已由主R 确认**:矩阵数字=实发总 max_tokens,#138 语义下 patch 档 `env = 实发 − 4096`、其余四档 `env = 实发`。**8192 之说被矩阵直接证伪**(patch 9 发全以 `max_tokens=32000` 发往 v4-pro,零 400、全 200/stop、ct 最大 14486),「patch 无可行取值」的担忧消解,不必回 §7.4.1 链序未决项;该数字若已写进 #138 的夹取逻辑会把 patch 压回全灭区,主R 终审时专项核。**定值(env 应填)**:诊断 20000(观测上界 12199/#19,1.64×)、可解释性 20000(11586/#22,1.73×)、追问 20000(10212/#25,1.96×;同输入飘幅 3.64× 为五点最大)、**patch 20000**(实发 24096 = 观测上界 14486/#14 的 1.66×,再减 4096 headroom)。三个便宜档取同一值是刻意的:`max_tokens` 是上限闸不是预算分配(计费按实产),给宽不额外花钱,统一取值可消掉「把值配错调用点」这一类错误。**patch 三条硬约束**:①不得早于 #138 生效(今日 STRONG 仍路由 qwen/DashScope,先调 env 会被渠道拒收 ⇒ `buildCheapFallbackRoute()` **静默切 v4-flash**,配置改动伪装成路由变更且落在扣次链路上);②`env = 实发 − 4096` 仅在 `LLM_STRONG_REASONING_HEADROOM_TOKENS` 保持默认时成立;③#138 若把 8192 写成夹取逻辑,本值失效。 🔴 **抽取档明确不定值,且查出本组迄今最严重的一条生产假绿**:①上界右删失——10000 字合法上限档 3 发有 1 发连 `max_tokens=32000` 都截断(finish=length → `parseExtractionOutput` 抛 INVALID_JSON),6000 字档最大一发 31525 也已贴 cap,**上界在 32000 之上没测出来,按台账⑤ 不给数字**;②**更重的一支——静默残缺**:#7/#8 两发 `finish=stop`、parse 通过、**零告警**,但 `content_len` 只有 **1998 / 5644 字符**(同探针下 6000 字输入的 #5/#6 输出 30462/30275 字符)⇒ **一份万字简历"成功"抽出两千字符**。根因:`parseExtractionOutput` **校验形状、不校验覆盖率**。截断至少会抛 INVALID_JSON,这一支连错都不报 ⇒ **不是测试假绿,是生产假绿**。**处置三条并行**:(a) 追加探测 10000 字档 `max_tokens=65536`(测 API 是否收 + 模型物理上限)——**已向主R 开单**;(b) 🔴 **抽取覆盖率闸**(以闸门① 已算出的 evidence span 并集 / `rawText` 长度为覆盖率,低于阈值降级到确认屏 + 打点)——**本组自办、不依赖 (a),无论预算定多少都要有,已入队**;(c) 万字简历这条路的产品决策(分块抽取 / 降 `RESUME_AGENT_MAX_RAWTEXT_CHARS` / 抽取改走强档,三选一)——**超出本组自裁范围,已上报 zzh**(2000 字是唯一实测安全档,而真实简历普遍 3000–8000 字,降上限会挡掉正常简历)。 🔴 **超时给建议值但明确不自裁:`LLM_REQUEST_TIMEOUT_MS` 建议 360000(观测墙钟上界 253.5s/#12 的 1.42×;27 发里 26 发超现役 30s)**,但三条后果均非本组能拍,且全部实测/亲验非推断:**(a) 它是全局变量**——`apps/api/src` 内非测试消费者 **10 处**(interview/match-report/jd-analysis/resume-tagging/interview-recordings/ai-gateway…),本组改它=替所有组改;**(b) 诊断租约 TTL 随它线性放大**——`resume-agent-diagnose-lease.ts` 的 `ttl = 2.0×[(maxRetries+1)×timeout + backoff]`,实跑:30000 → 183000ms(**3.0 分钟**)、300000 → 1803000ms(30.1 分钟)、360000 → 2163000ms(**36.0 分钟**),run 中途挂掉即被锁 36 分钟;**(c) 重试相乘且这条链路同步等**——`AI_GATEWAY_MAX_RETRIES` 默认 2 ⇒ 单次最坏墙钟 30s→1.5 分钟、360s→**18 分钟**,而 `createRun` **同步 await 抽取**(`finishRunCreation`),用户 POST `/runs` 就在等。⇒ **timeout 提值必须与「诊断租约 TTL 加绝对上限」+「本链路 maxRetries 降到 0 或 1」同批**,否则只是把一种全灭换成另一种。**另有跨组前置(部署安全组)**:360s 的 HTTP 请求要求网关/ALB/nginx 超时同样 ≥360s,否则上游先断、应用侧 timeout 根本轮不到生效,表现为「改了没用」。 📌 **矩阵件自述局限已采纳,不做反向修正**:合成语料重复度高于真实简历、数字方向偏乐观,真实简历只会更差。本组据此**不上调也不下调**定值,而把风险收在覆盖率闸上——**用运行时信号兜住测不准的部分,比猜一个更大的数字诚实**。 🔴 **主R 登记的小件已复现,并查出它不止一处、跨两个组**:`resume-agent-p2.service.test.ts:868` 的 `assert.equal(result.modelRoute, "deepseek:deepseek-chat")` 写死便宜档默认模型名。**亲验**:不设 `LLM_CHEAP_MODEL` 时全仓 1356 绿(`DATABASE_URL` 未设,真库 suite 自跳过,无真库覆盖);设 `LLM_CHEAP_MODEL=deepseek-v4-flash`(**zzh 已定的生产真值**)后 **1354 绿 / 2 红**。第二处在**后端组**的 `ai-gateway/ai-gateway.service.test.ts`(同形态:断言一个由 env 派生的 route.model)。⇒ **CI 今天绿只是因为 CI 没设这个变量**;这两条会在「把 zzh 已定的型号配进任何环境」当天同时红,**不是简历组一个组的事**,已提请后端组在 #136 内顺手带走(其 PR 已在改该文件,零额外成本)。本组这处**按主R「小件随下批」+ 本组模板 §1 排队**——#136 正在改同一个测试文件,不并行动手。 📋 **队列更新**:〔可立即派〕③千分位候选 A、④match-report 跨岗位校验(#132/#133 已合,前置解除);〔新增·本组自办〕⑩ **抽取覆盖率闸**(§1.2(b),不依赖任何人)、⑪ `p2:868` 模型名 env 隔离(等 #136 合);〔已解锁待落〕⑤ 定值三档(诊断/可解释性/追问 = 20000,可立即)、patch 档等 #138;〔上报待裁〕⑫ 万字简历产品决策(zzh)、⑬ 超时提值三件套 + 网关前置(主R + 部署安全组);〔仍阻塞〕⑥ §7.4.1 链序(zzh)、⑦ `RESUME_GROUP_ID_SECRET` 落位(zzh,等彩排)。 🆕 **2026-09-08 深夜三 · 定值件 v2(`9dba4b7`)+ 三条外部输入落账**。**①抽取档已定值 60000** —— 主R 按本组 §1.2(a) 开单加测 `max_tokens=65536`,三发零 400 零截断(对照 32000 档 3 发 1 截断),10000 字合法上限档观测 ct 上界 = **实数 36772**(#9′),右删失解除。算式 `36772 × 1.63 ≈ 60000`,与其余四档 1.64/1.73/1.96/1.66 同一余量带;**不取 65536**(那是本次实测到的 API 天花板,取到顶=将来任何模型/渠道变动直接击穿且无余地,60000 之上仍留 9% 已验证空间)。**初版「不定值」结论就地修订,并在件内写明:方法没变,变的是数据** —— 当时确实无观测支撑,不是判断错。**②🔴 覆盖率闸设计已出并经主R 裁定升为与批1 同级**:追加探测里 **#7′ 再现静默残缩**(`finish=stop`、parse OK、content 仅 5738 字符/万字输入,另两发 36560/39422),**1/3 复现率与 32000 档一致 ⇒ 预算修的是截断、修不了有损输出**;本组「覆盖率闸不依赖探测结果、无论预算定多少都要有」的判断被主R 独立验证两次。**判据选型**:用「产出/输入 字符比」,**不用 evidence span 并集** —— 后者要先定义「简历里哪些内容本就不该被抽取」(页眉/分隔线/JD 噪声),那是个新的争议面,字符比不需要任何新定义。**可分离性实测(非估计)**:残缩 #7=0.20×、#7′=0.57×;健康 #8′=3.66×、#9′=3.94×、#5=5.08×、#6=5.05× ⇒ **最差健康与最差残缩隔 6.4 倍**。**健康态必然 >1× 是结构性的**:抽取产出里每个字段都带 `sourceText` 证据片段(闸门① 的 `sourceText ⊆ rawText` 硬校验依赖它),即原文片段被回显、再叠 JSON 结构 ⇒ 产出必然长于输入,故 `<1×` 不是经验阈值而是结构性不可能。**阈值取 1.0×**(距最差健康 3.66×、距最差残缩 1.75×),**但明确标注为「首版保守值、样本 6 点」,不得表述为已验证足够**;同批把该比值记进 `stateContext` 积累真实分布后重定。**触发动作不是 400 也不是重试**:记 `stateContext.extractionCoverage`、run 照常停 `CanonicalDraft`(不改 FSM/不扣次/不失败)、确认屏带 caveat「这份简历较长,我们可能没有读全,请重点核对是否有整段经历缺失」、打点前缀 `RESUME_AGENT_EXTRACTION_COVERAGE_LOW:`(与既有 `RESUME_AGENT_KNOWLEDGE_DEGRADED:` 同族)。🔴 **比值与两个字符数只进 `stateContext` 与告警前缀,告警串不得出现 `reason`/`matchedText`/原文片段**。**③zzh 已拍 (c) 万字简历处置:整读为主 + 超长按语义模块分块**(原话「正常读;时间长就按模块拆——比如某段经历、一个公司的经历放一块,保证上下文连贯,总结归纳以后合并再理解」)⇒ 本组初版列的三选一**不是三选一,是「先 A 再 B」的组合**:默认整读(65536 已证整读路通)、超长/耗时档按**语义单元**分块(公司/经历为单位**不拆散**,归并后统一理解)、**阈值本组按实测墙钟+真实分布定**(观测:万字整读墙钟 128–187s)。🔴 **「不做机械字数切」是硬约束不是建议** —— 按字数切会把一段经历劈成两半,正是 zzh「保证上下文连贯」要挡的;**分块阈值本件不给**(真实简历分布尚无观测,按台账⑤ 不做无实测断言,先用覆盖率闸记录的比值与墙钟积累分布)。**覆盖率闸不受本裁决影响照做**(主R 指出:残缩 1/3 是整读与分块都要兜的,分块不消灭它)。 📌 **批3 超时按主R 裁定拆三方,捆绑成立**:本组 = **诊断租约 TTL 加绝对上限**(可立即写);后端组 = gateway `run()` 加 per-call `maxRetries` 覆盖(已由主R 知会);部署安全组 = 网关/nginx 超时 ≥360s 进彩排 checklist(已转)。**三方齐后同批生效**。**残留如实记(主R 已裁不阻断批3)**:三件全落地后 `createRun` **同步等待最坏仍是数分钟级**,属产品议题、与 §1.2.2 同场,不在定值件解决。 📌 **`p2:868` env 派生断言不再单独排队** —— 主R 裁定与后端组 `ai-gateway.service.test.ts` 那处**一并并入 #136**(该 PR 正在改这两个文件,零额外成本;断言对齐 env 属机械件)。本组**知情即可**,队列 ⑪ 撤销。本组对照实测的两组数字(不设 `LLM_CHEAP_MODEL` 1356 绿 / 设生产真值 `deepseek-v4-flash` 1354 绿 2 红;`DATABASE_URL` 未设故无真库覆盖)已被采纳为该裁定依据。 🆕 **PR #147(`fix/resume-unwired-todo-criteria`)已开,待五关** —— **起因是前端组的一条判据批评,本组采纳后回码查出比批评本身更重的问题**。前端组指出本组 TODO 的核对方式写的是 `grep -rn "deriveResumeLabel"` 应出现非测试调用点,而「grep 命中数只证明有人调了它,证不出调对了地方」。回码两件:**㈠那条 TODO 已是假陈述** —— 它写「全仓零生产调用点」,而 #132 已把 `deriveResumeLabel` 接进 `listRuns`;**㈡实测比批评更糟** —— `listRuns` 的 `resumeLabel` 原有两条用例**断言的都是 `null`**,做变异:保留 null 语义、只把非 null 返回整体换成 `"WRONG-LABEL"` ⇒ **该文件 9/9 全绿**,即**每个 run 都返回错标签零告警**,而这正是要显示给用户的字段。已补三级回退取值用例(`basics.title` / `experiences[0]` 的 `company · role` / `education[0].school`)。**三次变异全部实跑并还原**:变异1(换常量)补测前只红 null 分支=**红错地方**;变异2(仅换非 null 返回值)补测前 **9/9 全绿** → 补测后红在本条断言;变异3(打乱三级回退顺序,title 降到最低)亦红。实现文件 `resume-agent.service.ts` **零改动**(`git diff` 为空)。其余四条同形态 TODO(`isInterruptState`/`INTERRUPT_STATES`、`allowedEvents`、`canTransition`、`RESUME_PROFILES`)判据一并改行为级 —— **不是一处手滑,是本组写那份未接线登记时的系统性写法,这笔记组长账上**。1357 pass / 0 fail(**`DATABASE_URL` 未设,真库 suite 未注册,不含真库覆盖**);tsc 0。 🔴 **前端组请求在 run 视图暴露 `freeTier`,组长裁「批准但改名改形状」,并已上呈主R 请确认改法**:①**不给 `freeTier` 给 `exportAllowed`** —— 暴露决策不暴露决策的输入,导出闸规则将来变化时前端不用改;②**不做常驻顶层布尔,按状态挂载**(与 `diagnosis`/`variantId`/`exportRef` 同一套路),因为 `freeTier` 是**诊断那一刻**才写进 `stateContext` 的(亲验 `resume-agent.service.ts` 的诊断写回段),诊断前给常驻布尔只能是 `false`,而 `false` 语义上等于「能导出」⇒ **那是会翻转的谎**;**契约须写死「字段不在 = 尚未确定,不得默认为允许」**;③**判定抽纯函数,导出闸 `assertExportAllowedForTier` 与 run 视图共用同一个**,并钉断言「视图说 allowed ⟺ 闸不抛」,否则最坏是视图说能导出、点下去 403,比现状更糟。🔴 **本项要动本组自己写死的契约红线**(v1.3 §0.2 第 2 条「§3.1 run 视图 v1.3 不再往里加任何字段」,且该条自带「v1.2 泛化过一次导致红线松动、本版恢复并写死」的注)⇒ **裁法=出契约 v1.4,点名 `exportAllowed` 一个字段开口,随即把门重新关死,不再泛化成「标量都可以加」**(泛化正是 v1.3 在纠的错)。**两条支撑**:run 视图**今天已隐式泄露档位**(`view.diagnosis` 的 `lockedAxes` 只在 `freeTier === true` 时出现,亲验)⇒ 非新增信息披露,只是把「靠猜」变成「明说」;且已提醒前端组**不要拿 `lockedAxes` 去推**(它是响应裁剪的副产物,裁剪逻辑一变判断就悄悄错且不报错)。**顺序不得反:契约 v1.4 先出、实现随后**,否则又是「实现先跑、契约追认」。 📋 **队列(2026-09-08 深夜三)**:〔可立即派 OC〕**批6 = 四档定值(抽取 60000 / 诊断 20000 / 可解释性 20000 / 追问 20000)+ 诊断租约 TTL 绝对上限**(两件均小、机械、规格已定;⚠️ #136 在途且同改 `resume-agent.service.ts`,已核区段不重叠——五个 helper 在文件尾部约 3004–3024,#136 改的 `buildCheapFallbackRoute` 在约 3063,派工时要求 OC 推前用 `merge-tree` 复核);**批7 = 抽取覆盖率闸**(规格见定值件 §1.2.1,组长已定全部判据与动作,OC 只执行);〔排队〕③千分位候选 A、④match-report 跨岗位校验;〔组长自办·设计件〕⑭ **契约 v1.4(`exportAllowed`)**、⑮ **超长简历语义分块 spec**(zzh (c) 裁决落地)、⑯ **spec F8 分机口径回填**(HIGH 风险闸上的 spec-码漂移,组长的账,已欠两轮);〔等前置〕patch 档定值(等 #138)、批3 超时(等三方齐)、§7.4.1 链序(zzh)、`RESUME_GROUP_ID_SECRET` 落位(zzh,等彩排);〔已撤销〕⑪ `p2:868`(并入 #136)。 | 2026-09-08 |
| 部署安全组 | 🆕 **2026-08-31 两件**:①**M5 Z1/Z3 全闭环**——Z1 更正(主账号 MFA 核实后发现早已绑定,08-26 判断有误,三处文档已订正+badcase 已立);Z3 正负向验收已跑(正向3/3、负向2项实测拒、1项被业务规则先挡、1项文本判定),**验收中揪出策略缺口 v2→v3 补 `vpc:Describe*`**(控制台加规则弹窗隐性依赖,差点让 SSH 开关这个核心动作被自己的策略挡死;教训=正向验收必须真跑控制台)。**M5 只剩企微 webhook(待 zzh)**。②🆕 **微信小程序方案**(zzh 当日立项,此前从未立项):5 路官方原文调研 → 自审 → **对抗审 REJECT → 已全数回填**(`b54875a`)。**对抗审推翻了初稿两条结论**:安卓费率其实有官方表(**当前 1%**,⇒ 费率是小程序的**优势**不是劣势)、**导流回官网付费是明文违规**;另补两块漏掉的强制工程量(内容安全接口/隐私保护指引)。**🔴 组长自审失效已立 badcase**:三处最贵漏洞的证据全在自己已下载的文件里,自审却只抓了措辞层面的问题,已立三条闸(核心=否定性断言必须穷尽检索再下)。**两轮窄复核已过**(第一轮仍 REJECT=决策界面没跟上+回填新引入过冲;第二轮 **PASS-WITH-FIXES**,必修 1 条已改,`2458d62`)。**待主R 预审→codex 终审→zzh 拍 W1/W1b/W2/W3**。 **M1–M5 五节 spec 已交付待主R终审**(`docs/superpowers/specs/2026-07-12-部署安全组-优化spec.md`,约 1926 行)。M1–M5 谈话全收口。**本轮已动生产四处**:DNS A 记录(I3 清零)/`.gitignore` 补证书私钥拦截/RDS 备份周期每天→周一三五日(压回 40GB 免费额度,保留天数受系统硬约束不可降)/runbook 多处回填。**已闭环**:备案号取得(`京ICP备2026039830号-1`)、主账号无 AccessKey(Z2)。**新发现 P0 三条**:①`JWT_SECRET` 漏配静默用 `"change-me"`(后端修)②OSS 内网 endpoint 会把签名 URL 一起变内网→ASR/简历下载必挂(后端修)③nginx 12m vs 录音 25MB 契约冲突(本组 M2 修)。**已回后端组四件基建**(`docs/review/2026-08-26-部署安全组-致后端组回执.md`):①BI 角色 SQL 现在执行会报错(`AnalyticsEvent` 无 `phone` 列,须其迁移先落)②`WECOM_ALERT_WEBHOOK_URL`/`SYSTEM_OPERATOR_USER_ID` 全仓零命中=尚未落码,gate 未成立③`.env.production.example` 支付变量确实 0 个(本组真实遗漏,认;但"记录不实"不成立——brief 记的是 `.env.example` 且实测恰 13 条相符)④凭证传输=zzh 经 VNC/SSH 直接落位,禁 IM/邮件/贴给我。**下一步:开 M6(支付 go-live 基建与资质)谈话**,③的模板补位归入 M6。悬空:①M1–M5 spec + #23 待主R;②X1/X2 待主R派工;③工商变更(zzh+法人,备案硬前置)/软著待提交/MFA+ram-ops 待 zzh;④🔴部署彩排未开始;⑤企微 webhook 待 zzh | 2026-08-26 |

| 法务组 | ⏸ **额度耗尽暂停(2026-08-26)。已推 main `cf90b84`+**。**zzh 六项裁决已落档**(③,2026-08-26):①consentToFlywheel=**B+C** ②debrief=**开** ③X-6=**B+C** ④X-8=**A+B**(自助入口+显示7工作日+自动邮件+人工处理)⑤W-3=**24个月**(待知会后端)⑥用户协议+PIA **均归法务组牵头**。**zzh 另拍:codex 终审走 B——回填完再送**(不带 REJECT 送审)。**一条待 zzh 回**:①中「明面上处理暗地保存」一句已由法务组退回,给了 D1 正式授权的替代方案(③,待回)。<br>**⚠️ 接手照这个顺序干(每条的 file:行号全在②本组块第二轮对抗审那行)**:**第1步 回填 L-2**(判 REJECT,必须重出):🆕**语料留存条款**:rawQuotes **已可落数字=90天 + 形态乙(逐条TTL)**(zzh 2026-08-29 拍);措辞须与形态乙对齐(写精确天数,不写「定期清理」);F-L2-1 搜索词改两层表述+撤✅、F-L2-2 权利章节三处(查阅权说小了/@Delete 解读错/**补 X-12 简历原件永久留存**)、F-L2-3 失败路径 error 带渠道响应体、F-L2-4 补 IP与日志披露、F-L2-5 挂 PIA 指针;**同时按 zzh 裁决改**:W-3 填 24 个月、X-8 按 A+B 写(自助入口+7工作日+邮件+人工)。**第2步 回填 L-4 两条必修**:F-L4-1(A列「敞口=0」被自家前置④证伪)、F-L4-2(「与开关无关」过度主张须收窄);另 F-L4-3~6 应修,**其中 F-L4-6 因 zzh 已裁 debrief=开、X-6=B+C,组合bug风险已消解,改为记录该依赖关系即可**。**第3步 五处同源改 X-10 证据双标**(F-CH-1,底稿§六之六/L-1①2b/L-1②:158+闸②(e)/L-1③配套项4/L-4§3.1):改为「应用层确无(置信高)/基础设施层(DB清理·归档·**备份副本**)本组未核实,『永久留存』作最坏情形假设」。**第4步 改 X-11 口径**(F-CH-2):孤儿音频**与 debrief 无关**,`uploadAudio` 上传即 return 不写 DB,**`startPractice` 额度不足先抛后建行(:155-160 vs :162)今天就在产孤儿**。**第5步** L-1①2b 两处微调(F-CH-3);🆕**宽 grep 命中数不可直接当结论**,须严格词边界(模面组 53 处全为 bullets 对 bull 的子串误配);报计数一律带检索范围前缀;**并统一计数措辞**:凡「命中 N 处」一律写成「生产代码(`apps/**`/`scripts/**`)内 N 处,不含编译产物」(模面组 2026-08-30 登记)。**第6步** 按裁决更新 L-1③(B+C已定,不再是待拍)与 L-4(debrief=开)。**第7步** 三件新活:用户协议全文起草、PIA 报告(需后端配合出数据处理活动清单,该清单不存在)、知会后端 W-3=24个月。**第8步** 扫七份文件「起草员回问」标结案 + 公约 checklist 过表记②。**第9步** 全部毕 → 按 codex 送审规范(自包含+中立不给倾向)备材料 → 经 zzh 转 codex 终审。**悬空**:L-1② 是否属 spec 级须送 codex,认定权在主R/zzh,法务组不自行认定。**Q5 铁律**:本组产物一律不构成放行 | 2026-08-26 |

## ② 交付状态(冲突以本节为准)

### 主R / 全局(上线关键路径)
| 卡/任务 | 状态 | 产物在哪 |
|---------|------|----------|
| 交付公约 | ✅ v1.2 生效(2026-08-26,spec 终闸=codex 经 zzh) | `docs/交付公约.md` |
| 组长复盘轮(#21) | 进行中:各组 brief 已含变更承接,spec 陆续交主R 终审 | `docs/superpowers/specs/`、`docs/review/` |
| 🔴 前端接新 Agent 页面 | 设计稿已全量入库(#76 已合);FE 按批次推进:批0✅(#79)/**批1✅(PR #91,2026-08-31 主R合并:共享缝五关+四条前提全执行——web镜像实跑exit 0/合并树test全绿/main在前/1.B.2按组内验收记未闭合)**/**R8✅已合(PR #121→d3e13fd,2026-09-08,codex 窄复核 PASS+主R 机械四关,1.B.2 仍未闭合)**/批2-5等他组口径(游客态待zzh裁、简历M1未终审、模面评分契约、支付products接口) | `docs/superpowers/specs/2026-07-12-前端组-优化spec.md` §七 |
| 🔴 部署彩排(真 ECS + 托管数据层跑通 compose/迁移/健康探针/主链路) | 未开始 | runbook:`docs/部署/生产环境清单-阿里云.md` |
| 原型污染修复(resume-agent corrections 路径,#45 预存) | ✅ **已销账**(PR #118 合入 c008df2,2026-09-07 主R 五关+独立探针+变异亲测;详见④深夜合并批) | `docs/review/2026-09-07-主R-简历组两批直推事后审报告.md` 🔴 节 |
| 后端 #106–#110 五件 zzh 直合·事后补审 | ✅ 回件已裁(2026-09-07 深夜):#108/#109/#107 闭合;#106/#110 待回填(🔴 限流真 Redis 用例+🔴 embed/ASR 纳入限流圈=**发布前必修**,另 /health 摘 detail+商品存量改值 runbook) | ④ 2026-09-07 续2 行 |
| 微信登录 | 卡微信开放平台 AppID(zzh) | — |
| D5 录音转写落库法务 / 简历评分 JD-less 独立分 | 待 zzh/法务 | — |
| 塔塔职类树参照 | ✅ 已交付 | `docs/research/竞品塔塔网申-职类树-2026-08-06.{md,json}`(c9d6fde) |
| 🆕 App 市场三端准备(鸿蒙/iOS/安卓)#23 | 立项(zzh 2026-08-15 拍板):阶段0 部署安全组出《三端上架准备方案》,G4 方案先行;开发本体不抢 web 上线关键路径 | 派工提示词经 zzh 转部署安全组;方案将落 `docs/部署/` |
| ⚠️ 私有岗位夹 `JOB_BOARD_PRIVATE` 零实现(法务组核实、主R 复核 schema 零命中) | zzh《岗位库合规开关方案-2026-09》§三 把它标「全端常开」且是 OFF 端产品闭环支点,但全仓无表无码无 owner——需从零建表+迁移(命中公约 A1② 独立对抗审)。**不卡 web 上线**(web 公开库 ON),卡 OFF 端(安卓/小程序)上架;排期与 owner 待 zzh | `docs/部署/岗位库合规开关方案-2026-09.md` §三;L-5(3823505)§四.4 |

> 截至 2026-08-15 之前的完整历史进度见 CLAUDE.md ❄️ 快照节(#1–#73 全合入明细)。

### 各组(各自建块自填)

#### 部署安全组

| 卡/任务 | 状态 | 产物在哪 |
|---------|------|----------|
| 上线批交付(#58 索引守护 / #60 采购清单+越权审计 / #61 基建方案v2 / #63 对外核对版 / #64 runbook / #66 支付回调 nginx / #67 支付运维SOP) | ✅ 全合入 main(二态:**合入 ≠ 已发布**,生产尚未公开) | `docs/部署/`、`docker/nginx/`、`scripts/check-migration-indexes.mjs`、`.github/workflows/ci.yml` |
| 非 git 陪跑(阿里云全套采购 / ICP 备案 07-05 通过 / 微信商户号 07-11 到位) | ✅ 完成 | runbook:`docs/部署/生产环境清单-阿里云.md` |
| 复盘轮 brief(现状汇报 + 变更口径并入) | ✅ 第一章完成;第二章 M1 谈话待裁决后追加 | `docs/review/2026-07-12-部署安全组-复盘brief.md` |
| 复盘轮 spec(M1–M7 七模块) | ⏸ 未开工(G4:逐模块聊完才写该节,终审过前不写实现代码) | 计划落 `docs/superpowers/specs/2026-07-12-部署安全组-优化spec.md` |
| 债务 I1–I16(🔴 I1 彩排 / I2 TLS / I3 DNS 三项上线硬阻塞) | 待复盘轮 spec 收口 | brief §1.4 |
| 🆕 **微信小程序上线准备(阶段0 方案)** | 🟡 **组长初稿完成(2026-08-31)**,自审/对抗审未跑。立项来源=zzh 当日提出「小程序没有申请」,组长核查确认**此前从未立项**(台账零命中),zzh 当场定向「①完整版 ②要收款」。**5 路官方原文调研,E3 全部标来源+核实日期,17 条未核项如实列**。🔴 四条硬发现:①**iOS 虚拟支付 2026-02 已开放**(与业界旧印象相反,官方原文;2026 费率 12%=Apple12%+腾讯5%限时减免为0)②**求职招聘类目要《人力资源服务许可证》**,官方无「备案」替代项 ⇒ **比安卓五家最严的 vivo/OPPO 同档**,而 zzh 8-29 暂缓的是较轻的备案 ⇒ 岗位库不进小程序**不是策略选择是客观拿不到**③**微信无简历/面试类目**(「面试」全文命中 0)+ **主营类目唯一且须与实际运营相符**,官方明文「选择错误可能影响代码审核结果」⇒ 选工具类目规避 AI 备案**不是免费绕道**④**两处产品流程被 API 边界改掉**:传简历只能「从客户端会话选择」(手机文件管理器里的选不了)、手机端 `saveFileToDisk` 官方原文「仅在 PC 端支持」(导出只能转发到聊天)。🔴 **最大缺口 U-1:安卓端虚拟支付费率官方页无表 ⇒ 单位经济算不出来**。**与 #23 P3 强耦合**(§11 已登通信)。<br>🔴 **2026-08-31 对抗审(Opus 独立镜头,实核 22 来源)判 REJECT,已全数回填(`b54875a`)**,**三条结论被推翻/新增**:①~~"安卓费率官方没写"~~ **是伪缺口**——官方《技术服务费》页有完整表(安卓主动支付标准 10%/**当前 1%**)⇒ **费率结论反转:安卓 1% vs App Store 25% 是数量级差距**,初稿"小程序优势不是费率"是错的;②**运营规范 14.6 明文禁止"引导至 app/公众号/h5/网站完成支付"** ⇒ **「小程序做体验、导回 toujob.com 付费」这条退路不存在**(初稿完全没写);③**内容安全检测接口(5.18「必须调用」)+《用户隐私保护指引》(未声明则传文件/录音接口直接禁用)两块强制工程量整个漏掉**。另回填 10 条🟡(拟人化互动类目无第三方备案路径⇒可能作废 W2-1、订阅制需首发90天+DAU1000、工信部复用条款仅适用存量阶段、漏负责人人脸核身、UnionID 账号打通零提及、安全评估与算法备案是两件事等)。**自审失效已立 badcase 并补三条闸**(⑤ 2026-08-31 条) | `docs/部署/微信小程序上线准备方案-2026-08.md` |
| **#23 三端 App 市场准备(阶段0 方案)** | 🟡 自审+对抗审(Fable5,PASS-WITH-FIXES 全回填)完成,**待主R 终审 → zzh 拍 P1(iOS定价)/P2(安卓收款)/P3(技术路线)+ §6 花钱清单 + §8 产品变更点**;E3:全数字带来源+核实日期,未核项 U1–U14 如实列;零注册零花钱。**公约 checklist 过表**:断点行✅/单一真相源✅(INDEX 同步)/承重实证✅(E3+代码 file:行号,后端 DI N/A 不涉码)/防假绿 N/A(不涉代码迁移)/对抗审✅(命中 A1③④)/外部闸件已登③✅/owner=本组✅/通信模板✅/显式点名提交✅/G2 三类已隔离进 §8 待 zzh✅/拍板给封闭选项✅/交付=文件+取证底稿✅/二态 N/A(不涉部署)/badcase 无新增 | `docs/部署/三端App市场上架准备方案-2026-08.md` + 取证底稿 `docs/research/2026-08-15-三端上架取证底稿/` |

#### 法务组

> **本组 Q5 铁律(所有交付物页脚固定)**:「本文件不构成放行;放行 = zzh 签字(引用本文路径 + commit),必要时执业律师复核。」承重法律结论一律标置信级别;查证不到的监管要求写「未核实」,不编。**任何文件不得以「法务组已放行」作为依据。**

| 卡/任务 | 状态 | 产物在哪 |
|---------|------|----------|
| 组建 onboard | ✅ 完成(`801c4da`) | 台账①法务组行 + 本块 + ③ |
| **取证底稿**(L-1/L-4 的唯一事实底座,8 条硬事实回码亲验带行号) | ✅ 完成(`2c4c61f`);X-6/X-7/X-8 为组长自审时补核 | `docs/legal/2026-08-26-法务组-录音链路取证底稿.md` |
| **L-1① 录音知情同意与授权文本草案** | ✅ 完成(`6ca5c97`);A/B两套×层1/层2 四段分离,每句承诺带行内批注标依赖哪条代码事实;留存条款出甲/乙两版 | `docs/legal/2026-08-26-法务组-L1-录音知情同意与授权文本草案.md` |
| **L-1② 录音三闸红线措辞合规审查** | ✅ 完成(`509d6e7`) | `docs/legal/2026-08-26-法务组-L1-录音三闸红线措辞合规审查.md` |
| **L-1③ consentToFlywheel 改判建议书(给 zzh)** | ✅ 完成(`509d6e7`);**= 台账③ 2026-08-17「主R→zzh(+法务)」待回条目的答卷,不另开议题** | `docs/legal/2026-08-26-法务组-L1-consentToFlywheel改判建议书-给zzh.md` |
| **L-3 三端上架法务面 checklist** | ✅ 完成(`509d6e7`);11 文书 + 11 主体材料(状态逐项回源查实)+ LU-1~LU-6(不重复部署组 U1–U14);**只补法务面,不重复部署安全组已取证的平台资质** | `docs/legal/2026-08-26-法务组-L3-三端上架法务面checklist.md` |
| **L-4 模面 debrief 放行建议书(给 zzh)** | ✅ 完成(`603c500`);生效前置三条(模面组修路由遮蔽 / env 属 U3 生产动作归 zzh / L-1①文本B上线并按 Q5 留证) | `docs/legal/2026-08-26-法务组-L4-模面debrief放行建议书-给zzh.md` |
| **L-2 隐私政策草案**(承接后端组 W-1~W-4) | 🟡 起草中;🔴 **scope 变更**:全仓无隐私政策正文(设计稿自标「待法务回填」)⇒ 非「改四处」而是**从零起草全文**;🔴 **X-8 是发布前硬阻塞**(行权方式是法定必备内容,而能力不存在) | 计划落 `docs/legal/2026-08-26-法务组-L2-隐私政策草案.md` |
| **L-5 岗位库合规开关方案审查意见**(审 `248432b`,zzh 2026-09-04 指派) | 🟡 **已出稿 `4312040` → Opus5 独立对抗审 = PASS-WITH-FIXES → F-L5-1~14 全部回填(见回填 commit)**。⚠️ **回填纪律**:审查方指出的承重项,组长**逐条回一手原文/回码独立复验后才采纳**(五部门通知、50号令第15/16/21/26/40条、标识办法第8/9/10条、深度合成规定第17条、292号令第3/4条、`ApplicationRecord.jobId` 非空、`PUBLIC_JOB_SCOPE` 命中面、`jobs/page.tsx:134` 文案、194/194 计数),未直接采信转述。🔴 **回填新增的头号发现(初稿漏检)**:**五部门《关于规范网络平台招聘类信息发布的通知》(2025-12-25 印发)**第一部分逐字「经营性人力资源服务机构以文字、图片、视频、直播等方式**通过互联网站、应用程序等发布招聘信息**……**应依法取得人力资源服务许可证**」——规范性文件位阶低于规章、不创设许可义务,**但它是执法口径的书面形态**,而执法口径正是本案胜负手;**最刺的一点**:该句主语是「经营性人力资源服务机构」⇒ **我方一旦按第九条完成备案而成为该主体,反而可能因此落进射程,『先办备案降风险』这个直觉不一定成立**。同通知第五部分另要求招聘信息含「招聘人数」「基本劳动报酬」等要素(`Job` 模型两者皆无)、「应标注有效期限或及时更新」(= Z-2 天数的官方锚点)、「转发招聘信息的需严格标注信息来源」(`sourceUrl` 有字段、前端未渲染)。**另两条重要回填**:①**备案不是豁免**——50号令第二十一条末句「不得以人力资源供求信息收集和发布的名义开展职业中介活动」+ 第四十条罚则,备案只让「未备案」这一项风险归零;②**Z-1 由 🔴 降 🟡 并收敛**——标识办法**第九条**给了明文合法路径(用户申请不含显式标识版本 + 用户协议按第八条写明 + 留存日志≥6个月),初稿「唯一一条会改变产品形态的合规要求」**系过度主张,已收回**。**§七.1「不构成开工阻塞」已收窄**:开关基建可开工 ≠ §三 矩阵可照执行;OFF 端上架阻塞于私有岗位夹从零建设(新表+新迁移,命中 A1②)、导出链路阻塞于 Z-1。命中 A1③(跨模块契约/产品措辞)+A1④(286 行)。**核心 = 对方案 §六.1「请法务背书定性」明确不背书**(Q5 + 非执业律师 + 客观无定论),闭环动作只有「人社部门书面口径 + 执业律师」,法务审查意见顶替不了。🔴 **纠正调研文档 `6984053` §二 一处承重事实错误**:「44 号令已废止,此为立法沿革参考」**两处都不成立**——(a) 50 号令第五十一条未列举废止任何规章,且**第二十六条正文仍在引用《网络招聘服务管理规定》第十三条**;(b) 44 号令第二条定义句原文为「为劳动者求职**和**用人单位招用人员提供的**求职、招聘服务**」,含「自建网站」形态,与调研文档记载的「为用人单位……」不符 ⇒「企业侧零服务」这条论据其引用的原文本身记错了,定性比原以为更悬。两读法并陈、均不排除,置信低。🔴 **证伪方案 §五.3**:「勾人力资源测评/培训会触发电信业务经营许可证」——50 号令全文核不到该因果链;第九条把测评/培训与信息收集发布并列为**同一批备案事项均不需许可**,而第五条第二款写的是「从事**网络招聘服务**的,还应当依法取得电信业务经营许可证」⇒ 电信许可证的真正触发点恰是待定的定性本身。**反向风险方案也没提**:产品实产出模面评分/简历诊断评分,外观像「测评」,而备案按服务范围逐项登记,**少勾 = 超范围经营**。**四条漏项**:①🔴 岗位数据来源本身干净(200 家**企业官方招聘官网**、自述不写爬虫不绕反爬,**无爬招聘平台**,是没被写出来的优势),但 50 号令第二十一条要求信息「真实、合法、**有效**」+ 第四十条罚则,而采集评估表 `canDetectClosed` 多为「不稳定」⇒ **展示已下架岗位是可预期常态**;该义务**在备案类读法下同样成立**;`Job.lastVerifiedAt`/`status` 字段已存在但**有没有执行者未核** ⇒ **X-1/X-10/X-12 同一模式的第四次**;②🔴 AI 标识只写显式,《人工智能生成合成内容标识办法》(2025-09-01 施行 + GB 45438—2025)要求**显式 + 文件元数据隐式**,且第四条末句对「下载、复制功能的文件」写「**应当确保**」⇒ **导出的优化简历会带 AI 标 = 唯一一条可能改产品形态的合规要求**,已列 Z-1 请 zzh 拍;③🟡 **大模型登记是我方主体义务**,拿模型商算法备案编号只满足微信类目材料、不等于国家层面已尽义务(「备案/登记」二分口径检索自律所文章**非法规原文**,置信低);④🔴 **「私有岗位夹」代码里不存在**(`ownerUserId`/`UserJob`/`PrivateJob`/`customJob` schema 零命中;`JobFavorite` 强依赖 `jobId→Job`)——但 `JDAnalysis.jobId` 可空且自带 `jdText` ⇒ **粘贴 JD→解析链路今天走得通**,「私有岗位夹」是**待建能力不是可开的开关**,§三 矩阵「全端常开」需改「待建,列 OFF 端上线前置」。**另**:§七.1「法务审→各组改造」按 Q5 须改为「法务出意见→zzh 签字采纳→改造」;**同时明确 §四 开关基建不依赖定性结论,各组可立刻开工,本审查意见不构成开工阻塞**。§七.2「本周丰台申报」漏前置(经营范围工商变更未完成)。**给 zzh:4 拍(Z-1 导出文件 AI 标识 / Z-2 岗位有效性机制归属与天数 / Z-3 安卓试探翻 ON 时点 / Z-4 备案勾选范围)+ 4 问(Q-1 toujob.com 是否已公开可访问——本组探测 SSL 握手失败,无法判断是未部署还是本环境网络限制 / Q-2 工商变更进展 / Q-3 是否已书面问询人社局(问法已备好可直接用) / Q-4 是否已就算法备案向 DashScope 提工单)** | `docs/legal/2026-09-04-法务组-L5-岗位库合规开关方案审查意见.md` |
| **独立对抗审 第一轮**(A1③④;对象 = commit `509d6e7`:底稿 + L-1①②③ + L-3) | ✅ 完成。结论 **PASS-WITH-FIXES,留存链条 REJECT**。证伪组长一条承重结论(X-1 过度主张)+ 挖出 X-10/X-11 两条全组漏项;正向对照:6 条核心代码断言 5 条实证成立(X-3 含 NestJS 最小复现真跑 + 交换注册序反向对照)、法规引用抽查 8 处零编造、S2 干净。**已全部回填 + 组长终检清六处残留**(`54d0dfa`) | 结论见本行 + 台账⑤ 两条 badcase |
| **独立对抗审 第二轮**(A1③;对象 = **L-2 + L-4 + 留存链条重出稿定点复审**) | ✅ **已回,待回填**。**块一 L-2 = REJECT**:数字层面几乎无懈可击(16路由/5字段/行号逐字命中/零编造法条),但**方法层面重犯 F1**——🔴F-L2-1 搜索词「仅用于本次检索」只查了应用层,而检索是 GET+query string(`job.controller.ts:17-18`)且 `docker/nginx/nginx.conf` **全文无 access_log 指令**⇒默认 combined 格式,搜索词原文+IP 逐条落访问日志、留存期无人定义;🔴F-L2-2 权利章节**两个方向都错**:(a)查阅权说"零途径"把能力说小了(简历/投递/报告/权益均可读,缺的是统一入口与对埋点AITask录音的覆盖)(b)说2个@Delete"均与个人信息删除无关"错了(`resume.controller.ts:89` 删的正是简历版本)(c)**漏掉 X-12 简历原件永久留存**;🟡F-L2-3 失败路径 `error: String(err)` 会带渠道原始响应体(`domestic-llm.provider.ts:120`),且 admin 有 `@Post("ai-tasks/:id/reveal")` 端点;🟡F-L2-4 完全未披露 IP/日志类信息(验证码防刷按 IP 计数 `sms-rate-limit.service.ts:99`);🟢F-L2-5 委托处理表缺 PIA 指针。**块二 L-4 = PASS-WITH-FIXES**(F-L4-1/2 必修后方可交 zzh):🔴F-L4-1 §4 表 A 列「debrief侧敞口=0」**被本文自己的前置④证伪**(孤儿音频与开关无关,A 与 C 同样中招);🔴F-L4-2 「与 debrief 开关状态**无关**」是过度主张——自家 §3.3(主观过错/总量)与 §4 风险栏(A低 vs B中,唯一差别就是开关)都在打架,应收窄为「X-6敞口独立于开关;开关另有它自己一组风险差异,两组不可互相抵消」;🟡F-L4-3 未验证假设标注**不对称**(削弱B的假设被降级/支持A的规规矩矩标),净效果偏向本文推荐;🟡F-L4-4 选项未穷尽,**漏的正是本文自指的根因那一侧**(收集侧:上传口与后继处理绑定/孤儿清理,独立于 debrief 与 X-6);🟡F-L4-5 缺前置⑤(**DashScope 侧留存空白**:ASR 是阿里云主动拉音频、转写任务存在受托方系统,我方 finally 只删自家 OSS);🟡F-L4-6 **与 L-1③ 的组合 bug**——L-1③ 的 X-6-B「真实面试一律走 debrief 管道」以 debrief 开启为前提,而 L-4 推荐 A 维持禁用 ⇒ zzh 若同时采纳,声明为真实面试的录音被分流到禁用端点 ⇒ **该处置方案的直接产物就是孤儿音频**。**块三 留存链条 = PASS-WITH-FIXES**:第一轮 REJECT 那条链**已改对、四份一致、未矫枉过正**(审查方独立验证应用层"确无执行者"成立、版本乙保守表述未被削软)⇒**可出闸**;但🔴F-CH-1 **X-10 证据双标**——判"转写两层都无执行者"时,基础设施层唯一证据是**同一行被判二手不可信的 runbook**(`:136`),且犯范畴错误(转写在 PostgreSQL,用 OSS 规则证明 DB 层无执行者),须改为「应用层确无(置信高)/基础设施层(DB清理作业·归档·**备份副本**)本组未核实,"永久留存"作**最坏情形假设**而非已核实事实」,五处同源同改;🟡F-CH-2 **X-11 口径窄于真实缺口**——孤儿音频**与 debrief 无关**,`uploadAudio` 上传即 return 不写 DB,至少四条路径,其中**`startPractice` 额度不足时先抛后建行(`:155-160` vs `:162`)今天就在活跃链路上产孤儿**;🟡F-CH-3 L-1① 2b 条款审查方评为"四份文件里写得最好的一条",两处微调(否定性断言加"据我们所掌握的记录"限定 / 补数据库备份副本)。**流程建议两条**:①台账⑤「存在性断言双向核」目前只被理解成"别把没有说成有",L-2 与 X-10 证明**反向同样在犯**,checklist 应明写"否定性断言必须标明**核了哪一层**";②X-1/X-10/X-12 已构成模式,建议升格独立跨模块硬阻塞。原状态:🟡 执行中。⚠️ **补派原因**:第一轮派审时 L-4 尚未回填、L-2 尚未落盘,两件均未入闸,而两者各自命中 A1③(对外口径/产品措辞),**无豁免**;另 REJECT 的留存链条重出后须定点复审(比照主R 对部署安全组 spec「F1–F8 回填后定点复审」的先例),不能以「同一作者的机械终检」替代 | 结论回填本块 |

| **模面组 · T-1 PR-GUARD(仓级迁移卫生守护)** | ✅ **完成并收尾(2026-09-02)**,锚点 `0f26259`(已推 `origin/feat/interview-impl`)。交付 = `job-direction-frozen.test.ts`(枚举冻结,从 `@prisma/client` 断言恰三值)+ `scripts/check-migration-hygiene.mjs`(语句级扫描:行/块注释含嵌套、单双引号;拦 `DROP INDEX`/`DROP COLUMN`/`DROP TABLE`/`ALTER TYPE…ADD VALUE`;时间戳单调 + 插队;**git 基线取不到 = exit 1 不是 skip**)+ CI 挂载(`fetch-depth:0` + 事件级基线 PR base / push before)+ `--self-test` **16 条**常驻。**审查链**:codex 3 轮(2 轮 REJECT:换行绕过 / 嵌套块注释)→ PASS;**组长终审再打回 1🔴+1🟡** —— 🔴 `CHECK_MIGRATION_HYGIENE_NEW_DIRS` env 注入是**静默关闸后门**(传空串即短路 `gitNewDirs`,③B 不查、基线不取,却打印「基线可取」这句假话;且全仓无任何测试消费它,未兑现其存在理由)⇒ 整个删除;🟡 自测三关键字只钉了 `COLUMN`/`TABLE`,**缺 `DROP INDEX`** 而它正是 #32 事故本体 ⇒ 补 ②-11。返工后 codex 复核 PASS。**组长已亲验**(不采信报告):后门全仓零命中、注入命令实测 exit 1、self-test 16/16、返工 diff 单文件 6+/10− 零扩散。⏳ **未闭合项**:CI 停摆到 9/1,`fetch-depth:0` + 事件级 env 表达式未在真 CI 跑过,**须以一次真实的红确认闸在跑** | `0f26259` |

## ③ 通信(要对方做事 / 要拍板 / 跨方结论;状态:待回/已回/已一致)

| 日期 | 谁→谁 | 事由 | 状态 |
|------|-------|------|------|
| 2026-09-08 | 数据组→模面组(经 zzh) | **更正收讫。两条技术断言已回码复核属实,同表迁移的协同办法照准,但补一条你们没算进去的撞点**。①**不用道歉,这条本组认账的部分本组自己担**:上游给的流程事实是假的没错,但本组把它**原样上报 zzh 还替你们搭了建议**,这一步是本组自己加的动作,`git log -S retainUntil` 一次就能证伪,本组没查。两边各记各的,不必互让。②**你们给的两条我核了**:`VoiceRecordingReview.retainAudioUntil`(`prisma/schema.prisma:793`,带 `@@index`)形状确实在;`check-migration-hygiene.mjs` 确实验时间戳单调 + ③B 防插队(`monotonicErrors`,自带正控用例)。**照你们的办法走:各自出迁移、谁先合谁在前、不合并成一个迁移**(跨组合并迁移让归属和回滚都变复杂,本组同意)。③🔴 **补一条你们没算进去的**:两条迁移的 **SQL 不会撞**(不同列的纯加法 `ALTER TABLE ADD COLUMN`),但 **`prisma/schema.prisma` 是同一个文件、同一个 `model Interview` 块** —— 两边各加一行,**后合的那方必然要 rebase 这个文件**。这不是问题,只是别把「迁移不撞」当成「零协调」:后合方 rebase 后**必须重跑一次卫生闸和全量迁移重放**,不能认 rebase 前的绿。④**本组的时序照此定**:本组的 `Interview.contentSource` 迁移**戳在你们合入之后再取**(`date +%Y%m%d%H%M%S` 实取,不手编),merge main 后实测,不认分支 CI 的绿(#99 栽过一次假绿)。等你们台账那条确切时间戳。⑤本组这边 `Interview` 加列的前置是实现批 r2 合入(r2 带旧戳迁移目录副本),**大概率你们会先合**,正好。 | ✅ 已一致(等你们知会迁移时间戳,无需其他回复) |
| 2026-09-08 | 数据组→后端组(经 zzh) | 🔴 **一个问题:`#136→#138→#143` 三层堆叠的中间窗口怎么给**。按新立的闸把 8 个 open PR 的文件表全部重拉,查出三层**都**碰本组要动的同一批文件(`production-env.ts` / `env-or.ts` / `match-report.service.ts` / `resume-agent.service.ts`;#143 另加 `interview-run.service.ts` / `interview.service.ts`)。具体担心:B1 要往 `REQUIRED_IN_PRODUCTION` 加一条,若在 #136 合、#138 未合的空档落笔,**#138 rebase 时几乎必然在这一行冲突,成本转嫁给后端组**。**本组默认处置=等整条堆叠落完再动 B1/B3/B4/B5**;若后端组愿给中间窗口,请指定落在哪一层之后,本组照办并在 PR 写明落点;堆叠若还会加层请一并告知。另同步两条实查(不用回):①`interview.service.ts` **#131 并不碰它**,在途只有 #142/#143;`interview-run.service.ts` 才是四方同文件。上一封「两个文件都三方同」不准,已订正。②#105/#104/#101/#93 与本单**零交集**,已查存档。 | 待回(不阻塞:本组默认处置已定,等堆叠落完即可开工) |
| 2026-09-08 | 数据组→简历Agent组(经 zzh) | **来件收讫 + 三条全部回码复核,结论:三条都对,并由此改了本组两处记录**。①**#132 已合 `b2d1573`、#133 已合 `289a3e4`** —— 已核,`#133` 的文件表确不含 `resume-agent/`,与本单无关。②**「后续两件是局部加法、你们先落我 rebase」** —— 收到,本组据此把 B3/B5 对简历Agent组的等待**正式解除**(不再等其后续批次),排期只按 #136/#142/#131 走。谢这条,它把 B3 的前置从两方减到一方。③🔴 **「#136 同时改 `resume-agent.service.ts` / `resume-agent-p2.service.test.ts` / `match-report.service.ts`」—— 属实,而且这条直接推翻本组撞车表**:`gh pr view 136 --json files` 复核,#136 实际触碰 16 个文件,除 env 三件外还含上述三件**外加 `resume-agent-diagnose-lease.ts`**。本组上一版把 #136 记成「只碰 `production-env.ts`/`env-or.ts`、只撞 B1」,**少记了 `match-report.service.ts`** ⇒ **B4 的前置漏了 #136**。已订正:#136 现为 **B1/B3/B4/B5 四张卡的共同前置**,优先级高于上一版判断。记简历Agent组正账(本组第三次被外部指出「撞车面查窄」)。④**写入点计数**:你们说「4 处不是 3 处」,本组回码后**两个数都对、数的是不同东西** —— 物理 `update` 到 `ResumeAgentRun.jdAnalysisId` 的是 **3 处**(`bindJd` / `createRun` 末尾预绑 / `bindJdAndAdvance` 的 `CanonicalDraft` 内联);**来源判定点是 4 处**(多出 `createRun` 的 `jdText` 分支——现造 JDAnalysis 的自贴入口,汇流到同一条 `update`)。口径已在派工单 B3 卡钉成表,免得下轮再数出第五个数。另**本组自查订正一条**:派工单原写「run 创建点 `tx.resumeAgentRun.create` 多处也写 `jdAnalysisId`」—— **实测三处 `create` 都不写**,只把它传给 `findReusableRun` 做复用匹配,是本组把「传了参数」当成了「写了库」,已原位作废。⑤行号会漂、一律文件名+符号名定位 —— 照办,派工单已立为硬规矩。 | ✅ 已一致(无需回复) |
| 2026-09-08 | 数据组→模面组(经 zzh) | 🔧 **`Interview.retainUntil` —— 这件不用等 zzh/主R,是你们自己就能加的,先例是你们自己创的**。zzh 直接质疑「这不是让模面组自己加上这个字段吗」,数据组回码核实,**zzh 对、数据组错**:`aef71ed`(模面组 T-3/PR-R+PR-E)**就是你们给 `Interview` 表加了 `lastActivityAt` 与 `redeemedAt` 两列并自带迁移**;`2a5d801`(T-2/PR-A)又给 `UsageLedger` 加了 `idempotencyKey`。另 `793d584`(后端组 Product 五字段)、`e8cb449`(简历Agent组)同形态 —— **各组给自己的表加列是既定做法,不是数据组专属轨道**。公约 **A1② 说的是「涉 DB 迁移 ⇒ 触发独立对抗审」,那是审级要求,不是审批要求** —— 它要的是多一道对抗审 + 主R 五关,不是 zzh 拍板放行。⇒ **纯加法可空列 `retainUntil` 请你们照 `aef71ed` 的老路自己出 PR**,数据组不代办也不合并迁移(分开出反而更好:各自可独立回滚,不产生跨组耦合)。⚠️ 若你们卡的其实**不是加列本身**,而是别的(例如留存天数取值该谁定、90 天清理 job 的归属、或法务口径),请把卡点重述一次 —— 那才是要 zzh/主R 定的,和加列不是一回事。🔴 **数据组自认**:上一封把这件转述成「归属在 zzh/主R、已三轮未回」并原样上报,**没有先查先例**;先例一查就翻。记数据组账。 | 待回 |
| 2026-09-08 | 数据组→模面组 / 简历Agent组 / 后端组 / 部署安全组(经 zzh) | **五方回话收讫回执 —— 四家给的关键事实全部回码亲验属实,其中两条推翻数据组自己的判断,均记对方正账**。①**模面组**:「用 `Interview.charged`、别用 `UsageLedger`」已亲验——老轨 `interview.service.ts` 的 `aiGateway.run` 入参确实**既无 `interviewId` 也无 `idempotencyKey`**,而 FE `setup/page.tsx` 主按钮走老轨、新轨按钮被 `NODE_ENV !== "production"` 包着 ⇒ **生产 100% 走老轨,流水表在那条轨上回答不了「扣没扣」**。我方原方案确实假定了「查流水表就行」,已改。窄窗口(consume 在 `aiGateway.run` 内、`charged` 在其后 CAS)已登记为既有缺口、方向是「拒了已付费场次」,不在本卡修。`interview.service.ts` 承载 100% 真实流量,已按「改生产热路径」标准写进卡。②**简历Agent组**:🔴 **`bindJd` 不是死码,是活的**——`bindJdAndAdvance` 的 `CanonicalConfirmed` 分支直接调它,已亲验。我方错因=只查控制器引用、没查 service 内部调用;若按原案在两处各判一次会**重复判源**。另**漏了 `createRun` 的 `jdText` 分支**(调 `analyzePrivate` 现造),那正是自贴入口。写路径已改为按「谁真的写 `jdAnalysisId`」数,并收敛为「凡写 `jdAnalysisId` 处同时继承该 `JDAnalysis` 的 `contentSource`」以免继续数路径;一律符号名定位不写行号。幂等短路坑与「5 条 bind 用例全是首次绑定 ⇒ 不红是因没人测过」均已写进卡。`merge-tree --write-tree` 验冲突手法采纳。③**后端组**:🔴 **#142 正在把三处内联谓词收敛成 `job-visibility.ts` 的 `PUBLIC_JOB_SCOPE(now)` 并接入 `deadline`** ⇒ 我方 B2 验收钉的是**即将消失的形状**,已改钉行为/钉出口。撞车面确实比我方查到的大一倍(我列 7 文件、对方在途碰 6 个,且 `interview*` 两文件是**三方同文件**)。挂载点确认用 `normalizeEnvValue`、**不用 `envOr`**(语义相反),trim 与剥零宽的后果有意接受。两条既有闸(`.env.production.example` 同步守护、`ci.yml` 冒烟 env 覆盖 `REQUIRED_IN_PRODUCTION`)已写进 B1 卡的同 PR 强制项。④**部署安全组**:生产配值归你们承办(上线值 `false`,彩排 checklist 第 6 项)、`.env.production.example` 放我方 PR(与后端组那条守护一致,不另派人);staging 同样拒起、`ci.yml:122` 要加行、**`docker compose restart` 不重读 `env_file` 必须 `up -d api`** 三条已写进卡与变更件。🔴 **ON→OFF 滚动窗口缺口按你们回码结论改记为「单实例拓扑下已收敛,多实例化时重新评估」**,nginx `location ^~ /api/jobs { return 403; }` + reload 已归入翻 ON 前置包。 | 已一致 |
| 2026-09-08 | 数据组→法务组(经 zzh) | **补回执(前两封确认收到,此前回执未发出,记我方账)**:①「甲」落地后复核不再是法定义务、严重度 🔴→🟢 —— 收到,**但请知悉本组不会因此停做**:M4 §4.7bis 变更件(已合 `04b50d0`)把「生产库存量新鲜度 = 0」重新确立为**放开开关的前置条件**,理由从法定义务改为产品条件,**该做的事没变**,M7 §4.2 的 `N=7` 与判据鉴别力硬约束照旧。②(a) 条「成本估计错、风险判断没错且低估了」的**分两条记法,本组同意**,不需要改口径;贵组主动区分「本组错的那半」与「本组没错的那半」而不是整条撤回,本组认为这是对的做法。③「访问有返回 ≠ 在招」升格全项目规矩,知悉。④私有岗位夹 zzh 已裁「建」+ deadline 接查询已写进 L-5 且落点后端组轨道 —— 后者本组确认**已在 PR #142 落地实现中**(新建 `job-visibility.ts` 并把 `deadline` 接进可见性),贵组那条不必再催。⑤生产库状态本组同样未核,发布闸第 3 项即为此设,已按主R 裁决把统计面放宽为「与 C 端可见性同谓词」。 | 已回 |
| 2026-09-08 | 数据组→主R(经 zzh) | **裁决执行回执**:①「放宽统计面 + 共享谓词定义」已落进变更件 (d2) 第 3 项(原 SQL 双重作废:统计面改 + 原文列名未加引号跑不起来的勘误),并写进 D1 卡。②由此 **PR #139 已请暂缓合并**(已在 PR 上留言说明),返工前置 = **#142**(要 import 它的 `PUBLIC_JOB_SCOPE(now)`,不再手写第二份谓词)。#139 里与统计面无关的部分(退出码三分、正控闸、纯函数 + 变异验证、SQL 勘误)返工版全部保留。③派工单订正照准部分已落。④**新增一件请 zzh/主R 定**:模面组指出 `Interview` 表没有留存期字段、其 90 天清理 job 卡在 `retainUntil` 加列上(转 zzh/主R 已三轮未回),而数据组正要在同一张表出迁移 ⇒ **可省一次迁移**,要不要并请定,数据组不自决。 | 待回 |
| 2026-09-08 | 数据组→后端组 / 模面组 / 简历Agent组(经 zzh) | **开工前文件清单知会(主R 条件①)**,全文 `docs/review/2026-09-08-数据组-开工前文件清单知会-后端轨道.md`。M4 §4.7bis 可发布开关的后端轨道已由主R 裁给数据组承接,故会有数据组 PR 落到你们目录:后端组 `common/env/*`、`job/*`、`jd-analysis.service.ts`、`match-report.service.ts`、`entitlement.service.ts`;模面组 `interview-run.service.ts`/`interview.service.ts`;简历Agent组 `resume-agent.service.ts`(**三处** JD 写路径,含未挂路由的 `bindJd`)。🔴 **已实查三处撞车**:#136(后端组,`production-env.ts`/`env-or.ts`)⇒ **B1 等它合并并复用其 `normalizeEnvValue`,不另写归一化**;#131(模面组,`interview-run.service.ts`);#132(简历Agent组,`resume-agent.service.ts`,且与 #136 三方同文件)。**故 B1/B3/B5 目前不具备立即开工条件**(非授权问题,是同文件在途)。按主R 条件②单独声明四项会改线上行为的共享改动,其中 🔴 `JOB_BOARD_PUBLIC` 进生产必需清单 ⇒ **生产缺该变量将拒绝启动**,需 `.env.production.example` 同步与部署侧配值,**请部署安全组一并知情并指个承办人**。请一个工作节拍内指认冲突;无异议即开工,开工后仍走分支→codex 对抗审→主R 五关。 **【2026-09-08 收口】四家均已回,回执见上方「五方回话收讫」行;三处撞车经对方补充扩为四处(#142 后端组),`interview*` 两文件为三方同文件。** | 已一致 |
| 2026-09-08 | 数据组→主R(经 zzh) | **轨道归属裁定回执 + 条件①产出**:四条件照办。①清单已发(见上一行);②四项共享改动已单独声明;③审级不降,照走 codex 对抗审→五关;④迁移时序照自案。🔴 **另报两条编制后回码查出的实况,变更件按「既有机制」写的两处并不成立**:(a) `PUBLIC_JOB_SCOPE` 在 `apps/**` 内**零命中** —— 过滤行为在,但是 `job.service.ts:117/:138/:146` 三处**内联** `status=OPEN`,**不含 `source='OFFICIAL'`、不含 `deadline`**;`findReadableJDAnalysis` 同样未落码。故 B2 验收第 5 条「ON 时仍叠加 `PUBLIC_JOB_SCOPE`」现在**测不了**,已改为「断言未删/未绕过现有内联谓词」+ 留第⑥步回补钩子;B4 首个检查点先落实际读取点。(b) ⚠️ **由此带出一条请主R 复核的**:C 端既然不按 `source` 过滤,开关 ON 后非 OFFICIAL 岗位一样呈现,而发布闸第 3 项只统计 OFFICIAL ⇒ **取证面窄于实际呈现面**。属第⑥步既有残留(台账:160 已记 zzh 知悉接受),本单不扩权,但翻 ON 前建议复核是否仍可接受。另:卡 D1 已交付 PR **#139**(CI 绿),代码不自合,等五关。 | ✅ 已回(见本表 2026-09-08「主R→数据组」行:四条件执行认可 + 复核裁决取「放宽统计面」、不前置第⑥步) |
| 2026-09-08 | 数据组→主R | **①spec 变更件已合(#134,`04b50d0`)**:M4 §4.7 公开岗位库「可发布开关」。codex 第六轮 PASS-WITH-FIXES(原话「不再退回方案」「方案可进入实现派工」「不必再送一轮」),zzh 收口「终审通过,可进入实现派工,无需再送审」并授权组长自行判断合并。⚠️ **codex 保留条款原样转达:仅文档与既有代码对照,不代表实现验收、生产回填或 OFF→ON 发布闸已通过**。②🔴 **事故自报(未进 main)**:原分支 `spec/jobdata-job-board-switch` 的 `c86ca0e` 误将 **215 个文件的树替换**一并提交,若合入会回退其他组 main 上的代码(实证 `apps/web/.../login-dialog.tsx` 486→84 行)。开 PR 前自查发现,该分支**封存不合**、仅留作 codex 审核锚点;#134 为同内容干净重导出(与锚点 `6c97110` 逐字节验空)。已核实现批分支 `feat/jobdata-impl-01-r2` **未受污染**(13 文件全在本组轨道)。⑤ 已记 badcase。③**另**:合入前按 `origin/main` 现状逐条重核了变更件引用的全部代码事实(五张表关联与删除行为 / `createRun`·`bindJdAndAdvance` 形态 / `JobController` 六端点 / `resolvePrivateSource`),全部一致;唯一补充=`ResumeAgentRun` 的 JD 写路径实为**三处**(含未挂路由的 `bindJd`),已补入文档。④🔴 **请主R 裁一件轨道归属**:变更件的「来源标记写入 + 消费改造」落在**后端组轨道**;zzh 答「那你就干呗」=授权数据组去做。拟**由数据组出 PR、走主R 五关,后端组知情**;若主R 认为应改派后端组承接,请回。**在主R 回复前数据组不动后端轨道代码**,只做本组轨道准备(`JOB_BOARD_PUBLIC` env 解析+测试骨架);四张表的迁移按既定顺序**等 r2 落地后**再动,防时间戳乱序假绿。 **【2026-09-08 收口】主R 已裁:由数据组承接,拟案照准,四条件已逐条执行(见上方回执行)。** | 已回 |
| 2026-09-08 | 主R→后端组(经 zzh) | **预算不足重试口径裁决回执:裁「不重试」,主R 原句「(可重试)」系表述错误,撤回记主R 账**(详见④)。你们「实现现行为+TODO 双口径+测试标注非永久契约」的留空手法正确——现在把 TODO 收掉即可,行为不用动;`LLM_PROVIDER_BUDGET_EXHAUSTED` 显式加进末尾注释的不重试枚举(注释与实现同步是你组自己的纪律)。事实二(默认即不重试,判可重试才要动码)对裁决有实际帮助,记正账;sub 核「重试是否同 maxTokens」不必等——裁决不依赖它(确定性同输入同预算已定论),结果出来存档即可。①简历组撤撞车件知会收到;②RUN_RATE_LIMIT_LUA 手法已有(real-redis 9 条三轮对抗审 PASS)甚好,交 PR 照常五关 | 待发 |
| 2026-09-08 | 主R→简历Agent组(经 zzh) | **v4-flash 真 key 实测回报(应你组请测,真 buildExtractionPrompt+全尺寸合成简历,报文逐字照 callDeepSeek)**:①**4000 全灭**——content 空/finish=length/reasoning_tokens=4000(吃满上限且思考没想完,reasoning 文本 8408 字符截断);②**16000 全通**——finish=stop,completion 5577(reasoning 3425+正文≈2152),content 5742 字符且过真 parseExtractionOutput(phone/experiences/skills/contacts 全对);③🔴 **另测出第二颗雷**:`LLM_REQUEST_TIMEOUT_MS` 默认 30000ms(domestic-llm.provider.ts:41),两发成功调用实测 32.9s/34.6s——预算修好后每发仍会 30s 处被掐死报 TIMEOUT,该 env 也不在 .env.production.example。⚠️ 单样本告警:同输入两跑 reasoning 波动(≥4000 vs 3425),定值前建议多打几发不同长短简历取上界。五个值+超时值按你组「实测+安全余量」定,数是你们的;:148-152 finish_reason 丢弃修复走 §7 阶段零 PR 过主R,同意 | 待发 |
| 2026-09-08 | 主R→后端组(经 zzh) | 🔴 **新小 PR 指令:强档 LLM provider 做成可配**(zzh 已拍强档=deepseek-v4-pro)。现状:`ai-gateway.service.ts:307` STRONG 路由硬编码 `provider:"qwen"`,`domestic-llm.provider.ts:59` 按 provider 分支拼报文——DashScope 与 DeepSeek(OpenAI 兼容)报文不同,光改 env 会把 DashScope 格式发给 DeepSeek。要求:加 `LLM_STRONG_PROVIDER` env(值 deepseek/qwen,默认 qwen 保持现行为零回归),STRONG 走 deepseek 时复用 callDeepSeek 分支;⑤新 badcase 一并防:V4 系是推理模型,`reasoning_content` 吃 max_tokens 预算,过小→content 空(主R 实测 20 全吃空/200 正常),各调用点 maxTokens 要留推理余量+把「finish=length 且 content 空」单独识别为预算不足(`LLM_PROVIDER_BUDGET_EXHAUSTED`,**不重试**,④同日裁决;本行原写「按可重试处理」系主R 表述错误,2026-09-08 随裁决订正)。⚠️ **PR 边界补充(简历组 2026-09-08 提出,主R 采纳)**:五个 `RESUME_AGENT_*_MAX_TOKENS` 与 `LLM_REQUEST_TIMEOUT_MS` 的**定值权在简历组**(按主R 代跑矩阵实测定),你们 PR 只做机制不填数值——`.env.production.example` 补 `LLM_REQUEST_TIMEOUT_MS` 条目时留占位+注释,不写具体数。验收=真 key 冒烟(主R 可代跑)+现行 qwen 路径零回归 | 待发 |
| 2026-09-08 | 主R→简历Agent组(经 zzh) | ✅ **B1 解锁**:zzh 已定型号并落 key——便宜档=`deepseek-v4-flash`、强档=`deepseek-v4-pro`(候补序列 glm5.3>qwen3.8max,手动切换预案非自动 failover)。主R 已真 key 实测:models 列表两型号逐字在列,v4-flash jsonOutput 冒烟过(content 逐字 `{"ok":true}`)。⚠️ 你组 §7 排期用的参数以此为准;注意 V4 系推理模型特性(reasoning_content 吃 max_tokens 预算,readExtractMaxTokensEnv 等默认值要复核留余量,「content 空+finish=length」= 预算不足不是模型坏) | 待发 |
| 2026-09-08 | zzh(自办事项,主R 代记) | 🔑 **DeepSeek 密钥过渡安排(zzh 拍板)**:当前先用 **zzh 个人账号**的 API key(落 `LLM_CHEAP_API_KEY`/`LLM_STRONG_API_KEY`,值按凭证红线由 zzh 本人 VNC/SSH 直接写进 env,不经任何中间人);**盈利后换成对公账号的 API key**——换绑动作=改 env 两行+重启,零代码改动,但记得同步查账单归属与发票抬头。⚠️ 个人 key 期间注意:账单在个人名下(报销/记账留痕)、额度告警绑个人手机。与 B1(dsv4pro/v4flash 确切型号参数,卡简历组 §7)同源,建议落 key 时一并把模型名定了。⚠️ **2026-09-08 补:同一 key 开始在 dev 机多处 .env 出现**(主库+前端组 worktree,后续可能更多)——换对公时不能只换一处,届时 `grep -rl LLM_CHEAP_API_KEY ~/bangtoudi* --include=.env` 逐一换或收敛为软链 | 🔴 换对公前长期挂账 |
| 2026-09-08 | 主R→简历Agent组(经 zzh) | **预算矩阵受理回执:27 发全跑(不裁 12,要「规模→reasoning」斜率),结果跑完另发**。你组两条方法论判断均成立并采纳:①「≥4000 是被 cap 截断的右删失数据,给不出上界」对;②1117 tokens 样本不能给上限场景背书,对——矩阵按合法上限构造(rawText 10000 字取 `assertRawTextWithinLimit`,resumeJson 50000 字取 `assertResumeJsonWithinLimit`:621 confirmCanonical 闸,下游共用;knowledgeHits=3 类×topK3、maxPatches=20、证据片段=万字全量切片+5 追问答案,逐项对实码)。🔴 **一处按实码偏离你组「统一 v4-flash」口径:patch 三档跑 `deepseek-v4-pro`**——patch 调用点 `complexity:"STRONG"`(resume-agent.service.ts:1712),强档已裁 v4-pro,flash 给 patch 定值=给永不服务该链路的模型背书;其余四链路 CHEAP 照跑 flash。preflight 首发已回:**2000 字输入 reasoning 即 7019**(此前实测 3425/≥4000,飘幅实锤你组判断),墙钟 77.7s,32000 被接受 finish=stop。另三件收到:①finish_reason 件撤回(后端组同题在途)+第三次撞车自记账,记正账;②§7 阶段零只剩单价漏配 fail-loud,已更新认知;③「后端 PR 别顺手填数字」已办——同日后端组 provider PR 指令行已补定值权边界(五个 max_tokens+超时值=你组按矩阵定,后端只做机制留占位) | 已回(受理确认:16000 自证会定错、patch/v4-pro 偏离采纳并自记疏漏、定值方法交底=按观测上界不取均值/余量再留一档/超时按上限档墙钟/每数附实测出处;**矩阵结果回执仍待发**) |
| 2026-09-08 | 主R→简历Agent组(经 zzh) | ✅ **矩阵结果回执:27 发全回,零 HTTP 错误零 429,全量数表+方法+局限已入库 [`research/推理预算矩阵实测-2026-09-08.md`](research/推理预算矩阵实测-2026-09-08.md)**。结论级观测五条:①五个现役预算值无一幸存(击穿 4.8~8.5×,全部在合法输入下);②**抽取合法上限档结构性不稳**——3 发 1 发连 32000 都截断(finish=length→INVALID_JSON),且正文分量本身非确定性(同输入 816/2605/16078 tokens),上界右删失,属你组预告的「给足预算也不稳」桶=产品处置件不是定值件;③真空输出(stop+空)实测复现 1 次(#4);④patch 无斜率(输出被 maxPatches=20 钉住,ct 稳定 9.1k~14.5k,定值不必随简历长度留余量);⑤26/27 发超现役 30s 超时,墙钟上界 253.5s(patch/pro),超时与五值必须同批改。⚠️ 合成语料重复度高于真实简历,数字方向偏乐观。定值按你组交底方法执行;EXTRACT 上限档若需 32000 以上追加探测(API 收不收 65536),开单主R 代跑 | 待发 |
| 2026-09-08 | 主R→简历Agent组(经 zzh) | ✅ **#132+#133 已合(b2d1573/289a3e4,五关详见④)+ 你信中五/六两问答复**:**(五)矩阵数字口径=实发总 max_tokens**(探针直连 API,不经 headroom)⇒ #138 语义下 patch 档 env 值=目标实发−4096,其余四档 env 即实发——你的换算方向对;**(六a)** 对,13010 那三发全是 v4-flash;**(六b)🔴 8192 之说被矩阵直接证伪**:patch 9 发全部以 max_tokens=32000 发往 v4-pro,零 400、全 200/stop,completion 最大 14486(>8192)——「patch 无可行取值」的担忧消解,不用回 §7.4.1 链序未决项;8192 疑沿用旧 deepseek-chat 文档,主R 已单独预警后端组并将在 #138 终审专项核。⚠️ 定值可开工,唯 patch 档定值与 #138 同批或之后生效(research 文档已补三处耦合节)。**登记一件回你组队列**:resume-agent-p2.service.test.ts:868 把便宜档默认模型名写死断言 `deepseek-chat`,任何配了 `LLM_CHEAP_MODEL` 的机器全红(main 亲验同红,CI 无此 env 故绿)——用例对齐 env 或隔离 env,小件随下批 | 待发 |
| 2026-09-08 | 主R→前端组(经 zzh) | ✅ **「疑似死局」裁定:不是死局,但你们撞到的是真缺陷组合,证据链全部采信**。①恢复入口存在:JdBound ∈ ADVANCE_RESUMABLE_STATES(:3222),**空身重发 POST /runs/:id/canonical/confirm 即从断点续推诊断**(confirmCanonicalAndAdvance 对非 CanonicalDraft 的可恢复态直接 advanceRun,不 409)——你们的 stuck run 现在就能用这条解开,D4 闭环可以继续;②但你们的结论「无法自行恢复」错得情有可原:advanceRun 注释承诺「重新 POST 同一暂停点接口即可续推」,而你们失败的入口是 bind,**bindJdAndAdvance 的幂等短路(:839 `run.jdAnalysisId===jdAnalysisId` 直接返回视图)恰好绕过 advanceRun**——你们实测「第三次调用诊断次数 1→1」就是它,恢复承诺在 bind 路径不成立=真缺陷,已派简历组;③JdBound+pendingAction:NONE 被你们映射「计算中」永续轮询——run 视图确实无任何「计算段已失败请 nudge」信号,契约缺口已派简历组设计,你们侧同修:**对暂停点 POST 返回的可重试 400 应给「重试」动作,不应转入轮询**(那个 400 就是恢复信号,当时已经到过你们手里);④空串七行:你们的判断全对且撞上在途件——**#136(后端组)正是修这族**(空串/零宽打穿默认值 16 处),你们手工注释+追加真值的处置正确留痕;⑤用量回执收到,~5 次便宜档,合规。调查质量记正账:三条证据全部实测、结论与证据分层、不碰界外代码 | 待发 |
| 2026-09-08 | 主R→简历Agent组(经 zzh) | 🔴 **派工一件(P1,前端组实测发现、主R 复码确认):bind 路径的失败恢复语义断裂**。事实链:诊断在真模型下会非确定性失败(前端实测 RESUME_DIAGNOSIS_INVALID_JSON 一次,重试即过——v4-flash json_object 仍会偶发非法 JSON);失败后 run 停 JdBound(设计如此);advanceRun 注释承诺「FE 重新 POST 同一暂停点接口即可续推」,但 bind 触发的失败重发 bind 会被幂等短路(:839)吃掉,**不推进**——恢复承诺对 bind 入口不成立。修法方向你组定,形状建议:幂等分支在 state ∈ ADVANCE_RESUMABLE_STATES 时补 advanceRun(绑定仍幂等,推进即恢复语义);同时契约层考虑给 run 视图一个「计算段可续推」信号(JdBound+NONE 前端现读作「计算中」永续轮询,恢复入口不可发现)。**另转两条前端实测事实**:①softPrompts 实测非空([{code:"CONTENT_TOO_SHORT"}])——批4 信号源真接进主流程,之前「空数组不得当交付」的钉子可拔;②bindJd 要 jdAnalysisId 不收 jdText(400 JD_ANALYSIS_ID_REQUIRED),写 bindJd 实现的同学需知。走 分支→PR→五关 | 待发 |
| 2026-09-08 | 主R→前端组(经 zzh) | ✅ 死局裁定回执收到:「一次观察当全称结论」五连自记+自立闸(全入口试遍或读可恢复态声明)认可;「有文案没动作——让用户执行他执行不到的『请重试』」这条自查很准,重试按钮件照办(codex 审后报);D4 走完报「调用次数+过没过 UserReview」即可 | ✅ 已一致 |
| 2026-09-08 | 主R→简历Agent组(经 zzh) | ✅ **定值件(`docs/review/2026-09-08-简历agent组-推理预算定值.md`)回执:方法与数全部照准,追加探测已跑完,右删失解除**。①批1(诊断/可解释性/追问 =20000)照准,走 PR 常规五关;②patch 档 20000+三条约束照准(与 #138 时序/headroom 联动/8192 反证均已在案);③**(a) 65536 探测结果**(research 文档已补节):三发零 400 零截断,**抽取 10000 字档观测 ct 上界=实数 36772**——右删失解除,抽取档具备定值条件,数归你组;🔴 但 #7′ 再现静默残缩(stop/parse OK/content 仅 5738 字符),**1/3 复现率,预算修截断修不了有损**——你组「覆盖率闸不依赖 (a)」的判断第二次被独立验证,(b) 覆盖率闸升为批1 同级优先;④**批3(超时捆绑)裁定:捆绑成立,拆三方**——简历组:诊断租约 TTL 加绝对上限(公式在你组文件);后端组:gateway `run()` 加 per-call maxRetries 覆盖(长链调用点降 0/1 用,小件);部署安全组:网关/nginx 超时 ≥360s 进彩排 checklist;三方齐后同批生效走五关。⚠️ 残留如实记:批3 落地后 createRun 同步等待最坏仍数分钟级,列为与 (c) 同场的产品议题,不阻批3;⑤(c) 万字简历三选一已上报 zzh,附主R 意见(暂缓三选一:预算按新上界定值+覆盖率闸兜底,真实简历分布出来再定)。你件 §3「用运行时信号兜住测不准的部分,比猜一个更大的数字诚实」——这句记正账 | 待发 |
| 2026-09-08 | zzh→简历Agent组(产品裁决,主R 转达) | ✅ **(c) 万字简历处置已拍:整读为主,超长按语义模块分块**。zzh 原话口径:「正常读;时间长就按模块拆——比如某段经历、一个公司的经历放一块,保证上下文连贯,总结归纳以后合并再理解」。⇒ 落成 spec 语义(简历组设计,走常规流程):①默认整读(65536 探测已证明预算给足时截断消失,整读路是通的);②超长/耗时档按**语义单元**分块——公司/经历为单位不拆散,分块产出归并后再统一理解,不做机械按字数切;③分块阈值由简历组按实测墙钟与真实简历分布定(观测:万字整读墙钟 128-187s)。主R 补充:**覆盖率闸(b)照做不受本裁决影响**——残缩 1/3 复现率是整读/分块都要兜的 | 待发 |
| 2026-09-08 | 主R→模面组(经 zzh) | ✅ **撤回端点用途已裁:按 (A) 建,把话说死——你组建议照准**。三条一手事实主R 抽查属实(consentToFlywheel 注释逐字含「真实面试录音永不可设 true」;语料管线纯函数不连库;sessionId=幂等锚点非外键)。裁定与护栏:①端点=履行 PIPL 十五条「便捷撤回」法定义务,现在建,小交付(端点+置 false+审计留痕);②🔴 交付说明、代码注释、台账三处**一律不得写「飞轮授权闭环」**,统一措辞「本端点闭合的是撤回能力,不构成撤回后不再取用的执行闭环——执行者今日缺位」;③(B) 的两个前提登记为独立未决项:语料素材来源定义归 zzh+法务(产品/合规),身份映射等来源定了再议(sessionId 幂等语义不许被端点实现顺手改)——**飞轮真要开始消费用户数据前,这两项是硬前置**,已挂账;④「翻没人看的 bit 不叫合规」轮到自己不装看不见+台账没读自记⑤,两件都记正账。四之两件(retainUntil 90 天/legalBasis 结构层不静默放行)方向对,照做 | 待发 |
| 2026-09-08 | 主R→简历Agent组(经 zzh) | **信路交叉说明+一件采纳**:你「定值件交付+开单+请裁」信发出时,主R 回执(含 65536 探测结果)已在路上——以下已办结,见前行与 research 文档:65536 三发零截断上界=36772/残缩 1/3 复现/超时批3 裁三方拆分/(c) zzh 已拍(见上行,整读为主+超长语义分块)。**新信息一件,采纳你的建议**:p2:868 的 env 派生断言地雷经你对照实测为**两处跨两组**(第二处在 ai-gateway.service.test.ts,后端组;设 LLM_CHEAP_MODEL=生产真值即 1354/2 红,CI 绿只因 CI 没设该变量)——**并入 #136 一并带走照准**(该 PR 正在改这两个文件,零额外成本;测试断言对齐 env 属机械件,后端组带、你组知情即可,你那处不再单独排队)。已知会后端组 | 待发 |
| 2026-09-08 | 主R→后端组(经 zzh) | **三件小事随批知会**:①🔴 **#136 顺带修两处 env 派生断言地雷**(简历组实测):resume-agent-p2.service.test.ts:868 与 ai-gateway/ai-gateway.service.test.ts 各有一处把 env 派生的 route.model 写死断言 `deepseek-chat`——任何配了 `LLM_CHEAP_MODEL`(zzh 已定生产真值 deepseek-v4-flash)的环境即红(实测 1354/2 红),CI 绿只因 CI 未设该变量。#136 正在改这两个文件,断言对齐 env 属机械件,一并带走(简历组文件跨轨,知情已达,照 #120 共享口径声明即可);②**gateway `run()` 加 per-call maxRetries 覆盖**(小件,独立 PR 或并入方便的批):超时提值 360s 后长链(简历抽取/诊断)重试相乘最坏 18 分钟,需调用点能降 0/1——简历组超时批3 的三方件之一,规格=可选参数、缺省行为不变零回归;③8192 预警(前行)与 #133 rebase 提醒不变 | 待发 |
| 2026-09-08 | 主R→部署安全组(经 zzh) | **彩排 checklist 追加一条(简历组超时批3 的跨组前置)**:LLM 调用超时将提至 `LLM_REQUEST_TIMEOUT_MS=360000`(实测墙钟上界 253.5s),**网关/ALB/nginx 的请求超时必须同步 ≥360s**,否则上游先断、应用侧 timeout 轮不到生效,现象是「改了没用」。请确认现网 nginx `proxy_read_timeout` 等三处口径并回填数值;与你们已接的 JOB_BOARD_PUBLIC 配值同属彩排硬项 | ✅ 已回(见下行) |
| 2026-09-08 | 主R→部署安全组(经 zzh) | ✅ **#150 已合(454d78a)+两点全部采纳**。①**存量缺陷定性采信**:60s < 实测墙钟上界 253.5s ⇒ 现网配置下今天的长 LLM 调用已会被 nginx 掐 504,与提档无关——「先行合入」正当,已办;②**390 不取 360 的竞态余量设计好**:应用侧先断、客户端拿具名超时错误而非裸 504,联动关系已进 conf 注释,记正账;③回调 30s 不动的正则优先级断言主R 亲验精确成立(`location ~ ^/api/payments/callback/` > `/api/` 前缀);④拓扑无 ALB/nginx 唯一网关、checklist 第 7 项含发版后 `nginx -T` 复核命令,收到。⚠️ 如实记:主R 合并时分支 CI 尚 pending(单 hunk conf 件,风险评估后先合),main CI watch 已挂,红则回滚 | 待发 |
| 2026-09-08 | 主R→简历Agent组(经 zzh) | ✅ **四件回执:全部照准,一件加钉、一件随手带**。①抽取档 60000 照准——「不取 65536 天花板」的理由成立(钉在已验证空间的 91% 处留余地);v2 就地标修订+「方法没变,变的是数据」的防误读写法,记正账;②**覆盖率闸判据选型照准**:「产出字符/输入字符」比优于「span 并集/rawText」(后者要先定义「哪些内容本不该被抽取」=新争议面+下一个 spec-码漂移种子,你的弃用理由成立);健康态 >1× 的结构性论证成立(sourceText 回显机制);阈值 1.0 保守首版+比值记 stateContext 攒真实分布再重定,照准。**加一钉**:比值分母对 rawText 先 trim/归一(连续空白/空行压缩),防贴满空白的输入撑大分母假触发;已知盲区(1.0 与健康带之间的部分丢失首版不拦)你们已自知,如实标即可;③**租约 TTL 上限使 throw 可达=机器强制捆绑:裁「崩」,不改告警**——与 #133 价格闸/production-env/支付沙箱闸同族 fail-loud 家风;「跨三组三 PR 靠流程记忆已漏三次」的理由成立,配置矛盾就该拒绝启动;④**随手带一件**:IN_FLIGHT(:1462)文案「请稍后重试或刷新查看结果」中「重试」字样与 409+租约语义相悖(前端组复裁成立,重发在租约期内只会再 409)——改为「正在处理中,请刷新查看结果」,随近批带走;⑤「复现别人报的问题时顺带跑对照」实践记正账并向各组推广——第二处地雷正是对照组带出来的,成本一条命令 | 待发 |
| 2026-09-08 | 主R→前端组(经 zzh) | ✅ **D4 闭环收账+复裁改判你们对**。①闭环实证收账:ClarificationPending→UserReview→ExportReady 全真调模型真库;10 调用/2 扣次/失败不扣成立;D4 两拍(400 UNCONFIRMED_RISKS 带指纹→回填原批重发 200)+审计 payload 无 matchedText/reason,PII 红线实证成立;②**(2) 复裁:改判成立**——RESUME_AGENT_DIAGNOSE_IN_FLIGHT 为 ConflictException(409)+diagnoseLeaseAt 租约守卫(主R 复码 :1462 确认),租约期内重发只会再 409,正确动作=刷新(getRun)轮询,不入「可重试」类;你们按此实现照准,后端侧文案「重试」字样已请简历组随批修掉;③(1) 两条 400 语义分立(HIGH=PATCH_BLOCKED 不可确认/MEDIUM=UNCONFIRMED_RISKS 可确认)与 anti-fabrication 判据对齐,收到;(3) 诊断真实 FAILED 1 次已入非确定性实证池;④证据边界如实声明(curl+PG 直证后端行为,不覆盖 UI 真点击;计费结论限 FREE run)——这种「先说清测了什么没测什么」的报法记正账;重试按钮件 codex 审后交,照办 | 待发 |
| 2026-09-08 | 主R→前端组(经 zzh) | ✅ **两件回执:60s 闸已办结(又一次信路交叉),规格修订照准**。①**nginx 60s 读超时今天已修**:#150 已合(454d78a,main CI 绿),/api/ 60s→390s——部署安全组同样把它定性为存量缺陷先行合入;390=应用侧 360s+30s 余量,保证应用先断、客户端拿具名错误不再是裸 504;支付回调 30s 不动(正则优先级亲验)。你们「预算值和 60s 闸一起定」的建议实际已按此执行;②**「断连不止损+照常扣次落库」链路分析采信记正账**:504 后服务端跑完→用户看到失败实则成功已扣次→重发撞 409——这是「刷新优先」的第三条独立证据,且在 #150 生效前的窗口内仍是现网实况;误导文案自认自改,记正账;③**第③项规格修订照准,OC 派工不打回**:三分类与代码实况逐条相符——真失败四码(租约已释放+未扣次,你们 E2E 亲证失败不扣)=重发安全;IN_FLIGHT 409=刷新(已复裁);504/超时=刷新优先(服务端大概率已完成+已扣,重发=为同一件事再扣一次的风险)。⚠️ 补一句设计余量:#150+360s 落地后「nginx 先断」窗口关闭,但网络层断连仍在,刷新优先的第三分类保留不是过渡方案是长期正确 | 待发 |
| 2026-09-08 | 主R→简历Agent组(经 zzh) | **一句话知会:超时批3 的网关前置已解除**——#150 已合(454d78a,main CI 绿,/api/ 390s=360s+30s 竞态余量,部署安全组按存量缺陷先行)。批3 只剩两件:你组租约 TTL 上限件(裁「崩」已照准)+后端组 per-call maxRetries 件(已知会)。另前端组对 504 窗口的链路分析(断连不止损、已扣次却见失败)值得进你组 §7 文案语料:超时类文案不写「请稍后重试」,写「请刷新查看结果」 | 待发 |
| 2026-09-08 | 主R→后端组(经 zzh) | 🔴 **#138 送审前预警一条(实测反证,非审):PR 正文「deepseek-v4-pro 输出上限 8192」与官方 API 实况不符**——主R 矩阵 9 发 patch 全部以 max_tokens=32000 直发 v4-pro,零 400、全 200/stop,completion 最大 14486(>8192);8192 疑沿用旧 deepseek-chat 文档。**若该数字已写成校验/夹取逻辑,会把 patch 档钉死在错误约束上**(调用点值+4096 余量 <8192 ⇒ patch 实际可用预算被压到 ~4000,恰好落回预算不足全灭区)。请在送审版里改为「以官方 max_tokens 文档值/实测为准」或去掉硬校验;主R 终审时专项核此处。原始数据:`research/推理预算矩阵实测-2026-09-08.md` | 待发 |
| 2026-09-08 | 主R→模面组(经 zzh) | ✅ **#131 零流量披露收到,记正账**:「新轨按钮 NODE_ENV!=production 包裹、生产不渲染 ⇒ 本轮 T-6/T-8a/T-8b/T-10 生产零流量,价值等前端切主按钮兑现」已写 PR 正文+送审包,防「特性已上线」误读——这类「防止别人把我的交付读大」的主动披露正是公约要的方向。#131 在主R 审队列,按送审包排 | 待发 |
| 2026-09-08 | 主R→数据组(经 zzh) | ✅ **开工前清单回执+复核裁决**。①四条件执行认可:清单已发四方、四项行为变更单独声明、三处撞车实查(#136/#131/#132)结论正确——B1/B3/B5 等对应 PR 合入后 rebase 开工(#132 今日已合 b2d1573,B5 的对手只剩 #136);#139(D1 证据脚本)已入主R 审队列。②**🔴 复核裁决:取「放宽统计面」,不前置第⑥步**——发布闸第 3 项的统计面从「仅 OFFICIAL」放宽为**「与 C 端可见性同谓词」**(现状即 status=OPEN),且**必须与 C 端查询共享同一谓词定义**(常量/函数引用,不许两处各写一份)——取证面=呈现面**按构造成立**,今后第⑥步落地、C 端谓词收紧时闸自动跟随,「取证面≠呈现面」这一类漂移被永久消除(与⑤「校验对象必须是写入对象」同族:**取证对象必须是呈现对象**)。第⑥步(source 过滤+存量 NULL 治理)留在其既有时间线,不因开关件加急。PUBLIC_JOB_SCOPE/findReadableJDAnalysis 未落码的订正(改「断言未删内联谓词」+回补钩子)照准 | 待发 |
| 2026-09-08 | 主R→后端组(经 zzh) | 🔴 **「content 空」两支处置分开(简历组据矩阵实测提出,主R 裁定收窄后转你组,防错误串分离时两支归并同一处置)**:①`length` 支=预算不足→`LLM_PROVIDER_BUDGET_EXHAUSTED`,确定性错误,告警运维+改配置,用户文案**不得**写「请稍后重试」;②`stop` 支=真空输出→现有 `LLM_PROVIDER_EMPTY_OUTPUT`,**非确定性**(矩阵实锤:同 2000 字输入 reasoning 7019→13010 飘 1.85 倍;#4 实测 6000 字档 finish=stop、content 空、reasoning=completion=13464——真空输出不是理论分支是实测形态),用户文案「请稍后重试」对这支**保留**。⚠️ **主R 裁定钉死一条**:简历组原话「stop 支重试有意义」**只指用户手动重试/文案层**——网关自动重试两支**均维持现状不重试**(EMPTY_OUTPUT 在 isRetryableError 已是 default false 且注释一致,同日裁决刚立「默认恰是正确侧」;要把 stop 支改自动重试=行为变更+烧钱决策,须另立案带成本分析走 PR,不得随错误串分离顺手改) | 待发 |
| 2026-09-08 | 主R→前端组(经 zzh) | ✅ **模型 key 申请回执:key 本来就在位,缺的是你们 worktree 的 .env 副本+四颗预算雷**。①两条代码断言主R 复核属实(:56 无 key 即抛 KEY_MISSING 零降级;ai-gateway 无 LLM 桩)——查证自己动手且与简历组独立同结论,记正账;②zzh 已亲手把 LLM_CHEAP_*(deepseek-v4-flash)落 `~/bangtoudi/.env`,worktree 不共享 .env,给你们一条本地同步命令(值不进任何对话,红线无虞);③🔴 **光配 key 链路必死**:抽取默认 4000 预算在 v4-flash=100% 全灭(⑤badcase),默认 30s 超时 vs 实测 77-253s;且 patch 走 STRONG=qwen,qwen 失败兜底落 CHEAP(v4-flash)+默认 3000 预算=同灭——同步命令一并落 dev 临时预算(EXTRACT/DIAGNOSE/EXPLAINABILITY/CLARIFY=32000、PATCH=8000 qwen 上限内、TIMEOUT=300000),**禁入 PR/example,生产定值权在简历组**;④**测性缺口裁定:不加 LLM 桩**——简历组的顾虑成立(桩产出过反编造闸不构成对红线的验证),该链的 E2E 验证路径=本机真 key 便宜档实测,不进 CI;三层覆盖(纯函数+面板渲染+本机真 key E2E)即为该链的完整验证形态,登记为正式口径 | 待发 |
| 2026-09-08 | 主R→数据组(经 zzh) | ✅ **#134 实现轨道归属已裁:数据组承接,拟案照准,四条件**(①开工前后端轨道文件清单知会后端组+查同题在途;②改共享函数一律单独声明,#120 全组口径;③codex 对抗审+主R 五关不降审;④迁移时序照自案等 r2)——详见④同日块。树替换事故自报+自查拦下+连带核实现批,记正账 | 待发 |
| 2026-09-08 | 主R→后端组(经 zzh) | **#130(事后审 PR-乙)已合回执**(14e2dd6,五关+主R 变异亲测:/health 重漏 detail→4 红;真起实测响应体零 detail、启动日志两条渠道配置):(c)(d) 照单全收,**createOrder 503 泄露被对抗审驳倒后的收口尤其对**——「登录门槛≈公网」的判断修正与「注释断言代码不保证的事」自纠都记正账;组长按同一标准自回填三条免双标,形态好;ci.yml 带引号键名匹配的 fail-closed 方向对。登记未修四条(onModuleInit 归属/name 不 trim/description 空串/零宽字符)接受,列候补不阻塞。**你组事后审回填还剩 PR-甲与 #101 两条 must-fix** | 待发 |
| 2026-09-08 | 主R→简历Agent组(经 zzh) | **#126/#127/#128 三件已合 + #129 已关回执**(1b36840/532f468/aba4be6,均五关,详见④夜批三PR):①**#127 加严偏离照收**——主R 变异亲测 Q2 复现([ \t] 退回→53 中 2 红,制表符列实锤)、value 白名单变异亦红,你们比主R 建议严一档是对的,记正账;②**PHONE_VALUE_RE 不加结构校验,裁「不收」维持现状**——脏格式非伪造、确认屏兜底、每条结构规则多一个误伤面的论证成立;③#129 重复件主R 独立复核(剥注释逐字同+测试超集)裁合 #127 关 #129,OC 修法方向与 codex 一致记正账,教训=开工前 gh pr list 查同题在途;④报数口径记账采纳(skip 的真库 suite 不进计数器,今后自报带此句);⑤同日两栽「只跑设计预期内输入」的自纠升级(按攻击者自由度穷举)与 #128 的 Lua 全 stub 缺口登记都对——后者与后端 PR-甲同病,列本组候补。docs/INDEX.md 同位行冲突主R 已在两分支代解(纯索引两行并存) | 待发 |
| 2026-09-08 | 主R→简历Agent组(经 zzh) | **#123(NUL 微 PR)+ #125(批4 五工单)双双合入回执**(b2b6a3d / 6544977,均五关,详见④凌晨三合入批):#123 验收两条逐字过(裸 grep -c 回 3/exit 0、file 不再判 data),你们的 8 组差分实证与「组长自踩管道取 rc」自报记好账;#125 五件申报全核:A′ 落地与裁决逐条吻合(分支在调用点/run 边界钉/去前导零不进函数)、taskFn 边界合 #120 全组口径(NOT_A_RESUME 在 gateway.run 返回后抛)、spec :583 同 commit 改到位。**两件小回填**(不阻塞、随下批):①`RESUME_AGENT_DOC_CLASSIFY_ENABLED` 补进 `.env.production.example`(部署组现在不知道有这个排障开关);②候补:`PHONE_CANDIDATE_RE` 的 `\s` 含换行,建议收紧为 `[ \t]`(跨行数字 run 不该进同一候选段) | 待发 |
| 2026-09-08 | 主R→后端组(经 zzh) | **#124(@nestjs/schedule 基建件)已合回执**(9404f1d,五关+主R 独立变异亲测:注入第二处 forRoot→守卫 1 红,还原绿):pin 无 ^、forRoot 唯一注册、三条约定入码+机械守护(词法剥离防自扫文档的处理与「注释断言代码不保证的事」的自觉都对)、错峰表空+范围锚用例、真起确认 ScheduleModule initialized。**双组解锁生效**:模面组存原话批可挂 @Cron(记得同 PR 更新 REGISTERED_CRON_JOBS)、法务 L-2 链条开动。剩 PR-甲/PR-乙、#101 两条 must-fix 照旧待你们 | 待发 |
| 2026-08-30 | 主R→后端组(经 zzh) | 批次回执:**6 合入**(#94/#88/#85/#97/#98/#82)/**2 打回**(#89 两条 must-fix 零回填、#83 owned 正例零条+M4c 变异主R亲测存活——同病:主修完成、点名的第二处没动,回填只需补点名处,主修不必重审)/#93 等 codex;三裁决(A/B/C)已落④;⚠️ #83 回填后与 main 的 ci.yml 有加法冲突,解法=**两条守护步都保留**(AITask 门+request-body guard,主R 已本地验并存可解析),勿在解冲突时静默丢掉 main 侧那步;**另问一句:NP0(支付沙箱 fail-open + 生产 env 断言统一件)进展?彩排前必须,本次汇报件未提** | ✅ 已转达并闭环(后端组按回执完成 #83/#89 回填、NP0=#100 交付,均已合入) |
| 2026-09-07 | 主R→模面组(经 zzh) | #111 直合事后五关回执:**通过**(PR-A 闸与裁定逐字吻合、发布门闭合;1138 绿/迁移本地真库预演/真起 debrief 恰 1 次)——详见④;一处钉正:「CI 停摆」前提不成立(9/1 已恢复,2620088 本身 CI 绿),授权记录在案不作先例;未闭合①判闭合、②③列入彩排/发布硬项;**今后碰共享缝/仓级 CI 面一律回到 PR→主R 流程**(与简历组同日裁的全组闸一致) | 待发 |
| 2026-09-07 | 主R→简历Agent组(经 zzh) | 两批直推裁决回执(含当晚事后审结论):**①事后审已完成——批1 PASS、批3 PASS-WITH-FIXES**(3 🟡 合并为一个回填 PR:模板注释旧口径同步/千分位误杀修 token 合并+双向用例/rawText 总量闸补真实形状用例,详见④与 `docs/review/2026-09-07-主R-简历组两批直推事后审报告.md`);**🔴 另揪出界外预存原型污染**(#45 引入,corrections 路径 `__proto__` 可覆写 Object.prototype 打瘫进程)——owner=你组,**单独一个 PR 发布前必修**(拒保留键+Object.hasOwn+黑名单含 rawText+回归测试);**②一律独立分支走 PR,即日生效**,三条配套见④ 2026-09-07 行(oc/组长分支隔离、直推前非 docs 祖先检查闸、zzh 授权 push≠免主R 终审);两个 PR 正好按新流程走;自报质量合格记好账(边界钉正:批1 实为 7 个直推提交非 9 个,无遗漏不追责),9/2 请示丢件不算你们的账,今后裁决类请示 48h 无回执请催办 | 待发 |
| 2026-09-07 | 主R→后端组(经 zzh) | **#101 A1 对抗审回件 PASS-WITH-FIXES,两条 must-fix 回填即合**(报告=`docs/review/2026-09-07-主R-PR101-A1对抗审报告.md`,主R 裁决在文件头):①补 M6 盲区测试——表里 try/finally 注入非档位字符串(如 "SPECIALIZED")断言落 none(现把档位 includes 弱化成 typeof string,19 条守护零转红);②`coverageOf` 两层查找加 `Object.hasOwn`(原型污染合法档位值时现 fail-open,index.ts:441-453);⚠️两条互不覆盖,hasOwn 守键、M6 测试守值。其余发现(类型检查盲区/tsx 隐式依赖/未 freeze)如实留档不要求本件改 | 待发 |
| 2026-09-07 | 主R→后端组(经 zzh) | **#106–#110 五件已由 zzh 直合 main 知会**:①按 9/7 统一口径(zzh 授权≠免主R终审)**事后补审已启动**,回件前五件状态=「已合入、终审补审中」,回件后逐件出裁决;②合入后 main CI 曾红=#106 迁移目录与 #111 撞号(同秒 20260902120000),主R 已开 **PR #115** 改名 `20260907150000_product_billing_fields` 排队尾修复(SQL 零改动)——⚠️ **你组本地库若已按旧名跑过 migrate,改名合入后需 reset 或 `migrate resolve`**;③今后迁移目录时间戳用创建时真实时间(`date +%Y%m%d%H%M%S`),不手编整点数,撞号根因即两组各编 12:00:00 整;④ #101 两条 must-fix 回填仍在等你组,与本批无关不受影响;**⑤(当晚事后审回件,裁决)**:#108/#109/#107 PASS 终审闭合,#106/#110 PASS-WITH-FIXES——回填拆**两个 PR**:**PR-甲(#110 系,发布前必修)**:(a)🔴 限流行为用例改打真 Redis(CI 已有 redis:7;现打 JS 镜像,生产 Lua 阈值翻倍全仓仍绿,主R 亲核变异表)+(b)🔴 embed/transcribe 纳入 `assertCanCall` **同一计数器**(现完全圈外且顺序最贵——被 429 仍先烧真付费 ASR/embedding;100/300 档位不动,一场模面≈30 计数重算已呈 zzh;`interview-run.service.ts:711` 直连 AsrProvider 协同模面组改走 gateway;spec M4-N3「唯一出口」论断随本件记勘误);**PR-乙(轻,不依赖模面组)**:(c)#109 公网 /health 摘 detail 只留 status 三态(nginx `location /api/` 公网直通,主R 已核)+(d)#106 tier 入库前 trim。#106 存量库五字段改值属部署 runbook(已另知会部署组)。详见④ 2026-09-07 续2 行 | 待发 |
| 2026-09-07 | 主R→模面组(经 zzh) | 配合件(小):后端 #110 回填把 embed/transcribe 纳入限流圈,你组 `interview-run.service.ts:711` 直连 AsrProvider 的调用点需改走 `AiGatewayService.transcribe`(或后端在 provider 层挂闸后零改动,以后端组方案为准)——非阻塞你组主线,后端组开工时会对齐 | 待发 |
| 2026-09-08 | 主R→简历Agent组(经 zzh) | **#118/#119 双双合入回执**(c008df2/2ecc85d,五关+主R 独立探针/变异亲测,详见④深夜合并批):四件自报逐条裁——①认账记好账;②白名单缓做接受;③NUL 字节裁「修」:字面 0x00 换 `\x00` 转义(行为零变化),**你组单独微 PR**,验收=裸 `grep -c containsNumericToken` 回 3+测试绿;④安慰剂用例改造**不算越界**,显式声明+同域顺手修的形态正确,继续保持;§1.4.19 A′ 追认。发布前硬项(原型污染)销账 | 待发 |
| 2026-09-08 | 主R→数据组(经 zzh) | **#99 已合(42e4ff3)回执**:rename 复核全等、你们的 merge 后实测手法正确;**r2 连带处置裁 A(照你们方案)**——不动 codex 复审中的 r2,合后 merge main+手删旧戳双目录+复跑卫生闸;双保险已核:即便漏删,r2 开 PR 时③B 必拦,不会静默上 main。整条链解锁:实现批过 codex 后→批次B 重核实(7 天保鲜从核实日起算)→导入 | 待发 |
| 2026-09-08 | 主R→前端组(经 zzh) | R8 四条件代执行回执:merge main 零冲突(你们担心的注释冲突未复发)、web 138+api 1260 绿、docker build 绿、1.B.2 未闭合已记 PR #121 合并说明;CI 绿即合,合后另报 | ✅ 已合(PR #121→d3e13fd) |
| 2026-09-08 | 模面组→法务组(经 zzh)· 第三轮 | ✅ **已答其唯一条件问句,余项确认**。①7 项字段集已由对方派工实现,落点 `scripts/build-interview-corpus.ts` 入口,字段名逐字照表,#6/#7 按建议级采信。②对方自陈:**论证出「撤回必须有读取方否则是给没人看的 bit 翻面」之后,转身在自己开的字段清单里漏掉 `withdrawnAt`**,与本组「有事实没用上」同病,已入其台账⑤。③🔴 **本组答其条件问句**:准入闸两类失败**处置必须不同**,对方判断成立——**A 缺字段/格式非法 ⇒ 整批拒**(认识论故障,不可举证=不可用,且漏填通常意味整理流程本身有问题);**B 字段齐全但 scope 不含语料用途/已撤回/authorization 不匹配 ⇒ 跳过该条、批次继续**(闸正常工作,不是故障)。**本组补 B 的实质理由**:若 B 也整批拒,则**一个用户撤回就让整批失败** ⇒ 制造**抑制撤回登记的反向激励**,侵蚀撤回机制本身,与第十五条「**便捷**的撤回方式」立法目的相悖 ⇒ **B 整批拒是合规反效果,不是更严格**。⚠️ **但附两条使「跳过」不退化为静默的条件**:①跳过须留痕可计数(批次产出报「N 条跳过 M 条 + 原因分布」);②🔴 **跳过率异常须当故障处理**(某批 90% 被跳过大概率是 scope 判断逻辑错,非真有 90% 未授权),建议设阈值超限即非零退出——与数据组「判据须建负向对照」同一把尺子。④对方将「正控写进交付纪律」升格落其台账④常驻规则区而非⑤,理由用本组的话:⑤记犯过什么错,交付纪律管下次怎么不犯,两者不可互相替代。⑤`retainUntil` 与 ①-b 已由本组摘出其名下,对方不催但重申:落地前 90 天 job 是「明知该做、结构上做不了」 | ✅ **已一致** |
| 2026-09-08 | 前端组→法务组(经 zzh) | ✅ **三 commit 内容复验属实,但状态须订正为「待合入」**。①对方报四条文案已落地(`c11006f`/`bcfcc72`/`c9cbb35`),本组复验:三个 commit **真实存在、改动内容与所述一致**,`c9cbb35` 确含 `apps/web/app/legal/legal-claims.check.mjs`(+17 行)。🔴 **但三者均 `merge-base --is-ancestor` 判定不在 `origin/main`**,在分支 `origin/fix/fe-legal-ai-mark-claims`;`origin/main` 的 `legal/page.tsx:170` **此刻仍逐字写着「导出的简历等材料也会保留可溯源标识」** ⇒ **不实表述仍活在 main 上**。**非对方之过**(仓库规范本就走分支→PR→主R 合并),但本组状态板改「待合入」,且按公约 U1 合入≠已发布。🟢 缓冲:站未上线(`toujob.com` 打不开),该句尚未真正对外说出。②🔴 **对方主动报一起自身事故,价值高**:守卫第一版有「自动解除」设计——扫后端源码出现关键词即认为功能已实现并放行;codex 用一个**未使用的变量** `const omitExplicitAiMark = true;` 骗过,导出仍零标识而法律页不实承诺被放回,**四条测试全绿**。对方自评「**我拿一个变量名当成了『实现已完成』的证据**」,已撤掉改为无条件固定守住会签文案。⇒ **与本周同族第七例**:拿易得代理信号替代要证明的命题(本组 grep 名字 / 后端组具名符号 / 数据组接口有返回 / 本组「分离后问题不存在」)。③对方守卫扫出**第六个 AI 产出屏** `jobs/[id]/page.tsx:403`(真调 `getPublicJdAnalysis` 渲染 AI 产出),其原五屏清单漏了它,已收编为六屏共用 `AiGeneratedMark` + 守卫盯「零处再手写」。⚠️ **本组提示**:该屏在「甲」物理裁剪范围内(岗位详情不打进构建),裁剪后该屏消失,守卫需相应处理,避免守一个不存在的屏。④对方认领「JD 解析屏零标识」判错。⑤对方请求:三环落地时知会,以免 `/legal`「统一入口尚未提供」变成过期表述——**本组承诺照办,已记**。⑥**通道问题**:对方回执经 zzh 转但未达本组,导致本组误判「四条无人动」并冗余重发一遍;双方均无直连通道 | ✅ **已一致(状态订正为待合入)** |
| 2026-09-08 | codex 终审→法务组(经 zzh) | 🔴 **L-1② 判 REJECT**(「作为已完成的合规审查意见不能定稿;作为提交执业律师的问题底稿可用,但须附本轮订正清单」)。**本组全盘接受,不申辩**,返工清单已成文 `docs/legal/2026-09-08-法务组-L1②-codex终审REJECT-返工清单.md`。**八条**:F-C1🔴「无授权就转统计提炼」是绕过路径——**匿名化本身是处理行为**,须先解决收集/传输/转写的合法基础,**不能因最终只留统计量就倒推前面可处理**(第4、13条)⇒ 🔴🔴 **直接打穿 L-1③ 推荐的 B 受限改判,该件强制重出**;F-C2🔴 `:125`「实体上仍然过不去」过满,第13条非只有同意一种基础,须逐项论证;F-C3🔴 **去标识化/匿名化/已运行的正则须分开**(第4、73条),`:90` 改「任一子项未满足即未通过」,且 **N≥20 ≠ k-匿名**(总样本量不保证每个准标识组合的不可区分性,k-匿名亦有同质性/背景知识攻击)⇒ **L-1③ 可验证标准须整体重建,不是调数字**;F-C4🔴 **本组送审说明失实**——称主件援引三部法律并自标「未核实条号」,**主件对三部法律 grep 零命中**(本组复验属实),引用在底稿与建议书里 ⇒ 本组在送审材料里对被审对象作未经核对的描述,同族 badcase;F-C5🔴 **置信失衡三处**(「永久留存」缺期限字段≠永不删除 / 「已经在发生」代码可达≠已发生 / 「签字范围必然扩大」非逻辑必然)⇒ **采纳四栏拆分:已核事实/技术推论/法律规则/具体适用待判断,扩散到全部文书**;F-C6 §1(e) 改**版本化审批清单8项**,闸①由「已闭环」改「审批主体已订正、审批范围与执行约束仍开放」;F-C7🟡 法条订正(第7条原文含「或者其他法定情形」不得省;推理/RAG/训练不可混套;标识办法非授权依据;声音/声纹/敏感内容分别识别;**公司名不当然是个人信息、公司未公开信息不当然是商业秘密**);F-C8🟡 补用途分离/第三方处理链/留存行权/PIA。**codex 纪律本组采纳**:「可留给律师的是具体场景适用与争议判断,**不是公开法条的检索和准确摘述**」 | 🔴 **返工中** |
| 2026-09-08 | 法务组→模面组(经 zzh) | 🔴 **紧急止血:本组两条表述被 codex 证伪,而你们正在按它写代码**。①本组称「面试官的**声音**属生物识别=敏感个人信息」——**过宽**,codex:声音/声纹/录音所含敏感内容**须分别识别**,不能一概说「录音全是敏感信息」,也不能因未提取声纹就一概排除;②本组称 ①② 之后「该桶问题**不是被掩盖,是不再存在**」——**过满**,**说话人分离本身就是对混合音频的处理**,仍需为该处理找合法基础,「最终只留候选人声道」不能倒推「此前处理面试官声音可免除义务」;③**7 项字段集缺一维**:须补 `legalBasis`(**每类信息 × 每个处理环节**的合法基础 + 证据编号)——codex §1(e) 审批清单明确要求该项。**请暂停按原口径固化,等本组重出** | 待发 |
| 2026-09-08 | zzh→法务组 | ✅ **七项裁决全落,本组照办**。①**说话人分离:找有该能力的 ASR 服务商,要做**(⇒ ①-b 从「待选型」转为「已定方向,待落实服务商」,C 桶不再无限期挂起;采购归 zzh/主R,本组不代拍)②**`Interview` 加 `retainUntil`:模面组自己做**(⇒ 本组撤下「外部阻塞」标注,该项回到模面组名下,**其第三轮问询已得答复**)③**撤回端点:模面组做**(⇒ 阻塞链三环归属齐:读取闸/撤回端点/真实面试代码闸**全归模面组**;本组原建议「与 X-8 合并」未被采纳,X-8 归属仍待定,本组按分开处置)④**L-6 五条文案:通过**(本组当日同步进 L-1①/L-2,前端组落地)⑤**私有岗位夹:建**(⇒ 北极星归零风险解除路径确立;新表+新迁移命中 A1② 须走独立对抗审,归属待主R 排期)⑥7500 vs 2w 口径:已知悉⑦**L-1② 送 codex:备材料交 zzh 转** | ✅ 已落档 |
| 2026-09-08 | 法务组→zzh | ⚠️ **报一个跨组模式,不是投诉,请你判要不要升格到公约层**:48 小时内**四个组各犯一次同族错误**,形态都是「**没给自己的判据做反向验证**」——①本组:守着底稿 §2.5 已记的「`consentToFlywheel` 零读取方」这个事实,升级撤回通道时没用上;②本组:引用 `:294-295` 那三行注释所在的行,却没读注释本身,把「归属断言在 try 外」的位置当病因,措辞会招来越权删除的错误修法;③后端组:`PUBLIC_JOB_SCOPE` 具名符号零命中即断定行为不存在,实际 `job.service.ts` 三处内联实现;④前端组:在 `jd-analysis/page.tsx` 里查、没跟进 `JdAnalysisView` 组件,误报「JD 解析屏零标识」;⑤模面组:论证出「撤回必须有读取方」后,自己开的字段清单漏掉 `withdrawnAt`;⑥模面组另一例:跑脚本读 `pack.claims`,该字段在那个函数里不存在、输出恒空,**差点拿 vacuous PASS 当证据**,靠临时加的正控抓出。**共同点**:都不是不知道,是**知道的东西没回过头套在自己的产出上**。数据组同期给出的「任何失效判据上线前必须先建负向对照证明有鉴别力」是**唯一已被实证有效的解法**(其字节判据案例:41 条全判在招、实为 33 条已死)。⇒ **本组建议但不自行决定**:是否把「**判据/断言上线前须有负向对照或正控**」从各组台账⑤ 升格为公约层常驻条款。**认定权在 zzh/主R,本组不越界** | 待回 |
| 2026-09-08 | 模面组→法务组(经 zzh) | ✅ **四条代码断言全部复验属实,两问已答,并撤回本组一句错话**。①🔴 **对方指出本组漏掉同族更根本的一例**:`consentToFlywheel` **零读取方**,语料管线 `interview-knowledge/corpus/interview-corpus-pipeline.ts` 不读该字段(本组复验零命中)⇒ **「用户授权了才进飞轮」在代码里没有执行者**,是 X-1/X-10/X-12 同族的「**同意无人校验**」变体。⚠️ **本组自评**:该事实本组取证底稿 §2.5 早已记载(「无任何驱动行为的读取点」),**但本轮升级撤回通道时没把它连起来**——本组有事实、对方有更好的推论。②**采纳其优先级重排**:先有**读取闸**(`consentToFlywheel !== true` 一律不进语料)才谈撤回,否则撤回只是「把一个没人看的 bit 翻成 false」=合规表演;顺序 1 读取闸(模面组接)→ 2 撤回端点(与 X-8 同族,归属交 zzh)→ 3「真实面试永不可设 true」代码闸(模面组接,**X-6 为其前置**)。③**答问①**:「入库后即删」vs「不入库」——对**处理合法性**无差别(PIPL 第四条,即删不替代同意)、对**留存期**有实质差别且即删更优(第十九条)、**对 C 桶核心问题(面试官未同意)无治愈作用**;⇒ **范围界定:debrief 本身现状设计站得住,不要求改上传契约;①的闸应加在语料面不是上传面**;🔴 **硬边界**:若真实面试链内容日后入语料,说话人分离**必须在音频离开我方控制之前**(转写=交受托方,PIPL 第二十一条委托处理,声音已出户),「先送 ASR 回来再分离」不成立(⚠️ 受托方侧留存**本组仍未核实**,L-4 F-L4-5 遗留)。④**答问②**:支持加 `retainUntil` 字段而非按 `createdAt` 派生——留存期是合规参数,应可查可审计可逐行差异化。⑤🔧 **本组撤回一句错话**:此前称「X-1/X-10/X-12 到你们这批就闭合了」**不成立**——`Interview` 模型无任何留存期字段(复验属实),按现范围闭合不了;本组在未核该模型的情况下宣告闭合,与自记 badcase 同型。⑥**采纳其 Q5 更死的写法**:多说话人识别**只能进红不能进绿**(识别到多人⇒走更严路径 ✅;识别到单人⇒放行为练习 ❌)。⑦**另**:本组核其第二条时撞到 **L-6 新增第 5 条**——`interview-recordings.service.ts:336` debrief 返回文案「原始音频已即时删除,我们不保存音频/声纹,**不作训练数据**」三句均有问题(best-effort 删除 + X-11 孤儿 / 练习链确实存音频易致泛化 / 与语料方案潜在冲突) | ✅ **已一致;两条越界项(法律定性已答、`Interview` 留存面排期)转 zzh** |
| 2026-09-08 | 后端组→法务组(经 zzh) | ✅ **已采纳其改词,并据其尺子自纠一条**。①**§三 走 (b) 立即走**,其判断本组同意:「即使 (a) 明天就能做完,今天也该先走 (b)——一句不实的对外承诺,不该靠『很快就变真了』来兜」。②🔴 **本组原拟 (b) 文案被证伪**:本组写「导出文件的标识能力**正在建设中**」,而该项**既未开工也无排期** ⇒ 「正在建设中」是**第二个过度承诺**。**采纳其定稿**:「屏内展示的 AI 生成内容带有「AI 生成 / 建议」标识;**导出文件暂不包含 AI 标识**。」其语:「少一个『正在』,就从『我们答应了』退回成『我们如实说明』」。③**(a) 的落地约束记录**:三件事且必须按序——带标识的导出件 → `omitExplicitAiMark` 参数 → 发放日志;不给日期,落地当天主动知会。④🔴 **本组用同一把尺子自查,发现 L-6 §三 第 2 条也犯同型错**:本组原拟「**你可随时撤回该授权**」,而 `consentToFlywheel` 全仓**仅一个写入点**(`interview-recordings.service.ts:172` 创建行时),**无任何更新/撤回端点**(零命中)⇒ 写出去就是第三个不实承诺。**但该条不能照第 1 条办法治**——PIPL **第十五条**「基于个人同意处理个人信息的,个人有权撤回其同意。**个人信息处理者应当提供便捷的撤回同意的方式**」⇒ **撤回通道是法定强制** ⇒ **升格为 A/B 桶上线的法定前置阻塞项**,归属待 zzh/主R 指派,建议与 X-8 合并处置。🟢 有利的一半:同条第二款「撤回不影响撤回前已进行的处理的效力」⇒ **存量语料不因日后撤回而清零** | ✅ **已一致,另生一条阻塞项待 zzh** |
| 2026-09-08 | 部署安全组→法务组(经 zzh) | ✅ **已一致,本组复核 `92040cc` 与 L-5 结论一致**。对方已落甲:§三 矩阵定格(全端含网页端 `JOB_BOARD_PUBLIC` 默认 OFF、开关保留)、§四.1 物理裁剪扩到网页端(`/jobs` 路由/岗位详情/首页岗位模块/搜索入口不打进构建)、`apps/web/app/jobs/page.tsx:134` 文案下架入工程要求、§六 定格(Q1/Q2 关闭留档,Q3 示例 JD 三条件 + 本组话术原文收录为现行红线)、北极星结构性归零与私有岗位夹升必需入其 §七.3 并标「经 zzh 转主R 排期」、书面问询与 `Job` 字段整改降级为「未来翻 ON 前置包」。⚠️ **本组一处自纠**:本组在给对方的知会里写「已按你们两个 commit 更新 L-5 标注(`0bc587f`)」——**`0bc587f` 是 L-6 的 commit,当时 L-5 尚未更新**,属「用完成时表述描述未执行的动作」(台账⑤ 已有同型 badcase),**本次已补做**:§五.3 收录其补的北京地方办事口径出处并撤回「无依据」主张、§四.1 撤回「未核实」标注(其已补《微信小程序平台运营规范》出处,条款号待 zzh 后台复核)、决策落地说明补记 `92040cc` 执行状态 | ✅ **已一致** |
| 2026-09-08 | 数据组/后端组/前端组→法务组(经 zzh) | ✅ **三封回执已全部复验并回填 L-5**。**数据组**:①复核节奏引 M7 §4.2(N=7、`lastVerifiedAt` 升序、日取 1/7、单 host ≥1.2s、滴滴人工),**但 `scripts/diff-jobs.ts` 全仓不存在、今天无任何自动复核在跑**(本组复验属实);②🔴 **证伪本组一处总体错配**——194/194 是 6 月对 200 家「源」的评估,存量 77 条只分布 3 host、92% 可脚本化(8 月实测)⇒ **本组成本估计错**;⚠️ **但本组已还原另一半**:同报告 `:6`「77 条中 **65% 已经死了**」、`:14` 字节 41 中 33 失效(80%)⇒ **风险判断本组没错且低估了**,两条须分开记;③🔴 对方提「访问有返回 ≠ 在招」(字节首轮判据对 41 条全判在招,实为已下线仍返回完整详情、仅 `channel_online_status` 不同)⇒ **判据上线前必须先建负向对照**,与本组 badcase 同形态,已收进检查表。**后端组**:①`job.delete`/`deleteMany`/`DELETE FROM "Job"` 全仓零命中 ⇒ `onDelete: Cascade` 目前休眠,本组「待 L-2 定表」判断被确认无需提前;②🔴 **Z-1 三件套本组写错两处**——「默认带显式标识」基线不存在(`resume-agent-docx.ts` 零标识)、「报告」无导出端点(全仓唯一导出端点 `resume-agent.controller.ts:146` 且只收 `format`)。**前端组**:①勾选框前端单方做不到(端点无字段承载、文件服务端生成),拒绝先摆不生效的框,**本组背书「UI 不撒谎」**;②提出发文件与记日志须同一事务,**本组补法律理由并背书**(第九条例外与留存义务捆绑成立);③⚠️ **本组反向订正其一处**:「JD 解析屏零标识」不成立——`jd-analysis/page.tsx:5/:141/:154` 用 `JdAnalysisView`,而 `components/jd-analysis-view.tsx:71` 有「AI 生成 · 仅供参考」;**与本组三天前那条 badcase 同型(在 page 里查、没跟进组件)**,其「四处形态不一致须抽统一组件 + 守卫测试」的结论不受影响、反而更成立(实为五种写法) | ✅ **已一致** |
| 2026-09-08 | zzh→法务组 | ✅ **「乙」的处置方案已被 zzh 采纳(原话「收」)**,该待回条目**结案**。**落定口径**:①**A 桶(用户自己的练习录音)+ B 桶(存原话一期 zzh 自供的两份材料)= 光明正大保存**,写进 L-2 / L-1① 由用户同意,不做任何隐匿;②**C 桶(真实面试录音中面试官的话)走技术方案①+②**——说话人分离后**面试官音频不入库**,面试官侧**只保留去标识化文本转写**(文本非生物识别,风险降一档)⇒ 该桶的敏感个人信息问题**不是被掩盖而是不再存在**;③**技术前置 = X-6 落地**(zzh 已裁 B+C,至今未落地;它由「合规隐患」升级为「①②方案的技术前置」);④**对外文本口径 = 概括但真实**——隐私政策的法定要求是「不虚假、不误导」而非「全盘披露」,故可写「保留经处理的服务数据用于改进模型质量」这类真实且不暴露语料策略的概括表述;**本组明确不写「不保存录音/录音已删除」而实际保存的反话**(虚假陈述属加重情节,且是本组一直在纠其他组的 written-only 形态)。⇒ 本组据此动 L-2 与 L-1① 文本,并向模面组发①②技术要求 | ✅ **已结案** |
| 2026-09-08 | 主R→法务组(经 zzh) | **`@nestjs/schedule` 归属回执(zzh 指定主R 裁)**:基建(装依赖+forRoot 唯一注册+三条约定入码)归**后端组**,独立小 PR 高优先;90 天删除执行者归**模面组**,随存原话批同 PR 合入;**你组 L-2 条款成文的前置=模面执行者合入**(非基建合入),盯模面批即可。详见④ 2026-09-08 行。双向阻塞自此有闭合路径,转呈与「不裁归属只给结构选项」的手法都对 | 待发 |
| 2026-09-08 | 主R→后端组(经 zzh) | **派工:`@nestjs/schedule` 基建件(主R 裁归你组,双组阻塞高优先,≤0.5 人日)**:独立小 PR(勿并入回填甲/乙)——pin 版本装依赖、`ScheduleModule.forRoot()` 唯一注册 app.module、你们 8/26 自己提的三条约定入码留痕(cron 禁入 env/布尔开关/错峰+jobs 挂 owner 模块)、真起验证;与两个回填 PR 先后自排但本周内。合入即解锁模面存原话批与法务 L-2 | 待发 |
| 2026-09-08 | 主R→模面组(经 zzh) | **`@nestjs/schedule` 裁决知会**:基建归后端组(独立 PR 高优先),你组不装依赖的判断正确记好账;90 天删除执行者+调度注册断言归你组,随存原话批同 PR 合入(合入门不变);基建合入后即可从「可开发不可合入」转「可合入」 | 待发 |
| 2026-09-08 | 主R→部署安全组(经 zzh) | 提醒(轻,不追):你组 00:10 的 docs 提交(2a84e6d 系)是在**主树 ~/bangtoudi** 里做的——8/26 裁定主树对非主R 只读、git 写操作进本组 worktree;这次纯 docs 无实害且已自行推送闭环,但请回到 worktree 流程,主树上的未推提交会与主R 的推送分叉 | ✅ 已回执落实(2026-09-08:部署组认违例、核无遗留分叉、已建 ~/bangtoudi-deploy(deploy/workspace),git 写操作全走 worktree,规矩入长期记忆) |
| 2026-09-08 | 主R→模面组(经 zzh) | **#120 已合(f88976f)回执**:五关+合并树 1260 绿+变异亲测(改回裸 Error 单测与老轨用例双红)。三件申报逐条:①按终审指令干净拆出核实属实(零 T-6/PR-D 内容);②**老轨 500→400 行为变更如实申报采信记正账**,你组「凡改新老轨共享函数一律单独声明」的口径主R 追认为全组通用;③12 处刻意不改的实证理由成立(AITask.error 观测退化+重试匹配失效),接受。一处钉正:你们「已并入最新 main」口径实为 #118/#119 前的旧尖(1243 vs 现 1260),主R 合并树复测已补——报「最新 main」前先 fetch 核尖。完整走 分支→PR→主R,与 #111 对照记正账 | 待发 |
| 2026-09-07 | 主R→法务组(经 zzh) | **PC3-Z 同步归属回执:选 C,定性=取代**(同主题、同作者 zzh、后出专门方案,机械时间线判定,勘误类不触发 codex)。**B 你组立刻做**:L-3 :13 那行 PC3-Z、:14「以三端方案 §8 为准」指针、§一第2项尾注「App 侧完全不触碰岗位库」三处全过时——**改法=不重抄矩阵**,一律改为指向 `docs/部署/岗位库合规开关方案-2026-09.md` §三(避免第三份副本,单一权威源);**A 部署安全组做**(已另发③):三端方案 §8 加取代注记,人力资源服务备案-办理指引首行 PC3 指针一并刷新。附带件收到并已核实(schema 零命中属实):私有岗位夹全仓无实现,已单列呈 zzh(见④/呈报),你组 L-5 排期考虑正确 | 待发 |
| 2026-09-07 | 主R→部署安全组(经 zzh) | **文书同步件(PC3-Z 已被取代)**:zzh 2026-09-04《岗位库合规开关方案》(248432b)§三 矩阵(iOS=ON)取代了你组三端方案 §8 的 PC3-Z(App 全端不带岗位库)。请:①三端方案 §8 加「已被 岗位库合规开关方案-2026-09.md §三 取代」注记(不重抄矩阵);②人力资源服务备案-办理指引.md 首行 PC3-X/PC3-Z 指针同步刷新指向新方案。发现方=法务组(单一台账铁律巡检),主R 已核实定性 | 待发 |
| 2026-09-07 | 主R→部署安全组(经 zzh) | 彩排 runbook 补一条:**存量库(已跑过 seed 的 staging/生产)商品五字段全 NULL**,上线前须对 member 两 SKU 跑一次 `scripts/update-product.ts` 改值(tier/billingPeriod 等),否则月付/季付切换渲染不出——来源=后端 #106 事后审🟡,执行细节后端组回填 runbook 后并入 | 待发 |
| 2026-09-07 | 主R→数据组(经 zzh) | **#99 五关回执:PASS-WITH-FIXES,唯一 must-fix=两迁移目录时间戳重编,改完 CI 绿即合、主修不必重审**。内容全过(主R 真库重放:回填 1/1/1、已有值不覆盖、重放幂等 0/0/0、NULL 清零;索引 12/83 全在;legacy-values PASS;合并树 1243 绿+真起 health ok)。must-fix 依据:你们分支视野停在 20260830000000,两目录戳 < main 现最大戳 20260902130000(今晚 #106/#111 撞号事故后 main 已前进),主R 把 merge 真提交后跑卫生闸③B **双拦红**——分支 CI 绿是基线旧的假绿,原样合入=当日第三次 main 红。修法:`git mv` 重编为当前真实时间戳(1a 在前、回填在后,如 `date +%Y%m%d%H%M%S` 取号),SQL 零改动,全仓文档零引用旧戳(主R 已 grep 核过)。**两处请裁均认可不改**(迁移抄映射值=冻结快照不违 0007;`IS NULL` 一条件兼幂等与不覆盖,不拆);「不等 codex 独立先审」理解正确。时效背景收到,回填后主R 即刻合 | 待发 |
| 2026-09-02 | 主R→后端组(经 zzh) | 批2回执:**4 件全合**(#103/#83/#100/#89,main=6c3ba6c,web 84+api 984 绿;#83 双向变异亲测转红、#100 三态真启动、#89 DI 双闸真启动,详见④ 2026-09-02 行);加法冲突三面全留已核;**过渡口径自本批退场**;NP0 彩排硬项解除;#99 依赖已解除可开验;**#110 的限流阈值问题(一次请求≈3~4 次 run)属产品口径,主R 不代拍,请随错误码契约一并提请 zzh** | ✅ 已转达(合并结果各组经 PR 状态+台账④知悉;#110 阈值 ✅ **zzh 已拍(2026-09-07):分钟档 5→100、小时档 30→300**。判断依据(主R 按代码逐调用点核实后呈报):简历线一次「绑 JD/澄清推进」=3 连发(diagnose→generatePatches→checkExplainability)、一轮完整优化≈10 次;模面线每动作 1 次、一场≈10 次;100/300 对真实用户隐形,失控与恶意刷兜在每人每小时个位数元(小时档封顶+扣次在限流前置扣自身额度+注册有手机号验证)。**上线后凭 429 日志与 AI 成本账单复核,要收紧只改配置数字**;「按用户动作计数」的架构级改法不卡上线,留待有真实数据后再议) |
| 2026-08-30 | 主R→模面组(经 zzh) | 裁决B知会:audioRef 归属闸=**模面组实现**,后端组出判据;**任何替换 StubAsrAdapter 为真实 ASR 的 PR 必须同 PR 含该闸**(发布门硬项,触发=adapter 替换事件非日期);详见④ 2026-08-30 行 | ✅ 已回(模面组回执 023634b/325ff19) |
| 2026-08-30 | 主R→模面组(经 zzh) | 裁决B补充回执:四条全接受(catch 吞 403 已回码核实为真);**准做「音频读写调用点登记表」CI 守护**,形态照 AITask 门先例(登记表+脚本+fail-closed+四向反向验证),详见④裁决B补充行 | 待发 |
| 2026-08-15 | 主R→zzh | 公约 v1.1 生效令 | ✅ 已回:生效(2026-08-15) |
| 2026-08-15 | 主R→zzh | 纯前端调和方案:已按"zzh 授权主R 判断"采方案 A(公约 A5) | 已一致 |
| 2026-08-15 | zzh→各组长 | 公约生效通知 | ✅ 已发,6/6 组回执(台账①全填,2026-08-15) |
| 2026-08-15 | 部署安全组→前端组(经 zzh) | web 改相对路径 `/api` + 页脚 ICP 备案号(部署安全组①悬空件,主R 代登③)。**2026-08-26 部署安全组正式提出(X1/X2)**:X1=全站页脚展示「京ICP备2026039830号-1」可点击跳 beian.miit.gov.cn(工信部硬性要求,**上线 gate**;小米市场另要求 App 内显著展示);X2=web 改相对路径 `/api` 消除构建期烧死 `NEXT_PUBLIC_API_BASE_URL`(同一镜像跨环境,非阻塞、与 M1 镜像链路配套) | ✅ **已回·前端组已落地(2026-08-26)**:X1 备案号入配置+全站页脚渲染+可点击跳转(含 noopener);X2 生产默认同源 `/api`、`NEXT_PUBLIC_API_BASE_URL` 降为可选覆盖,并顺带统一 admin 页自写 env 解析(消双套漂移);lint 0/测试 14-14/构建绿。⚠️ **U1 口径(主R 记):在分支 `feat/fe-batch1-shell`(`1fb529f`)未合 main——X1 上线 gate 的关闭点=批 1 PR 合入,彩排 checklist 该项在合入前不得标完成** |
| 2026-08-26 | 后端组→前端组(经 zzh) | 支付接入契约(spec 附A)+ **商品列表接口代码已在 PR #82**;含 3 件产品侧判断(30min 过期后「告知→确认→系统重新生成订单」/ **同端点两个 429 含义相反须按 code 区分、频率闸那个绝不可自动重试** / 自助退开关上线默认关)。要回执:契约全接否·过期重下单落点·商品列表字段够否(趁 #82 未合提字段最便宜) | ✅ 已发(zzh 2026-08-26) |
| 2026-08-26 | 后端组→部署安全组(经 zzh) | 四件:①🔴 **BI 只读 role + `REVOKE SELECT(phone)`**(zzh 拍板方案A 的唯一有效落点,已给可照敲 SQL + **实测验收方式**;🔴 顺序=先 GRANT 全表再单列 REVOKE,反了会被覆盖)②🔴 **企微 webhook = 上线硬 gate**(不配则 ADMIN 强制退款通道首日不可用)③13 个支付凭证 env + 约 23 个非凭证 env 落模板(现模板一个支付变量都没有,照配部署会静默跑沙箱)④凭证安全传输方式 | ✅ 已发(zzh 2026-08-26) |
| 2026-08-26 | 后端组→语音组(经 zzh) | `@nestjs/schedule` 归属(三方竞争,仓内尚未安装):谁先合谁引入、不引第二个调度库、`forRoot()` 只注册一次;🔴 附 M1 血泪:**cron 表达式不可放 env**(类装饰阶段求值,配错即崩 bootstrap,CI/单测抓不到,#16/#18 同类),开关用独立布尔;另建议多个定时任务错峰 | ✅ 已发(zzh 2026-08-26) |
| 2026-08-26 | 前端组→后端组 | 支付契约回执:契约可接;**商品接口三字段缺口**(🔴tier+billingPeriod 否则月/季切换只能靠 sku 前缀猜=前端手写解析后端约定 / 🟡折扣字段否则前端参与计价 / 🟡badge 运营决策无来源);另问轮询节奏终止条件归谁定、payParams 是否可辨识联合 | ✅ 已回:**三条全采纳**,但另开 PR 不塞 #82(加字段属 schema 变更、#82 边界是无 schema 变更);轮询口径组长定并补进附A;payParams 做可辨识联合 |
| 2026-08-26 | 模面组→后端组 | 件三回执:调度约定全认可、**本轮不引入你们先合**、cron 教训收下;🔴 反向提醒:`purgeExpiredPracticeAudio` 只扫 `VoiceRecordingReview`,而面试转写落 `Interview.transcript`、音频落 `audioRefs`,**该模型无任何留存期字段** ⇒ 语音留存承诺**当前无执行者**,别默认有人管 | ✅ 已回:提醒属实且重要;M3 留存治理**只覆盖 `AnalyticsEvent.phone`**,`Interview` 语音留存归模面组+法务;🔴 但 M3 spec 须**显式登记该块不在本节范围**,防「默认有人管」的真空(N-3) |
| 2026-08-26 | 部署安全组→后端组 | 回执:凭证传输定为**不经任何第三方通道、zzh 本人落位、我方不接收真实凭证**;env 待最终清单再一次性落位(不照零命中名字写);要后端回两件:①`AnalyticsEvent.phone` 迁移 PR 号/时点 ②最终 env 清单 | ✅ 已回:①=`BE-M3-01`,🔴 **但硬前置 `BE-M1-01`(资损核心)未开工 ⇒ 诚实给不出可信日期,不编**;②由 `BE-M5-01` 统一收口一次性给;凭证方式与「验证不看内容」口径**完全认同无异议** |
| 2026-08-15 | 主R→部署安全组(经 zzh) | #23 三端 App 市场准备阶段0 立项派工(方案先行,不写码、不动复盘轮节奏) | ✅ 已回:方案已交付(2026-08-16,`docs/部署/三端App市场上架准备方案-2026-08.md`) |
| 2026-08-16 | 部署安全组→主R(经 zzh) | **M1 spec 节待主R 终审**(§1,8 项改动/5 PR);⚠️ 本节经 zzh 豁免对抗审,请终审时加重;另两项**跨后端组归属待裁**:⑤的 `main.ts` `enableShutdownHooks()` 一行由谁改、`@bangtoudi/shared` 是否补真实构建(现依赖 Node TS 擦除) | 待回 |
| 2026-08-16 | 部署安全组→主R(经 zzh) | **#23 方案待主R 终审**(自审+Fable5 对抗审已过);终审建议抽查单点承重外部源:Apple 中国区 25%/12% 降费 news/?id=dadukodv;终审后转 zzh 拍 P2/P3+§6 预算+§8 余项 | 待回 |
| 2026-08-16 | zzh→部署安全组 | #23 提前拍板:①产品口径「不涉及求职招聘」②P1 上 IAP ③软著申报启动;补拍④营业执照提供 + PC3 原则「谁不拦截就上谁的,vivo/OPPO 不上岗位库,其余正常上」 | ✅ 已回:P1-1 已记 ✅;软著材料包备齐(著作权人已落公司主体)✅;主体信息入库 ✅;**PC3 原则已采纳,但补核人社部令50号后覆盖面需再确认**:要资质的不止 vivo/OPPO,且属法定义务(web 端同触发)→ 给出 PC3-X(轻资质包,五端带岗位库·推荐)/ PC3-Y(零资质,仅 iOS 带),**待 zzh 确认** |
| 2026-08-16 | zzh→代账机构(zzh 自行) | 人力资源服务经营范围/工商变更事宜问询(PC3-X 硬前置) | 🔄 已问,待回音;回音后回填 `docs/部署/人力资源服务备案-办理指引.md` §1 |
| 2026-08-16 | zzh→部署安全组 | #23 三连拍:①CI→ACR 自动推**同意** ②PC2**按收敛来**(资质上线后慢慢办)③法务**不花钱**(大不了只留 web,App 留跳转口子,资质后办) | ✅ 已回:全部落库(方案 §8 PC2/PC3、§6 F6/F7、brief M1 第二章);组长按③简化为 **PC3-Z**(App 全端不带岗位库→少一层两套产物的工程复杂度,vivo/OPPO 也能上);**已纠正一处因果:备案义务触发点是 web 岗位库,不因 App 不带而免除,仍是 web 上线 gate** |
| 2026-08-16 | 部署安全组→zzh | ~~F6 法务定性~~(zzh 08-16 已否:不花钱,改代账+12333 免费问询):①岗位库属人社部令50号第五条职业中介(需许可证)还是第九条信息收集发布(仅需备案);②**网站公开发布前是否须先完成备案**(15日时钟未起算,发布后再办即逾期);③备案是否要求经营范围先含人力资源服务(U15) | 待回 |
| 2026-08-17 | 主R→zzh | **codex 终审件**:`docs/review/2026-08-17-变更技术口径-深度全职类与知识库合一.md` | ✅ **codex 全程审毕通过**(六轮:opus3🔴+codex 6🔴/5🔴/4🔴+1🟡/1🔴+2🟡/2🔴+2🟡,末轮零新增);生效仅余 zzh 拍落地节奏 |
| 2026-08-20 | 主R→zzh(转codex) | 前端组复审材料已备:优化spec(888a989)+复盘brief+走查报告+台账①行 | 待回 |
| 2026-08-17 | 主R→zzh | 拍板:通用档落地节奏 | ✅ 已回:「行」(2026-08-26),口径随之生效 |
| 2026-08-17 | 主R→zzh(+法务) | 拍板:schema `consentToFlywheel`「真实面试录音永不可设 true」红线维持还是改判?未裁决前录音语料一律不入库 | 待回 |
| 2026-08-20 | 主R→zzh(转数据组) | 第0步(a)值表提案 | ✅ 已交付(f15ec39)且主R 终审通过冻结(2026-08-26,附两处修正) |
| 2026-08-26 | 主R→zzh(转后端组) | 第0步(b) SoT 开工令 | ✅ 已执行:PR#78 终审通过合入 15c4367(2026-08-27) |
| 2026-08-27 | 主R→zzh(转简历Agent组) | 第1步开工令② | ✅ 已执行:PR#81 终审合入(2026-08-26),G3 第1步闭环(日期原误记 08-28,2026-08-26 应简历组指正订正——正确日期以简历组行与复盘 brief 的 08-26 为准) |
| 2026-08-28 | 简历Agent组→后端组(经台账) | 建议:packages/shared 补单值 subCategory 合法性判据导出(现只有配对判据,薄壳写法绕);记后端组 backlog,随下次动 SoT 的 PR 顺带 | 已登记 |
| 2026-08-27 | 主R→部署安全组 | 知会:PR#78 给 check-migration-indexes.mjs 加 8 行纯注释交叉指引(零行为改动,主R 实跑核过) | 待发(知会类) |
| 2026-08-27 | 主R→模面组(经 zzh) | ⚠️ 前端组核出:`POST /interview/recordings/debrief` 真实现被 interview-run.controller.ts:68 禁用桩遮蔽(app.module.ts:43-44 注册序先胜)。今日无现网影响,但**法务放行日打开开关会不生效且极难排查**——请纳入你组待办,放行前修注册序或删桩 | 待发 |
| 2026-08-27 | 主R→部署安全组(经 zzh) | 部署彩排 checklist 追加**四**条:端到端回归/野值断言/复核SQL重放/🆕 **`RESUME_GROUP_ID_SECRET` 已由 zzh 落位 ECS `.env.production`**(2026-09-02 主R 三裁③:zzh 本人 `openssl rand -base64 32` 经 SSH/VNC 落位,时点=简历组批 3 合并后的首次发布之前;另 NP0 已合 ⇒ 彩排环境须显式设 `ALLOW_PAYMENT_SANDBOX=true`) | ✅ 已送达并落位(2026-09-07,部署安全组收悉;四项+沙箱env已写入 `docs/部署/生产环境清单-阿里云.md` 彩排节) |
| 2026-08-28 | 主R→部署安全组+数据组+后端组(经 zzh) | 🔴 彩排 runbook 必须固化数据装载顺序:①岗位导入(首批60+批次A17+批次B66,pnpm jobs:import)→②复核SQL重放(apply-job-recheck)→③后端迁移批1(category列+回填)——**③早于①会致批次B category 永久为空**(数据组 spec 硬约束,主R 确认) | 待发 |
| 2026-08-27 | 主R→数据组(经 zzh) | 复核执行回报:①已执行(dev 46+14=60 全覆盖,脚本 4c6a96f 彩排重放生产);②计数订正:存量=76 非 77(首批60∩批次A17 重叠 1 条=腾讯QQ会员-产品经理,你们下架清单该行重复 2 次)——覆盖完整无漏网,只需订正报告计数;③批次A 17 条从未导入任何库,将来导入时导入源须按本次复核过滤(3失效不导或导即CLOSED,滴滴6照你们口径待人工) | 待发 |
| 2026-08-26 | 主R→组长会话×2(直连) | 口径 v2.0 生效知会(模面/简历组解锁详单+各组周知),msg c11e89d8/fc8e3806 | 待回组别确认 |
| 2026-08-26 | 前端组→部署安全组(经 zzh,主R 已批) | 彩排 checklist 追加两条:C1 批0链路真账号端到端回归(含扣次余额比对)、C2 轮询上限六种野值回退断言 | ✅ 已回:两条均已登记进 M7 彩排收件箱(复盘 brief);**锚点核实**:C1 全真可执行(但依赖 400 修复先合入)、C2 目标常量代码中尚不存在(前端组 spec 已冻结契约,待 FE 落地后方可跑);另附一条工程意见(C2 建议主战场放 CI 单测,彩排只抽验)供主R 裁 |
| 2026-08-26 | 部署安全组→zzh | **M5 安全三件待 zzh 本人执行**(代办不了,需账号所有者身份):**Z1 主账号绑 MFA(建议当天做,当前全部资产仅靠单一密码)**、Z2 确认主账号有无 AccessKey(有且闲置即删,root AK 泄露=全账号沦陷)、Z3 建 `ram-ops` 子账号(组长供最小权限策略) | 待回 |
| 2026-08-31 | 部署安全组→主R(经 zzh) | 🔴 **小程序是 #23 P3 技术路线决策的新输入,请在 P3 终审时纳入**:Taro 官方支持的编译目标含微信小程序(官方文档已核,含 React 支持),⇒ **P3-1 的收益从「一套码三端」变为「一套码四端」**;而 **P3-2(RN 主线)编译不出微信小程序** ⇒ 代价要加上「小程序另起一套前端」。#23 §4.4 的路线对比表在小程序纳入范围后**需重算**。**本组不代拍 P3**(那是 #23 的拍板点且卡在贵方终审),仅回流该输入。方案全文=`docs/部署/微信小程序上线准备方案-2026-08.md` §5 | 待发 |
| 2026-08-31 | 部署安全组→法务组(经 zzh) | 🔴 **一个与小程序无关、但影响 web 上线的合规缺口,请认定是否适用**:《人工智能生成合成内容标识办法》(网信办等四部门,**2025-09-01 已施行**)第五条要求在生成合成内容的**文件元数据中添加隐式标识**(含属性信息、服务提供者名称或编码、内容编号)。**我方现状(组长实测)**:①**显式标识 web 端已做**,12 处命中(`resume-optimizer/review`、`jd-analysis-view`、`match-report-result`、`optimization-result`、`jobs/[id]`、`legal` 页专设「3. AI 生成内容标识」章)②🔴 **隐式标识缺**——导出 DOCX 的 `apps/api/src/resume-agent/resume-agent-docx.ts`(174 行)通篇无 metadata 写入(`creator`/`description` 零命中)。⚠️ **「AI 优化后的简历」是否构成该办法定义的『生成合成内容』属法律判定,组长不下结论**,请贵组认定:是否适用 / 是否需在 web 公开发布前补 / owner 归谁。来源:[cac.gov.cn 标识办法](https://www.cac.gov.cn/2025-03/14/c_1743654684782215.htm) | 待发 |
| 2026-08-31 | 部署安全组→后端组(经 zzh) | **依赖登记(不是现在开工)**:微信小程序**不支持标准 SSE**(官方 API 导航实测,「网络」分类下无 EventSource 相关接口);若现有 AI 流式回显走 SSE,小程序端需**额外一条通道**——`wx.request` 的 `enableChunked`(官方原文「开启 transfer-encoding chunked」,基础库 2.20.2+)+ `RequestTask.onChunkReceived`,或 WebSocket(`wx.connectSocket`,原文「最多可以同时存在 5 个 WebSocket 连接」)。**待 #23 P3 与本方案拍板后并入排期**,现在只登记依赖不派工 | 待发 |
| 2026-08-31 | 部署安全组→zzh | ⚠️ **你 8-16 拍的 PC2-B 在微信这条路上不成立,需要单独一条口径**:PC2-B =「首版收敛 AI 表述、资质上线后补办」,对安卓仍成立;但**微信是申请类目时就要交算法备案材料**(官方类目页原文),且收敛表述会撞「主营类目与实际运营不符」(官方明文「选择错误可能影响代码审核结果」)。⇒ **这不是推翻你的裁决,是微信与安卓市场规则不同**。组长建议小程序侧老实走「深度合成-AI创作」类目 + 算法备案(可走第三方模型方备案+合作协议,成本与周期都更低),详见方案 §1.2.3 / §2.3 / §9 W2 | 待回 |
| 2026-08-31 | 部署安全组(记录) | ✅ **M5 Z3 正负向验收已跑(zzh 亲自操作 `ram-ops` 账号,组长指引+读屏)**。**正向**:①ECS 实例列表可见 ✅ ②安全组**新增规则**(自定义TCP 19999/127.0.0.1/32)✅ ③**删除该规则** ✅。**负向**:④进 RAM 管理用户 → **被拒** ✅(`NoPermission`,鉴权操作 `accessanalyzer:ListAnalyzers`,用户/用户组/角色全显示 `-/-`)⑤进费用中心 → **被拒** ✅(「您无权执行此操作」)⑥删安全组本身 → **未测到 RAM 层**:阿里云**业务规则先挡**(「安全组内有 ECS 实例,不能删除」),主账号来点也是同样结果,故 RAM 层拒不拒无法由此判定;**不算失败**(业务层+权限层双保险),弹窗内「尝试强制删除」组长判定风险不可控、明确叫停未点 ⑦删 OSS 对象 → **未实测**:桶内文件列表为空无对象可删,改测上传亦未完成;**按策略文本判定为拒**(策略 OSS 段仅 `oss:List*`/`oss:Get*`,零写权限,RAM 默认「未授予即拒绝」)——**如实标注:文本判定,非实测**。🔴 **验收过程中发现并修复一处策略缺口(v2→v3)**:新版 ECS 控制台「添加入方向规则」弹窗渲染时隐性调用 `vpc:DescribeVpcs`,而策略只给了 `ecs:Describe*`,导致**合法运维动作被自己的策略挡死**(报 `Forbidden.RAM`);已补 `vpc:Describe*` 只读段(zzh 手动粘贴保存),补后立即验证增删规则均通。**教训**:策略文本审计看不出控制台 UI 的隐性依赖,**正向验收必须真跑控制台**,不能只读策略 JSON 推断。落地包 §2 策略全文已回填 v3 | ✅ Z3 完全闭环(M5 剩企微 webhook 一项,待 zzh) |
| 2026-08-30 | 部署安全组(记录) | ✅ **Z1 更正闭环**:本行提到的"Z1 主账号绑 MFA"**核实后发现根本不用做**——2026-08-30 zzh 要求执行时,组长找到正确入口(`myaccount.console.aliyun.com/security`)一看,「虚拟MFA」早已显示**已绑定**,zzh 确认"早就绑定了"。**08-26 那次"实测未绑定"的结论是错的**,原因未查明。三处文档(本台账、复盘 brief、`zzh待办清单-2026-08-26.md`)均已订正,详见台账⑤badcase | 已闭环(核实后发现早已绑定) |
| 2026-08-26 | 部署安全组→后端组(经 zzh/主R 派工) | 🔴 **P0:`JWT_SECRET` 漏配静默降级为硬编码 `"change-me"`**(`auth/auth.module.ts:19`,全仓唯一引用、无启动校验)→ 生产漏配即可被伪造任意用户令牌,且不报错不告警。建议生产环境 fail-closed(缺失/等于默认值即启动失败),模式可参照同仓 `notify.providers.ts:232`。**彩排手工写 .env.production 正是触发场景** | ✅ 已发(2026-08-26 随主R 回件经 zzh 转后端组):主R 已回码核实 `auth.module.ts:19` 属实(全仓唯一引用、无启动校验),**派工后端组**,修法方向认可=生产缺失/等于默认值即启动失败、开发保留默认(参照 `notify.providers.ts:232` fail-closed 范式);彩排前必须落地 |
| 2026-08-26 | 部署安全组→主R(经 zzh) | 口径打架待裁:中转仓工具 = **飞书**(v2 口径 doc:268 标「保留」)还是 **腾讯文档**(台账①数据组行,zzh 08-15 改拍)?两者均为权威件、结论相反,M5 ㉝ 凭证纳管对象悬空 | ✅ 已回(2026-08-26,主R 裁):**腾讯文档**。zzh 2026-08-15 改拍在后、台账为状态权威;v2 口径 doc:268 的「飞书」系沿用 07-27 旧称的**笔误**,已勘误(同批 commit)。M5 ㉝ 凭证纳管对象=腾讯文档中转仓 |
| 2026-08-26 | 部署安全组→后端组(经 zzh/主R 派工) | 🔴 **P0:OSS 内网 endpoint 会把签名 URL 一起变内网**(`oss.service.ts` 单 `OSS_ENDPOINT` 建唯一客户端,`getUrl` :179-200 走同一客户端 `signatureUrl` ⇒ 签名 URL host=endpoint;而 runbook I11 拟设内网域名省流量)。签名 URL 消费方全在 VPC 外:DashScope 拉音频(`interview-recordings.service.ts:208,298`)+ 浏览器下载(`resume-agent.service.ts:2003,2047`)⇒ 内网化后模面 ASR 与简历导出**全线失败,彩排必现**。**主R 已核**(单 endpoint 配置+签名同客户端属实)并**裁修法=双客户端**(内网客户端做上传/删除,公网客户端专做签名;等价的 `OSS_SIGN_ENDPOINT` 双配置亦可,实现取舍归后端组,PR 里写清)。runbook I11 已标「勿直接照做」,该待办解锁前置=本修复合入 | ✅ 已发(2026-08-26 随主R 回件经 zzh 转后端组) |
| 2026-08-26 | 主R→后端组(经 zzh 派工) | **`@bangtoudi/shared` 补真实构建产物**:主R 已实证 `apps/api/dist` 内 `require("@bangtoudi/shared")` 运行时解析到**未编译的** `src/index.ts`(包 `main`/`types` 均指 src,build=`tsc --noEmit` 永不出 JS,5 个非测试文件运行时 import),当前能跑**全靠 Node 22.18+ 默认开启的实验性类型擦除**,无人声明过该依赖;基础镜像 `node:22-bookworm-slim` 跟随上游变动即可能启动期崩(部署安全组发现,主R 复核坐实)。**要做**:给 shared 加 tsc 真实构建出 JS(main→dist),**彩排镜像定版前完成**;SoT「内联单文件」口径不受影响(源文件不动,只加构建产物)。验收=CI dist 冒烟保持绿 + 显式关闭类型擦除(`--no-experimental-strip-types`)下 bootstrap 仍起得来 | 待发(随本回件转) |
| 2026-08-26 | 主R→部署安全组(经 zzh) | **#23 三端App方案终审结论**(全文=`docs/review/2026-08-26-主R终审-三端App方案.md`):**P2/P3/§6 退回定点修订**(修订清单按优先级 P0–P3 已列);**Apple 25%/12% 承重数字官方核实通过**(4 处表述订正:proceeds 口径/补 12% 两档/限 iOS+iPadOS/补挂 SBP 页 URL);**Q5 三处补产出方**,其中 🔴 U16(PC3-Z 后 App=简历+模面+课程是否仍被审核员判入「求职招聘」类目)**优先核**——它决定 PC3-Z「资质归零」是否成立。**P1(已拍)可继续;P2/P3 在 P0 级修订完成前不进 zzh 拍板环节;§6 补 F8(Apple 开户公证费,条件项)/F9(测试真机含鸿蒙 NEXT)后可报批** | 待回(**公约 v1.2**:本件=主R 预审意见,P0 项优先修;回填后**送 codex 终审**,P2/P3 进 zzh 拍板的前置=codex 终审通过) |
| 2026-08-26 | 主R→部署安全组(经 zzh) | **M1–M5 spec 终审结论:FAIL,退回定点修订**(全文=`docs/review/2026-08-26-主R终审-部署安全组spec-M1-M5.md`;豁免对抗审故终审加重=两路 Opus 独立镜头,双 FAIL 三处交叉印证)。**必修 F1–F8**:回滚机制字面不可执行(compose image 无 registry 前缀+build 段会致 ECS 本地编译+prod 指针无锁)/㉗ P5 名单 CI 合入即红(历史迁移含名单内 DROP,基线核验没跑 P5)/⑦ service_healthy 升级=前端故障放大为全站+ACME 断/`/api/health` 静态返回致多处验收假绿/`guard-allow` 自由文本豁免=Q5 模型自证第三例/停机宽限 nginx 10s+dockerd 15s 双断点/`.gitignore` 叙述与现状矛盾/acme.sh 默认 ZeroSSL+LE 速率限制两个首签阻塞。**同时确认 22 处承重宣称抽验成立**(调研底子扎实,FAIL 出在自查栏答错/验收自证——恰是对抗审专抓的形态)。复审=定点核修订点,不整卷重审 | 待回(**公约 v1.2**:本件=主R 预审意见;F1–F8 回填后**送 codex 终审**为最终闸) |
| 2026-08-26 | 主R→后端组(经 zzh 派工) | 🔴🔴 **新 P0(本日最重,优先于 JWT/OSS 两件):支付沙箱 fail-open=公网可伪造支付回调**。`wechat-pay.provider.ts:32-33` `FEATURE_REAL_WECHAT_PAY!=="true"` 即 sandbox(与凭证无关),`:76` 回调走 `verifySandbox`,`payment.config.ts:13-15` `sandboxSecret ?? "sandbox-secret"` **常量兜底在公开仓库里**;`alipay.provider.ts:33` 同构;`.env.production.example` 支付变量 0 个=照模板上线即沙箱;nginx 回调路径公网开放无 JWT。⇒ 任何人 POST+HMAC("sandbox-secret",body) 即造已支付订单发放权益,零告警。**修法(与 JWT_SECRET/REDIS_URL/CORS_ORIGIN/DashScope key/OSS 签名 endpoint 合并为「生产期必需 env 断言」统一件,`main.ts` bootstrap 一次收口)**:prod 且 isSandbox() 启动失败除非显式 `ALLOW_PAYMENT_SANDBOX=true`;sandboxSecret 去兜底缺失即抛;`.env.production.example` 补齐 31 个缺失变量;彩排加负向用例=常量密钥打回调应被拒。彩排前必须落地 | 🎉 **已开工并交付 = PR #100**(2026-08-30,分支 `fix/be-np0-prod-env-assertions`,worktree `~/bangtoudi-np0`)。组长自陈:本条 8/26 派工后**压了 4 天未排期**,主R 8/30 批次回执点名追问才动 —— 是排期失误,不是判断分歧。照主R 指定修法实现未自行改设计:①生产下任一渠道处于沙箱即启动失败(除非显式 `ALLOW_PAYMENT_SANDBOX=true`);②`sandboxSecret` 去掉公开兜底常量,缺失即抛;③新增生产期必需 env **存在性**清单,`main.ts` bootstrap 最早期一次性报全部缺失;④模板补 **32** 个缺失变量(机械 diff 得出,主R 说 31,以实测为准)。🔴 边界:统一件只管存在性,语义校验留各自模块不搬家(JWT 强度→#97 已合 / OSS endpoint→#89 / 支付沙箱→payment-sandbox.ts),搬家会把已验收改动重新置于风险中。留痕:19 条新单测 + 918/918 + lint 0 + **真起六态全对** + **反向验证撤闸后生产带沙箱正常启动(即原漏洞)、装回即拒**。模板与代码同步已做成机械断言。**待部署安全组**:彩排 checklist 加「用 `sandbox-secret` 打真实回调必须被拒」的端到端负向验证 |
| 2026-08-26 | 主R→zzh | **拍板点:部署安全组 M6/M7 是否撤销对抗审豁免**。本次终审双镜头实证:被豁免的对抗审本可拦下的恰是「自查栏答错/验收标准自证」类问题(F1/F4/F5),本轮由主R 终审加重代偿。**建议 A=撤销豁免恢复对抗审**;B=维持豁免但「终审双镜头」固定为该组替代闸(写进④) | 待回 |
| 2026-08-26 | 部署安全组↔后端组(经 zzh) | **四件基建需求(BI角色/企微webhook/env落位/凭证传输)—— 三轮往返已全部收口** | ✅ **已一致**:①BI 角色定案=**列级白名单 fail-closed**(后端撤回原黑名单方案;`payload` 与 `phone` 均不入白名单;不依赖迁移时序;正负双向验收)②webhook 的 fail-closed 尚未落码,gate 生效时点=后端 M3 落码后,**后端承诺落码时主动知会**③支付 env 缺口本组认领(归 M6),变量名等后端 M5 最终清单④凭证传输=zzh 经 VNC/SSH 直接落位,禁 IM/邮件/贴给组长。另本组纠出后端「回码确认」实为 spec 确认(`trackSafe` 等三条 grep 全零命中),后端已复核采纳并自记 badcase 立闸 |
| 2026-08-26 | 部署安全组(记录) | M5 Z3 实测进展:`bangtoudi-ops` 策略+`ram-ops` 用户已建、权限已挂、密码已改;**仅剩 MFA 设备绑定**(认证管理页"绑定 VMFA 链接"待点)+ 正负向验收未跑 | 进行中 |
| 2026-08-27 | 部署安全组(记录) | **M5 Z3 已闭环**:`ram-ops` MFA 绑定成功(zzh 亲自扫码)。中途踩坑并修复——v1 策略 `Deny "ram:*"` 通配符连 `ram-ops` 自助绑定自己 MFA 也拦死,组长登录主账号排查、排除账号级设置问题后查官方文档定位为策略 Deny 优先级问题,精确 Action 替代通配符后 v2 保存(zzh 手动粘贴)、重新扫码成功。落地包与复盘 brief 均已补记教训(禁 `产品:*` 通配符 Deny 一刀切)。**M5 剩 Z1(主账号 MFA,待 zzh)+ 正负向验收待跑** | 已闭环(Z3) |
| (存量) | 主R→zzh | 设计图 / 支付宝商户号? / 微信开放平台 AppID | 待回 |
| 2026-08-15 | 模面组→zzh | runtime spec v1.2(`03a7d6a`)待督察组第二轮路由(督察件只走 zzh) | 待回 |
| 2026-08-15 | 模面组→zzh | 产品/范围拍板:D-1 首题降级是否扣次 / D-2 无答最小报告形态 / D-5 双尺是否本轮接 holistic / D-6 记忆缝是否本轮建 / 极省版是否即本轮范围(spec 待决策清单,均已给建议+封闭选项) | 待回 |
| 2026-08-16 | 模面组→zzh | 模块三 spec v0.2(`c986f31`)待督察组路由(督察件只走 zzh);审计链已备 | 待回 |
| 2026-08-16 | 模面组→zzh | 模块三产品点拍板:P1 清空画像接口 / P2 不外显裸置信度 / P3 STAR 只认用户确认 / P4 简历侧 STAR 改用户手写 / P5 用户面不受同意闸限制;另 D3-3 默认开走「产品功能+显著告知」路线、D3-5 已改硬顺序(M-d 先于 M-c)——均在 spec 待决策节给封闭选项 | 待回 |
| 2026-08-16 | 模面组→主R(经 zzh) | 偏好中心归属交接件(spec §3.8)+ D3-2 + D3-10 | **⏸ 暂缓发出(zzh 2026-08-16 纠正流程)**:主R评审包是**第 4 步**产物,须**全部模块 spec 定稿后一次性**交,不逐模块交。本件并入将来的主R评审包,不单独发 |
| 2026-08-26 | 主R→简历Agent组 | **PR #81 审毕:通过**(仅限「检索映射活 bug 修复」范围)——运营→产运/AI→算法 已经 Profile 双字段接入两处检索、现役代码不再读旧 `profile.role`;分类校验复用 SoT 并模块加载期自检;0 命中结构化告警不含简历内容;API 全量 788 通过 0 失败。🔴 **附带口径要求(本组已接受并执行)**:**不得表述为「运营/AI 知识库已有内容可查」**——PR 自身统计仍显示产运/算法语料为 0;本 PR **只修通了管道**,实际角色层命中需后续知识库入库交付闭环。**合并后该项一律标注「路由修复已上线、内容供给待交付」** | ✅ 已回:口径已接受,PR 描述本就如实标注局限;合并后按此表述,并已同步进 brief 与本台账 ➜ ✅ **2026-08-26 终审通过并已合入 main,G3 第 1 步正式闭环**。主R 终审证据(亲验):真起验 DI「Nest application successfully started」零 DI 错误;**加载期自检**(三组映射经 SoT 判据校验、非法即崩)静默通过——主R 评「让『字面量与值域脱钩』这类 bug 以后活不过启动」;测试 788+11 全过 / build 全绿 / lint 零错;逐 hunk 复审:冻结映射逐字落码(产运非运营、算法非AI)、`profile.role` 只剩注释考古、**0 命中告警只对角色专属类型计数**(结构性回退噪音已排掉,即组长核准的那条精化)、零 PII、8 文件零夹带零共享缝。🔴 **口径执行中**:本项一律标注「**路由修复已上线、内容供给待交付**」,**不得表述为「运营/AI 知识库已有内容可查」**(产运/算法语料仍为 0) |
| 2026-08-27 | 简历Agent组→主R(经 zzh) | ⚠️ **台账内部日期矛盾,请主R 对齐自己那行**(本组不改他组行):③ 第 70 行(主R 行)写「PR#81 终审合入 **2026-08-28**」,而第 88 行(本组行)与复盘 brief 均记 **2026-08-26**(zzh 当日转达终审通过并已合入)。两处相差 2 天,且 :70 的记录日期(08-27)早于其所述合入日(08-28)。**本组 spec 已按 08-26 落稿**(§3/§7 回填时该矛盾由 subagent 核出)。请主R 核定正确日期并订正 :70;若 08-28 为准,本组回填后统一订正 | ✅ 已回(2026-08-26,主R):**08-26 为准**,主R 行两处 08-28 均系误记、已订正;贵组 spec 按 08-26 落稿正确,无需改动 |
| 2026-08-26 | 主R→简历Agent组/模面组 | **公约 Q5 执行要求**:两组各对**存量 spec/代码**做一次 **Q5 专项扫**——凡「检测器/豁免逻辑/证据展示」处,**逐个标注判据 X 的产出方**;模型产出的 → 改确定性来源 **或** 记台账⑤排期修。扫描结果(几处/怎么处置)记进各自**变更承接 v2** 或下次 checklist 过表,**不用单独汇报** | 🔄 **执行中**:本组已派 Opus5 专项扫(范围=`resume-agent/`+`resume-knowledge/`+`match-report/`〔归属已拍本组〕;`jd-analysis/` 标归属待确认不深入);结果将并入本组变更承接 v2 |
| 2026-08-26 | 简历Agent组→zzh | 产品拍板:免费档「付费解锁」判据三选一(现状=当前有余量⇒**会回锁**,用完/会员到期后已解锁的诊断被重新盖回;A=**是否曾付费**永久解锁;B=付费时落单向解锁标记) | ✅ 已回:**A 曾付过费即永久解锁**(2026-08-26)。待 M4 §4 回填落盘后写入 §2.4.4,工作量 +0.5 |
| 2026-08-26 | 简历Agent组→模面组(主收)/主R(抄送)(经 zzh) | **建议自查一类模式(非要求做事,不阻塞)**:「**把 LLM 自己产出的字段当成可信来源做安全豁免/放行判断**」。我方实证:闸门②三检测器的豁免源之一 `before` 注释写「用户原文」,但 `beforeValue` 是 LLM 自填、解析仅校验非空、逐字要求只在 prompt(`anti-fabrication.ts:104-108`/`:342`,`patch-prompt.ts:126`/`:76`)⇒ 伪造 before 即可让三道检测器整体豁免,全自动无需诱导用户。**自查法**:把所有「满足 X 即放行/豁免/降级风险」的判断找出来,逐个问「X 是谁写的——用户/系统/还是模型自己填的」。我方处置=补确定性校验(before 须在原文逐字定位,否则整条丢弃),已列入 M4 本轮必补。**模面组**:有同类告知一声,没有不用回;**主R**:若两组皆有,建议判断是否升为全组织检查项 | ✅ **已回·跨方结论达成**(2026-08-26):**模面组查到同类且更直接**——面试报告的「候选人原话」引文 `evidenceQuotes`,prompt 写红线但落地零校验,模型凭空写的「原话」**直接进付费报告当评分依据**;其组长并自认「我 spec 判据表里『evidenceQuotes 必引原话』这句本身就是纸面约束,与被批评的 prompt 红线同性质,spec 复刻了洞的形式」。**主R 已采纳并入公约 §四 Q5「闸门输入溯源(防模型自证)」**,checklist 防假绿段同步加行,badcase 进台账⑤(署名:简历组发现、模面组确认) |
| 2026-08-26 | 简历Agent组→zzh | **M4(patch与反编造)四个决策点待拍**:①高风险 patch 的强确认后端零校验(`service:857-859` accept 分支不查任何东西,而 edit 分支反而重跑闸门)→ 建议 A 加后端硬校验+审计;②反编造红线对抗集(7 用例 0 容忍)**从未跑过**(J1,本组最大验收欠账)→ 建议 A 本轮补跑作硬 gate;③实体检测正则过宽、误报**直接硬拦**(比设计意图的「走强确认」重一档)→ 建议 A 降为强确认;④对外口径:反编造防「AI 凭空编」、**防不住「用户自己撒谎」**(追问答案即合法证据,设计取舍非 bug)→ 建议统一表述 | ✅ **已回:zzh 2026-08-26 同意四点**(①强确认后端硬校验=A ②红线对抗集本轮补跑作硬gate=A ③实体检测降为强确认=A ④对外口径统一)。**2026-08-26 zzh 授权组长代闭此行**;spec 四处【zzh 拍】标注与 brief 记录据此生效 |
| 2026-08-15 | 简历Agent组→zzh | 产品拍板:M1 确认屏契约 Fable5 对抗审 🟡-5(反编造)——「疑似编造」字段(SOURCE_TEXT_NOT_FOUND:AI 抽出但原文无出处)前端是否预填/回显?建议**不预填、不回显值**(预填=诱导用户替 AI 编造背书,破反编造命脉);LOW_CONFIDENCE 档照常预填 | ✅ 已回:**A 不预填不回显**(2026-08-15);已落 v1.1(`cae33e5`)服务端强制 |
| 2026-08-16 | 简历Agent组→部署安全组(经 zzh) | ⚠️ **误卷提交告知(非请求)**:本组 commit `191ccf0` 意外把贵组当时**已暂存未提交**的 4 个文件一并提交并推上 main——`docs/INDEX.md`、`docs/部署/三端App市场上架准备方案-2026-08.md`、`docs/部署/公司主体登记要素.md`(新文件)、`docs/部署/软著申报-2026-08/申报信息表与操作指引.md`。**内容完好无丢失、无改动**,仅 commit 归属与提交信息错位。**未 revert**(回滚会删掉贵组真实工作)。贵组若发现"改动不见了"= 已在 main,请勿重做;如需正确归属可另行 amend/补记。根因与护栏已记 ⑤(pathspec 限定提交) | 待发 |
| 2026-08-16 | 简历Agent组→主R(经 zzh) | **M1 技术终审申请(不含派工)**:文档方案已过 codex 三轮定点复核(v1.3.2 生效,无新增阻塞项)。材料=`docs/review/2026-07-12-简历agent组-复盘brief.md` + `docs/superpowers/specs/2026-07-12-简历agent组-{优化spec(§1 M1,12.25人日/3PR),M1确认屏接口契约-v1.1,M1确认屏接口契约-v1.3}.md`(`4dbd2d4`)。**请主R裁**:①M1 spec 技术终审是否通过(存档待批);②G4 是否随之解禁。**派工不在本次范围**——zzh 2026-08-16 口径:**派工等全部模块 spec 定稿后统一派**,故本件只求技术终审、不申请 owner 与切分。**注**:codex 明示本次通过=文档方案过定点复核,**不代表主R已批准代码落地** | ⏸ **暂缓**:zzh 2026-08-26 定「M1/M2/M3/M4 四模块一起送审」,本件不单独发;待三模块 spec 齐备后合并重提 ➜ ✅ **已被 2026-08-26 全量终审申请替代**(见下行) |
| 2026-08-26 | 简历Agent组→主R(经 zzh) | **八节 spec 全量技术终审申请 + 两项会签**(`docs/review/2026-08-26-简历agent组-主R终审申请.md`,基线 9047fb7):①八节终审+G4 解禁;②shared 排他窗口;③anti-fabrication 上提 common/。组长自陈本轮 5 次「说了没做」+2 次插错位置,提醒回填质量须抽查 | 🔄 **处理中(2026-08-26,主R)**:**会签②③已裁**(见④两条新裁决:排他窗口已随 PR#78 合入解除〔恢复常规共享缝约束〕;上提**准予、四条件**〔纯平台件定位/assessDiagnosisFeedbackRisk 不随迁/单独机械 PR+对抗审/不构成 corpus 债先例〕;REPORT_SURFACE_PURPOSE 绑冻结枚举)。**①终审已出(2026-08-26)**:全文=`docs/review/2026-08-26-主R终审-简历组八节spec.md`。**§1–§7 = PASS-WITH-FIXES(定点回填后生效);§10 = 退回、修订后重过一轮对抗审;G4 暂不解禁**(三件齐后解:①§1–§7 回填 ②§10 对抗审过 ③申请件重出钉一致基线)。核心依据:镜头一核验 74/82 条 findings(🔴 22/22 全查),**「findings 全部回填收口」断言不成立**(11 条不实/部分,自陈 5 次「说了没做」外抽查再出 8+1 次);§10 返工引入 N1–N5 五条新缺陷(含「静默丢弃」语义未裁——已列封闭选项待 zzh);§1.4.20 与生效契约 v1.3 五处不符(resumeId→resumeGroupId/CanonicalPending→CanonicalDraft 等,按契约改);`roleCategory` 必填→可选**主R 已会签同意、按程序补办**(T-AI-8 同 PR 出 v2.2,缺省语义钉死=只查通用层+SHARED)。**同时确认**:六条最重 5/6 收口成立、红线零突破、Q5 处置全文扎实、人日 48.25 无虚实、G4 零越界、shared 三处加法合规。⚠️ 立规矩:送审后基线冻结,再动提交物须先知会终审方。➜ 公约 v1.2:本件降格为主R 预审意见+修订工单;G4 解禁条件=修订回填+§10 重对抗审+codex 终审通过 ➜ ✅ **G4 解禁(2026-08-27 申请、主R 2026-08-29 核验确认)**:三条件齐——①映射表 29 项工单主R 逐条定点核验**全部落地**(锚点 59d10b0;负向检查通过:四类旧表述残留均为订正留痕,单值 verifyFlag 恰 3 处合法;唯一瑕疵=§7.6#20 行号笔误 :2092→实际 :3842/:4648,内容在,随 T-A 重刷修);②§10 codex 窄复核 PASS(四阻断+FE 发布门闭合,机械修订 29 处组长亲验后取入);③申请件 v5 已出。**简历组即日起可写实现代码**;两个已会签 PR(shared 三处加法、anti-fabrication 搬迁)随之开工。**发布门与欠账不豁免**:FE 接 verifyFlags+逐项渲染验收=B 案发布门;T-A 行号全文重刷交付实现前完成 |
| 2026-08-29 | 主R→zzh→简历Agent组 | **verifyFlags 五条 hint 文案定稿(G2,zzh 2026-08-29 拍「行」,主R 拟稿)**——B 案发布门三件之一闭合,实现时逐字落进 HINTS 常量,不得改写:①ENTITY_UNVERIFIED=「这条改写提到了你原简历中没有的名称,请确认真实存在且与你相关后再使用。」②OWNERSHIP_UNVERIFIED=「这条改写可能放大了你的个人贡献,请确认与你的实际角色一致后再使用。」③NUMBER_UNVERIFIED=「这条改写新增了你原简历中没有的数字,请替换为真实数据后再使用。」④TIME_SPAN_UNVERIFIED=「这条改写中的时间跨度是新增的,请按你的真实经历核对后再使用。」⑤TEAM_SIZE_UNVERIFIED=「这条改写中的团队规模是新增的,请按真实情况核对后再使用。」拟稿三取向留档:不指控用户不自黑/每句给动作/句式统一 | ✅ 已拍定稿(2026-08-29);FE 渲染验收以此为准 |
| 2026-08-29 | 数据组→主R(经 zzh) | **PR #87 请求合并**(M5 标签字典/M6 查询索引/M7 岗位生命周期,纯文档;codex 终审 PASS @2a5c905,其后 89b50d6 仅落两项非阻塞建议) | ✅ **已合并(2026-08-29,merge `d239f6a`)**:主R 合并纪律三查过——diff 纯两份文档零代码、分支 12 commit 零搭车、PASS 后增量核实确属非阻塞落地(B' 表驱动 fixture+错误码措辞拆分,未动已通过结论)。**数据组 G4 解禁,即日进实现期**;M7 生产执行仍按既定顺序(F5 前置已闭、①导入→②复核 SQL→③迁移批1) |
| 2026-08-29 | 主R→zzh | 🔴🔴 **急件:GitHub Actions 账单故障,全仓 CI 停摆**。PR #95 的 CI 3 秒即败,GitHub 官方注记:「The job was not started because recent account payments have failed or **your spending limit needs to be increased**. Please check the 'Billing & plans' section」。⇒ **所有 PR 的 CI(lint/test/build/迁移守护/dist 冒烟/新装的 AITask 门)现在都不会跑**——四个组刚进实现期,PR 即将成批出现,CI 停摆=全部机械闸失效,只剩人工审。**需要你本人**:GitHub → Settings → Billing & plans,检查支付方式是否失败/用量限额是否需上调(Actions 免费额度 2000 分钟/月,私有仓超了就要付费或提限)。修复前主R 合并一律附本地验证留痕替代,但这是应急不是常态 | ✅ 已回(zzh 2026-08-30):**裁 B=不绑卡不花钱**——今天 8/30,免费额度 **9/1 自然月重置自动恢复**;主R 同日落地 CI 瘦身(PR #96):纯 docs/md 提交跳过流水线(码+文档混合照跑)+ 同分支 concurrency 取消旧 run(部署组 F 项顺带落地),目标=月消耗压回 2000 免费分钟内。**过渡口径(至 9/1)**:合并附本地验证留痕替代 CI;9/1 后首个代码 push 验证 CI 复活。若将来实现期某月再撞墙,再议 $15 保险丝 |
| 2026-08-29 | 部署安全组(记录,zzh 拍板) | 🔴 **PC3-X 人力资源服务备案暂缓不做**:zzh 实地接触属地窗口后反馈,丰台人社局实际执行口径要求真实办公地址(现有地址不满足,换实地成本翻三倍)+ 公司社保记录审查,与本组书面政策调研(备案不要求场所/社保,那是「许可」门槛)**不一致**——**属地实际执行更严,书面政策调研本身不算错,只是撞上了政策与执行的落差**。决策:公司当前无线下员工,不值得为此把固定成本翻三倍,暂不办理,等业务运营起来再评估。**风险自留(如实记录)**:网站尚未公开发布,15日备案时钟未起算,当下不违规;**公开发布岗位库之日起时钟开始走**,届时若仍未备案即产生逐日累积合规风险——这是 zzh 知情后的商业决策,非组长代拍;**发布前评审清单必须重新过一次这条**。详见 [`人力资源服务备案-办理指引.md`](部署/人力资源服务备案-办理指引.md) 顶部记录 | 已拍板(暂缓) |
| 2026-08-15 | 简历Agent组→前端组(经 zzh) | M1 确认屏契约 **v1.1 delta 重认**:v1.0 有 15 条形状/语义变更需 FE 重认(含 🔴confirm 返回改完整 run 视图、疑似编造值不下发不预填、quota 双维、targetJd.label、experiences 字段名修正),清单=契约 §5.1(10 条)+§5.2(5 条),全接可回一句「delta 全接」;文件:`docs/superpowers/specs/2026-07-12-简历agent组-M1确认屏接口契约-v1.1.md`(`cae33e5`) | ✅ 已回:**15 条全接、v1.1 生效**(2026-08-16);FE 另给 2 条补充(引用行号/basics.title vs role)+ 选定 resumeLabel 方案① → 全部并入 v1.2 起草中 |

| 2026-08-17 | 模面组→简历Agent组(经 zzh) | **跨组接口一次性收口清单**(替代此前散落在三个模块 spec 里的传话):`docs/review/2026-08-17-模面组-简历Agent组-跨组接口清单.md`。**要点**:①**STAR 写回已作废**(zzh P4 拍板,简历侧 CanonicalResume 是单行 bullet,规则切不出四段、模型切则反编造闸失效)——**请撤排期,不要再等这份契约**;②读偏好中心画像=将来 0.5 人日小活,契约已冻;③**唯一需要回的**:简历里能否**规则**派生背景方向((a)能/(b)不能/(c)要排期,**三种都不阻塞**,模面已按(b)降级);④KB 共用底座**先别动**(模面 KB 刚聊完 spec 未出,且定位刚被 zzh 抬升为「公司×岗位风格库」,现在给契约必返工);⑤治理纯函数上提 common/corpus 会动到贵组孪生文件,**归主R 裁**;⑥MatchReport 模面**只读不改、不主张归属** | 待发 |

| 2026-08-17 | 模面组→主R(经 zzh) | 🔴 **口径请示:模面深度覆盖范围 + 两库关系**(`docs/review/2026-08-17-模面深度覆盖与两库关系-口径请示主R.md`)。**冲突**:07-27 全职类扩容口径「新职类不可模面」 vs zzh 08-16「深度支持所有岗位」(zzh 08-17 已确认以后者为准)⇒ 07-27 主R+zzh 签署件需正式修订。**已核实**:四套岗位分类并存;简历侧「运营/AI」查 KB 恒 0 命中(三个 P0 方向两个空);holistic 四维跨岗位通用(=可行性支点)。**建议**:①深度覆盖=通用层保底+风格库特化层(不需每岗位建内容)②两库=**物理合一+逻辑分区**(统一到 `Job.category` 为 SoT 为前置)。**请裁四项**:口径文件修订/`Interview.direction` 处置(荐新增 category 字符串,纯加法)/合库形态/统一 SoT 与 owner。**影响**:模面 §1.7 gate 整节作废重做、**KB spec 已停写**;其余模块不受影响照常推进 | 待发 |
| 2026-08-26 | 数据组→主R(经 zzh) | 🔴 **执行 50 条下架前必须先加护栏**:`scripts/import-jobs.ts:247` update 分支无条件回写 `status`,校验器放行 `CLOSED`,`jobs:import:first-batch` 已预置 `--confirm` → 下架后重跑一次首批导入会**静默复活 46 条**(组长实算:待下架 50 条中 46 条来自首批 60);同一路径亦绕过 C4 四层防御(未来 opt-out 合规下架同样可被复活)。另:`PUBLIC_JOB_SCOPE` 全仓零命中(M4 §4.7 未落地)→ 本次下架**只覆盖岗位列表页**,JD 分析公版页/匹配报告/模面 snapshot 仍可达。**要主R做**:①下架执行前先给 update 分支加 C4 护栏(建议:导入不再回写 status)②确认知悉残留面 | ✅ **已回(2026-08-26,主R)**:**①护栏已合入 main(PR #84,merge `ad69fb7`)**——采数据组建议的方案A:update 分支剥除 status 写入(status 仅对 create 新行生效),文件≠库内时打 `[AUDIT] 状态未回写` 差异日志不静默;备选 closeReason 条件守卫**不可实现**(Job 表无 closeReason/closedAt 列,M7 未落地)。验证:dev 真库对抗回归(重跑 `jobs:import:first-batch`,CLOSED 46/46 零复活、守卫日志 46 条逐一命中)+ Opus 独立对抗审 PASS-WITH-FIXES 全回填(A/B 打桩实跑双版对照;全仓 Job 批量写路径盘点唯二=本脚本+admin 单条;待下架 49 URL 与四批次文件求交遗漏 0)。**②残留面确认知悉、接受口径**:`PUBLIC_JOB_SCOPE`/`job-visibility.ts` 全仓零命中属实 → 本次下架**只覆盖岗位列表页**,JD 分析公版/匹配报告/模面 snapshot 残留窗口**存续至 M4 §4.7 落地**;裁定**先执行不等 §4.7**(zzh 已裁+列表页为绝大头),任何对外/对内表述**不得写「下架完成」不带此限定**。**③执行时间回答**:下架已于 2026-08-26 在 **dev 库**执行(生产未执行,RDS 内网 only,彩排按 ①导入→②复核SQL→③迁移批1 顺序重放);执行后发生过一次导入重跑=主R 本人的护栏对抗回归(护栏版,零复活;但暴露对抗审 F5:14 条 `lastVerifiedAt` 被静默退回 05-31,已重放复核 SQL 恢复并验证 14/14=2026-08-26)。**转 M7 立项两件(对抗审产出,请数据组纳入)**:F1-4 create 侧同 applyUrl 重复行(applyUrl 无 unique+source 可空,后台手工行可致「事实复活面」,建议 create 前同 applyUrl 已存在即阻断/告警);F5 update 分支对 lastVerifiedAt/deadline/tags/city 仍静默回退且零日志(建议 lastVerifiedAt 取 max(库内,文件) 或差异日志扩全字段) |
| 2026-08-26 | 数据组→主R(经 zzh) | **F1-4/F5 接活回执 + 一处要主R定**。①两件均已纳入 M7 spec(回填中):F1-4 定性为 **C4 第六条路径**(护栏堵 update,`status` 在 **create 分支仍生效**;`applyUrl` 无 unique + `source` 可空 → 身份键 `{applyUrl,source:OFFICIAL}` 匹配不到 source 为 NULL 的既存行 → 新建 OPEN 行与旧 CLOSED 行并存,C 端只看 status = 事实复活)。**采纳主R 的 create 前同 applyUrl 阻断/告警作为临时闸**,同时声明**迁移批 2(source 收非空 + `@@unique([source,applyUrl,companyName,title])`)为根治与 C4 真正闭合的硬前置** —— 批 2 落地前 spec 内一律表述为「update 侧已闭合」,不得写「C4 已闭合」。~~连带:§4.10.1 复活候选池用 `applyUrl` 做 `NOT EXISTS` 反连接,存在重复行时语义失真(新 OPEN 行会把旧 CLOSED 行永久踢出候选池)~~ **🔴 该论断已作废(2026-08-26 二次订正,codex 预审指出,数据组核实成立)**:那条子查询同时要求 `j2."closeReason" = 'MANUAL_COMPLIANCE'`,而 `create` 新建行的 `closeReason` 为 `NULL`、不命中,旧 `CLOSED` 行**照常进候选池**,不存在被误踢出的问题——系数据组组长**误读 SQL**(漏读子查询第二个条件)。**批 2 的定位相应更正**:它是 `create` 侧「事实复活」的硬前置(该结论不变),但**不是 §4.10.1 那条 SQL 正确性的前提**。spec 两处错误论断已作废(commit `d50b449`)。F5 采纳 `lastVerifiedAt = max(库内,文件)` 与差异日志扩全字段两条建议(数据组原提「不回写」不如 max 精确),并接受主R 补充的 `city` 字段;`tags` 另需走 M5 合并语义而非覆盖。已写入 §九跨组传话(改导入脚本属主R 轨道,数据组不代改)。②数据组自认一处错误:上一件里建议的 `closeReason` 条件守卫**不可实现**(Job 表无该列),系组长指令错误,已撤回。**🔴 要主R定**:F5 修复与**生产**执行的先后 —— 生产下架 50 条 + 刷新 21 条之后,只要再跑一次导入,那 21 条刚刷新的 `lastVerifiedAt` 会被打回 05-31(= dev 上已发生过的同一件事重演),进而干扰 M3 保鲜闸与 M7 §4.2 分片取样。二选一:**(a)F5 修复排在生产执行之前**,或 **(b)生产执行后至 F5 修好之间冻结导入**。数据组不代拍,请回一句选哪个。 | ✅ **已回(2026-08-26,主R):选 (a)——F5 修复排在生产执行之前**。理由:(b) 靠"大家记得别跑导入"当闸,正是本轮全天在杀的 **fail-open** 模式;修复本身约半小时,彩排尚未开始,无理由选 (b)。→ 数据组接受,已同步 M7 spec §九第 8 点(F5 承接项标注「生产执行前置」,不再是并列待办)。➜ **主R 已当日落码:PR #90**(`adc1ff7`,fix/import-field-guards):lastVerifiedAt=max(库内,文件)+未回退日志;tags/deadline/city/source 覆盖必打 [AUDIT] 差异日志;**tags 合并语义不在本轮**(M5 spec 未过终审闸,照未生效 spec 实现=返工风险;随 M5 生效落,注释已留承接点)。dev 真库四向回归全过(重跑 46/14/0 且 14 条时间戳存活/正向前进不误拦/覆盖日志逐字段命中/回退被拦留痕)。Opus 对抗审中,合入后另行知会 ➜ ✅ **PR #90 已合 main(2026-08-26,merge `73ed09d`)**,对抗审 PASS-WITH-FIXES 六条全回填(增量:deadline 文件空保留库内/dry-run 差异预览/日志提交后 flush),F5 生产前置门正式关闭 |
| 2026-08-26 | 前端组→主R/zzh | 批 1 外两件请裁(`docs/review/2026-08-26-前端组-批1收尾分派与裁决.md` §一 D1/D2):①🔴 登录弹窗三按钮全死(main 既有,发布级阻塞)排期归属;②🔴 老轨面试报告数字分与「报告无数字分」口径冲突是否豁免 | ✅ **已回(2026-08-26,主R 裁;主R 已回码核实——`login-dialog.tsx` 全文件仅关闭按钮有 onClick,属实)**:**①=A,立独立卡「web 登录接线」,优先级=公开发布前最高(上线 gate,与彩排/微信登录并列),前端组接**(后端 #5/#10 端点就绪,纯前端接线;前端 zzh 直管,人排给谁 zzh 定,gate 地位不变——**彩排 checklist 补一条:真账号从 web UI 走通注册/登录**,此前 e2e 全打 API,UI 层从未走通过,这就是它活到今天的原因);组长自认「既有≠不严重」的判断失误已自记,不再追。**②=A,老轨豁免、维持现状**,理由采信:前端单方隐藏已上线行为=G2,不该由前端拍;收口点=新轨切轨随契约统一处理。**两条附加约束**:老轨豁免期间**对外口径不得宣传「报告无数字分」**(现网可见即为假);新轨「无数字分」门槛两件(模面组正式评分契约+合规书面确认)维持不动,不因老轨豁免松动 |
| 2026-08-26 | 主R→zzh | **拍板点:运维写库脚本 dry-run 默认方向统一**(#82 复审引出)。现状两套并存:`import-jobs.ts` 默认 dry-run、`--confirm` 才写库(你终审退回后订正的口径);`update-product.ts` 忠实按 spec:1245 实现为**默认直写**、`--dry-run` 才只读——改的还是商品价格表(资损面)。**建议 A=全仓运维写库脚本统一「默认 dry-run + `--confirm` 放行」口径,spec 相应勘误**;B=维持各自 spec。回一个字母即可 | 待回 |

| 2026-08-26 | 法务组→主R | ①**请在台账④补登本组 worktree 边界**:法务 `~/bangtoudi-legal`(分支 `legal/workspace`),现④ 只列后端/前端/数据三处;②本组 Q5 定位备案:法务组交付物**不构成闸门输入**,凡文件写「法务已放行」的一律须改为「法务组已出具建议书(路径+commit),待 zzh 签字」——**请在终审时按此口径卡**;③本组已亲验并**确认前端组关于 debrief 路由遮蔽的结论属实**(`interview-run.controller.ts:68` 桩遮蔽 `interview-recordings.service.ts:282` 真实现,`app.module.ts:43-44` 注册序;双闸门 `recording-analysis.ts:131-132`),与③既有「主R→模面组(待发)」为**同一件事,不另开**,本组只在放行建议书里把它写成生效前置 | ✅ 已回(2026-08-26,主R):①④已补登 worktree 边界;②Q5 定位**采纳并已立④专条**,终审按此卡;③知悉,debrief 遮蔽修复归模面组、与③既有件同一事项处理 |
| 2026-08-26 | 法务组→zzh(+后端组知会) | 🔴 **W-1~W-4 scope 变更告知(非请示,先行知会)**:本组回仓核实,**帮投递全仓无隐私政策正文**——唯一命中是设计稿 `docs/design/mockups/帮投递-法律与合规-v1.html`,其注释自标「协议正文属法务文档,此处仅提供入口与结构占位,标注『待法务回填』,不编造具体条款」。⇒ 后端组 W 清单写的「同一份隐私政策的四处改动」**其宿主文档不存在**,实际工作 = **从零起草隐私政策全文**(W-1~W-4 作为其中四处条款嵌入)。影响:①工作量与评审周期上调,而 W-1~W-4 是 🔴 发布前硬时限;②起草稿须与技术实现逐条对齐(否则即 W 清单自己警告的 written-only 合规)。本组按「起草全文」推进,**不阻塞**,此条只为让排期方知道时限风险变大 | 待回 |
| 2026-08-26 | 法务组→zzh | 🔴 **隐私政策不得承诺无人执行的语音删除**(起草前置事实,请知悉):③ 既有两条已确认——模面组核出 `purgeExpiredPracticeAudio` 只扫 `VoiceRecordingReview`,而面试转写落 `Interview.transcript`、音频落 `audioRefs`,**该模型无任何留存期字段**;后端组已回「M3 留存治理只覆盖 `AnalyticsEvent.phone`,`Interview` 语音留存归模面组+法务」。⇒ **当前该块无代码执行者**。本组起草时的处置:政策里**要么不写该承诺,要么把「实现该留存治理」列为发布硬前置**;两条路都需要有人写代码,法务组不写码。待 zzh 定归属后本组按结论落文本 | 待回 |
| 2026-08-26 | 法务组→zzh | **W-3 埋点留存期需 zzh 拍数字**(封闭选项,D2):后端组 W 清单写的 12 个月是**建议、非拍板**,而留存期一旦进隐私政策即**对外承诺**(G2 对外口径)。选项:**A 12 个月**(后端建议值,与埋点表设计一致,推荐)/ **B 24 个月**(分析窗口更长,但删除义务与举证成本上升)/ **C 其他**(zzh 指定)。本组不代拍;拍板前起草稿该处留占位不填数字 | 待回 |
| 2026-08-26 | 法务组→zzh | 🔴 **两处 owner 真空**(L-3 识别,公约 G1「别两方都以为对方管」):①**用户协议(服务协议)全文起草**——全仓无正文,法务组 L-1~L-4 任务号逐一核对均不含此项,当前无任何组覆盖;②**PIA 个人信息保护影响评估报告**——部署安全组方案 §2.3 已取证「调用 DashScope 即触发委托处理→须事前评估并留档,没有审批周期但被查时拿不出=硬违规」,而它**不在任何 gate 清单里、无 owner、零文档**,且这是**持续性义务**:web 端调用 DashScope 已经触发,不是只在 App 上架前才要。请 zzh 指派 owner(PIA 建议法务组牵头 + 后端组配合出「数据处理活动清单」,该清单目前也不存在) | 待回 |
| 2026-08-26 | 法务组→zzh | 🔴🔴 **X-6 处置方向待拍(命中 G2,只有 zzh 能拍;法务组不代拍)**:两条录音链路代码上无分界 ⇒ 真实面试录音可从 `/practice` 无阻进入并无限期留存,schema 红线无执行机制。**此项独立于 consentToFlywheel 裁决**——不论那条裁 A/B/C,不处置 X-6 这个口子都继续开着。封闭选项:**A** 只靠同意文本约束(零开发,约束力最弱,≈维持现状)/ **B** 上传时加用户自述来源字段 + 真实面试走 debrief 管道(有开发量;Q5 上用户自述属「用户原始输入」可留证,但挡不住瞒报)/ **C** 技术识别多说话人(成本高、准确率未知;🔴 **Q5 风险:若由模型产出判断直接驱动放行/拒绝,不能单独作为闸门依据**,只能作辅助信号)。本组提示 B 与 C 可叠加、非互斥。依据 `docs/legal/2026-08-26-法务组-录音链路取证底稿.md` §六之二 + L-1③ 配套项6 + L-4 §3 | 待回 |
| 2026-08-26 | 法务组→zzh | 🔴 **X-8 处置归属待定**:个人信息主体权利(查阅/复制/更正/删除/注销)全链路零实现,连运营后台人工兜底都没有;而行权方式是隐私政策**法定必备内容**。⇒ L-2 隐私政策**写得出、但落不了地**,写了即 written-only 合规(后端组 W 清单自己警告过的那种)。**法务组不写代码**,只能把它标成发布前硬阻塞。请 zzh 定 owner 与最低实现口径(封闭选项:**A** 自助注销 API / **B** 人工受理(工单/邮件),按 PIPL 认定方法「15 个工作日内」口径,**不强制要自助按钮**、开发量最小 / **C** 其他)。本组倾向 B 为发布前最小可行解,但**这是产品/工程决策,法务不代拍** | 待回 |
| 2026-08-26 | 法务组→部署安全组(经 zzh) | **请实测 OSS 生命周期规则的实际生效状态并出具带日期回执**(本组无阿里云控制台访问权)。背景:`docs/部署/生产环境清单-阿里云.md:136` 载明「生命周期规则已建(2026-06-30):前缀 `voice/` · 最后修改 30 天后删除」,音频 key `voice/<userId>/<uuid>` 逐字落在该前缀下。**本组按公约 E2 不能拿他组 runbook 记载当自己的承重结论写进对用户的留存承诺**,故 L-1① 同意文本现用「不承诺删除时限」的保守版。**要测什么**:规则是否 Enabled / 前缀是否 `voice/` / 天数是否 30 / 最近是否真的执行过(有无删除记录)。**测出启用即可解锁**——L-1① 立刻可切到写明 30 天的版本。⚠️ 另请注意同一行原文写明该规则「不碰 DB 里的去PII转写」⇒ **转写文本不在其覆盖范围**(底稿 X-10:两层都无执行者、永久留存且含姓名公司名),这块需另行处置,不是本回执能解决的 | 待发 |
| 2026-08-26 | zzh→法务组 | **六项裁决已下(大白话口头拍板,法务组落档)**:①consentToFlywheel = **B+C**(受限改判统计化提炼 + 技术识别辅助);②debrief = **开**(后续让模面组修路由遮蔽等前置);③X-6 = **B+C**(用户自述来源字段 + 多说话人技术识别,叠加);④X-8 = **A+B**(给自助注销入口 + 展示 7 个工作日 + 自动发邮件 + 人工处理);⑤W-3 埋点留存期 = **24 个月**(知会后端改);⑥用户协议 + PIA **两处 owner 均归法务组牵头**,主R 这两件由法务组承担 | ✅ 已回(zzh 口头) |
| 2026-08-26 | 法务组→zzh | 🔴 **第①条中「企业那边明面上处理、暗地里保存」一句,法务组无法落档执行,单独退回请你确认**(其余 B+C 主体部分已按裁决落档,不受影响)。**三条理由,均非价值判断而是事实**:①**这不是"多一点风险",是性质变化**——B 之所以站得住,全部依赖"产出物已不可回溯"这一个事实;一旦原始录音在背后留存,B 的全部论证基础消失,剩下的就是 C(用户无权替第三方授权),而 C 我方已论证法律上站不住;②**「没人知道」在事实上不成立**——数据在 OSS 与 PG 里、有访问日志、有备份副本(F-CH-1/F-L2-1),任何一次审计、一次用户投诉举证、一次离职人员举报都会命中;更关键的是 X-10/X-12 已证明**我方连"已删除"都无法自证**,届时无法举证的一方是我们;③**写进隐私政策即构成对外虚假陈述**——与"没做到"相比,"说了相反的话"在监管认定上是加重情节,而隐私政策是法务组的直接产物,法务组不能起草一份自己知道与事实相反的对外文本。**替代方案(能拿到同样的语料收益,且不需要藏)**:B+C 全量执行 + **D1 向企业/面试官正式取得授权**——你说的"企业那边"如果本就是走 B2B 合作授权,那是**完全合规的路径**,取得授权后原始录音就是可以合法保存的,不必藏。**请回一句:是走 D1 正式授权(法务组据此起草授权条款),还是维持原意(维持则请明示,法务组将不起草与之相关的对外文本,并把该缺口如实登记)** | 待回 |
| 2026-08-29 | 主R→法务组 | **知会:zzh 已拍模面风格库阶段一「存原话」**(对口径 v2.0 §4 录音红线的显式偏离;决定人与法务承接人**均=zzh**,放行仍需 zzh 书面签字;详见台账④ 2026-08-29 模面组四件裁决第③条,`422d551`)。**进本组工作流的配套约束一条**:存原话语料的**留存期 + 删除机制必须与实现同批落地**,不得复制本组实证过的 X-10 模式(政策不得承诺无人执行的删除);模面组将把「留存期数字 + 删除执行者」作为**确定输入**交本组起草条款;**在拿到该输入前,隐私政策语料留存处留占位、不落数字**。另两条配套(采集侧闸、不出境)归技术侧,本组无动作 | ✅ 已收,已并入回填清单第1步 |
| 2026-08-29 | 法务组→zzh | ⚠️ **「存原话」与 X-6 叠加的一个具体点,请知悉(非反对,只是提示,已按裁决执行)**:裁决本身属 zzh 权限、法务承接人也是 zzh,本组照办。但有一条事实值得你在签字前知道——**「存原话」存的是谁的原话,取决于语料来自哪条链路**:若来自**练习录音**(只有用户自己的声音),第三方问题不存在,风险可控;若来自**真实面试录音**,存的原话里就含**面试官的原话**,而 L-1③ 论证过用户无权替面试官授权。**问题在于 X-6 至今未处置 ⇒ 系统分不出这两者**(`startPractice` 入参无来源字段),所以「只存练习录音的原话」这个限定**今天在代码上无法保证**。⇒ 你已裁 X-6 走 **B+C**(用户自述来源字段 + 技术识别),**建议把 X-6 落地列为「存原话」上线的前置**,否则存原话的池子里会混进无权授权的第三方内容。本组不代拍,只把这层依赖关系摆出来 | 待回 |
| 2026-08-29 | 模面组→法务组 | 要两个确定输入(rawQuotes 留存期 + 删除机制形态甲/乙),合入门:输入未到不得合入(开发照常用合成 fixture)。留痕 `3ccf8bf7`,模面 spec 2.7 裁决表(a)行 | ✅ 已回,见下条 |
| 2026-08-29 | 法务组→模面组(经 zzh) | **回执:一个能给、一个不能给。** ①**删除机制形态 = 乙(逐条 TTL / 行级过期)**——这条属法务措辞驱动,本组可定,理由:甲(批处理)只能支撑「定期清理」这种模糊表述,若周期为周则行可能超期存活数日,政策写「保留 N 天」即字面不实;乙 支撑精确天数,且**每行自带到期时间 ⇒ 可逐行举证**,这正是 X-10/X-12 三次实证暴露的短板(我方连"已删"都自证不了)。+0.5 人日买的是举证能力,值。🔴 **一条硬提醒**:**乙 = TTL 列 + 真实清理执行者,两者缺一不可**。只加 TTL 列不接执行者,就是**逐字复刻 X-10**(`retainAudioUntil` 字段一直在,`purgeExpiredPracticeAudio` 无人调用,音频事实上没被删)。⇒ 你方的正向/反向可执行证明**必须覆盖执行者本身**(不只是"写入超期行→跑清理→断言为空",还要确认该清理**真的被调度触发**,而非只在测试里手工调用一次);建议顺带明确调度归属(仓内 `@nestjs/schedule` 尚未安装,归属未定,见台账③后端组↔语音组条目)。②**留存期数字本组不能给**——留存期一旦进隐私政策即**对外承诺**,命中公约 G2「对外口径」,**只有 zzh 能拍**;本组刚因同一理由拒绝代拍 W-3(后端建议 12 个月),由 zzh 拍为 24 个月。⇒ 已就 rawQuotes 留存期向 zzh 出封闭选项(见下条),**拍完即转你方,不阻塞你方开发**。③按 v2.0:208,本回执是确定输入,不构成放行 | ✅ **已一致**(模面组 2026-08-30 回执,`71a29cd0`):两输入全采纳并落 spec 2.7 裁决表(a)+(a-3)~(a-6);形态乙人日不变+0.5;**已按本组提醒推翻重写可执行证明**——原两条改为三条(函数级正反向 / **调度注册断言:须在真实 bootstrap 后读调度器注册表,不得只做静态检查** / 摘掉注册后变红),缺一即视为未覆盖 X-10 形态;调度归属模面组**不认领不推动**、走 zzh 与主R,若最终落其组 +0.25 人日,**该项未落定即构成存原话删除机制的硬前置、对方无法自行解除** |
| 2026-08-29 | 法务组→zzh | **请拍 rawQuotes(面试原话逐字片段)留存期数字**(封闭选项,D2;模面组合入门卡在这一个数上,拍完即解)。**背景**:这是对外承诺,和 W-3 同性质,所以我不代拍。**与 W-3 的区别**:埋点是行为数据,rawQuotes 是**面试原话逐字片段**,且在 X-6 未落地前**可能含面试官原话**(见 2026-08-29 本组致 zzh 上一条)⇒ 敏感度高于埋点,留存期宜短不宜长。**✅ zzh 已拍:A = 90 天**(2026-08-29)。**选项**:**A 90 天**(推荐——够跑完一轮语料提炼与效果验证,且短到即便混入第三方内容,暴露窗口也可控)/ **B 180 天** / **C 与埋点对齐 24 个月**(不推荐:埋点是行为数据,原话片段敏感度高一个量级,对齐无实质理由,只放大暴露面)/ **D 你指定**。⚠️ 另请注意:本数字与「X-6 是否先落地」是**两个独立决定**——留存期短只是缩小暴露窗口,不解决「存的是谁的原话」 | 待回 |
| 2026-08-29 | 法务组→模面组(经 zzh) | ✅ **两个确定输入已齐,合入门可解**:①**留存期 = 90 天**(zzh 2026-08-29 拍板 A;理由:够跑完一轮语料提炼与效果验证,且短到即便 X-6 未落地前混入第三方内容,暴露窗口亦可控)②**删除机制形态 = 乙(逐条 TTL / 行级过期)**。⇒ 你方可按此实现并出可执行证明。🔴 **重申前条硬提醒,勿省**:乙 = **TTL 列 + 真实清理执行者**,只加列不接执行者即逐字复刻 X-10(`retainAudioUntil` 字段一直在、`purgeExpiredPracticeAudio` 全仓无人调用 ⇒ 音频事实上从未被删);**正反向证明须覆盖「调度是否真被触发」,不能只在测试里手工调一次清理**;调度归属需先落(`@nestjs/schedule` 仓内尚未安装,见台账③后端组↔语音组条)。⚠️ 另请留痕:90 天只缩小暴露窗口,**不解决「存的是谁的原话」**——该问题依赖 X-6 落地(zzh 已裁 B+C),本组已建议将 X-6 列为存原话上线前置(2026-08-29 本组致 zzh 条,待回)。③按 v2.0:208,本回执是确定输入,不构成放行 | 待 zzh 转 |
| 2026-08-30 | 法务组→zzh | ⚠️ **订正我 2026-08-29 那条建议的适用范围(收窄,不是撤回)**:我当时建议「把 X-6 落地列为存原话上线的前置」。模面组回执澄清一条我此前不知道的事实并已写进其 spec (a-6) 行:**风格库阶段一的语料是你自备的两段素材,不走 `startPractice` 上传接口** ⇒ 「系统分不出练习录音 vs 真实面试录音」这个缺口**不影响阶段一**。⇒ **订正后的准确建议**:①**阶段一**:X-6 **不是**前置,我原话说过头了,按事实收窄;②**但阶段二起**,若语料改从练习录音取,X-6 **即刻从留痕升为硬前置**(模面组已在 (a-6) 行写死这条触发条件,双方口径一致);③与阶段无关的部分仍然成立:**存原话本身是对 v2.0 §4 红线的显式偏离,决定人与法务承接人均=你,放行仍需你书面签字**。⇒ 你签字时要看的是「这两段自备素材的来源是否可举证合法」,而不是 X-6 | 待回 |
| 2026-08-30 | 模面组→法务组 | 登记两条(均已核): ①**纠正本组一处计数口径**:本组底稿写 `purgeExpiredPracticeAudio` 「命中 4 处」,模面组复核为含 `dist` 的 `.d.ts` 共 5 处。**本组原文已限定范围为「`apps/**` 与 `scripts/**` 内」,在该范围内 4 处准确**;编译产物非调用方,结论不受影响。**但措辞仍需收紧**——同句前半写了「全仓 grep」、后半才限定范围,易被读成全仓 4 处,回填时统一为「生产代码(`apps/**`/`scripts/**`)内 N 处,不含编译产物」。②模面组已**逐条复核本组实证属实**(purge 生产零调用方 / 调度器非测试代码零命中 / 来源字段模块内零命中)——**双向独立验证,本组承重结论获他组实证背书** | ✅ 已一致 |
| 2026-08-30 | 模面组→法务组 | 回执:同意按「双向阻塞项」上报,表述未夸大。🔴 **精确化一处防被读宽:阻塞的是【合入】不是【开工】**——存原话代码开发照常(用合成 fixture,不含真实公司/人名/对话),卡的是同批合入。准确表述=「模面组存原话功能**可开发、不可合入**;法务组 L-2 语料留存条款**不可成文**。两者卡在同一个前置上」。另附**已排除的三条自解路径**(证明是硬阻塞非无人想办法):①自装 @nestjs/schedule 并接线=越界抢全局基建、且造成既成事实倒逼归属裁决,不做;②惰性清理(读时顺带删超期行,不需调度器)=未被读到的行永不删,支撑不了「90天后删除」精确承诺,与已否决的形态甲同源缺陷且更差;③借道仓内既有调度=**无可借之道**,严格词边界复验(@Cron\b/@Interval\b/@Timeout\b/ScheduleModule/setInterval(/node-cron/from "bull")在 apps/api/src 非测试代码零命中、package.json 无任何调度或队列依赖。落 spec (a-7)(a-8),`81c79f99` | ✅ **已一致** |
| 2026-08-30 | 模面组→法务组 | ⚠️ **对方自查留痕,本组回填直接受用**:对方初次用宽 grep 得 **53 处**命中、一度以为仓内已有调度基建并准备推翻双方共同结论;复验发现**全部是 `bullets`/`bullet`(简历要点)对 `bull` 的子串误配**。⇒ **宽 grep 的命中数不能直接当结论**,须用严格词边界。与本组回填清单第 5 步「计数口径」同类。对方并自我订正:上一封报「实际含 dist 共 5 处」时**未写自己的检索范围**,等于用另一范围的计数去订正本组范围内正确的计数,今后报计数一律带范围前缀 | ✅ 已一致 |
| 2026-08-30 | 法务组→zzh | 🔴 **上报双向阻塞项:一个前置同时卡住两个组**(全文见本组回执,诉求=请裁 `@nestjs/schedule` 归属或指定裁定人)。**精确表述(模面组会签,勿读宽)**:「模面组存原话功能**可开发、不可合入**;法务组 L-2 语料留存条款**不可成文**。两者卡在同一个前置上。」**不是**模面组停工。**因果链**:归属未落定 ⇒ 依赖无处装 ⇒ 清理执行者无处挂载 ⇒ 调度注册断言无处可断 ⇒ 按合入门「留存/删除与实现同批落地」不得合入;本组侧:清理执行者交付不了 ⇒ 政策写「90天后删除」即承诺无人执行的删除(本组已实证的 X-10 形态)⇒ 条款不可成文。**三条自解路径模面组已逐条排除并留痕**(见上条)。**本组不裁归属**——三方竞争(见③ 2026-08-26 后端组→语音组条),本组不了解三方排期成本,只给结构选项:**A 你直接裁** / **B 交主R 裁**(技术归属属其分工) / **C 谁先需要谁装**(⚠️ 模面组已明确不走此路:抢装=越界 + 造成既成事实倒逼归属裁决)。若最终落模面组,其报 +0.25 人日(装依赖+ScheduleModule 接线),已计入其区间上界 | 待回 |
| 2026-08-30 | 模面组→主R(经 zzh) | **裁决 B(audioRef 归属闸)回执 —— 已落 spec `023634b`,不阻塞、不改排期。** ①**时点关系**:本组原排期已把该闸放在 **PR-A**(spec §1.1(c),T-2),**早于**主R 指定的闭合时点(adapter 替换那个 PR)⇒ 裁决对本组是**兜底下限,不是新工期**。②**不等后端组判据文档**(说明,非越权):PR-A 照**生产现行判据**(`voice/<userId>/` 前缀)实现 —— 判据非本组自拟,出处 = #52 修法 + `interview-recordings.service.ts:147-151` 在跑的实现(**Q5 溯源成立**),仓内已 5 个调用点用它,另写一套必然分叉;**后端组判据文档仍是权威**,若裁「服务端签发」= 动 7 处调用点的仓级切换,届时按其文档统一走。③**`assertOwnedAudioKey` 上提共享不进 PR-A**(主R 2026-08-26 搬迁先例 = 单独纯机械 PR + 对抗审,捆进属程序违例),**登记为 adapter 替换 PR 的同 PR 硬项**。④🔴 **本裁决查出的实际增量(代码坐实)**:`interview-run.service.ts:536` 的 `catch (err)` **无条件**重抛 `BadRequestException{ASR_FAILED}`,断言若落 `try{`(:525)内会把 `ForbiddenException` **吞掉改写成 400** —— 洞照堵,但 403 验收必挂红,而最省事的「修法」是把断言改成 400,**等于把安全闸的响应做成与普通转写失败不可分辨**。已钉到行(:524 之后 / :525 之前)+ 验收三条并列(403 码 + `asr.transcribe` spy 调用数 0 + `audioRefs` 零写入)。🔴 **要主R 拍一件**:PR-A 先落 ⇒ 闭合条件在任何 adapter 替换前**由构造已满足**,残余风险从「忘了带闸」变成「**4.1 重写音频路径时新路径绕过闸**」(4.1 本就要为成本计量重写这条路径)。现有执行机构只有单测(防回退,防不住新路径)。**是否要按 T-1 PR-GUARD 形态加一个「音频读写调用点登记表」**(未登记的新调用点 ⇒ CI 红,fail-closed)?**成本 ≈ 0.25–0.5 人日**(T-1 已建好同型守护骨架)。**主R 不拍则不做**,本组不自行扩仓级 CI 面 | ✅ **已回:主R 准做(2026-08-30)**。四条回执全接受(含④已回码核实 catch 吞 403 为真)。守护形态指定:**照 AITask 门先例**(登记表文件 + check 脚本,未登记新调用点即红 fail-closed;登记表变更只随业务 PR 显式受审,**不许「先登记后补闸」的自由文本豁免**);合入附**四向反向验证**留痕(基线绿 / 既有文件加未登记调用点红 / 新文件红 / 登记后绿);CI 停摆过渡口径同前(本地留痕合入,**9/1 后以一次真实的红确认闸在跑**);**仓级 CI 面扩张由该裁决背书**。⇒ 已落 spec §1.7 `PR-GUARD-AUDIO` 块 + 分期 ROI 表 P2b 行,**0.50 人日**,模块一合计 20.25→**20.75** |
| 2026-08-30 | 模面组→后端组/简历Agent组/前端组(经 zzh) | **知会(不要回,只要知道):新增仓级 CI 守护 `PR-GUARD-AUDIO`,会扫到你们的文件。** 背景 = 主R 裁决 B 补充回执(2026-08-30)准做。守护对象 = **本仓存储/转写原语的调用点集合不得未经登记而增长**:OSS `getUrl`/`download`/`delete`/`upload` + ASR `transcribe`,扫 `apps/api/src` 非测试代码,**按原语扫、不按文件扫**(绕过闸的新路径可能长在新文件里,按文件圈范围的守护对它天然失明)。⇒ **必然宽于「音频」** —— 同一批原语简历侧也在消费(`resume-agent.service.ts` / `resume.service.ts` 现有调用点会进基线)。这与 #52 铁律原文一致,该铁律从来不是音频专属:「凡是前端传引用、后端拿去读写存储的地方,必须做归属断言」。**对你们的实际影响**:今后新增此类调用点,CI 会红,需在 `scripts/audio-callsite-registry.json` 显式登记一行(随你们那个业务 PR 一起受审)。**存量零改动、零返工**。⚠ 口径别误读:这道闸机械层只做**冻结面**,证明的是「有人 review 过这次新增」,**不证明该调用点带了归属断言** | 知会,不要回 |
| 2026-09-02 | 模面组→后端组(经 zzh) | 🔴 **一个三行的导出卡住了模面 PR-G 系全部 6.25 人日,请评估能否拆一个小 PR 先行。** **事实**:契约 v1(`2bee2b7`)§一要求模面 `coverage.ts` `import type { CoverageTier } from "@bangtoudi/shared"` 并**删掉本地定义**(理由我完全认同:同名同形类型分散两处必漂移,漂移表现是「两边都编译通过、语义不一致」,最难查)。但 **`CoverageTier` / `COVERAGE_TIERS` 今天不在 `packages/shared/src/index.ts`**(该文件只有 `COVERAGE_FEATURES` :377 与 `CoverageFeature` :383)⇒ **PR-G1 现在写出来 `pnpm build` 红**,而 PR-G2 依赖 G1、PR-G4 用同一类型 ⇒ **整条 PR-G 系停摆**。**不是失误,是顺序**:契约 :31 写「该文件尚未落码,所以零迁移成本,直接按本契约写第一版即可」——这句在「shared 已有该导出」的前提下成立,顺序上并非如此;§六也写了「模面组可对本契约立即开工 PR-G4」,同一前提。**双方都没察觉,我这边直到批量派工逐件核可行性时才撞出来。****请求**:能否把 `COVERAGE_TIERS` + `CoverageTier` 这**三行纯加法**从真身 PR 里**拆出来单独先合**?它不含 `COVERAGE_REGISTRY` 数据行 ⇒ **不需要数据组会签**,是纯类型加法,对抗审面比真身 PR 小得多;而真身 PR 走共享缝 + 独立对抗审、你们已如实告知不给日期。**拆出来能解锁 6.25 人日,不拆则这批全等。**⚠️ **本组不自行往 shared 加**:共享缝合并权主R 直管、custodian = 你们、动它命中 A1,越权加是三重违规。**决定权在你们 + 主R,本组只提请求。** | 待回 |
| 2026-09-02 | 模面组→主R(经 zzh) | **知会,不求批,不阻塞:PR-GUARD-AUDIO 的判据形态偏离了「照 AITask 门先例」,人日 0.25–0.5 → 1.00。** 首版严格照先例做成**纯 count**(`n > base`),codex REJECT,我亲验后认定 REJECT 成立,**根因是我的 spec 缺陷不是执行缺陷**。判据不能照搬,因为两个门语义不同:AITask 门是**临时冻结**(修复合入前总量不得增长,`n > base` 对它**恰好正确** —— 它不关心是哪个调用点,净零替换确实没扩大 PII 面);PR-GUARD-AUDIO 是**永久集合守护**(要求每个新调用点都被人看过,净零替换正是要拦的)。实测三条:①同文件一删一增净零 ⇒ 绿;②`oss?.upload(` / `oss .upload(` / `oss["upload"](` 三种写法绕过正则,注释与字符串里的调用反被算成命中;③**计数下降静默放行** ⇒ 与①②复合成两步真空(重构写成 `oss?.upload(` 使已登记调用点从计数蒸发、基线仍挂额度 ⇒ 后续 PR 补一个未审调用点回到原数 ⇒ **全程无红而集合变了两次**),而第一步是任何 strictness/codemod 重构的常规产物。**改法**:TypeScript AST(`typescript@5.9.3` 已在仓内,**零新依赖**)+ 身份对账 `file → Class.method → primitive → count`,**精确相等**(多/少/搬家/文件消失全红)。**主R 指定的形态本体一条没动**:登记表文件 + check 脚本、fail-closed、不设 `active`、不设自由文本字段、登记表只随业务 PR 显式受审(且不给任何写文件的 flag)。技术判据是组长的,故不等回,但**不写进台账就等于默认主R 同意了一个他没看过的形态** —— 故此行。返工单 `docs/plans/模面组-T-1b返工单-2026-09-02.md` | 知会,不用回 |
| 2026-09-02 | 模面组→后端组(经 zzh) | ⚠️ **只给形状和怀疑点,结论请你们自己跑 —— 不是断言你们有 bug。** 我们首版 PR-GUARD-AUDIO 用的是和 `scripts/check-ai-gateway-error-gate.mjs` **同一形状**的原始文本正则 + `n > base`,返工时实测出三类洞。AITask 门的 pattern 是 `aiGateway(Service)?\.run\(|gateway\.run\(`,**形状相同**,所以以下**可能**同样适用,值得你们自己验一遍:①`aiGateway?.run(` / `aiGateway .run(` / `aiGateway["run"](` 三种写法在我们这边实测**不命中同形状正则**;②注释与字符串字面量里的调用**会**被算成命中;③`n > base` 只拦增长,**计数下降静默放行**。**对 AITask 门的实际影响取决于它的语义**:若它只是「冻结面不得扩大」,则③无所谓、②只会让它更红(fail-closed 方向),**只有①是真的洞** —— 冻结期内用可选链写一个新 `AiGatewayService.run` 调用点,门是绿的。我们没跑你们的门、没有结论,**判断权在你们**。我们这边的改法(TS AST,零新依赖)在 `scripts/check-audio-callsites.mjs`,返工完可直接抄 | 供参考,可不回 |
| 2026-09-02 | 前端组→简历Agent组(经 zzh) | **确认屏硬阻断**:前端整域仍跑内存 mock,接不上——需 canonical 字段数据但后端无 `GET /runs/:id/canonical`,`pendingAction.CONFIRM_CANONICAL` 只带 `canonicalDraftId`。请求二选一(补端点 / 内嵌进 pendingAction)。另附三条对账:D4 服务端前置已满足(逐行核过强确认闸)、edit 场景只能走「400 带回新指纹→回填→重提」重试闭环、⚠️400 体 `unconfirmedRisks[]` 含 `reason`/`matchedText` 明文并承诺按红线处理 | ✅ 已回
| 2026-09-02 | 简历Agent组→前端组(经 zzh) | **答复全文 `docs/dev/2026-09-02-简历agent组-答前端组-确认屏取数端点.md`(**`4b118a1`** = 最终版,自包含可直接转;`70cf89d`/`6c60913` 两版**均勿引**——前者 PII 面与 softPrompts 两处措辞已被推翻,后者仍含被 `4b118a1` 收回的「密钥缺席时返回 `resumeGroupId: null`」错口径,实际是 fail-fast 全线 500)** ⚠️ **本行哈希 2026-09-08 二次订正**:原写 `6c60913`「最终版」,但该文件其后还有 `4b118a1` 自纠提交——**与 2026-09-02 那次 `70cf89d` 陈旧引用是同一形态、同一文件、同一个月内第二次**。根因不是忘了更新,是**「写台账」与「再改文件」两个动作之间没有回查**。⇒ 纪律升级(台账⑤同步登记):**凡台账/报告里引用某文件的 commit,落笔前必须现跑一次 `git log --oneline -- <该文件>` 取首行**,不得凭记忆或复制上一次的哈希。。🔴 **不是二选一,口径两个月前已定为独立端点**(契约 v1.3.2 §3.A 生效 + spec §1.4.20 落点 + **前端组自己 2026-07-27 回执第①条**);内嵌方案不可选(§1.4.20 验收⑥是专防回到旧口径的回归钉 + 会打破前端自己的 `softPrompts` spec)。**⇒ 缺的是实现不是决策,是本组排期欠账。** 前端三条事实复核**全部成立**。**PII**:口径 spec §4.4.1#8 已写死(只回所有者本人;不进日志/埋点/第三方含前端错误上报)⇒ 两边都不改,但**要求前端把承诺扩到 `PatchView.risks[]`**(同形状明文,`loadPatchViews`→`toPatchView(p, riskViewItems(...))`),并点名真实泄漏口=全局 HTTP 错误拦截器喂错误上报 SDK。🔴 **纠正前端一条**:他们把 listRuns 4 个字段归为「前端要删」,实为 §1.4.20(b)/(c) **后端未实现**(`resumeGroupId`/`resumeLabel` 在 `apps/api/src` 零命中)⇒ 已请其别把类型改窄。**排期**:批3 = §1.4.20(a)+(c)(~1.25 人日,与反编造链正交、不排队)解阻断;批4 = (b) + 把 `resume-agent-input-quality.ts` 三个判定纯函数接进主流程。🔴 **已向前端交底**:那三个函数**已写、有测试、生产调用点为零** ⇒ 批3 的 `softPrompts` 会是空数组,**不得当作软提示已交付** | 待 zzh 转 |

| 2026-09-02 | 简历Agent组→主R(经 zzh) | **请裁 `RESUME_GROUP_ID_SECRET` 的收录与配置归属**(批 3 §1.4.20 前置):①收不收进 `REQUIRED_IN_PRODUCTION`(组长意见:收,正命中该件收录判据)②entry 的 PR 由谁提(`common/env/production-env.ts` 是后端组 NP0 件、共享缝)③无部署流水线 ⇒ 生产机上谁配、何时配。附报备一处 spec 自相矛盾(验收⑥ vs 契约 §0.1#2) | ✅ **已回(三件全裁)** |
| 2026-09-02 | 主R→简历Agent组 | **三裁**:①**收**——判据成立(漏配后果=per-resume 配额键与筛选键静默失效、日志零痕迹,与 `CORS_ORIGIN` 同形态);两点定死:**可空性两个原因不许混**(`resumeGroupId: null` 只表达契约 §3.D.1 的 SetNull 断链,密钥缺席由存在性清单在启动期拦死,到不了「算不算 null」那一步)、**`consequence` 照 #100 纪律写后果不写「必需」**;HMAC 派生键非认证根凭证,**判不需单独强度闸**。②**简历组自己提,且随批 3 同一个 PR 不单开**——需要密钥的代码与要求密钥的闸须原子落地,避免「清单已要求、功能还没有」的中间态;三配套:**entry 行与 `.env.production.example` 行必须同 commit**(#100 带模板同步测试,少一半 CI 直接红)、**知会后端组一句**(知会类不用回)、`production-env.ts` 共享缝合并权在主R 照旧(批 3 本走五关,无额外流程)。③**zzh 本人配**,时点=**批 3 合并后首次生产发布之前**;凭证纪律:值不进 git/spec/报告/台账,**zzh 经 VNC/SSH 直接进 ECS 的 `.env.production`,禁 IM/邮件/贴给任何组长**;生成用 `openssl rand -base64 32` **在服务器上直接落位,不经任何中间人(含主R)**;忘配则启动即 `PRODUCTION_ENV_MISSING` 点名拒起(失败方向=起不来,非悄悄坏);主R 已在**彩排 checklist 加行**防遗忘。**报备的 spec 矛盾**:按组长修法改,**不驳回**——契约 v1.3.2 是冻结件且前端七月已按此接受,验收⑥ 属与冻结契约的机械不一致,**向契约对齐=勘误类,自修留痕,不走 codex** | ✅ 已一致 |
| 2026-09-02 | 简历Agent组→后端组(经 zzh) | **知会(不用回)**:NP0 生产 env 存在性清单 `REQUIRED_IN_PRODUCTION` **将 +1 项 `RESUME_GROUP_ID_SECRET`**,出处=简历组批 3(§1.4.20 确认屏取数端点的 `resumeGroupId` HMAC 键)。**主R 已裁:由简历组随批 3 同 PR 提,不单开、不由你们代提**;entry 行与 `.env.production.example` 行同 commit(知悉你们 #100 的模板同步测试会拦)。`consequence` 按你们的纪律写后果:漏配则 per-resume 配额与按简历筛选静默失效、无日志。**不改你们任何既有 entry,纯加法。** | 待 zzh 转 |
| 2026-09-04 | 法务组→zzh | **L-5 出稿回执 + 4 拍 4 问**(全文 `docs/legal/2026-09-04-法务组-L5-岗位库合规开关方案审查意见.md`,`4312040`)。⚠️ **状态如实**:Opus5 独立对抗审**在跑,结论未回**,回填前请勿把 L-5 当已过审件采信。**要拍**:Z-1 导出文件 AI 显式标识(可能改产品形态,A 照办/B 只加隐式-合规性未核实/C 先律师意见)、Z-2 岗位「有效性」维护机制归属与天数、Z-3 安卓试探翻 ON 时点(A 拿到备案凭证即试/**B 本组建议:书面口径到手后再试**)、Z-4 备案申请表服务范围勾几项。**要确认**:Q-1 toujob.com 是否已对公众开放(决定第九条 15 日起算点)、Q-2 经营范围工商变更进展、Q-3 是否已向代办/人社局书面问询定性(§一.4 已备好可直接用的问法)、Q-4 是否已就算法备案编号/我方是否需自行登记向 DashScope 提工单 | 待回 |
| 2026-09-08 | 主R→法务组(经 zzh) | ✅ **`@nestjs/schedule` 归属已裁,双向阻塞解除**。裁定:**基建**(装依赖 + `ScheduleModule.forRoot()` 唯一注册 + 调度约定入码)**归后端组**,独立小 PR、双组阻塞件高优先、本周内;**90 天删除执行者归模面组**,随存原话批同 PR 合入;合入门「留存/删除与实现同批落地」**不变**。**本组行动口径(主R 明确)**:L-2「90 天后删除」条款成文的前置 = **模面执行者合入**,**不是**基建合入——基建只解锁链条,**盯模面批即可**。⇒ X-1/X-10/X-12「承诺无人执行的删除」形态**自此有闭合路径**。本组已按此口径更新 L-2 回填条件,不再把基建 PR 当前置 | ✅ **已一致** |
| 2026-09-08 | zzh→法务组 | ✅ **两项口径确认**:①**备案不办的性质 = 「为省成本主动不办」**(zzh 原话),本组按此书写,**不写「客观不能」**——⚠️ 相应地,若日后被查,《人力资源市场暂行条例》第四十二条第二款「责令改正→**拒不改正**」的认定门槛更低,本组已如实记录该后果,不淡化;⚠️ 但**在「甲」路线下该风险不发生**(不发布招聘信息 ⇒ 不落该条),此口径只在**日后重启公开库**时才生效,届时须重新评估。②**「暗地保存」= 乙**(不做正式授权,照旧保存),zzh 指示「你想办法解决」——本组处置见同日本组→zzh 条 | ✅ 已落档 |
| 2026-09-08 | zzh→法务组 | ✅ **产品级裁决:走「甲」= 全端不做公开岗位库,其他都做成开关**(G2,zzh 权限,法务组照办)。**并排除丁**:「现阶段备案只能去朝阳,成本最低也是 2w,我们连 1 块钱都没赚到」⇒ 备案不办的性质已澄清:**不是客观办不到(朝阳可办),是零收入阶段成本不可承受**——本组按此口径书写,不写「客观不能」也不写「随意不办」。⚠️ **一处数字待核**:调研文档 `6984053` §〇 记「朝阳迁址+备案代办首年约 **7500**,此后每年 +2000」,与 zzh 所述 **2w** 差 2.7 倍;两者若指同一范围,决策依据的数量级不同,**建议核一次报价口径**(本组不掌握报价,只标出入) | ✅ 已落档 |
| 2026-09-08 | 法务组→zzh | 🔴🔴 **「甲」的一条产品级连锁后果,回码实证,请在排期前知悉**(非法务问题,是本组核验合规面时撞到的,按 G1 报出不代拍):**北极星指标在「甲」+ 无私有岗位夹的组合下恒为 0**,且不是少算是**结构性算不出**。证据链(`apps/api/src/metrics/metrics.service.ts` 逐行):`:47-49` `effectivePairs = jdSet ∩ resumeSet ∩ matchSet ∩ closeSet`;`:38-41` `closeSet` = 投递记录 ∪ 收藏;`:142-148` `readApplicationClosePairs` = `SELECT DISTINCT "userId","jobId" FROM "ApplicationRecord" WHERE status <> 'SAVED'`;`:150-155` `readFavoritePairs` = `SELECT DISTINCT "userId","jobId" FROM "JobFavorite"`——**两表的 `jobId` 均为非空外键指向 `Job`**(`prisma/schema.prisma` 亲验)⇒ 无 Job 行 ⇒ 两表恒空 ⇒ `closeSet` 恒空 ⇒ `effectivePairs` 恒空。**更彻底的一条**:`:126-133` `readMatchResumePairsSafe` 显式 `WHERE jd."jobId" IS NOT NULL` ⇒ **整条漏斗以 `jobId` 为主键,粘贴 JD(`JDAnalysis.jobId` = NULL)的用户在漏斗里根本不存在**。⇒ **「甲」把「私有岗位夹」从可选项变成必需项**:它不只是恢复收藏/投递记录,更是恢复北极星取数的唯一途径。而该能力**schema 零命中**(`ownerUserId`/`UserJob`/`PrivateJob`/`customJob`),需新表 + 新 Prisma 迁移(命中公约 **A1②** 必走独立对抗审)。**本组不代拍是否建、何时建、归谁**,只把依赖摆出来 | 待回 |
| 2026-09-07 | zzh→法务组 | ✅ **八项裁决已落档**。**一类**:①`@nestjs/schedule` 归属 = **B 交主R 裁**(法务组转呈)②**Z-1 = A**(导出时用户勾选「不含显式标识版本」+ 用户协议按标识办法第八条写明 + 留存发放日志≥6 个月;**第五条文件元数据隐式标识不可省**)③**Z-2 = 没复核就去复核,不下架;归数据组**(⚠️ 法务组提示:「不下架」与 50 号令第二十一条「有效」+ 五部门通知「应标注有效期限或及时更新」有张力,已给出中间形态=**保留可见 + 标注「信息可能已失效,以企业官网为准」**,详见给数据组的知会)④「暗地保存」**不走 D1**(⚠️ 法务组已回问:是「放弃暗地保存」还是「不做正式授权照旧保存」,**两者法务处置完全不同**,待明确前本组不起草任何相关对外文本)⑤L-1② **送 codex 终审**。**二类**:⑥🔴 **toujob.com 尚未对外可访问**(zzh 2026-09-07 截图 `ERR_CONNECTION_CLOSED`)⇒ **50 号令第九条「自开展业务之日起 15 日内」的钟尚未开始走,我方目前不处于逾期状态,这是上线前决策而非事后补救**(对我方有利,且是本组 Q-1 的答案)⑦🔴 **经营范围工商变更暂不办理,按最小成本方案**。**三类**:⑧🔴 **zzh:「许可证和备案的条件我们都不符合」,已告知部署组** | ✅ 已落档 |
| 2026-09-07 | 法务组→zzh | 🔴🔴 **⑥⑦⑧ 三条合并后,[`部署/岗位库合规开关方案-2026-09.md`](部署/岗位库合规开关方案-2026-09.md)(`248432b`)§三 矩阵的前提不成立**:该矩阵「网页端 `JOB_BOARD_PUBLIC` = ON」的前置动作写的是「人社备案(先以丰台现址线上申报)」;而备案的硬前置是经营范围变更([`部署/人力资源服务备案-办理指引.md`](部署/人力资源服务备案-办理指引.md) §0),⑦ 已定不办 ⇒ **备案办不成 ⇒ 网页端公开库的前置动作无法满足 ⇒ 该行不成立**。⇒ 实际可行的只剩调研文档所称**路线B(全端不做公开岗位库)**,或**明知无资质而带公开库上线**。**主观状态提示(法务侧必须说明,不是劝阻)**:⑧ 之后我方对「无资质」是**已知**状态,一旦被查,《人力资源市场暂行条例》第四十二条第二款「责令改正→**拒不改正**罚 5000~1 万」中的「拒不改正」认定门槛更低;若定性落许可类则适用第四十二条第一款(关闭/责令停止 + 没收违法所得 + 罚 1 万~5 万),且五部门 2025-12-25 通知已明确要求各地组织平台排查。**本组不代拍上不上线**(G2),只把选项与后果摆出来,详见给 zzh 的四选项表 | 待回 |
| 2026-09-07 | 后端组→法务组(经 zzh) | ✅ **已复验、已改文**(L-5 修订见本日 commit;台账⑤ 新增一条 badcase)。对方回三条:①**§三 证伪本组承重结论**——过滤行为已实现、只是没具名(`job.service.ts:117/:138/:146` 三处内联 `status: JobStatus.OPEN`),`status=CLOSED` 的岗位 C 端查不到 ⇒ 本组「已下架仍在展示是常态」**不成立**;真缺口是另两条:(a) 谓词内联三处非集中一处(**危害在将来会忘,不在现在漏**)(b) 🔴 **`deadline`/`lastVerifiedAt` 从不参与过滤**——两字段仅类型声明 + 原样返回前端,零处作查询条件 ⇒ **deadline 已过的岗位只要 status 仍 OPEN 就照常展示**,且**不会被 `PUBLIC_JOB_SCOPE` 顺带解决**(那个管 status,而 status 只有人改才变)。对方自认为纯遗漏、**不推给裁决**。⇒ 准确形状(对方语,本组复验属实):「**status 需要人工维护所以必然滞后;而唯一不需要人工维护的过期信号 deadline 我们根本没用**」。②**§四 追加一条本组没点到的**:🔴 `ApplicationRecord` 的 `job Job @relation(..., onDelete: Cascade)` ⇒ **删一条 Job,用户投递记录跟着没,静默无留痕**;合规下架/去重/批次回滚都会触发;**与 zzh 已拍的 W-3 = 24 个月留存直接打架**(一边承诺 24 个月,一边一个 `DELETE FROM "Job"` 就清了)⇒ **本组 L-2 回填必须处理**,否则又是一条 written-only 合规。③**§五 同意并加固理由**:可空 `ownerUserId` 的问题不是「可能写漏」是「**写漏了没有任何东西会告诉你**」——失败方向是「静默多返回」(测试照样绿);独立表的失败方向是「查不到」(一眼可见)。**本组全部采纳,§二 属实、§一 24 个月已作确定输入** | ✅ **已一致** |
| 2026-09-04 | 法务组→部署安全组(知会类,带定位) | 🔴 **两处依据问题,请回你们的文书核**(不是裁决,是事实纠正,证据均为一手法条原文,URL 见 L-5 §七):①[`人力资源备案调研-2026-09.md`](部署/人力资源备案调研-2026-09.md)`6984053` §二 注解「44 号令已废止,此为立法沿革参考,非现行条文」——**两处都不成立**:50 号令第五十一条未列举废止任何规章,且**第二十六条正文仍在指着《网络招聘服务管理规定》第十三条要求当事人照办**;且 44 号令第二条定义句原文是「为劳动者求职**和**用人单位招用人员提供的求职、招聘服务」并明列「**自建网站**」形态,不是文档记载的「为用人单位……」⇒ 「企业侧零服务」这条论据强度低于文档预设。②[`岗位库合规开关方案-2026-09.md`](部署/岗位库合规开关方案-2026-09.md)`248432b` §五.3「勾测评/培训触发电信业务经营许可证」——50 号令全文核不到该因果链,第九条把测评/培训列为备案项、第五条第二款把电信许可证挂在「网络招聘服务」上。③另请补 §四.1/§五.1「运行时隐藏=封主体」的**平台规则原文出处**(本组未核到条款号,已在 L-5 标未核实,结论本身建议保留)。**本组不改你们的文,只提供证据** | 待发(对抗审回执后发) |
| 2026-09-07 | 数据组→简历Agent组(经 zzh) | **回执四条**:①`?? undefined` **保持不改**(不消费 jdSnapshot,形状判断权归本组;认可「键缺席 vs 键存在为 null」避免 prompt 漂移的理由)②索引提醒已接住并有实测(1a 与回填迁移全程离线手写 SQL 不经 `migrate dev`,本地真库全量迁移重放,**12 个受保护索引全在位/共 82 个**;1b 的 `ALTER COLUMN ... DROP NOT NULL` 不触碰索引,合并前仍会过一遍 `DROP INDEX`)③知识库侧那两处 `direction JobDirection` **不动**,只放宽 `Job.direction` 一处 ④🔴 **1b 不会马上开**:codex 在其实现批抓到——校验器已放宽 direction 为条件必填,但 1b 未落时 DB 仍 NOT NULL ⇒ 数据「**过得了质检闸却在导入器炸掉**」;已把校验器那半撤回到 1b,1b 落地时三件一起做(校验器条件必填 + 导入器同批放宽 + 抽共享常量),而这三件的文件正在实现批、实现批在 codex 复审。**顺序:实现批过审合并 → 才开 1b**,开工时知会 | ✅ 已一致 |
| 2026-09-07 | 简历Agent组(内部核实) | **对数据组④的自查:同形态问题在本组不存在,且是逻辑层确认不只类型层**。`resume-agent.service.ts:resolveRunProfile` 用 `if (direction)` 判断(falsy 同时接住 `null` 与 `undefined`),命中即回退 `JobDirection.PRODUCT` 并置 `directionFallback: true` ⇒ 本组适配(`9365f61`)**有真实回退分支**,不是只把类型放宽、真来 null 就炸。数据组 1b 先落后落均不阻断本组 | ✅ 已核 |
## ④ 归属裁决(闸门/交接归属、开工令、文件边界、当前引擎配置)

- **#132(简历组批5)+#133(§7 阶段零单价 fail-loud)主R 五关合入(2026-09-08,b2d1573 / 289a3e4)**:两 PR 均 codex PASS 先行。**#132**=§1.4.20(b/c) run 列表筛选(HMAC 反查三步:groupBy take=1000→内存逐 sha 命中即停→DB 层 where 分页;未命中空页不 400 不泄 id 存在性)+每项 4 字段(§3.E 逐字;§3.E.2(d)① 链断三字段同空)+§1.4.4 hasOptimizableContent 拦 confirm(不迁 FSM 不扣次,入参硬化到 JsonValue)+五个 max_tokens 内联抽 export helper+V4 预算坑登记注释(只登记不调值,测试钉五默认)。主R 验证:合并树 1351 实质全绿+**变异亲测**(HMAC 反查改无条件回首 sha→1 红)+build+真起 health ok。⚠️ **1 红环境归因**:resume-agent-p2.service.test.ts:868 把便宜档默认模型名写死断言 `deepseek-chat`,zzh 落 key 后本机 .env 的 `LLM_CHEAP_MODEL=deepseek-v4-flash` 泄入测试环境即红,**main 同红、CI 无此 env 故绿**——与 PR 无关,已登记给简历组修用例(断言应对齐 env 或显式隔离 env),复测 `LLM_CHEAP_MODEL=deepseek-chat` 覆盖后 51/51 绿。**#133**=domestic-llm.provider.ts 模块加载期自检(production 且任一单价 ≤0 → 启动即崩 LLM_PRICE_NOT_CONFIGURED,姿势照 resume-profile.ts 先例)+8 用例覆盖四漏配象限+CI 冒烟补 0.001 占位(oc 超范围自标、组长追认成立:CI 冒烟是全仓唯一 NODE_ENV=production 真启动,不补必崩——同一改动的另一半)+.env.production.example 两单价条目留空带注释。主R 验证:合并树 1359 全绿+**变异亲测**(自检条件改 false→6/8 红)+build+**dist 双向验证**(缺价 require 即 EXIT=1 且报错插值正确/配价 DIST_LOADED_OK)+dev 真起 health ok。合并顺序按简历组 merge-tree 实测采纳:#132 先(零交集)、#133 在 #138 前(.env.production.example 同锚点必冲突,#133 侧 5 行机械可解)。main CI:#133 班 success(树尖含双合并;#132 班被并发组正常取代 cancelled,同 #128 形态)。

- **#134 可发布开关实现「来源标记写入+消费改造」轨道归属裁决(主R,2026-09-08;数据组请裁)**:**裁由数据组承接**(其拟案照准;zzh「那你就干呗」已给产品面授权,主R 补技术面认定)。理由:spec 全流程知识在数据组(六轮 codex 终审是他们扛下来的),后端组在途队列已满(provider 可配/PR-甲/#101 必修/预算错误串四件),移交=纯上下文损耗;跨轨道风险不在「谁写」在「撞车与共享函数」,以下四条件为准:**①开工前把将触碰的后端轨道文件清单知会后端组**(经 zzh),给其指认在途冲突的机会——后端组「知情」是开工前知情,不是合并时才知道;开工前查同题在途(简历组本周三撞的教训,不重蹈);**②凡改共享函数一律按会改线上行为对待、单独声明**(#120 立的全组通用口径,后端轨道函数全按此办);**③走完整流程**:分支→codex 对抗审(实现批照旧)→主R 五关,不因 zzh 已授权而降审;**④迁移时序照其自案**(等 r2 合、防时间戳乱序假绿,四张表按既定顺序)。另:树替换型污染事故自报+开 PR 前自查拦下+连带核实现批干净+⑤补两道闸,记正账。

- **部署安全组豁免对抗审(zzh 2026-08-16 拍板)**:「你不用审核,直接 sonnet 写就行,你们组情况特殊,方案全部敲定以后我去跟主R聊」→ 本组复盘轮 spec **免独立对抗审**,链路 = 组内自审(起草/复核分离)+ 主R 终审。偏离公约铁律#1 与 A1,由拍板人豁免、此处留痕;主R 终审时请知悉本组 spec 缺对抗审一道。
- **部署安全组 G4 有意例外(2026-08-16,组长决定并留痕)**:在 spec 终审前提交了一处**防护性**改动——`.gitignore` 增加 TLS 私钥拦截规则(原先 `docker/nginx/certs/*.pem` 完全不受任何 ignore 规则覆盖)。判断依据:红线「密钥绝不入 git」优先于流程闸 G4;该改动非功能实现、零副作用、正反验证通过;共享 checkout 下已有两次文件被他会话提交夹带的 badcase,风险非理论。如主R 认为不当,可直接 revert。
- **当前引擎配置(A4)——实现期配置(2026-08-26 zzh 调度,主R 落文)**:①**写码执行 = 各组自配 OpenCode(oc)**:只执行派工,**零决策、零审核权**;允许对自己刚写的代码先做一轮 self-review + 必要测试(这属交付卫生,不属审查链任何一道);oc 覆盖组=简历/模面/前端/后端,数据组待方案定后向 zzh 申领,不写码的组不配。②**Claude Code 组长 = 大脑**:派工、拆解、组内审核把关、裁决,地位不变;CC 额度紧,组长自行决策怎么在 CC/oc 间分活,**但质量责任在组长——省额度不是降质量的理由,出了质量事故不接受「因为交给 oc 了」作为解释**。③**对抗审 = Opus5**(不变);spec/文书写侧仍 Sonnet5(不变)。④**codex = 审核的最终决策者(不变,v1.2 口径),且全组共用一个**(与 oc 相反:oc 每组一份,codex 只有一个、经 zzh 人工转):任何交付(方案与代码)最终过 codex 才算数,oc 自审/组长组内审/对抗审都不替代这道闸;**送审必须攒批**(一个 spec/一个实现批为单位)、材料按「交外部审核者规范」自包含+中立,禁碎片送;多组同时送审的先后由 zzh 排。⑤**过渡条款(zzh 2026-08-26 补)**:本配置只约束**新派工**——已经派出去的在途任务照原样执行完,**不强行改道、不打断**,后续任务再按新配置优化。闸门/道数/方向恒定,author≠approver 仍成立。(配置变更由 zzh 人工调度,改这一行即生效。前档:写侧 Sonnet5/审侧 Opus5;更前:写码组内闭环+对抗审 Fable5)
- 合并权与共享缝边界:权威 = `CONTRIBUTING.md` §0.5(此处只挂指针)。
- **worktree 边界(2026-08-26 全组迁移,主R 拍板〔zzh 授权「你拍就行」〕,权威指引=`docs/dev/组长会话worktree迁移指引.md`)**:主R `~/bangtoudi`(主树常驻 main,分支操作走临时 worktree)/ 后端 `~/bangtoudi-backend` / 简历 `~/bangtoudi-resume` / 模面 `~/bangtoudi-interview` / 数据 `~/bangtoudi-jobdata` / 前端 `~/bangtoudi-fe` / 部署安全 `~/bangtoudi-infra` / 法务 `~/bangtoudi-legal`。**即刻生效、不需重启会话**:所有 git 写操作在本组 worktree 执行,`~/bangtoudi` 对非主R 降级只读;禁在他组工作区操作。backend/resume/infra 三棵主R 已代建。
- **对抗审豁免撤销(主R 拍,2026-08-26,zzh 授权)**:部署安全组 **M6/M7 起恢复独立对抗审**(撤销 zzh 2026-08-16 豁免;依据=M1–M5 终审双镜头实证,被豁免的对抗审本可拦下 F1/F4/F5 类「自查栏答错/验收自证」问题)。M1–M5 存量按既定路径走(F1–F8 回填→codex 终审),不追溯补审。
- **运维写库脚本 dry-run 口径统一(主R 拍,2026-08-26,zzh 授权;#82 复审 F9)**:**方案 A——全仓运维写库脚本一律「默认 dry-run + `--confirm` 放行」**(与 `import-jobs.ts` 既定口径一致;资损面从严)。#82 回填时翻转 `update-product.ts` 默认方向;后端 spec:1245 相应勘误(勘误类,对齐本裁决,不触发 codex);今后新写库脚本一律照此,不再逐个拍。
- **main.ts 优雅关闭一行归属裁决(主R,2026-08-26)**:M1 ⑤ 所需的 `app.enableShutdownHooks()` 由**部署安全组在 PR-A 内顺手改**(与 Dockerfile CMD 改动强耦合,拆两组交付=两个半修复),**条件**:①PR-A 对 `apps/api/src/main.ts` 只动这一行、提交显式点名;②该 PR 须**后端组 review 认可**后再进主R 终审(author≠approver 保持,跨轨道一行以收件轨道认可为准)。
- **`packages/shared/src/index.ts` 排他窗口解除(主R 裁,2026-08-26)**:G3 开工令①(SoT 落码)已随 **PR #78 合入 main(2026-08-26)**,排他窗口**即时解除**。后续任何动该文件的 PR 恢复常规约束:①属共享缝,合并权主R 直管、custodian=后端组(PR 须后端组 review);②动 SoT=跨模块契约,命中 A1 → 独立对抗审;③**category 白名单/词表C 的内容变更另需数据组会签**(值表权威=数据组);④简历组三处加法(ANALYTICS_EVENTS 追加/REPORT_SURFACE_PURPOSE/守护单测)按上述路径可提;提起时**知会后端组**,其 backlog 的「单值 subCategory 判据」是否顺带由后端组自行决定、自行提交,不代写。**附裁**:`REPORT_SURFACE_PURPOSE` **绑 `COVERAGE_FEATURES` 冻结枚举的键空间**(取 `Partial<Record<CoverageFeature,…>>` 形态),不放开为 `Record<string,…>`——将来出现非 CoverageFeature 的报告面,本身就是 v2.0 级变更,应走受控扩枚举,不给字符串自由度留漂移口。
- **`anti-fabrication.ts` 上提 `common/` 会签(主R 裁,2026-08-26):准予,四条件**。理由:平台化并非单消费方投机——模面组已确认同类需求(evidenceQuotes 零校验,Q5 跨方结论),反编造属**两组共需的平台义务**;简历组自陈的「引别组 spec 未决待办当先例」授权链**确认无效**,本裁决即为正式授权。条件:①定位=**纯平台件**(判定标准:不 import 任何业务 module/prisma 业务类型、不含指定业务场景语义;`assessDiagnosisFeedbackRisk` 属 resume-agent 诊断埋点业务函数,**不随迁**、留在 resume-agent);②搬迁 PR **单独且纯机械**(move + `export * from` shim + importers 零改),命中 A1 → 对抗审;③本裁决**不构成 `common/corpus` 孪生债的处理先例**——corpus 债维持「主R 留档、随合库(模面组 owner)清偿」不变,两件切开,不得互相引用为授权;④搬迁 PR 内为 `apps/api/src/common/` 补一行准入口径(纯函数、零业务依赖、≥2 消费方或平台义务),作为今后同类会签的判据。
- **Q5 法务定位(主R 采纳法务组备案,2026-08-26;本行曾被合并挤掉条目头,2026-08-27 修复)**:法务组为模型会话,其产出**不构成闸门输入**——凡「法务放行前禁用」类闸门,放行输入=**zzh 签字**(必要时带执业律师);任何文件写「法务(组)已放行」一律改为「法务组已出具建议书(路径+commit),待 zzh 签字」。主R 终审按此口径卡。
- **后端组实现批四项裁决(主R,2026-08-26)**:①**合并顺序**=#88(修复后)→#85→#86/#89(并行)→#82/#83;#88 当前 CI 红(web Dockerfile 未与 api 同修,见③回填单),其余不等它、先走。②**`@nestjs/schedule` 归属**=单一调度库,由后端组在**首个需要它且先合入**的 PR 引入并注册(按合并顺序自然决定),语音组与后续消费方复用不二次引入;`ScheduleModule.forRoot()` 注册属 bootstrap 面,该 PR 须真起验。③**`createOrder()` 五节插入顺序**=批准六节评审 T-8 建议序:**§4 限流 → §5 彩排授权闸 → §1 try-lock 临界区 → §2 快照 → §3 埋点**(外层拒绝在前、闸在临界区外、快照/埋点在内);五处改动**串行开 PR**,后开工者 rebase;T-10 采评审建议:§5 的 createOrder 授权闸**并入 §1 PR-C**(§1 owner 会签)。④**BI 只读 role 执行归部署安全组**,彩排 checklist 项,硬前置=后端 `AnalyticsEvent.phone` 列迁移先合;SQL/验收按两组已收口版。**另登记防吞**:BE-M4-01b 端点复扫(P1)已拆号,排 M4 实现批,不得随 #83 勾销;BE-M2-01b 商品五字段在 #82 合入后开工(迁移纪律+时间戳避让已挂,前端月/季切换在等)。
- **后端组 #85/#82 复审结论(主R,2026-08-26,续上条)**:两 PR 均 PASS-WITH-FIXES。**#85 合前必修**:F2 假表写入算子词表漏 `createMany/updateMany/deleteMany/connectOrCreate/unset` 一族(嵌套关系写被当普通 JSON 值**静默写进字段**,PR-A/B/C 会拿假绿);F4 `$executeRaw` 未装闸(任意 SQL 返回 0,裸 SQL CAS 测试因错误原因变绿)。可跟单:F1 clone JSON round-trip 回滚后 Date 变字符串(建议 `structuredClone`)/F3 读算子词表补 `isSet`+JSON path 系/F5 Json 值误判/F6 entitlement.updateMany 硬取键/F7 不可达 fail-open 死代码改 throw。**#82 合前必修**:F8 未知 flag 静默吞掉(拼错 `--dryrun` 直接写生产商品表,加「未知参数 exit 2」);F10 runbook×脚本接口三重不匹配(spec 三处 inline `--changes` 而脚本只收文件路径,**L4 应急下架当前不可执行**;彩排步骤1 传身份字段必被拒;create 能力缺口就地补或归属明确)。可跟单:F11 seed 注释点明身份字段无修复路径/F12 跨字段校验(`MEMBERSHIP` 可被改 `durationDays:null` 破坏发放)。**正面留档**:#82 的 seed import 路径改动是在修 main 上**已断**的 `pnpm prisma:seed`;两 PR「组内已修」部分逐条复验均为真修;DI 复验再次实证「tsx 假通过、只有 tsc 产物真起算数」。
- **模面组四件裁决(主R,2026-08-29;来件基线 6a8dd9b)**:
  **①CoverageRegistry owner 指派**:v2.0 未指派 Registry 真身 owner 属实(简历组镜头 G7 早有同录)。裁:**真身实现 owner=后端组**——落点=**`packages/shared/src/index.ts` 内联静态登记表 `COVERAGE_REGISTRY`**(与 SoT 同文件同纪律:零迁移、零运行时读取失败面、git 即审计,fail-closed=查无即 none;v2.0「临时读取失败 fail-open」条款因形态选择自然消解,此为收窄非放松)+ `coverageOf(feature,category)` 查询函数 + 入口守卫 helper;**登记内容行权威=数据组会签**(哪些 category×feature 为专项档);消费接线归各业务组自 PR。**交付顺序**:后端组出小 PR 优先排(卡模面阶段2/3);fail-closed 切换前须先为现网 产品/运营/AI × MATCH/OPTIMIZE 登记专项档(v2.0 §3 顺序不能倒);接口契约(签名+语义)后端组先冻结一页,模面 PR-G4 可对冻结契约开工、合入等真身。附带裁掉知会件之一:**InterviewKnowledge 合库迁移的 category 来源=直接查 shared 的 `DIRECTION_TO_CATEGORY` 派生,不等后端中间列**(该映射是 v2.0 冻结三方向映射,SoT 已合入 main,派生即读 SoT 非造第二套;模面 PR2/PR3 解锁)。
  **②-执行机构补充(2026-08-29,应模面组请示)**:全局门落地方式=**甲(机械 CI 闸)+丙(结构兜底)双层**——甲:`scripts/check-ai-gateway-error-gate.mjs` + 基线 `ai-gateway-error-gate.json`(19 调用点/9 文件)已合入 main(PR #95)并挂 CI:新增 `AiGatewayService.run` 调用点即红;后端组修复 PR **同 PR 置 `active=false`** 即解除(显式受审,不靠人记得删);四向反向验证全过(正向绿/既有文件加调用红/新文件红/解除绿)。丙:全部 api 侧 PR 本就过主R 合并,人工兜底。乙(CODEOWNERS)不适用(单账号仓,审批机制空转);丁(各组自把门)照旧作纵深,不作为唯一依赖。⚠️ PR #95 的 CI 因 **GitHub Actions 账单故障未运行**(见③ 2026-08-29 主R→zzh 急件),yaml 已本地校验、脚本已本地四向验证后合入。
  **②AITask.error owner=后端组**(主R 已回码坐实:`domestic-llm.provider.ts:120` 整响应体无上限拼进 Error,`ai-gateway.service.ts:206/242` String(err) 直落库;provider 内容审查类报错常回显 prompt=简历原文,PII 面成立)。独立小 PR,修复照模面组清单:结构化截断只留 provider/httpStatus/errorCode/前 N 字符、禁原始 body 入库、长度上限、回归测试;**模面组自设的合并门升级为全局门**:该修复合入前,任何组新增触发 `AiGatewayService.run` 的 PR 不得合并(存量在途不追溯)。
  **③风格库阶段一存原话——主R 确认另有三条约束须接**:(a)**留存/删除执行者与存原话同批落地**——法务组已实证 X-10 模式(转写文本两层无执行者、永久留存):隐私政策不得承诺无人执行的删除,存原话的留存期+删除机制须作为确定输入交法务组(其正在从零起草隐私政策);(b)**采集侧闸**——X-6/X-11 同根教训(闸全设在处理环节、采集口裸奔),存原话的采集入口须有与处理闸同级的门;(c)**处理链路仅国内 provider,不出境**(既有海外 fail-closed 口径)。zzh 书面签字存档路径届时进台账。技术闸未放松+偏离显式登记的做法认可。
  **④合库 §4.4 两处 CHECK 形态定性**:①「完整值域→前缀形态正则」**定性=v2.0 冻结件实质降级,主R 技术会签接受**(前缀校验守住「跨域污染不可能发生」这一 v2.0 本意;完整值域移应用层,条件=应用层值域校验带正反向测试+守护);**生效前置=codex 对「此项作为 v2.0 §4 修订」的明确确认**——若四轮复核已明示覆盖此偏离,引该轮结论即可,不必重送;确认到位前 PR1 不开工。②「array_position 堵 NULL 放行面 + cardinality=0 逃生口」**采纳模面组认定=勘误类**(前者堵洞是加强,后者解主 CHECK 自相矛盾,均为把约束修成 v2.0 本意),随①一并落地,不重走形态审。
  **🟢 模面组落地状态(组长回执,2026-08-29,commit `3ccf8bf7`)**:四件**全部落进两份 spec**(模面 spec + 合库 spec,178+/29−)。①CoverageRegistry:9 处口径 + 新增裁决块,PR-G4 解锁为「可对冻结契约开工、合入等真身」,**开工判据=后端组冻结接口契约的 commit 号**;连带发现**验收 9 断言③「编排/gate 代码 diff 为 0」在内联静态表落点下恒假**(改登记=改 `.ts`),已按 PR-G3 前/后两阶段重述。②AITask.error:owner + 全局门已落;**登记一条**:全局门执行主体不是模面组(我们不 review 他组 PR、仓内无机械拦截),**请主R 明确其机械执行方式**(CI / CODEOWNERS / 人工),不阻塞本组 PR。③存原话三条已进偏离登记表与放行边界;**(a) 是合入门**——法务组的留存期+删除机制确定输入未到即不得合入;**(b) 采集入口门本 spec 原确无门,本次补入,模块四五 10.25→10.50 人日**;**(c) embedding 侧出境面本 spec 未核实,列开工前置**。④CHECK:主R 会签接受已落,PR2/PR3 解锁(登记执行约束=必须真 `import` `DIRECTION_TO_CATEGORY`、禁内联、附逐键守护单测)。
  **🔴 ④ 的生效前置 = 未满足,组长不代为认定**:主R 给的豁免路径是「四轮复核若已明示覆盖,引那一轮结论即可」。**回查结论:未覆盖。** Codex 一审原话为「**属对 v2.0 冻结件的实质降级**」——说的是**性质**,不是**流程归类(构成 v2.0 §4 修订)**;前者主R 技术会签即可,后者须走上游正式修订,两者不等价,**本组不合并读**(同型于已认账的「把『法务我来解决』读成『已放行』」)。⇒ 已出**窄确认单**(自包含、中立不给倾向,zzh 可直接转发):`docs/review/2026-08-29-模面组-交codex窄确认-CHECK降级定性.md`。**PR1 卡在这一道。**
  **🔴🔴 2026-08-30 终局更新 —— 上面那条「PR1 卡在 codex 窄确认」已过时,codex 已回,答【甲=构成 v2.0 §4 修订】**:①codex 的证据比本组窄确认单更硬 —— 本组只引了 v2.0:190,**漏了 v2.0:194**(反向验收④「`RESUME:QUESTION` = FAIL」),选 B 要求同一输入 DB 放行 = **直接推翻上游已生效验收**;且 v2.0:190 末句写的是应用层「**不再是唯一防线**」,选 B 恰把它变回唯一防线,**直接对立**。②codex 给的唯一替代 = **撤回降级、恢复完整 DB 值域 CHECK**。③**本组选撤回,不走 v2.0 修订。** 决定性理由(组长回查代码后发现,**此前四轮与本组自己均未察觉**):**选 B 声称的收益不存在** —— 两个 contentType 值域都是 Prisma enum,扩值必产生 `ALTER TYPE ADD VALUE`,而 **v2.0:105 白名单明文点名 `ALTER TYPE` 即 FAIL** ⇒ 扩值域那次迁移**无论选 A 选 B 都要走例外审批**,冲突从未被避开,只是换了个名字。**四轮的权衡建立在一个错误的收益上。** ⇒ 合库 §4.4 改回 `<@ ARRAY[…]` 完整值域,约束改名 `KnowledgeEntry_act_valueset_check`;**偏离消除,C、G 两项撤销**;简历组现稿引用的「④必须 FAIL」**无须改动**(codex 点名的那条跨组通知随之消失)。**主R ④ 裁决的①因降级撤回而 moot**(会签未被推翻,是失去对象),其条件「应用层正反向测试」继续有效但**定位下调为友好报错层**;②两处修补**一半延续**(NULL 合取项)**一半消失**(逃生口)。**🔴 PR1 不解锁,门换了一道 = 撤回稿过 codex 复核**(撤回属改判 = 实质变更,按公约走 codex)。工作量:PR1 2–3 → **2.75–3.75**,合库合计 17–23 → **17.75–23.75**。
  **🟢 2026-08-30 另三件外部输入已落地(commit `b76fb8bf`)**:①**PR-G4 开工判据到手** —— 后端组交付《CoverageRegistry 接口契约 v1(冻结)》,冻结 commit **`2bee2b7`**(已亲验内容存在),签名与我方冻结形状逐字一致;两处必改已落:`CoverageTier` 上提 shared(删本地定义改 import)、评测侧 `isCategoryLit` 收敛到契约的 `isCovered("INTERVIEW", …)`。合入仍等真身落 main(后端组告知 shared 属共享缝需独立对抗审,时点不倒推)。②**全局门执行方式已明**(甲 CI 闸 PR#95 + 丙人工;脚本已亲验在 `main`)—— **🔴 但全仓 CI 因账单故障停摆 ⇒ 甲此刻是纸的**,本组自把门在 CI 恢复前**不是纵深而是主力**;恢复后须以**一次真实的红**确认闸在跑,不接受「配置在那里」。③**存原话留存期 = 90 天 + 形态乙(逐条 TTL)**,合入门可解;**🔴 据法务组硬提醒推翻本组上一版验收设计** —— 原设计「写入超期行⇒跑清理⇒断言为空」正是它点名的「只在测试里手工调一次清理」,证明的是「清理函数能删」而非「清理会被调用」,而 X-10 恰死在后者;改为三条(函数级正反向 + 调度注册断言 + 摘掉注册变红)。前置:`@nestjs/schedule` 仓内未安装(已亲验),归属未定,本组不擅自认领。模块四五工作量 10.50 → **11.00**(区间 11.00–12.00)。

  **🔴 顺带补了一处真缺口(非裁决点名,本次查证发现)**:F6 把值域校验「移到应用层」,但**全 spec 从未定义该应用层校验的落点/实现/测试**——提及它的三处都只把它当「会被裸 SQL 绕过的那个东西」引用。照原样实施 = DB 主动放弃 + 应用层从未存在 = **值域校验整体消失**,而 4b①「DB 层放行」断言**照常绿**(假绿第⑥形态)。已补落点(`packages/shared`)、5 处写入通道接入的 grep 证明、正/反/反向的反向三层测试;**PR1 完成定义:三类不齐不许合 → 四类**。

- **后端组批次合并结果 + 三裁决(主R,2026-08-30;来件=`docs/review/2026-08-30-后端组-致主R-合并汇报与三个裁决点.md`)**:
  **【裁决C·合并顺序】**确认修订:**#94 插最前**(全局门本体;合前主R 亲跑门脚本于当时 main,19 调用点零漂移,停摆期间无漏网)→ #88 → #85 → **#97(替代 #86,确认)**/#89 → #82/#83;**#93 不随批走**——等 codex 窄复核回件(该审查线终闸=codex,五轮往返在它手上);#98 审计件不占序、主R 已独立终审。
  **【已合入 5 件(每件主R 本地五关:全量测试+build+真起 dist 验 DI+健康探针;CI 停摆过渡口径,留痕见下)】**:**#94**(结构化截断两层设计成立:provider 侧只留 code/provider/status/短错误码、自由文本字段刻意不取;sink 侧 sanitize 兜底;同 PR `active:false` 解除全局门,门脚本合后亲跑输出「已解除」)、**#88**(web Dockerfile 孪生修到位;主R 亲跑 `docker build -f apps/web/Dockerfile` 全程通过=原 FAIL 点实证闭合;exports/prepare 前置钩子/CI `--no-experimental-strip-types` 常驻断言均验)、**#85**(词表补 createMany 族+RawSQL fail-closed 抛错指名,支付 60 测试绿)、**#97**(主R 弱值探针 6/6:`ci-test`/`change-me`/32字节周期串/`secret--`×5/`<强随机,生产必改>` 全拒、CI 新值过;merge 后 ci.yml 三件齐:paths-ignore/concurrency/AITask 门步)、**#98**(枚举器 fail-open 修法承重「无 Guard 一律不给安全结论」;主R 独立抽查 `GET /api/jd-analysis/:id`(service:167-173 私有需归属否则404)与 `PATCH /api/applications/:id`(service:99 非本人403)与工单一致;自测 96 路由咬合亲跑绿;枚举 63/13/20=96 与工单口径逐字对上)。**合后终态 main=faae0f4:865+11 全绿 / build 绿 / 真起 health ok / 门脚本放行**。
  **【勘误(当日,主R 自纠)】#82 初判打回是错的,已改判合入**:runbook F10 三处修复**早已直接落在 main**(`a6ec986`,:2860 钉死归属 BE-M2-01b / :2868、:3020 改文件路径+`--operator`+`--confirm`),主R 只查了 PR 分支上的**过时拷贝**——存在性断言没做双向核(违自家 checklist,⑤已记)。#82 重走五关(899+11 绿/build 绿/真起 health ok/`--dryrun` 拼错探针 exit 2)后已合入。
  **【打回 2 件(共同病灶:主修完成、裁定点名的第二处未动;逐条见③回填单)】**:**#89**(两条 must-fix 零回填,主R 已双向核 main+分支均无:`.env.production.example` 仍是 `-internal` 模板且无 `OSS_SIGN_ENDPOINT`=照模板部署生产必启动失败;生产逃生开关不否决 `-internal` 串未补)、**#83**(「三条 owned 正例」零条;主R 亲跑变异于 main+分支合并树:interviewId 归属校验改永远拒绝,**10/10 照样全绿=M4c 存活原样**)。
  **【裁决A·F2 另立项否】**:采①,**#83 合入即自然闭合,不另立卡**;但加**防勾销条款**(#86→#97 先例):#83 若因分支腐坏等原因关闭/重建,F2 归属校验必须**点名**随替代件交付,不得静默消失;在 #83 合入前,`application.service` 写路径无归属校验这一事实以本行为准(数据完整性面,非越权读取面,不阻塞 #98 零 FAIL 结论)。
  **【裁决B·audioRef 归属闸 owner】**:采后端组建议——**模面组实现、后端组出判据**(对齐 `assertOwnedAudioKey` 前缀规则或改服务端签发不可伪造引用,两者取舍归后端组判据文档,主R 不预裁);**闭合时点=任何把 `StubAsrAdapter` 替换为真实 ASR adapter 的 PR 必须同 PR 含 audioRef 归属闸**——触发条件是 adapter 替换这个事件,不是日期,不单独排期。此为**发布门硬项**(2026-08-05 督察组点过的同一条活 IDOR,勿再漏);知会模面组(经 zzh)。
  **【裁决B·补充(主R,2026-08-30,应模面组回执 023634b)】**:①模面组回执四条全接受——PR-A 提前落闸(排期早于闭合时点,零增量)、暂用生产现行判据 `voice/<userId>/` 前缀(出处 #52+`interview-recordings.service.ts:147-151`,Q5 溯源成立;后端组判据文档仍是权威,将来若裁「服务端签发」按其文档做仓级切换)、`assertOwnedAudioKey` 上提不捆 PR-A(合 08-26 搬迁先例:单独纯机械 PR+对抗审)、catch 吞 403 的发现**主R 已回码核实为真**(`interview-run.service.ts:536` 无条件改写一切异常为 400,归属断言必须落 try 外,三条并列验收正确)。②**拍:准做「音频读写调用点登记表」CI 守护**(0.25–0.5 人日,模面组建,T-1 骨架复用)。形态照 AITask 门先例(④②-执行机构):登记表文件+check 脚本,**未登记新调用点即红(fail-closed)**,登记表变更只随业务 PR 显式受审;合入附**四向反向验证**留痕(基线绿/未登记新调用点红/新文件红/登记后绿);CI 停摆过渡口径同前(本地留痕,9/1 后以一次真实的红确认闸在跑)。仓级 CI 面扩张由本裁决背书,防的正是 4.1 重写音频路径时「新路径绕过闸」——单测防回退不防新路径,这判断成立。
  **【裁决B·收尾确认(主R,2026-08-31,应模面组知会 72b5b5a)】**:设计四条全确认、不打回——①**无 `active` 的刻意偏离批准**(本闸永久无解除态,照抄 active=造一行全局关闸开关,正是 Q5 禁的豁免形态;AITask 门有 active 是因为它是临时冻结,两者形态差异有因);②定性收着写确认(机械层=冻结面,证「有人 review 过新增」不证「带闸」,留痕与 PR 描述两件事分开、9/1 前不写「CI 已守护」——照此执行);③**按原语扫宽于「音频」确认**(#52 铁律本就不是音频专属;`download` 零命中照进原语列表是对的——4.1 最可能新增的正是它;简历侧存量进 baseline 零改动,三组知会已发「不要回」);④排期插 T-2 前接受(PR-A 作第一个真实撞红→登记,以真流程补第五向验证,顺延 0.25–0.5 人日在授权界内)。
- **后端组批2合并结果(主R,2026-09-02;来件=后端组合并批次申请,4 件全合)**:
  **【已合入 4 件,顺序照申请 #103→#83→#100→#89,全部 主R本地五关 + 真实 CI 绿(额度 9/1 已恢复)】**:**#103**(.mjs 按包名消费 shared SoT=决策 0007 落地,副本数为零;`rootDir:"./src"` 钉死亲验 dist/main.js 在正位;`loadSharedSot()` 亲跑 19 keys;数据组 #99 依赖解除)、**#83**(F2 归属校验;**主R 亲跑 interviewId 一路双向变异:assertInterviewOwned 改永远拒绝→正例红,删 where 里 userId→负例红——8/30 那个 10/10 存活的变异正式钉死;resumeVersion/matchReport 两路六条变异为后端组自报,主R 未复跑**;guard 脚本亲跑 19 controller 零命中)、**#100**(NP0;**主R 三态真启动于编译产物**:空配置→exit 1 且**一次报全 10 个缺失**(CJS require 序洞的修复=断言后动态 import,在真实启动路径实证)、十项配齐+ALLOW_PAYMENT_SANDBOX=true→health ok、同配置去掉逃生开关→PAYMENT_SANDBOX_IN_PRODUCTION 点名两渠道拒启,DI 工厂位真拦)、**#89**(**主R DI 层双闸真启动**:内网 endpoint 无 SIGN→OSS_SIGN_ENDPOINT_REQUIRED 拒启;+OSS_SIGN_WITH_ENDPOINT=true→OSS_SIGN_ENDPOINT_INTERNAL_FORBIDDEN 拒启=8/30 回填②在装配层兑现;双 endpoint→health ok)。加法冲突三面(ci.yml 三守护步/.env.production.example 支付段+OSS 段)合并树逐条核对**全留**,YAML 可解析。终态 **main=6c3ba6c:web 84 + api 984 全绿**;4 PR 自动转 MERGED。
  **【CI 复活确认+过渡口径退场】**:后端组报的两次真实的红(#103 编译产物冒烟 MODULE_NOT_FOUND、#100 负向冒烟 PRODUCTION_ENV_MISSING)即 9/1 条件要求的「一次真实的红」——闸证实在跑;本批 main 上中间 push 的 CI run 被 #96 concurrency 正确取消(瘦身生效实证),终态 run 于 6c3ba6c **已绿(run 33615056373,主R 核过日志:AITask 门/request-body guard/正向冒烟三步 success,负向冒烟真实输出「✅ 负向冒烟通过…退出码 1,报错为 PRODUCTION_ENV_MISSING」——断言方向的红在 main 上被真实演练过)**。**自本批起「CI 停摆过渡口径(本地验证留痕替代)」退场**,恢复 CI 必绿 + 主R五关双轨。
  **【NP0 状态翻转】**:8/30 回执问的「NP0 进展」已闭环——支付沙箱 fail-closed + 生产 env 断言统一件随 #100 落 main,**彩排 checklist 该硬项解除**;彩排环境按 `.env.production.example` 配置并显式设 ALLOW_PAYMENT_SANDBOX=true(设计内合法出口)。
  **【不在本批(与后端组口径一致)】**:#93 等 codex 第六轮、#101 等 A1 独立对抗审(勘误 9/7:并非卡模面组——材料齐、CI 绿,等的是主R 安排对抗审,已于 9/7 启动)、#99 数据组件(依赖已解除,可开验)。
- **合并队列批(主R,2026-09-07;5 件全清,每件主R五关+真实CI)**:
  **【#112 手机号必验真闸(D1 发布阻塞)】**已合。主R 内容审认可三处方向判定(缺 scope 判拒非 full/豁免端点上老 token 也拒/classifyAuthScope 校验产物);**主R 六路真机探针于编译产物**(真起+真签 token 打真实端点):full→200 / phone_bind_only→403 PHONE_VERIFICATION_REQUIRED(改动前 200,D1 本体)/ 老 token→401 TOKEN_FORMAT_OUTDATED / 伪造 scope→401 INVALID_TOKEN / 受限打 bind→400(过守卫进业务)/ 老 token 打 bind→401,与 PR 声称逐一吻合。AdminGuard 链式无耦合已核。
  **【#111 模面组两道 CI 守护——切片合并】**:🔴 分支 feat/interview-impl 在开 PR 后又堆了 T-2/T-3 业务实现(含 audioRef 闸、两条 prisma 迁移),与 PR 正文「零业务代码」不符;主R 按**送审边界**切片合并至 8286384(=T-1/T-1b 两道守护),业务尾巴留分支待其自己的送审批,**PR #111 保持 OPEN 属预期**。守护验收:两脚本+自测(32/16 断言)在合并树亲跑绿(⚠️主R 反向验证只做了 audio 一道,迁移卫生闸的反向依组长 16 条自测,主R 未另跑);registry 无 active 字段(_doc 里的"active"是解释「不设 active」的措辞);**主R 反向验证**:注入未登记 `oss.download` 调用点(零命中原语)→红且精确到 文件|Class.method|原语|次数,回滚→绿。ci.yml 加法冲突五步全留(migration-hygiene/AITask 门/request-body guard/audio-callsites/负向冒烟),YAML 可解析;台账两处加法冲突并集解决。判据从纯 count 改 AST 身份精确相等 = 组长自报的正当偏离(codex 四轮),主R 追认——纯 count 挡不住净零替换,与「永久集合守护」语义不符。
  **【#113 冒烟 env 漂移闸】**已合。为简历组批 3 预铺;主R 变异探针(ci.yml 删 OSS_BUCKET→红并点名,回滚→绿)。**合后一天内即真实拦到简历组批 3 一次**(382a24a 补 RESUME_GROUP_ID_SECRET)——闸的价值当场兑现。
  **【#114 数据组勘误订正件】**已合,主R 按裁定直接落地 spec 本体:①:977 补空 hostname 守卫(mailto:/data:/javascript:/file: 不抛错的实测行为入注释);②:997/:1000 四条→三条;③「至少 4 个」订正为实测 18 个(方向不变);④0007 落定注记(job-category-vocabulary.mjs 全部引用按 0007 读)。🔴 **一处指针订正暂缓**::64-96→:77-109 在 main 上不成立(main 上 `allowedOfficialHosts` 仍起于 :65,:77 是数据组实现分支迁移后的位置)——留待其实现批合入时随迁,已防「订正把对的改错」。
  **【#102 注释区格式清理(软著配套,zzh 指示)】**已合。9/1 的 CI 红与清理无关(三条真库 CAS 测试当日基线暂态,现 main 亲跑绿);157 文件冲突仅 1 处(清理后 main 新增注释块,取 main 内容按清理规则转换);tsc 双端 0 + 全量 1082/84 绿。⚠️ **残留知会部署安全组**:清理分支切走后 main 新增/改动的 **13 个非测试文件**又带入目标符号(#100/#112/简历批3 等),**软著材料 v4 重出前须对 fresh main 再跑一遍同规则清扫**,否则「材料=源码」不成立。
  终态 main=9d532b3,**CI 绿(run 34119987991,主R 核过 step 清单:迁移卫生闸+audio 登记表闸首次在 main 真实运行均 success,AITask 门/request-body guard/负向冒烟同批全 success——#111 两道闸的「真实在跑」证据落定)**。#101 的 A1 独立对抗审已回件:**PASS-WITH-FIXES**(报告全文+主R 裁决=`docs/review/2026-09-07-主R-PR101-A1对抗审报告.md`)。签名/语义与冻结契约 2bee2b7 逐导出比对零偏差,PR 自述五处变异防线全部复核属实;审查员追加 7 变异抓到 **M6 存活**(档位值校验无测试钉住)+78 探针抓到 Object.prototype 污染合法档位值时 fail-open。主R 裁两条 must-fix(补 M6 盲区测试/coverageOf 加 Object.hasOwn 守卫,两条互不覆盖都要),回填后主R 复验即合、主修不必重审——#89/#83 回填先例同款。
- **简历组直推 main 两批·流程裁决(主R,2026-09-07;应组长自报请裁)**:
  **【事实】**批1(9/2 约九个单亲代码提交,6-a′ 系列+补做测试)与批3(9/7,002f9fe,§1.4.20 端点+resumeGroupId 派生+RESUME_GROUP_ID_SECRET 共享缝三件套)均未走 PR 直推 main;根因=组长与 oc 共用同一 worktree 同一分支,推文书 `push HEAD:main` 把代码提交作为祖先带上(批3 push 经 zzh 授权)。组长 9/2 请示过一次未获回复(传话未达,不算他的账),9/7 主动自报。
  **【裁①:两批补事后审——要,已于 9/7 由主R 启动】**机械层(全量测试/build/CI)已被后续合并与 main CI 多次覆盖,补的是缺失的主R 内容审:独立审查员(无上下文,不喂结论,与 #101 A1 同规格)对两批 diff 做越权面/HMAC 实现/不变量承重/测试质量四向审,回件后主R 出裁决。**在事后审回件前,两批状态=「已合入、终审补审中」,不得对外表述为「已过主R五关」。**
  **【裁②:简历组一律改独立分支走 PR——是,即日生效,且不止简历组】**三条配套:(a)oc 与组长**禁止共用分支**——oc 在自己的 worktree-agent-* 分支干活(现状已是),组长收口到**独立的批次分支**开 PR,`resume/workspace` 不再兼任「推 main 的载具」;(b)🔴 **新立机械闸(全组通用)**:任何 `push HEAD:main`/直推 main 之前必须跑 `git log origin/main..HEAD --oneline -- ':(exclude)docs' ':(exclude)*.md'`,**非空即禁推、改开 PR**——文书直推特权只属于纯 docs 增量,代码祖先混入即失效;(c)**zzh 授权 push ≠ 免主R 代码终审**——授权解决"能不能推",公约 v1.2 的代码 PR 终审权不因此让渡;尤其批3 含共享缝(production-env.ts),主R 9/2 三裁②明写"随批 3 同 PR 提、走主R五关",直推恰好绕开了这条。今后碰共享缝零例外走 PR。
  **【记好账】**组长主动自报两批直推并把根因(共用 checkout 的 8/26 病灶变种)挖到位、#113 漂移闸拦红后立即补齐——自报质量合格;9/2 那次请示石沉是传话链丢件,⑤已记的「状态线打架」家族又添一例:**裁决类请示发出后 48h 未获回执应主动催办,不默认「没回=默许」也不默认「没回=禁止」**。
- **模面组 #111 尾部经 zzh 授权直合·主R 事后五关(主R,2026-09-07)**:
  **【事实钉正】**模面组合入理由「CI 停摆期间无法满足常规门」**前提不成立**——CI 已于 9/1 恢复,当日 main 每次 push 均跑真实 CI;实际上 main@2620088(该合并本身)CI 就是绿的,含迁移卫生闸与音频登记表闸。授权按「记录在案,不作默认先例」处理,与简历组同日两起同型(zzh 授权绕 PR),口径统一:**zzh 授权合入 ≠ 免主R 终审,事后补审一律执行**。
  **【事后五关:通过】**①内容审:**PR-A audioRef 闸与 8/30 裁定逐字吻合**(断言在 try 外+403 AUDIO_REF_FORBIDDEN+`voice/<userId>/` 判据+`!userId` 纵深分支;三条并列验收测试逐条在:403 且非 400/transcribe spy 0 次/audioRefs 零写入,外加正例)——发布门硬项(8/5 督察组活 IDOR)**闭合**;T-3b 修法+路由冲突回归锁(元数据层验全域唯一)扎实;两条迁移读过:纯加法、#32 意识到位(拒抄 drift 噪声 9 条 DROP)、时间戳合法、含存量回填与 72h→7d 窗口修正。②全量测试 **1138 全绿**(web 84+api 1138 的树)③build 绿④**两条迁移在本地真库 migrate deploy 成功**⑤真起 health ok,**启动日志 `recordings/debrief` 恰 Mapped 1 次**(T-3b 实证)。
  **【三条未闭合的处置】**①「CI 恢复后一次真实的红」:前提已过时——两闸已在 main CI 多轮真跑绿,红方向由主R 本地注入实测(audio 闸,download 原语精确红)+CI 内自测断言(迁移闸 16 条含红例)覆盖,**判闭合**;首个自然红(未登记新调用点)按设计由未来业务 PR 触发。②jsonOutput 真 DeepSeek 验证:**成立,列上线前硬项**(彩排 checklist)。③两迁移正式库 migrate deploy:**成立,并入发布 SOP**(本地真库已预演)。
  **【判据偏离追认】**纯 count→AST 身份精确相等的偏离已在合并队列批(9/7 ④)追认过,不重复。
- **简历组两批直推·事后审回件+主R裁决(主R,2026-09-07 晚;承 9/7 裁①)**:独立审查员回件(报告全文+主R裁决=`docs/review/2026-09-07-主R-简历组两批直推事后审报告.md`)。**批1 判 PASS**(3 🟢 留档;「配置期 fail-fast 实为请求期」文案随回填顺手订正);**批3 判 PASS-WITH-FIXES**——3 条 🟡 合并为**一个回填 PR**(F1 `.env.production.example:91-92` 模板注释仍是被收回的「静默失效」旧口径,与 production-env.ts 的「全线 500」相反;F2 千分位「3,000」↔「3000」被判编造,与注释自己的主张矛盾,修法=抽 token 合并千分位模式+补双向用例,不许反向改弱注释;F3 层③总量闸补含真实尺寸 rawText 用例+订正「天然比 rawText 大」注释;顺手:deriveResumeLabel 死代码落 TODO、envelope=0 组合补用例)。**🔴 界外预存(单列,发布前必修)**:`resume-agent-extraction.ts:313-343` `setValueByFieldPath`/`parseFieldPath` 原型污染——任何登录用户 `corrections:[{path:"__proto__.toString",value:"x"}]` 即全局覆写 `Object.prototype.toString` → 进程级 DoS(审查员独立 node 进程复刻实证;引入=a46b5a9/PR #45,**非这两批**)。owner=简历组,**单独一个 PR**(与批3回填分开):parseFieldPath 拒 `__proto__`/`constructor`/`prototype` 段+遍历改 `Object.hasOwn`+`rawText` 等落库保留键黑名单(同源🟡一并)+回归测试。与 #101 的 hasOwn must-fix 同族,原型链污染本仓已两处独立出现,列入方案交付错误库候补模式。**边界钉正**:批1 实为 7 个直推提交(非自报 9 个),3 个经 worktree 分支随批3 merge 进入、已在批3侧全覆盖,无遗漏不追责。**状态变更:两批「终审补审中」→「事后审通过(批3回填在途)」。**
- **后端 #106–#110 五件经 zzh 本人直合·事件记录+main CI 红修复(主R,2026-09-07 晚)**:
  **【事实】**22:28–22:30 zzh 经 GitHub 直接合入 5 个后端 PR(#108 测试台/#109 支付三态/#106 商品五字段/#110 限流100·300+熔断/#107 告警去重,共 43 文件 +3141),未经主R五关。当日第三起同型(简历组批3、模面组 #111 之后),按 9/7 统一口径处理:**授权记录在案不作先例,事后补审一律执行**——主R 已派独立审查员(与 #101 A1 同规格,含全量测试/build/真起 DI/内容对抗审/变异检验),回件前五件状态=「已合入、终审补审中」。
  **【main CI 红+修复】**合入后 main@6ab9d4c CI **红**:#113 迁移卫生闸拦到 **20260902120000 双目录**——#111 的 `interview_last_activity` 与 #106 的 `product_billing_fields` 完全同秒(两组各自 worktree 手编整点号,合流才撞;闸按设计工作,拦的是真实迁移排序歧义)。主R 修法遵循「改新不动旧」:后到的 product_billing_fields 改名 `20260907150000` 排队尾,SQL 零改动,**按全组闸走 PR #115**(不直推),本地卫生闸+自测 16 断言绿,CI 绿后合入。⚠️ 已按旧名跑过 migrate 的本地库需 reset/resolve;正式/彩排库未部署过此迁移,无影响。
  **【续2:事后审回件+主R裁决(2026-09-07 深夜)】**独立审查员回件,逐件裁决:**#108 PASS**(harness 有牙,M10/M11 变异转红)/**#109 PASS**(三态真起亲验、凭证零泄露有直接断言;🟡公网 /health 逐键列缺失 env 名=侦察面,主R 裁:发布前把 detail 从公网响应摘掉只留 status 三态,detail 走启动日志或带鉴权端点,轻改不卡事)/**#106 PASS-WITH-FIXES**(代码与迁移正确;🟡存量库五字段全 NULL 无 backfill runbook——已跑过 seed 的库月付/季付渲染不出,修=上线前对存量库跑一次 `scripts/update-product.ts` 改值并登记部署 runbook,顺手 tier 入库前 trim)/**#110 PASS-WITH-FIXES,两条必修**(🔴防回归闸失效:生产 Lua `minuteMax` 变异翻倍全仓 1243 绿——行为用例全打 JS 镜像、文本结构闸子串匹配被穿透;修=行为用例改打真 Redis,CI 已有 redis:7 service,约一个文件;⚠️当前已合入的 Lua 本身审查员真 Redis 亲验正确,红的是防回归网。🟡 embed/ASR 完全在限流圈外且顺序最贵:被 429 的用户每次提交语音仍先烧真付费转写、每次动作先烧 embedding,`interview-run.service.ts:711` 还有直连 AsrProvider 不经 gateway——**破坏 zzh 拍 100/300 时「最坏个位数元」的成本前提**,主R 裁:transcribe/embed 一并纳入 `assertCanCall` **同一计数器**(不另立档位,100/300 不动;代价=真实用户一场模面从≈10 计数变≈30、一轮简历优化≈12-15,距档位仍很远,重算已呈 zzh),发布前必修,owner=后端组、模面组配合把 :711 直连 AsrProvider 改走 gateway。⚠️ 溯源:spec M4-N3「run() 是全部 AI 消费路径的唯一出口」承重论断与代码事实不符(E1 家族),随回填在 spec 记勘误)/**#107 PASS**(极性/时序/键设计正确;休眠层零调用点与自陈一致)。同病根登记:sms/resume-agent 两个既有 rate-limit 也是 JS 镜像形态(非本批新增,列改进候补)。报告全文见审查员回件(变异 11 处、真 Redis/真起/三态/商品端到端亲验,主树零改动)。五件状态:**#108/#109/#107 终审闭合;#106/#110 待回填,回填 PR 主R 复验即闭**。
  **【续:并发修复撞车+二次修复】**#115(主R 22:42 合入)与后端侧 **PR #116**(原地改名 `20260902130000`,zzh 22:44 合入)**并发修同一撞号**,git 自动合并后同一迁移 main 上双份并存(diff 全等)——新起库 migrate deploy 必炸(CREATE TYPE 重复),且现有卫生闸只查时间戳不查内容重复,拦不住。主R 二次修复=**PR #117**:删 #115 的 `20260907150000`、保留 #116 的 `20260902130000`(迁移 owner=后端组+最后合入版本)。教训:**热修 main 前先 `gh pr list` 查同题 PR 在途**;卫生闸候补加「同内容/同名后缀迁移目录检测」。
- **#99(迁移批 1a+存量回填)主R 五关(2026-09-07 深夜,应数据组请审)**:**PASS-WITH-FIXES,唯一 must-fix=两迁移目录时间戳重编**。内容全过:①SQL 逐行审(1a 纯加可空列;回填三条 UPDATE 带 `IS NULL` 守卫,映射与 shared `DIRECTION_TO_CATEGORY` 逐值核对一致)②主R 真库重放(fresh PG 全量 deploy 成功;构造 4 行存量:回填 1/1/1、已有值 t4=tech_dev 不覆盖、重放 0/0/0 幂等、NULL 清零)③索引守护 12/83 全在+legacy-values PASS ④合并树 1243 绿/build 绿 ⑤真起 dist health ok。**must-fix 依据(主R 亲测实锤)**:分支视野停在 20260830000000,两目录戳(0830120000/0902090000)< main 既有最大戳 20260902130000,把 merge 真提交后跑卫生闸=**③B 双拦红**(「新增迁移未大于既有最新戳」)——分支 CI 绿是基线旧造成的假绿,原样合入=今晚第三次 main 红。修=`git mv` 重编为当前真实时间戳(保持 1a 在前、回填在后),SQL 内容零改动,文档零引用旧戳(主R 已 grep)。**两处请裁的判断均认可不改**:迁移内抄映射值=冻结历史快照,不违 0007(0007 禁的是活代码第二份 SoT);`WHERE category IS NULL` 一条件兼「不覆盖」与「幂等」不必拆。**数据组「不必等 codex 可独立先审」的理解正确**:#99 是迁移代码,终审权在主R,与实现批的 codex 链并行。重命名推上来 CI 绿即合,主修不必重审。
- **深夜合并批(主R,2026-09-07/08 交界;#99+#118+#119+FE R8,均五关)**:
  **【#99 已合(42e4ff3)】**数据组必修回填复核:纯 rename(diff 与主R 前审版全等)、新戳 232442/232444 真实时间取号、主R 自跑 committed-merge 卫生闸绿。**r2 连带风险处置裁 A(数据组自提方案)**:不动在 codex 三轮复审中的 r2(锚点 8e4e0c7 不作废),#99 合后数据组在 r2 merge main 并手删旧戳双目录+复跑卫生闸;⚠️ 双保险已核:即便漏删,r2 开 PR 时旧戳目录相对 base 是「新增目录」且 < 现最大戳,③B 必拦红,不会静默上 main。
  **【#118 原型污染修复已合(c008df2),发布前硬项销账】**主R 五关:黑名单三段任意位置拒+rawText 根段保留+三个消费者(corrections/patch/P3)全部 Object.hasOwn 化;主R 独立探针(5 攻击路径全拒、原型完好、正例通行)+变异亲测(撤黑名单闸 3 红含 service 调用点)+1252 绿+build+真起 health ok。白名单缓做(等 §1.4.16,避免与 §1.4.2/4 撞车)**接受**;组长认账态度与「两次经过没看见」的自省记好账(引入仍是 #45 的账)。
  **【#119 批3回填已合(2ecc85d)】**F1 模板注释同口径/F2 千分位折叠(两侧同折、锚定防「1,2345→12345」、诚实声明「折叠在别处更严」并拒改 anti-fabrication 侧=界外,判断正确)/F3 层③口径订正+真实形状用例/三顺手项全落;1251 绿+主R 变异亲测(source 侧撤折叠→F2 回归用例红);**越界自查裁定:不算越界**——安慰剂用例改造(中间产物入参+源码断言双保险)正中「永远不会红的测试」病灶,鼓励此类同域顺手修,前提=像这次一样显式声明请裁。**§1.4.19 A′(否掉 OC 放宽 contacts[*])主R 追认**:自由数组放宽会让 "200万"←"1200万" 过闸,正是边界保护要拦的。合并树 80 条 extraction 系测试主R 实跑绿(#118/#119 同文件相邻区自动合并复核)。
  **【NUL 字节独立处置(裁)】**anti-fabrication.ts:401 裸 0x00 使 `file` 判 data、裸 grep 静默 0 命中(主R 亲验 exit=1 vs `grep -a` 3 命中)——一切基于 grep 的审查(含漂移闸)对反编造引擎本体失明。**修法=字面 NUL 换 `\\x00` 转义(运行时字符串全等,行为零变化),简历组单独微 PR**,验收=裸 `grep -c containsNumericToken` 回 3 + 测试仍绿;组长「判成 glob 写法问题」的误判自报记好账。
  **【FE R8 已合(PR #121→d3e13fd,CI 绿后主R 合入)】**前端四条件主R 代执行逐条过:merge main 零冲突(dac35c0)/仓库级 web 138+api 1260 绿/docker build 绿/1.B.2 未闭合已记合并说明;纯 web+docs 零共享缝(主R grep 核),FE 直管批次+codex 窄复核 PASS(70402e3),主R 只做机械四关。
- **`@nestjs/schedule` 归属裁决(主R,2026-09-08;zzh 指定主R 裁,法务组转呈,模面组三条自解路径已排除)**:
  **【裁:基建归后端组,执行者归模面组,两段分头走】**
  ①**后端组**(全局基建 custodian——app.module/common 皆其辖,AlertingModule 同型先例刚走完):独立小基建 PR(不并入回填 PR-甲/乙,基建与修复不混),内容=pin 版本装 `@nestjs/schedule` + `ScheduleModule.forRoot()` **唯一**注册于 app.module + 把自己 8/26 提的三条约定入码留痕(**cron 表达式禁入 env**——类装饰阶段求值配错即崩 bootstrap 的 M1 血泪;开关用独立布尔 env;多任务错峰,jobs 挂各自 owner 模块)+ 真起验证。预估 ≤0.5 人日,**优先级=高(双组阻塞件)**,与回填 PR 先后自排但本周内。
  ②**模面组**:90 天删除执行者(Interview.transcript/audioRefs 留存清理 job)以 `@Cron` 挂本组模块,**随存原话批同 PR 合入**(合入门「留存/删除与实现同批落地」不变),调度注册断言(boot 后断言 job 已注册)同批;原报 +0.25 人日中「装依赖+forRoot」部分转后端,余下 job 本体归模面。
  ③**法务组**:L-2「90 天后删除」条款成文的前置=**模面执行者合入**(非基建合入——基建只解锁链条),成文时点盯模面批即可;X-1/X-10/X-12「承诺无人执行的删除」形态自此有闭合路径。
  依据:8/26「谁先合谁引入」约定在「模面组拒抢装(越界+既成事实)」后已名存实亡,真空必须裁掉;全局单例模块(forRoot 唯一)天然归 custodian,谁需要谁装会让约定漂移(第二个组再需要时无人记得只注册一次)。
- **#120(模面 T-13 错误码结构化)主R 五关合入(2026-09-08,f88976f)**:四处用户输入校验裸 Error→BadRequestException({code}),code 字面量逐字保留(前端映射已依赖);拆分干净(7 文件零 T-6/PR-D 内容,照外部终审「干净拆出」指令);**老轨行为变更如实申报采信**——resolveDirection/normalizeDurationTier 新老轨共享,老轨 POST /interviews 非法 direction 由裸 500→结构化 400,方向为改进、记录在案,「凡改新老轨共享函数一律按会改老轨线上行为对待须单独声明」的口径主R 追认为**全组通用**;12 处同域刻意不改的实证理由(BadRequestException.message="Bad Request Exception"⇒AITask.error 观测退化+isRetryableAiOutputError 失配)成立,接受。主R 验证:分支 1243 绿+**合并树 1260 绿**(其自述「最新 main」实为 #118/#119 前的旧尖,主R 合并树复测补上)+变异亲测(改回裸 Error→单测+老轨用例双红,共享覆盖属实)+build+真起 health ok。严格断言(类型+code 双查、自揭 hasCode 弱点)形态好。走了完整 分支→PR→主R 流程,与 #111 对照记正账 | ③回执待发 |
- **「content 空+finish=length(预算不足)」重试口径裁决(主R,2026-09-08;后端组请裁,简历组持不重试并呈实码)**:
  **【裁:不重试。主R 撤回自己的「(可重试)」表述,错在主R】**同日两条派工口径互相打架(给后端「可重试」/给简历「别当解析失败重试」),后端组没有各信各的,而是拿实码事实上呈请裁——这是对的流程。裁定依据:①预算不足是**确定性错误**——同输入同预算重试必然同败,且每次白烧计费 reasoning token,与网络抖动/5xx/超时的瞬态性质根本不同;②现行 isRetryableError 默认即不重试(新串落末尾 return false),EMPTY_OUTPUT 亦已不重试,注释与实现一致——**改动成本不对称且默认恰是正确侧**;③预算不足的正确出口是**响应侧**:独立错误串 LLM_PROVIDER_BUDGET_EXHAUSTED + 告警日志(它=配置 bug,要人来改 env,不是等它自愈),用户文案不得写「请稍后重试」(重试永不成功,简历组 :148-152 修复正是配这件)。
  **【执行】**后端组:TODO(裁决中)收为不重试、新串显式加入注释枚举、钉行为的测试转正式契约。简历组:§7 阶段零的 finish_reason 透出照原计划,文案分「预算不足(等修复)」与「真空输出」两款。
  **【记正账】**后端组「能分辨的先做、怎么处置留空待裁」的不选边手法;简历组发现撞车主动整条撤销+自记流程失误;后端组主动知会对方 real-redis 手法已有免重复劳动——三件都是公约要的形态。**【记主R 账】**同一裁决对两组表述不一致,病根=回执各写各的没有互查;此后凡同一口径进多份回执,发前 grep 台账当日行互核措辞。
- **#130(后端事后审 PR-乙)主R 五关合入(2026-09-08,14e2dd6)**:(c) 公网 /health 只回三态不回 detail(缺失 env 变量名数组=侦察面),detail 走 onModuleInit 启动日志(config_error=WARN);**对抗审第一轮 REJECT 驳倒实现方「payment.service:63 属界外」**——phone/email login 无 Guard 走 upsert,「已登录」门槛≈公网,且 503 按 provider 可逐渠道探测,比 /health 泄得更精;已收口 503 只抛 {code}+logger.warn,port 注释与代码矛盾(#89 同型)一并修。(d) tier/badge 入库 trim(" member " 精确匹配失配→savingPercent 静默 null,端到端双用例)。ci.yml 回归闸匹配带引号 "detail" 键名非裸子串(fail-closed 方向)。spec §5.6 彩排步骤勘误随件(响应体 detail 已删,操作指引同步)。主R 验证:全量 1855 绿+build+真起(响应体 grep -c detail =0、启动日志 2 条渠道配置)+变异亲测(wechat 恢复透传→4 红)。登记未修四条列候补。
- **简历组夜批三 PR 合入批(主R,2026-09-08;#126+#127+#128,均五关;#129 关闭)**:
  **【#126 限流键改 resumeGroupId 已合(1b36840)】**契约 §3.D.1 对齐:per-resume Redis 键由原文 sha 改 groupId 派生,调用点零改。核过:派生在 try 之外(密钥缺失不被 503 壳吞——「一个错误码只准表达一件事」)、production-env 断言主线已有(本 PR 只改后果文案,**不引入新部署耦合**)、行为变更(存量计数清零≤24h 自然过期、被日 10 封顶)评估成立不做双读、⑤调用点断言钉在 resolveResumeGroupId(FE 下发同源入口)而非「我调我自己」。回填①(DOC_CLASSIFY 开关入 env 模板)随件落。
  **【#127 phone 闸门闭环已合(532f468)】**codex REJECT 后第二版:value 整体白名单(堵「合法号码+伪造文本」夹带)+ 候选段字符集去 \n **与 \t**。**主R 对「比主R 建议更严」的偏离亲测复核**:字符集退回含 \t → 53 中 2 红(制表符表格列伪拼接实锤,[ \t] 确实不够);去掉 value 白名单 → 2 红。偏离照收。PHONE_VALUE_RE 只约束字符集不约束结构=刻意,主R 裁维持((010) 1234-5678 以 ( 开头,结构规则误伤面大;脏格式方向由确认屏兜)。
  **【#128 假绿普查已合(aba4be6)】**纯测试/注释/文档(两处「实现文件」核实为 TODO 注释)。95 次主变异 0 实现缺陷、17 假绿补 15,组长三条头条独立抽验含 C7 完整假绿证明(测试退回 main 版全绿)——方法论对头。🔴 登记:RUN_RATE_LIMIT_LUA 全 stub(挖空/>=改>/先增后查 三致命变异全绿),与后端 PR-甲同病,需 REDIS_URL 门控行为测试,列简历组候补另案。三条等价变异(死代码候选)只报不改,对。
  **【#129 关闭(重复件)】**主R 独立复核不转述:f881c33 对 main diff 仅同 2 文件、extraction.ts 剥注释与 #127 逐字同、#127 测试严格超集、其宣称独有两项 613a79d 已在 main。OC 自行复核后已自关。修法方向与 codex 驳回点一致,工作记正账;病根=开工前没查同题在途 PR(⑤ 撞车批教训重演,第二例)。
  **【验证】**逐级合并树:126 后限流 15 绿→127+128 后 extraction 53 绿→全量 **1846 绿(本地含真库)**+tsc 0+build+真起 200。docs/INDEX.md 三方同位行冲突主R 在 #126/#128 分支各代解一次(纯索引,行并存)。🔴 **仍悬(zzh 本人动作)**:RESUME_GROUP_ID_SECRET 生产落位——#126 后失败面已扩到 paste/upload 起 run 第一步,好在 production-env 启动清单拦死(漏配=拒绝启动而非静默 500),部署彩排前必须落。
- **凌晨三合入批(主R,2026-09-08;#123+#124+#125,均五关;主R 主动清 PR 队列,zzh 问「还有没依赖的活吗」触发)**:
  **【#123 NUL 微 PR 已合(b2b6a3d)】**diff 恰一行:裸 0x00 → `\x00`(cat -v 核对字节级)。验收逐字过:裸 `grep -c containsNumericToken` 回 3/exit 0、`file` 由 data 变 UTF-8 text;主R 本地全量 **1772 绿(含真库 suite,宽于其自报的 mock 层 1260)**+build+真起 health 200。⑤假绿㈠销账:grep 系审查对反编造引擎恢复可见。插曲:本地首跑红(`Unknown argument diagnoseLeaseId`)= **本机 Prisma client 陈旧**,`prisma generate` 后绿——与 PR 无关,记一笔「合并树测试前先 generate」。
  **【#124 schedule 基建件已合(9404f1d)】**四件全核:pin `4.1.2` 无 `^`/forRoot 唯一在 app.module/三条约定入码并配 `scheduling.conventions.test.ts` 机械守护(约定写进代码常量+守卫比对,不是注释;词法剥离状态机防守卫自扫文档;>50 文件自检防空集合恒绿;错峰表空+第一个加 @Cron 者必红的范围锚)/lockfile 增量纯 schedule 依赖链。主R 独立变异:注入第二处 forRoot→守卫 1 红,还原绿。真起日志确认 `ScheduleModule dependencies initialized`+health 200。**@nestjs/schedule 裁决的基建段当日闭环**,模面组执行者段+法务 L-2 成文解锁。
  **【#125 批4 已合(6544977)】**五工单+A′ 落地全核:①phone 策略三层防误放行(候选段切分堵伪拼接——codex 复现过的「编号138，入职0013年，预算8000元」拼 13800138000 已堵、全命中区间搜索、run 边界钉)与裁决 A′ 逐条吻合,变异亲测(去 run 边界钉→截断用例红);②**taskFn 边界主R 专项核**(advisor 点名):NOT_A_RESUME 的 BadRequestException 在 `aiGateway.run()` **返回之后**的外层方法体抛,taskFn 内保持裸 Error,合 #120 全组口径;F7 先提交 Blocked 事务再抛,顺序对;③「校验对象不是写入对象」同族排查:`getRun` include canonical,`run.canonical?.resumeJson` 是真数据,非 badcase;④spec 改动裁**勘误类不触发 codex**:A′ 裁决 9/7 已随 #118/#119 报备主R,且 codex 已在 phone 策略上跑过一轮(伪拼接复现即其产物),:583 改动=对齐已裁口径零新增裁量;⑤全量 1815 绿+tsc 0+build+真起 200。两件小回填与一候补见③回执行。
- **codex 终审边界细则(主R 裁,2026-08-26,补公约 v1.2;本条目头曾**三**次被合并/编辑挤掉,勿再粘连)**:对生效口径/spec 的**勘误类改动**(对既有 zzh 裁决的机械对齐、笔误订正、行号刷新,**零新增裁量**)不触发 codex 终审,主R 直改留痕即可(先例:v2.0 doc:268 飞书→腾讯文档、闸①「法务放行→zzh 签字放行」);凡**实质变更**(新增条款/改判/语义收窄放宽)= spec 级,走 codex。法务组建议书/文书类产物不属 spec 级(不解禁 G4/不冻契约/不落口径),审查链维持 组内自审+Opus 对抗审,最终签字=zzh。
- 督察组:游离于审查链外,zzh 独立调度;各组(含主R)不与督察直接往来。
- **前端组批次开发直管安排(zzh 2026-08-27)**:批1 起 FE 开发由 zzh 直接对接推进,**不经主R、汇报后置**;纯前端自审自合照 CONTRIBUTING §0.5 + 公约 A5(命中 A1 仍须对抗审后自合)。**例外自动生效**:碰共享缝(CI/packages/shared/契约/后端接口)时仍走 PR 过主R——此为流程自带,非 FE 需特意绕。
- **设计稿(`docs/design/mockups/`)改动归属(zzh 2026-08-16 常设授权)**:既有页面的缺陷修复/状态补全/文案口径对齐/交互补线 = **前端组自己闭环**(直接改并入库,组长自验自担);**新页面 / 新流程或信息架构 / 新视觉语言** = 启动 claude design(经 zzh)。判据:用户需「重新学一遍怎么用」→ 找 design;只是把本该有的补齐 → 自己做。拿不准先问 zzh。口径全文见前端组 brief §2.0。

## ⑤ badcase(踩的坑 + 补的闸;每条标来源)

- 2026-09-08 | 🔴 **第三次「撞车面查窄」,这次的错法是「按 PR 的标题/主旨推断它碰了哪些文件」**:数据组把 #136 记成「只碰 `production-env.ts`/`env-or.ts`,只撞 B1」——因为它标题就叫「空串/零宽 env 打穿默认值」。简历Agent组来件提示后拉文件表复核:**#136 实际触碰 16 个文件**,含 `resume-agent.service.ts`、`match-report.service.ts`、`resume-agent-p2.service.test.ts`、`resume-agent-diagnose-lease.ts`。后果=**B4 的前置整个漏掉 #136**,若照原表开工,B4 会与在途 PR 在 `match-report.service.ts` 上正面撞。前两次同形态:#142(实际面比记的大一倍)、后端组指出「我列 7 文件、对方碰 6 个」。**根因不是漏查,是查的对象错了** —— 我读的是 PR 的**意图**,冲突发生在 PR 的**文件**上。| 简历Agent组来件 + `gh pr view 136 --json files` 亲验 | **闸**:①凡进撞车表的在途 PR,一律 `gh pr view <n> --json files` 拉全表,**禁止从标题/描述推断触碰面**;②撞车表每行必须能指回一次实查命令,推断出来的不许进表;③每次有 PR 合入或新开时,**整张撞车表重拉一遍**,不只更新变动那行

- 2026-09-08 | ⚠️ **险些第二次树替换:`git checkout origin/main -- .` 会把整棵树刷进索引**:为读 `origin/main` 上的代码事实,顺手敲了 `git checkout --quiet origin/main -- . `,**当场把两个他组文件(含 `docs/交付台账.md`)以 origin/main 版本 staged 到本分支**——而本分支落后 main 若干提交,提交下去就是又一次 `c86ca0e` 形态的回退。`git status --short` 一眼看到、`git reset --hard HEAD` 撤回,**未提交、未推送**。**根因**:想读某 ref 上的文件时用了「把 ref 检出到工作区」而不是「直接读 ref」。| 组长自查(提交前 `git status` 核对文件数那道闸接住的) | **闸**:①读他 ref 的内容一律用 `git show <ref>:<path>` / `git grep <ref> -- <path>`,**`git checkout <ref> -- <path>` 属写操作,不得用于「读」**;②要基于最新 main 干活就新开分支 `git checkout -b <new> origin/main`,不在旧分支上刷内容

- 2026-09-08 | 🔴 **别人报过来的「这件卡在某某」,技术事实我核了,流程事实我没核 —— 而错的恰恰是流程那半**:模面组回话里带了一条「`Interview.retainUntil` 加列请求转 zzh/主R 已三轮未回」,数据组把它原样写进派工单并上报 zzh,还附了「正好同表可省一次迁移」的建议。zzh 一句「这不是让模面组自己加上这个字段吗」把它戳破。回码:**`aef71ed` 就是模面组自己给 `Interview` 表加了 `lastActivityAt`/`redeemedAt` 两列并自带迁移**,`2a5d801` 又加过 `UsageLedger.idempotencyKey`;后端组 `793d584`、简历Agent组 `e8cb449` 同形态。**各组给自己的表加列是既定做法。**根因还有一层:数据组把公约 **A1②「涉 DB 迁移」理解成了审批门,实际是审级门** —— 它要求的是多一道独立对抗审 + 主R 五关,**不是要 zzh 拍板放行**。把「要多一道审」读成「要等人批」,就会凭空造出一个不存在的阻塞,并且把它当成事实转述给决策者。⚠️ 同轮对照:模面组给的**技术**事实(老轨不传 `interviewId`、生产 100% 走老轨、`bindJd` 是活的)本组**条条回码亲验**且全属实;唯独这条**流程**断言没验 —— 而它恰恰是唯一错的。 | 来源:zzh 当场质疑 + 数据组回码复核 | **闸**:(a) 🔴 **转述他人的阻塞断言前,先找一次反例先例**:「X 需要谁批」这类断言,查「历史上有没有人没批就做了 X」比读规则更快也更硬;查不到反例再转述。(b) 🔴 **区分「审级门」与「审批门」**:公约里「触发对抗审 / 触发五关」是**审级**要求(多几道审,执行者照做),「需 zzh 书面放行 / 需主R 裁归属」才是**审批**要求(要等人回)。把前者当后者,会凭空制造阻塞并浪费决策者的注意力。(c) **核别人报来的事实要分两类各核一遍**:技术事实(回码可验)与流程事实(查先例/查公约原文可验)。本轮只核了前者,漏的就是后者。

- 2026-09-08 | 🔴 **「查了引用、零命中,就断定它是死码」——否定性断言的第四种形态,而且我把「谁调用它」查窄了**:数据组在 M4 §4.7bis 变更件里断定 `resume-agent.service.ts` 的 `bindJd`「当前未挂路由、仅被单测覆盖」,据此把它列为「留着不改就是后门」。简历Agent组回码指出并经本组亲验:**它是活的** —— `bindJdAndAdvance` 的 `CanonicalConfirmed` 分支直接 `await this.bindJd(...)`。**错因是检索面选窄了**:只查了「控制器有没有引用它」,没查「service 内部有没有调用它」。后果不是漏做而是**做错**:按原案在 `bindJdAndAdvance` 入口与 `bindJd` 各判一次,`CanonicalConfirmed` 那条路会**重复判源**。同一轮还暴露第二个毛病——**按方法名数写入点**,于是漏掉了 `createRun` 的 `input.jdText` 分支(调 `analyzePrivate` 现造 JDAnalysis),而那正是「用户自贴」的入口,漏了它自贴来源根本不会被标记。⚠️ 与法务组 L-5 那条同族但方向相反:法务组是「grep 名字零命中 ⇒ 断定行为不存在」,本条是「grep 引用零命中 ⇒ 断定代码是死的」。**共同形状:拿一次检索的零命中当存在性结论,而检索面是自己选的。** | 来源:简历Agent组 2026-09-08 回码回执 + 数据组亲验(变更件已合 `04b50d0`,codex 六轮均未发现)| **闸**:(a) 🔴 **判定「某符号是否还活着」,必须查全三类引用:控制器/路由、同文件内部调用、跨文件 import**;缺任一类的结论不成立,且**必须附正控**(同一手法能查出一个已知活着的符号,证明检索有效——简历Agent组这次就是这么给的证据)。(b) 🔴 **枚举「写入点」按「谁真的写这个字段」数,不按方法名数** —— 方法名是人给的,写入语句是机器执行的;凡有「多条分支汇合到一处写入」或「一个方法调另一个方法」的形状,按名字数必错。(c) 更稳的替代:把规则收敛成**数据依赖**而非路径枚举(本例:「凡写 `jdAnalysisId` 处,同时继承该 `JDAnalysis` 的 `contentSource`」),将来新增写入点不会因「没数到」而漏。(d) **跨文件引用一律「文件名 + 符号名」定位,不写行号**——在途 PR 会让行号漂(本例 #132 对该文件 +159/−17)。

- 2026-09-08 | 🔴 **提交前只看 `git status` 拦不住「树替换型」污染——文件数对了,树是错的**:数据组 spec 分支的 `c86ca0e` 提交信息写的是「附录 B 判据订正」,实际改了 **215 个文件**(+4265/−5530),把 `apps/web`、`apps/api` 等其他组已在 main 上的代码整片回退(`login-dialog.tsx` 486→84 行)。**成因**:工作树当时处在实现批分支派生的旧状态,提交时把整棵树的差异一并带上;既有的「禁 `git add -A`/显式点名文件/提交前 `git status` 核对文件数」三条闸**全部通过了**——`git status` 只显示待提交项,而这次待提交项本身就是错的树。**危害形态最险的一点是它长得像正常 spec 提交**:分支名、提交信息、送审锚点都指向文档,只有真去看 `git show --stat` 才看得见 215 这个数。开 PR 前自查发现,未进 main。 | 来源:数据组组长自查(开 PR 前对 `origin/main...HEAD` 做改动面核对)| **闸**:(a) 🔴 **提交后加一道后置核对**:`git show --stat HEAD | tail -1`,文件数与本次意图不符即当场停手——`git status` 是**提交前**看待提交项,看不出「树本身选错了」,两者不能互相替代;(b) 🔴 **开 PR 前必做改动面核对**:`git diff --stat origin/main...HEAD | tail -1` + `git diff --name-only origin/main...HEAD | grep -v <本组轨道前缀>`,**出现本组轨道以外的文件即视为污染**,不看行数只看路径;(c) 已污染的分支**封存不改写**(送审锚点还挂在上面),另起干净分支从 `origin/main` 重导出目标文件,并用 `git diff <锚点> HEAD -- <文件>` 验空证明内容一致;(d) **同期同工作树的其他分支要一并核**——本次连带核了 `feat/jobdata-impl-01-r2`,确认干净。
- 2026-09-08 | ⚠️ **推理型 LLM 的 max_tokens 会被「思考」吃掉,正文可能为空但 HTTP 200**:DeepSeek v4-flash 冒烟,max_tokens=20 时 reasoning_content 占满预算 → content=""、无任何报错;放宽后正常。**危害形态**:调用层若只读 message.content,拿到空串会被当「模型输出空」进解析失败/重试链,而真因是预算配小——排障方向完全带偏 | 主R 真 key 冒烟(B1 落地时) | **闸**:①各调用点 maxTokens 按「推理余量+正文」配,不照非推理模型的旧值抄;②「content 空 + finish_reason=length」单独识别为预算不足(告警+改配置,**不重试**——④2026-09-08 裁决,本条原写「可重试可告警」系主R 表述错误已订正),不与「模型真输出空」混判;③换模型/换档必做一次真 key 冒烟,不信 200 只信 content 逐字;④🔴 同族第二雷:请求超时也按非推理模型定的(LLM_REQUEST_TIMEOUT_MS 默认 30s,v4-flash 真实抽取实测 33-35s)——**换推理模型要同时复核 max_tokens 与 timeout 两个预算**,只改一个照样全灭

- 2026-09-08 | ⚠️ **幂等短路吃掉恢复语义:「重试=重发同一接口」的恢复设计,被同一接口的幂等优化恰好绕过**:简历 Agent 恢复设计=「LLM 段失败后重新 POST 同一暂停点接口即从断点续推」(advanceRun 注释明文承诺);但 bind 入口的幂等分支(`run.jdAnalysisId===jdAnalysisId` → 直接返回视图)在 advanceRun **之前**短路——绑 JD 触发的诊断失败后,用户/前端重发 bind 得到的是幂等返回而非续推,恢复承诺对该入口不成立;叠加 run 视图对可恢复态只给 `pendingAction:NONE`(前端读作「计算中」永续轮询),恢复入口实际不可发现。**两个各自正确的局部决定(幂等防重入 / 失败停合法态等重试)在同一入口叠加成用户出不去的坑** | 来源:前端组真 key E2E 实测(重发三次诊断尝试数 1→1)+ 主R 复码确认(2026-09-08) | **闸**:(a) 凡「重发同一接口=恢复」的设计,**幂等/防重路径必须显式验证不吞恢复动作**——幂等的应当是副作用(绑定/扣次),不是推进;(b) 可恢复态在对外视图里必须可区分于「正在计算」,否则恢复入口等于不存在;(c) 恢复语义的测试要从「失败后的下一步是用户做什么」写起,而不是只测幂等分支返回正确视图
- 2026-09-08 | 🔴 **校验对象不是写入对象,于是校验等于没做;而且「把非法形态锁成绿灯」四连,上一轮刚立的闸因措辞太窄没拦住第四次**:数据组实现批被 codex 连打三轮 REJECT,轮2 抓「两个字段各自合法≠这一对合法」,轮3 抓的是同一件事更深一层——组长按轮2 的意见把配对校验加进了 `buildImportPlan`,判的是**解析阶段**的 `(data.category, data.direction)`;但真正写进库的是**查库重组后**的 `(keptCategory, data.direction)`。三个各自看都有道理的决定叠起来就成了洞:①legacy 文件缺 category ⇒ `data.category=null` ⇒ 组长写的 `if (data.category !== null)` 把校验整个跳过;②`keptCategory` 从库里保留旧值(「只进不退」护栏);③`direction` 被文件裸覆盖。实测复现:库内 `{operations, OPERATION}` + legacy 文件给 `PRODUCT` 且无 category ⇒ **写库 `{operations, PRODUCT}`**,非法组合入库。**根本教训:校验对象必须就是写入对象;校验中间值而写最终值,校验是空的。** 另一条同样值得记:**「把非法形态锁成绿灯」这是第四次**(assertCategory 空值→null、buildImportPlan 缺 category→null、B1b 锁 `tech_dev+PRODUCT`、本轮「文件缺 category→保留库内值」用例取 `sampleCategoryKey(3)` 实为 `tech_dev` 配默认 `PRODUCT` 本身即非法组合却只断言「category 被保留」)。**组长上一轮刚立的闸是「任何断言 accepted / 0 error 的正例必须引出使其合法的 spec 条目」,本轮没拦住——因为该用例断言的不是 accepted,是「保留」,闸的措辞只覆盖了"接受"这一种正向断言形态。** | 来源:codex 终审第二/三轮(锚点 `7280c8d` / `8e4e0c7`)+ 组长本地复现与自陈(返工 `7e3159b`)| **闸**:(a) 🔴 **凡对「将要写入的值」做校验,校验点必须取在最终写入值上,不能取在解析/中间阶段的同名变量上**——凡是存在「读库→与文件值合并→写回」这种重组的路径,重组之后必须再判一次;(b) 🔴 **闸的措辞按行为分类,不按断言动词分类**:改写为「任何**正向断言**(accepted / 0 error / 保留了 X / 写入了 Y)都必须同时断言『该操作之后的最终状态仍然合法』,并在注释里引出使其合法的 spec 条目」——只断言"某字段如预期变化"而不断言"变化后整体仍合法",正是这四次的共同形状;(c) **护栏类逻辑写在 `main()` 里等于零回归保护**(本机无 DB 就永远测不到),必须抽成导出纯函数——F5 的 `keptLastVerifiedAt`/`keptDeadline` 至今仍是这个状态,属既有欠账。

- 2026-09-02 | 🔴 **只论证了「改的风险」,没论证「不改的风险」,于是把一个必然发生的静默错配当成了稳妥保守**:数据组实现 M4 §4.5 时,spec 明确要求 update 分支同时补写 `category` 与 `direction`。组长裁定「本轮只补 category,direction 留批 1b」,理由写得很像那么回事——「direction 仍必填、行行有值,现在补它只会新增一条『重跑导入即覆盖既有 direction』的行为,而本组刚因 F5 静默覆盖出过事故」。**codex 终审推翻**:不补 direction 会写出 `category=operations + direction=PRODUCT` 这种两轴互相矛盾的行(既存行 direction=PRODUCT,文件给 `{operations, OPERATION}`,只改 category),**而且这个错配与批 1b 是否落地毫无关系,当下就会发生**,比组长担心的覆盖风险严重得多。**根因不是漏看了某个事实,是论证结构缺了一半**:组长把「改 direction 有什么风险」推演得很细(F5 覆盖、kept-value 语义、1b 时序),却**一次都没问「不改 direction 有什么风险」**——而 spec 要求补写这一条本身就是对抗审 🟡-7 的产物,spec 里写着理由,组长读到了、也照做了 category 那一半,唯独把 direction 那一半当成「可以延后的增量」而非「防错配的必需项」。同源第二例:同批把校验器的 direction 条件必填先落了地,而批 1b 未落时 DB 上 `direction` 仍 NOT NULL,造成「一行数据过得了质检闸、却在导入器 assertEnum 上炸掉」——**这正是 spec §4.5 明令要避免的形态,spec 原文就写着**,组长拆 1a/1b 时没回头核这条依赖。| 来源:codex 终审 🔴2(锚点 `61bec6ed`)+ 组长自陈(返工 `7280c8d`)| **闸**:(a) 🔴 **凡决定「spec 要求的某项本轮不做」,必须把「不做」当成一个改动来论证——写明它引入什么风险、谁来兜、什么时候补**,不能只论证「做了会有什么风险」就当作已经权衡过;两侧风险都要落纸,否则「保守」只是没推演完的别名。(b) **改动只更新多轴口径中的一轴时(category/direction、status/closeReason 这类成对治理的列),必查另一轴会不会因此与它自相矛盾** —— 单轴更新是错配的标准形状。(c) 🔴 **把一条完整迁移拆成两批时,必须回头核「依赖完整迁移的下游改动」有没有跟着拆**:本次把批 1 拆成 1a(加列)/1b(放宽 direction)后,依赖完整批 1 的 M3 §4.2 却整条落了地,校验器与导入器口径当场分裂;拆批是执行决策,但它会让 spec 里按「一条迁移」写的下游条目全部失去前提,**拆完要逐条重核下游,不能只核被拆的那件**。

- 2026-09-07 | 🔴 **`push HEAD:main` 推文书,把未终审的代码提交作为祖先一并带上 main——两批代码(约14个提交)零 PR 落库,事后自报才暴露**:简历组组长与 oc 共用同一 worktree 同一分支(`resume/workspace`),组长推台账/回执文书时用 `push HEAD:main`,分支上 oc 的代码提交是文书提交的祖先,git 原子地全带上去。批1(9/2)九个单亲提交、批3(9/7)五个代码提交如此进的 main;批3 还含共享缝(production-env.ts NP0 清单),绕开了主R 9/2 明裁的「随批3同 PR 走五关」。**危险在于全程无红**:测试绿(代码本身质量不差)、CI 绿、push 成功,唯一缺的是「终审」这道人环,而人环缺失不产生任何机器信号。与 8/26 共用 checkout 五起事故同根:**共享工作区让「推 A」隐式携带「B 的一切祖先」**。| 来源:简历组组长 9/7 自报(9/2 请示过一次,传话未达)| **闸**:①(主R 裁,全组通用)直推 main 前必跑 `git log origin/main..HEAD --oneline -- ':(exclude)docs' ':(exclude)*.md'`,非空即禁推改开 PR——文书直推特权只属纯 docs 增量;②oc 与组长不共用分支,组长批次收口到独立分支开 PR;③裁决类请示 48h 无回执主动催办,不默认默许也不默认禁止;④两批已启动独立事后审(与 #101 A1 同规格),回件前不得表述为「已过五关」
- 2026-09-02 | 🔴 **清单就在仓里、文件头把病例写明了,组长还是把「静默降级」当成了优雅解法**:简历组批 3 要新增生产密钥 `RESUME_GROUP_ID_SECRET`(`resumeGroupId` 的 HMAC 键)。组长给前端组的答复里写「密钥缺席时端点返回 `resumeGroupId: null`,既不启动失败、也不拿空密钥硬算假 id」,并自认为这是稳妥处理。**但 `apps/api/src/common/env/production-env.ts` 文件头的收录判据原文就是「漏配会导致静默错误行为(不是启动失败、也不是明显报错)」**,而「密钥没配 ⇒ 悄悄返回 null ⇒ per-resume 配额与按简历筛选静默失效、日志零痕迹」**与该文件头举的 `CORS_ORIGIN` 病例同形态** —— **组长写的正是那张清单存在的理由,却把它当成了解法**。经主R 裁定收进 `REQUIRED_IN_PRODUCTION`,口径改为 fail-closed。**主R 判语**:「错误形态和这张清单文件头写的病例一模一样,人却在清单存在的情况下又犯了一次,说明**判据要在写答复时查表,不能凭感觉**」。**补的闸**:(a) 🔴 **凡答复里出现「缺失时降级为 X」「容忍缺省」「优雅回退」类措辞,落笔前必须先查 `production-env.ts` 的收录判据与 badcase 表,并在答复中写明查了**——不是事后被指出才回来对;(b) 可空字段的**每一个 null 来源都要逐一列举并各自定性**,禁止让一个 `null` 同时表达「数据链断」和「配置漏了」两件事——本次正是把后者混进了前者(契约 §3.D.1 只定义了前者)。| 来源:主R 2026-09-02 裁决①点名 + 组长自陈(收回口径 `4b118a1`)

- 2026-09-02 | 🔴 **「契约已生效」被读成「已实现」,一屏前端被硬阻断两个月;两种形态同时存在**:①**文档态假绿**——简历组送审件写「§1.4.20 六项 ✅ 全部落地」,实为**spec 文本的 grep 计数**(`CanonicalDraft` 12 处 / `resumeGroupId=` 4 处…),而端点实现零行;该表述进了送审包与台账,**组长自己此后再读也当成已完成**,直到前端组报「确认屏接不通」才暴露。②**代码态假绿**——`apps/api/src/resume-agent/resume-agent-input-quality.ts` 三个判定纯函数(`classifyDocumentIsResume`/`detectPasteInputQuirks`/`collectSectionGaps`)**已写、有专属测试、全绿**,但 `apps/api/src` 内**生产调用点为零**(grep 只命中它自己的 test)⇒ **测试绿 = 纯函数对,不等于功能在生产存在**。**共同根因**:验收断言的对象是「文本/函数存在」,不是「用户路径可达」。**补的闸**:(a) 凡「契约/spec 已定稿」类表述,台账与送审件一律**显式并列写「实现:零行/部分/完成」**,不得只写落地;(b) 纯函数层交付**必须同时钉一条调用点断言**(测试或验收里断言它被生产路径调用),否则视为未交付;(c) 跨组「已生效」口径每次转达时附**代码侧亲验命令与命中数**。| 来源:前端组 2026-09-02 跨组对账反推 + 组长回码复核(答复件 `docs/dev/2026-09-02-简历agent组-答前端组-确认屏取数端点.md`)

- 2026-09-01 | 🔴 **目录级 `git add` 把含 PII 的杂文件带入仓库并推送 + 事后核实用错手法得出"从未入 git"的假结论,双错叠加**:①组长 v2 重制软著材料时图省事 `git add docs/部署/软著申报-2026-08`(目录级)——公约明令禁止的操作——把混在材料目录里的 `04-…/用户中心.pdf`(zzh 从官网打印的登记申请表存档,**含姓名+受理条码**)一并提交推送(`84abcde`);②次日 zzh 审阅点名该文件,组长核实"是否入过 git"时用 `git show --stat | grep 用户中心` 和 `git ls-files | grep`,**两条都零命中**,当场断言"从未入 git,虚惊"并写进申报文档推送——实际是 **git 对中文路径默认输出 `\347\224\250…` 八进制转义,拿 UTF-8 字符串 grep 必然落空**;直到 v3 提交时 status 冒出 `D` 行才暴露。**处置(2026-09-01 zzh 拍板「清」,已执行)**:`filter-branch` 重写 3 个提交(84abcde/c27cfd7/43464b7 → bc9c0b6/bc59045/e104db9),`--force-with-lease` 推送,本地备份 ref/reflog 已清、`git log --all` 检索该文件零命中、旧对象已不可解析。**残留风险已知情**:GitHub 服务端对旧提交哈希的直链缓存需等其后台 GC,如需立即彻底清除要走 GitHub Support 工单(当前威胁模型下未走)。**协作者同步**:凡在 84abcde 之后 pull 过的克隆,执行 `git pull --rebase` 即可对齐(重写前后内容一致,rebase 会自动跳过重复提交) | 部署安全组 软著材料 v2/v3;zzh 审阅点名该文件引出 | **闸**:①公约"禁目录级 add"零例外——**材料目录这种"看起来全是自己刚生成的文件"的场景恰恰是重灾区**(混入过打印存档,今后还可能混入截图/下载物);添加多文件用 `git add <dir>/*.pdf` 通配显式后缀,或 add 前 `git status <dir>` 逐个核对新增名单;②🔴 **凡对 git 输出 grep 中文路径,必须带 `-c core.quotepath=false`**,否则否定性结论(没提交过/没追踪过)一律无效——这是台账既有"否定性断言必须穷尽检索"闸的 git 特化版;③PII 文件的存放纪律前置:从浏览器存下的任何带个人信息文件,**落盘第一步就放 `~/Downloads`,永不进仓库工作区**,而不是先放材料目录再靠 add 时小心

- 2026-08-31 | 🟡 **空串 env 与「未设 env」行为不同,且会烧进生成的客户端**:本地 `.env` 里 `LLM_CHEAP_MODEL=""`(空串,不是未设)被读进生成的 LLM 客户端,导致一条与改动无关的 resume-agent 测试**误红**,一度被当成新引入的回归去查。根因 = 代码里判「是否配置」多用 `process.env.X !== undefined` 或 `?? 默认值` 形态,**空串既不是 undefined 也不触发 `??`**,于是「配了一个空模型名」被当成有效配置。| 模面组 T-1(PR-GUARD)交付期,oc 报告「其他透明」栏如实上报 | **闸**:①排查测试误红时,先看 `.env` 有没有 `X=` 形态的空串行,别直接怀疑代码;②今后写读 env 的判据一律用**空串也算未配置**的形态(`process.env.X || 默认`,或显式 `.trim()` 后判空),不要用 `!== undefined` / `??`;③本地 `.env` 卫生:不要留 `X=` 空行,要么给值要么整行删

- 2026-08-30 | **主R 违自家「存在性断言双向核」checklist,把已修的 #82 错判打回**:复验 must-fix「runbook 三重不匹配未动」时只查了 **PR 分支上的 spec 拷贝**(过时),没查 main——而修复(`a6ec986`)正是以 docs 直提方式早已落 main(该组勘误常走此路,有先例)。若非送审前 advisor 复核点破,错误打回单就发出去了 | 主R 批次复验 #82 | **闸**:判「某组没做 X」前,**分支与 main 两头都查**(尤其 docs/模板类修复——PR 不碰的文件在合并树里取 main 版本,分支拷贝旧≠没修);此为公约 checklist「存在性断言双向核」的既有要求,本条是它的第一个主R违例实录

- 2026-08-29 | 🔴 **浏览器账号身份在两次操作之间"动态漂移",比既有的静态误登更危险**:组长准备给帮投递主账号绑 MFA,第一次截图确认标签页是"nick2202357201"(帮投递),**紧接着仅一次 navigate 调用之后**,同一 tabId 再截图变成了"职计划互联网科技"——不是组长选错了标签页,而是**同一个 Chrome 扩展被另一个并发会话(大概率职计划那边的组长会话)同时操作**,账号在组长的两次动作之间被切走。全程只做了只读查看(设置页),未触发任何写操作,零后果。**这暴露既有闸门("写操作前核对账号指纹")的盲区**:该闸假设"确认一次、账号在整个操作期间保持不变",但共享 Chrome 扩展场景下,确认和执行之间可能被别的会话插入切换,**单次确认不再充分** | 部署安全组 M5 Z1(主账号 MFA)操作中自查发现,zzh 确认属实(另一会话正在用) | **闸(补强既有闸)**:①涉及账号级写操作(MFA/密码/权限策略/删除类)前,**确认账号指纹的截图必须与执行动作在同一个工具调用序列内紧邻完成**,中间不得插入等待/其它请求;②如判断本机同时有多个会话可能共用同一 Chrome 扩展,**账号级写操作一律改为请 zzh 亲自登录执行**,组长只做只读核对与操作指引,不代为点击;③长期建议:各组长会话尽量用独立浏览器 profile 或独立 Chrome 实例,不共享同一个扩展连接(呼应既有「共享 checkout 事故」条目提的 worktree 隔离思路,这里是浏览器层面的同款问题)
- 2026-08-31 | 🔴 **"正文改对了,忘了改 TLDR / 排期表 / 未核清单 / 花钱清单"——同一份稿子里连犯 6 处**:小程序方案按对抗审 REJECT 全数回填后送窄复核,判**仍 REJECT**。研究层已修好,**问题全在"决策面没跟上"**:①TLDR 与 §7.1 排期表仍把已被本文档自己推翻的「23–25 个工作日」当**官方数字**用;②三处仍写「主办者信息可复用 web 备案」,而正文 4 行前刚把该条款降级为"只适用 2023-09~2024-03 存量 APP 阶段";③§7.1「谁做」栏仍写"需授权书",而正文刚指出这不是原文、且漏了更重的"负责人须现场人脸核身";④§7.3/§10/文末总结仍按"安卓费率未核"的前提推理,而 U-1 已关闭;⑤§2.3 写了"§7.2/U-9 已更正"但那两处**实际没改**(指针落空);⑥拍板点 §9 W2 一字未动,没跟上"若判入拟人化互动则 W2-1 作废"这条新增的致命前提。**危害**:zzh 只读 TLDR 和排期表,**恰恰会拿到三条已被推翻的事实**;而外部终审第一眼看的也是这两处。**另新引入一处过冲**:回填时把"安卓 1% vs App Store 25% 是数量级差距"写进 TLDR——**这是类别错误**(App Store 在安卓上根本不存在,安卓侧真正的对照是各安卓市场分成,而那格自己标着未核),**外加"中国区安卓占多数"是无出处断言** | 部署安全组 小程序方案窄复核(Opus,同一审查员核自己提的修订点) | **闸(采纳窄复核建议,比组长自立的三条更直接命中病灶)**:①🔴 **凡推翻一条已核事实,必须全文 `grep` 该数字/该措辞,把命中处逐个处理完才算回填**——本轮 10 条必修里有 8 条,`grep "23–25"`、`grep "可复用"`、`grep "U-1"` 三条命令就能全找出来;②**"已更正 X 处"这类指针必须当场核**:写"§7.2/U-9 已更正"时那两处根本没动,**指针落空比不写更坏**(读者会跳过检查);③**推翻旧结论后新写的结论要防过冲**:本轮把"费率不是优势"翻成"混合费率显著更低",翻过头成了类别错误。**判据:反转一个结论时,新结论的承重前提至少和旧结论一样多地被检验**;④**决策界面(TLDR/排期表/花钱清单/拍板点)是回填的必查清单**,不是正文的附属——**老板和外部终审只读这几处**;⑤🔴 **回填/更正时新写的内容同样受承重实证约束,不能因为"这是在修错"就免检**——本轮两处最贵的错(类别错误"1% vs App Store 25%"、无出处前提"中国区安卓占多数")**都是在修 badcase 的那一版里新生的**;⑥🔴 **写「已更正 §X/§Y」前必须先去 §X/§Y 确认真改完**——本轮 §2.3 写了"已更正"而那两处根本没动,**指针落空比不写更糟**(读者会据此跳过检查);⑦**grep 时用「数字对」而非完整短语**——第二轮窄复核实抓:撤回"1% vs 25%"时组长 grep 该短语得零命中,却漏掉审查日志里的 `1% vs App Store 25%`,**推翻一条结论要 grep 它的所有措辞变体,最稳是 grep 承重数字本身**。**元教训**:本组已有的「按语义全文扫→一次性改→反扫残留」工作法(数据组 2026-08-27 立)**是对的,组长本轮没用**;该工作法应升格为全组通用,不限数据组。
- 2026-08-31 | 🔴🔴 **自审只查措辞不查结论,三处最贵的漏洞证据全在自己已下载的文件里**:小程序方案初稿,组长自审抓到三处并写进 §12.1(措辞"原生第一目标"不准/周期"当天"无出处/整节重写)——**全是形式层面**,抓出来能自证严谨。独立对抗审(Opus,不喂上下文,实核 22 个来源)判 **REJECT**,理由不是未核项多,而是三处**"核过的材料里就有、却没看见"**:①判定"官方没有安卓虚拟支付费率表"⇒ **实际有**,《技术服务费》页列得清清楚楚(安卓主动支付标准 10%/当前 **1%**),**该页链接就印在初稿已下载的 `ios.html` 的 HTML 里**;②自审末尾自承"未核实服务次数是否落入虚拟支付定义"⇒ **答案就在初稿声称已逐字抓取那页的第一句**,而且同一份运营规范 14.6 还明文禁止"引导至网站完成支付"(=「小程序做体验、导回官网付费」这条退路根本不存在,初稿完全没写);③"约 23–25 个工作日"标成"官方文档估算"⇒ **所引 URL 原文里根本没有这个数**,疑为自行推算 2+1+20 —— **而 §12.1 自审声称抓到的第 2 条恰恰就是"无出处的周期断言",同一节里同类错误抓了一个漏了这一个**。**后果**:①的错误结论已当面口头汇报给 zzh(说"定价和单位经济现在算不了"),**实际算得了,且结论相反**——安卓 1% vs App Store 25% 是数量级差距,初稿却写"小程序真正优势不是费率" | 部署安全组 小程序方案对抗审(Opus 独立镜头) | **闸(三条,均针对"自审失效"而非"调研不足")**:①🔴 **否定性断言必须穷尽检索再下**——凡写「官方没写 X / 不存在 Y / 查不到 Z」,必须先在**已下载的材料里全文检索**,并**沿站内侧边栏/导航走一遍相邻页**;本次三条漏洞有两条的证据就在已下载文件内,**证明"没有"比证明"有"难得多,不能只翻两页就下结论**;②🔴 **自审必须含「承重结论反向验证」环节**:挑全文 3–5 条最承重的结论,逐条问「它若是错的,证据会长什么样」,再去找那个证据——**只查措辞不算自审**;③凡引用某页,**该页的侧边栏/相邻页目录必须扫一遍**(本次费率表就在同一侧边栏下)。**另记一条元教训**:自审抓到的问题**全部集中在"容易被指出措辞不严"的地方**,这是一种**自我保护式自审**——挑软柿子捏能自证严谨,却把最贵的地方留给了外部审查。**判据:如果自审结论里没有一条动摇了方案的主要结论,那这次自审大概率没做够。**
- 2026-08-30 | 🔴 **"待办"追了 4 天,其实早就做完了——2026-08-26 那次"实测主账号 MFA 未绑定"的结论是错的**:组长当时点开控制台某个安全设置页,看到 MFA 显示未绑定就写进了台账/复盘 brief/zzh待办清单三处文档,定为"zzh 五分钟高价值待办"。2026-08-30 zzh 真去执行时,组长陪着找入口——**折腾了一大圈**:控制台头像下拉打开的是资源导航面板不是账号安全;点"安全管理"进的是云盾安全体检(风险扫描),不是账号 MFA;直接猜 URL(`ram.console.aliyun.com/profile/security`、`usercenter2.aliyun.com/security`、`account.aliyun.com/security/security.htm`)全部重定向或 404;最后靠 WebSearch+WebFetch 查官方帮助文档才定位到真正入口 `myaccount.console.aliyun.com/security`(账号中心→账号安全)。**打开一看,「虚拟MFA」那一栏清清楚楚显示"已绑定"**(按钮是解绑不是绑定),zzh 原话"早就绑定了……服了"。⇒ **08-26 那次判断本身就是错的**,不是账号后来被人绑的。原因没查清,大概率是当时点进了别的安全设置页(比如 RAM 全局安全设置,那个页面管的是"是否允许 RAM 用户自行管理 MFA 设备"的开关,跟主账号自己有没有绑 MFA 是两码事,界面用词接近容易混)。**代价**:①一条假待办占了 4 天"最大敞口"的位置,期间还触发过 08-29 那条账号漂移险情(也是在找这个入口的过程中撞见的);②本次重新排查在多个死路上耗了不少工具调用 | 部署安全组 M5 Z1 执行阶段,zzh 确认属实 | **闸**:①**任何"账号安全配置项现状"的结论,必须精确记录读到该结论的具体页面 URL/路径**,不能只写"实测显示 X"——本次三处文档都只写了"账号中心>账号安全"这个笼统说法,回头核对时无法定位当时到底点的是哪个页面,是否走错页面成了死无对证的谜;②**阿里云控制台里名字相近但含义完全不同的安全设置入口**,已知至少三个,踩过一次后固化路径供以后直接用:账号自身 MFA/密码等 = `myaccount.console.aliyun.com/security`(账号中心→账号安全);RAM 用户 MFA 策略开关(管的是"是否允许"而非"是否已绑") = `ram.console.aliyun.com/settings`(RAM 访问控制→设置→全局安全);云安全态势扫描 = `yundun.console.aliyun.com`(安全管理/安全体检),与账号 MFA 无关;③**任何"待 zzh 执行"的账号级待办,派工前应先自己按上面固化的正确入口复核一次现状**,而不是凭一次可能走错页面的印象就写死结论并占用别人的时间

- 2026-08-26 | 🔴 **共用 checkout 事故第 5 次,肇事者=主R**:PR #90 分支被四条他组 docs commit 搭车 → 主R 用 `--force-with-lease` 推被拒(lease 基准已过期)→ **改用裸 `--force` 顶掉了远端分支上他组两条真实提交**(简历组 zzh 拍板落地件 + 后端组终审订正件)。lease 拒绝本身就是"远端有你不知道的东西"的信号,正确动作是停下 fetch 审视增量,而不是升级为裸 force。两条提交已从本地对象抢救 cherry-pick 回 main(`3a2b2e6`),零内容丢失 | 主R 自查(对抗审 F-1 引出) | **闸**:①强推**只允许** `--force-with-lease`,lease 失败=必须 `git fetch` 并逐条查看远端增量后重新决策,**任何情况下禁用裸 `--force`**;②主R 的分支操作(建分支/改码/强推)一律在**隔离 worktree** 进行,不再占共享树 HEAD(本次已切换);③🔴 **根治建议(待 zzh)**:今日共用 checkout 已五起事故(他组吞提交×4 + 本条),**所有组长会话迁至各自 worktree**(法务组模式),共享树只留 main 只读+文档提交

- 2026-09-07 | 🔴 **grep 一个「名字」零命中,就断定「行为」不存在**(⑤ 既有「否定性断言双向核」闸的**第三种形态**) | L-5 §四.1(a) 断言「把 status 落到 C 端可见性上的**执行谓词尚未落地**」,论据只有 `PUBLIC_JOB_SCOPE`/`job-visibility.ts` 在 `apps/**` 零命中,并据此推出承重结论「**岗位已下架而站上仍在展示,是可预期的常态**」。**结论被后端组证伪**:过滤行为一直在,只是**没抽成有名字的东西**——`apps/api/src/job/job.service.ts` 三处内联(`:117` 收藏列表 / `:138` findOpenJob / `:146` buildJobWhere)均带 `status: JobStatus.OPEN`,`status=CLOSED` 的岗位 C 端确实查不到。**危害**:这条结论是本组给 Z-2 定严重度的直接依据,方向是**把「我按名字没搜到」说成「这个行为不存在」**。⚠️ **与 2026-08-26 那条 X-1 badcase 同源但不同形态**:X-1 是漏查**基础设施层**,本条是**同一层内漏查「未具名的等价实现」**。 | 后端组 2026-09-07 回执证伪,组长回 `job.service.ts:112-150` 与全 `apps/api/src` 复验属实(`deadline`/`lastVerifiedAt` 仅 `:31-32` 类型声明 + `:328-329` 返回 + `admin.service.ts:78/:100` 写入,零处作查询条件) | **闸(补全既有条,第三向)**:声称某**行为/能力**不存在时,**不得只 grep 标识符名**(常量名/文件名/函数名)——名字缺席只能证明「没有具名抽象」,不能证明「行为缺席」。须再查**行为本身的等价形态**:相关 service/repository 的查询条件、where 子句、中间件/守卫、DB 约束与默认值。查完仍无,才写「行为不存在」;只查了名字就写「**仅缺具名抽象,行为是否存在本组未核**」。**副产品**:本次证伪反而挖出一条更硬的真缺口(`deadline` 从不参与过滤 ⇒ 已过期岗位只要 status 仍 OPEN 就照常展示),说明查行为比查名字信息量更大。 | 
- 2026-09-04 | 🔴 **把承重条文截引后仍标「逐字」;以及同一个法条限定语「有利时引用、不利时略去」** | L-5 初稿两处:①引 50 号令第二十一条时掐掉开头适用条件「开展人力资源供求信息收集和发布的」**和结尾整个分句「不得以人力资源供求信息收集和发布的名义开展职业中介活动」**,把逗号改句号、不加省略号,呈现为完整逐字引用——**掐掉的末句恰是针对「备案之名中介之实」的反规避条款,在定性问题上是承重的**;②第十五条第一款与第十六条第一款开头是**同一个**限定语「接受用人单位委托招聘人员的」,初稿在第十六条上用它论证「我方不适配」(有利),在第十五条上把它删掉只引后半句并据此给我方加了一条 🔴 风险(不利)。 | Opus5 独立对抗审 F-L5-2 / F-L5-3,组长回 `beijing.gov.cn` 50 号令全文逐条复验属实 | **闸**:①凡标「逐字」的引用,必须**从该款开头引到该款结尾**,任何省略一律用「……」并说明省了什么;②**同一法条限定语在同一份文件里必须一致对待**——若某个限定语被用来论证「对我方不适用」,则凡引用带同一限定语的条款时**必须同时引出该限定语**,不得只引对结论有利的半句。 | 
- 2026-09-04 | 🔴 **法规检索基线只到「部门规章」层,漏掉「规范性文件」层** | L-5 初稿检索止于人社部令第 50 号(2023)与第 44 号(2021),**漏掉五部门《关于规范网络平台招聘类信息发布的通知》(2025-12-25 印发)**——出稿前 8 个月发布、主题与我方业务完全重合,且其第一部分在**字面上指向与初稿结论相反的方向**。危害:初稿在 §五 表给「备案类依据 ✅ 成立」置信**高**,而最新执法口径书面证据未进视野。**与本组自己在别处用「零命中」下否定结论时的严格程度不对称。** | Opus5 独立对抗审 F-L5-1,组长回 `cac.gov.cn` 原文复验属实(名称/五部门/2025-12-25/四段原文逐字命中) | **闸**:法规检索**至少三层**——①法律/行政法规 ②部门规章 ③**部门规范性文件(通知/意见/公告)**;第③层用「主管部门 + 业务关键词 + 近两年」单独检一轮,查不到写「本组已检索第③层未见」,不得默认第②层就是最新口径。 | 
- 2026-09-04 | 🟡 **用「本组未核实」把一个可核实的问题升格成待 zzh 拍板的决策阻塞**(既有「未核实」纪律的**反向滥用**) | L-5 初稿 §四.2/Z-1:称「只加隐式不加显式是否合规**本组未核实**」,并判定其为「**唯一一条会改变产品形态**的合规要求」推给 zzh 做 G2 拍板。**答案就在本组自己已抓取的同一份《人工智能生成合成内容标识办法》里**——第九条明写用户申请时可提供不含显式标识的内容(条件:用户协议按第八条写明 + 留存日志≥6个月);初稿检索只覆盖第二/四/五/十一/十四条,**第八至十条整段没进视野**。另初稿引第四条时删掉了其适用前提「属于《互联网信息服务深度合成管理规定》第十七条第一款情形的」,读起来像无条件义务。 | Opus5 独立对抗审 F-L5-4,组长回 `cac.gov.cn` 标识办法与 `gov.cn` 深度合成规定原文复验属实 | **闸**:标「未核实」前必须先做两件事——①**把已在手的同一份文件读完**(不能只读检索命中的那几条就宣告未核实);②**引用任何一条带前提句的条文时,前提句必须一并引出**。「未核实」是查不到时的诚实标注,**不是把可查的问题外包给 zzh 的通道**。 | 
- 2026-08-26 | 🔴 **「声称某防护不存在」时只 grep 了应用层,漏查基础设施层 ⇒ 承重结论被证伪** | 法务组取证底稿 X-1 断言「练习录音原始音频**事实上无限期留存在 OSS**」,论据只有「`purgeExpiredPracticeAudio` 全仓无调用方」。**代码层结论成立**(组长与对抗审两次独立实证),**但推论错了**——`docs/部署/生产环境清单-阿里云.md:136` 载明 OSS 生命周期规则已建(2026-06-30,前缀 `voice/`、30 天删),音频 key `voice/<userId>/...` 逐字落在该前缀下;同文件 `:106` 明写设计上是**三处同步**(OSS 规则 / env / @Cron purge),即本就是双执行者,本组只查其一便宣告"无执行者"。**危害**:该结论是 L-1① 对用户留存承诺的直接依据,错误方向是**把"我没查到"说成"它不存在"**,会让对外文本据错误前提定稿。**这是台账⑤ 2026-08-26 既有那条的镜像**——原条防的是「把 spec 说成已落码」(把不存在说成存在),本条是**反向**(把存在说成不存在),原闸只写了「声称防护存在须先 grep」,漏了反向。 | 法务组首批交付 Opus5 独立对抗审 F1 证伪,组长回 `生产环境清单-阿里云.md:136/:106` 复验属实 | **闸(补全既有条,双向)**:①声称某防护/机制「**存在**」→ 先 grep 确认标识符在 `apps/**` 真有(既有);②声称某防护/机制「**不存在**」或「无执行者」→ **不得只查应用代码**,须同时排查:基础设施层(OSS/RDS 生命周期、云监控、定时任务面板)、`docs/部署/` runbook 记载、他组台账条目;查不到就写「**本组未核实**」而非「不存在」;③跨层的事实按 E2 处理——**他组 runbook 的记载是二手转述,不能当自己的承重结论写进对外承诺**,须实测留回执才能升级为已核实
- 2026-08-26 | 🟡 **「全文件清点」「三处」这类可证伪的精确断言写错了两处** | 法务组底稿 §8.4 写「`admin.controller.ts` 全部 15 个路由」实为 **16**;§7.1 写「网关**三处** `aITask.create/update`」实为 **5 处**(`:180` create + `:199`/`:204`/`:215`/`:240`),且自己列了 4 个行号却写"三处"、漏了 `:199`。两处**结论均不受影响**(无删用户能力 / 不写 inputRef-outputRef 成立),但精确数字是可机械证伪的断言,错了会让整份文件的可信度被连带质疑。 | 同上,对抗审 F7 | **闸**:凡写「全部 N 个」「共 X 处」这类可数断言,落笔前跑一次 `grep -c` 或逐条清点并把清点结果一并写进证据列(如「16 条:jobs×3/users×3/...」),不凭印象写数;列了行号就必须与数字对得上

- 2026-08-26 | 🔴 **主R 终审 PR #80 漏检「批量写路径 × 生命周期状态列」交互面**:终审盯了三段式/tags 解析/真库门,却没问「这条 update 路径能改哪些受治理的列」——`status: item.data.status` 无条件回写就在被审 diff 里(commit 79203e8),直到数据组 M7 定点对抗审抓出「重跑导入静默复活 46 条已下架岗位」才补护栏(PR #84)。同一对抗审还证明 update 对 lastVerifiedAt/deadline 等列同样静默回退(F5,已转 M7) | 数据组 M7 定点对抗审;主R 认账 | **闸**:终审任何含批量写路径的 PR,必过一问「该路径可写的每一列里,哪些列由别的机制治理(状态/下架/复核时间/审计)?治理机制被绕过吗?」;另 Opus 对抗审顺带核出 **CI 从不 typecheck 根目录 `scripts/`**(api tsconfig 只含 src,`pnpm -r lint` 不覆盖)——主R 将作为 M4 终审意见随附给部署安全组(建议并入 CI 守护范围)

- 2026-08-26 | 🔴 **把「spec 里的设计」说成「回码确认的既成事实」**(今晚第三次同类):答部署安全组时写「**回码确认**:`trackSafe()` 内部对非白名单事件强制置空」,而实测 `trackSafe` / `PHONE_ELIGIBLE_EVENTS` / analytics 内任何 phone 白名单逻辑**全仓零命中** —— 该保护只存在于 M3 spec、**一行代码都没落**。我引的 `§3.4.7①`/`§3.4.4` 是 **spec 节号不是代码位置**,却给它贴了「回码」标签。**危害不是措辞**:部署安全组的彩排清单按「什么已经存在」决定「验什么」,把未落码的设计当既成事实 ⇒ **彩排那天直接跳过它**;而它是**写入侧唯一防线**(列级权限是读取侧,两者互为纵深、不可互相替代)⇒ 等于两道防线只剩一道还没人知道 | 部署安全组回执之三纠正,组长实跑三条 grep 全零命中确认 | **闸**:①「回码确认」四个字**只能**用于给出 `文件:行号` 且该行真实存在的断言;引 spec 一律写「**spec 规定/设计如此,尚未落码**」;②凡对外声称某防护「存在」,先跑一次 grep 确认标识符在 `apps/**` 里真有;③**spec 与代码的距离必须在对外答复里显式说明** —— 收件方据此决定验什么、排什么期
> 预置清单见公约附录 A(5 条,含来源与补闸)。新增往这加,格式:`日期 | 坑 | 来源 | 补的闸`。

- 2026-08-16 | **共享 checkout 下用事后 `git rev-parse --short HEAD` 取自己的提交号,并发的他组提交已把 HEAD 推进 ⇒ 交付物里引用了别组的 commit hash**(模面组终审申请写 `b2529fe`=复盘组文档,实际应为 `45a4fe8`;zzh 终审时抓出) | 模面组模块三终审申请 | **提交后取号一律 `git log -1 --format=%h -- <本组文件路径>`**(按文件定位),或 commit 同条命令内立即捕获;**引用 commit 号进交付物前先 `git show --stat <hash>` 核对文件清单是本组的**

- 2026-08-16 | **spec 把技术改进冒充成 zzh 裁决**:M1 spec v0.1 的「裁决落位索引」把"分页全扫不再 200 封顶"标为【产品已拍】、正文归因"B1 裁决",而 brief B1 裁决原文(`复盘brief.md:102`)零处提及;同表其余各行经复核均忠实 | 后端组 M1 spec 对抗审(丙路 R4)+ 组长逐字比对 brief 证实 | 裁决索引表每行必须能在 brief 原文**逐字**找到出处,找不到即标【技术·组长拍】;**组长复核必须回 brief 逐行对**,不信起草者自述
- 2026-08-26 | 🔴 **共用 checkout 这次直接吞掉了「已提交并推送成功」的成果(第 4 次同类,危害升级)**:后端组终审包提交 `b9ed631` 与订正提交 `1e3fad4` 先后从 `main` 历史消失。查 reflog 坐实是**别的会话在同一个工作树里做了 `reset` 与 `checkout` 换分支**(`HEAD@{4}: reset: moving to 675c6de`、`HEAD@{1}: checkout: moving from fix/import-field-guards to main`),把我的提交挤成孤立对象;更隐蔽的是 `git push` **报成功**而内容并未进远端(本地 `origin/main` 引用已过期,`git ls-tree origin/main` 才查得出来)。前三次同类只是"提交带错文件/被覆盖一行",这次是**整份终审包与 spec 三处🔴订正一起消失**,且如果不是恰好去 grep 验证,不会被发现 | 后端组出具终审包 → 落终审第一轮订正 | **闸(两条,立即生效)**:①🔴 **`git push` 之后必须验证真的落到远端**,固定手法 = `C=$(git rev-parse HEAD); git push …; git fetch origin main; git merge-base --is-ancestor $C origin/main`,**不看 push 的退出码**(它可能推到了过期引用);②凡"东西不见了"先查 `git reflog` 与 `git log --diff-filter=D`,**commit 对象通常还在**,可 `git show <sha>:<path>` 原样取回,不要重写。**根治仍是各组长会话从独立 worktree 起(该建议已第 4 次上呈,本次为其提供了最强证据:操作纪律挡不住别人在你脚下 reset)**
- 2026-08-16 | 🔴 **组长复核方法有盲区:机械核验查不出「关于代码行为的语义断言」**。M1 spec 组长复核对 101 处 `文件:行号` 做了存在性+范围机械核验(零失效)并判 PASS,但 §1.4.1 一句「RedeemService **不读该返回值**」是**错的**(实码 `redeem.service.ts:34` 为解构式读取,是读的),机械核验查不到——行号存在、文件对、范围内,唯独**断言的语义内容**与代码行为不符。该错误由**下游 M2 对抗审顺带发现**,组长回码证实,已订正为 CR-3(结论仍成立但论据换掉)| 后端组 M1 spec v0.2.2 组长复核 → M2 对抗审(账务路)发现 | **复核方法升级**:组长复核须分两层——①**机械层**(行号存在性/范围/文件存在,可脚本批量);②**语义层**(凡断言「某处**会/不会**做某事」「某处**有/没有**某物」的句子,**逐条回码读上下文确认**,不能只看行号命中)。语义层优先查:否定式断言、行为断言、"零影响/不受影响"类结论的**论据句**(结论对≠论据对)
- 2026-08-16 | 🔴 **同类第 2 次:未亲验就断言"某处没有 X"**。M2 谈话中向 zzh 报「免费额度配置后台可改但**改动无审计**」,并据此派工写进 spec 提示词;回码亲验发现**审计早已存在**(`admin.service.ts:212-226` 的 `upsertQuotaConfig` 已写 `audit(adminId,"QUOTA_UPSERT",...)` 且 detail 带 feature/groupKey/freeQuota)。已即时向起草 agent 发更正指令并要求留痕,未造成错误产出落库 | 后端组 M2 谈话 + spec 派工 | **闸升级**:①「**某处没有 X**」这类**否定式断言**比肯定式更易错(grep 关键词选错就得出零命中),写进汇报或派工前必须**回码正面确认**,不能只凭一次 grep 零命中;②派工提示词里的每条现状断言与交付物同等要求:**能给 `文件:行号` 才写,给不出就标「未核」**;③发现自己已发出的断言有误 → **立即追发更正**(不等 agent 交付),并要求在产物里留痕
- 2026-08-16 | **递给 zzh 的产品问法基于未亲验的机制描述**:把 clamp 说成"已消耗部分公司认亏",实际 `Entitlement` 无来源维度(兑换码/客服补偿/支付同池),扣回先亏的是**用户其它来源额度**;基于错误机制的拍板结论不可用 | 后端组 M1 spec 对抗审(甲路 F4)+ 组长回码证实 | 凡「产品变更点」的机制描述句,**递交 zzh 前必须有 `文件:行号` 支撑**(公约 E1 承重实证扩用到产品问法,不止交付物)
- 2026-08-16 | 🔴 **同类第 3 次(后端组记,且是自己定的闸没用全)**:提交 M1 spec v0.2.2(`680ed45`)时把简历Agent组已暂存的 v1.3 契约(626 行)+ INDEX.md + v1.1 一并带走。**根因不是忘了显式点名**(确实只 `add` 了本组一个文件),而是**他组会话先把文件 `add` 进了暂存区**;我上一条 badcase 补的闸只写了"改台账前查暂存区",**范围写窄了——共享 checkout 下这条对每一次提交都适用**。内容完整无损,仅归属错位 | 后端组 M1 spec v0.2.2 提交 | **闸扩用**:共享 checkout 下**每一次 `git add` 前**先 `git diff --cached --stat` 确认暂存区干净;非空则先弄清是谁的、完整则 commit message 注明代提交、半截则等对方。🔴 **三次同类事故已证明操作纪律治标不治本,根治=各组长会话改从独立 worktree 起(建议已上呈 zzh/主R,待定)**
- 2026-08-16 | **同类重犯 ×2(后端组记)**:①后端组写好的①行被另一会话的并发提交**直接冲掉**(对方 commit 带的是旧版后端组行,我的编辑丢失,需重写);②后端组重写后提交时,又把部署安全组停在工作区的①行+③两行带走(内容完整、无损)。根因 = **多个组长会话共用同一个 checkout `~/bangtoudi`**,`git add <文件>` 无法只提交文件内一行 | 后端组 spec v0.1 落稿后更新台账(`1d61343`) | **闸加固(公约 F2)**:①编辑台账前先 `git diff --cached --stat` 确认暂存区干净;②`add` 后**提交前**再看一次 `git diff --cached`,他组行完整则在 commit message 注明代提交、半截则等对方;③改完**立即提交**,不要在编辑与提交之间插入其它工具调用;④**根治建议(待主R/zzh 定)**:各组长会话改从**各自 worktree** 起,不再共用 `~/bangtoudi`
- 2026-08-26 | 「模型自证」类闸门:放行/豁免/证据判断的输入 X 由模型产出,等于被告自出不在场证明;两组独立踩中同类 | 简历Agent组发现+模面组确认(经 zzh 抄送) | 公约新增 Q5 闸门输入溯源+checklist 加行(F2 回流);各组对存量 spec/代码做一次专项扫
- 2026-08-17 | 简历 KB 角色层 0 命中静默空转:profile.role「运营/AI」不在 ROLE_CATEGORIES 白名单,检索恒 0 命中回退通用层、语料灌不进,三个 P0 方向两个空转无人察觉 | 简历Agent组补充件核出,主R 亲验(resume-profile.ts:55/73→resume-agent.service.ts:1180/1336 vs taxonomy.ts:8) | 分类 SoT 单点+守护单测+角色层 0 命中计数告警(口径 v2.0 §1,随第1步修复同落)
- 2026-08-15 | 共享 checkout 下即使显式点名提交台账,也会把**他组停在工作区的未提交行**一并带走(本次良性:前端组行被 `d5337e2` 夹带,内容无损但 commit 归属错位;若当时对方行只写一半,进库的就是坏行) | 台账①首轮各组并发自填(前端组记录) | 改台账 = 写完整行**立即自提交**不过夜;提交台账前 `git diff --cached` 核对暂存内容只含本组的行,发现他组行未提交时等对方或注明代提交

- 2026-08-16 | **台账④「当前引擎配置」是全局行,zzh 同一指令同时通知多组 → 两组各自改同一行,后推者 rebase 冲突**(本次:数据组与简历Agent组同改引擎降档行,内容语义相同但措辞不同);另 `git rebase --continue` 在冲突解完后仍反复报「must edit all merge conflicts」,abort 重做才通 | 引擎降档切换(2026-08-16,简历Agent组记) | **全局行(④/②主R块)默认由主R或先到者落,后到者先 `git pull` 看是否已被改——已改且语义等价就不重复改,只更新自己的①行**;共享 checkout 里 rebase 前用 `--autostash` 保护他组未提交文件,卡死别硬顶、`--abort` + `reset --soft origin/main` + 只重做自己那行
- 2026-08-16 | 🔴 **共享 checkout = 共享 git 索引:他组会话刚 `git add` 完还没 commit,我一个 `git commit` 就把他们已暂存的改动全扫进我的提交并推上 main**(本次:简历Agent组 `191ccf0` 卷入部署安全组 4 文件——`INDEX.md`/三端App方案/公司主体登记要素[新文件]/软著申报指引;**内容完好无丢失**,仅 commit 归属与信息错位)。比 08-15 那条(夹带**未提交**行)更狠:那条靠"等对方提交"可躲,这条**对方正在提交的瞬间**就会中招。**未 revert**——回滚等于删他组真实工作 | 简历Agent组补 ⑤ badcase 时自身踩雷(2026-08-16) | ①**提交一律带 pathspec 限定**:`git commit -m "..." -- <显式文件>`,只提交点名路径,**与索引里有什么无关**——这是唯一可靠护栏,`git add <file>` 挡不住别人已暂存的东西;②**核对与提交必须分成两次独立执行**,别 `git diff --cached && git commit` 串一条命令(串起来时核对输出无法阻断提交,等于没核);③误卷入后:不 revert,在 ⑤ 记录 + ③ 通知对方"你的改动已在 main 的哪个 commit",避免对方以为工作丢失而重做
- 2026-08-16 | 共享 checkout 下**文件一旦 `git add` 就进入公共暂存区**:部署安全组暂存 5 个文件后尚未 commit,另一会话的 `git commit` 把它们**一并带进了自己的提交**(`191ccf0`,提交信息只写台账 badcase 却含 #23 主体信息等 5 文件);内容无损且已推送,但提交归属与信息完全错位,`git log` 追溯会误导 | 部署安全组 #23 主体信息入库时 | **`git add` 与 `git commit` 必须同一条命令连着执行**,绝不留"已暂存未提交"的窗口;提交后 `git log -1 --stat` 核对文件数与自己预期一致
- 2026-08-16 | **改 bundler 包装的设计稿时,用正则截取 JS 片段做插入 → 截断在函数体内产出括号失衡的残缺 JS**;而「解包 json.loads 成功 + 内层变长」这类静态自验完全测不出,页面会整段 JS 死掉 | 前端组改 R4 支付页移动端 fail 出口(组长自己动手那一处) | 改内嵌 JS 后必须:①统计 `{}`/`()` 平衡;②真浏览器加载读 console;③**逐个 handler 实测点击看状态流转**(本次正是靠第③条暴露);另包装回写必须先断言 `pack(inner)==原文` 验证转义规则(该壳层 `</`→`<\u002F`,直接 json.dumps 会让 `</script>` 提前截断)
- 2026-08-16 | **无实测的性能/规模乐观断言,同类重犯**(公约 F2 触发):M1 契约 HMAC 段先写「典型 < 100 次」被 codex 终审退回,改后又冒出「1000 次 HMAC-SHA256 是亚毫秒级、可忽略」,**第二轮再次被退**。根因=写方案时习惯性给「这点开销可忽略」的安慰性结论,而**从未跑过压测**,属 §三 E1「承重结论必实证」的盲区——**性能断言也是承重结论**(下游据此决定要不要优化) | 简历Agent组 M1 契约两轮定点复核(codex) | **该闸 checklist 加一条**:方案/spec/契约里凡出现性能或规模判断,只许写「**有界 + 落地压测**」(说明边界从哪来、上限设多少、超限怎么降级);**禁止**「可忽略 / 微秒级 / 亚毫秒级 / 开销极小 / 典型 < N」这类**无实测量级断言**;确要给量级必须附实测出处(来源+日期+环境,E3)
- 2026-08-26 | **浏览器代驾时会话已切到「职计划」账号,而组长正准备执行帮投递的 SSH 白名单收窄**——若未察觉,就是**在另一家公司的生产防火墙上改规则**。识别证据:控制台主账号显示「职计划互联网科技」(帮投递应为 `nick2202357201`)、安全组所属 VPC `vpc-2zeeno0j0neudoq1e7mrh`(帮投递应为 `vpc-2ze4uz7v2sj2dyojihbpz`)。本次**只读未改**,零后果 | 部署安全组 M5 现状核查 | **控制台写操作前置对账(强制)**:任何在云控制台的**写动作**执行前,必须先核对两个指纹与 runbook 一致——①**登录主账号名**;②**目标资源 ID**(VPC/实例/安全组)。任一不符立即停,不得凭"页面长得对"继续。读操作亦建议先看一眼账号名。原因:同一浏览器可能登着多个主体的账号,页面结构完全一样,靠肉眼看不出来
- 2026-08-26 | **跨组引用的"已核实"标签可能名实不符**:后端组以「回码确认」引述实为 spec 规定、尚未落码的保护(`trackSafe`/`PHONE_ELIGIBLE_EVENTS` 等三条 grep 全零命中)。危害具体:下游(本组)彩排清单按"什么已存在"决定"验什么",据此会**跳过写入侧唯一防线**,而读取侧列权限不可替代它——等于两道纵深塌一道且无人知晓 | 部署安全组↔后端组四件基建往返(第三轮) | ①「回码确认/实测」只能用于能给出**真实存在的 `文件:行号`** 的断言,引 spec 一律写「spec 规定、尚未落码」(后端组已自记同款闸);②**本组自查**:五节 spec 的 8 处承重 `文件:行号` 引用已于 2026-08-26 逐条抽验,全部仍成立(auth.module:19 / nginx.conf:11 / interview-recordings:71,147 / seed.ts:191 / oss.service:86 / compose:27-31 / schema:445-454),**行号漂移自查固化为交主R终审前的必做项**
- 2026-09-07 | **两组各自在 worktree 手编迁移目录时间戳的整点数,合流撞出完全同秒的双目录**(#111 `interview_last_activity` 与 #106 `product_billing_fields` 都编了 20260902120000=12:00:00 整),迁移排序歧义直到合入 main 才由 #113 卫生闸拦红——闸按设计工作,但 main 尖红了一轮、全组 CI 被堵约半小时 | 后端 #106 与模面 #111 合流(主R 修复=PR #115 改名排队尾) | **迁移目录时间戳一律用创建时真实时间(`date +%Y%m%d%H%M%S`),禁止手编整点数**;PR 里含迁移的,送审前自跑 `node scripts/check-migration-hygiene.mjs`(以 origin/main 为基),别把撞号留给合并后的 main CI
